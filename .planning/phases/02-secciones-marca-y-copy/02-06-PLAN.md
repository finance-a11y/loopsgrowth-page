---
phase: 02-secciones-marca-y-copy
plan: 06
type: execute
wave: 8
depends_on: [02-05]
files_modified:
  - src/content/landing.es.yaml
  - src/content.config.ts
  - PENDING-COPY.md
  - scripts/lib/copy-rules.mjs
  - tests/guards/copy.test.mjs
  - src/components/sections/ForWhom.astro
  - src/components/sections/Faq.astro
  - src/components/SiteFooter.astro
  - src/components/AgendaSection.astro
  - src/components/SiteHeader.astro
  - src/components/SkipLinks.astro
  - src/layouts/BaseLayout.astro
  - src/pages/privacidad.astro
  - src/pages/index.astro
  - tests/e2e/closing-sections.spec.ts
  - tests/e2e/a11y-base.spec.ts
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
autonomous: true
requirements: [CONT-10, CONT-11, CONT-12, CONT-13, COPY-01, DSGN-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`#para-quien` (tono `light`, con `SectionShell` sin cambios) muestra un h2 y dos tarjetas con h3 ('Para quién es' y 'Para quién no es'): una columna bajo 640 px con 'es' primero, y dos columnas iguales con gap de 24 px desde 640 px y la misma altura (diferencia menor o igual a 1 px). Sin CTA y sin movimiento (CONT-10)"
    - "La tarjeta 'es' tiene fondo amarillo de marca, borde sólido de 3 px oscuro, sombra dura e iconos de check oscuros; la tarjeta 'no es' es blanca, con borde discontinuo de 3 px oscuro, sin sombra e iconos de x. Cada `<ul>` lleva `role='list'` y `aria-labelledby` de su h3. La diferencia no depende del color: cambian el icono, el trazo del borde y el h3 (CONT-10; UI Considerations: a11y-color)"
    - "El doc de Ari no trae texto de Para quién es: el h2 y las tres líneas de cada columna son afirmaciones `pending` con el texto 'FALTA CONFIRMAR' y un `reason` que dice qué debe entregar Ari (la primera línea de 'es' debe comunicar el perfil de USD 200k o más al año); los dos nombres de columna vienen de CONTEXT y del UI-SPEC y quedan `pending` para que Ari los confirme (CONT-10, COPY-01)"
    - "La guarda de copy suma la regla INVERSION (de contenido: bloquea producción y solo avisa en otros entornos) sobre las rutas `for_whom.*` y `faq.*`: marca rangos de inversión mensual como '4 a 5k al mes', '4-5k al mes', '1.5k al mes' o '+1500 al mes' y cifras en USD o $ que no sean anuales, salvo en afirmaciones `verified` (aprobadas por Ari). 'USD 200k o más al año' no la dispara y el resto del YAML queda fuera (el caso de éxito del doc trae 'De $41K a $76K en ventas', que no es inversión). Se prueba por mutación en `tests/guards/copy.test.mjs` (CONT-10, COPY-01)"
    - "`#faq` (tono `yellow`) tiene un h2 y una lista de 6 `<details>` no exclusivos (el esquema admite de 5 a 6) con los temas de CONTEXT: qué es GEO, duración de la llamada, qué preparar, inversión, tiempos de resultados y si aplica a mi negocio (el tema va en el `reason` de su pregunta y de su respuesta). Pregunta y respuesta son 'FALTA CONFIRMAR' `pending`. Sin pregunta de garantía y sin cifras (CONT-11, COPY-01)"
    - "Cada `<summary>` es `display: flex` con la pregunta en Body 700 y un icono plus (cerrado) o minus (abierto) de 24 px, mide 44 px o más de alto, con padding de 16 px bajo 640 px y de 24 px desde 640 px; cada tarjeta es blanca con borde de 3 px oscuro y radio de 16 px, las tarjetas se separan 16 px y la respuesta va bajo una regla de 3 px oscura. Todas parten cerradas y Enter y Espacio abren y cierran (CONT-11)"
    - "El foco del `<summary>` es un anillo sólido de 3 px dentro de la tarjeta (offset negativo), sin recorte; el hover rellena el icono de amarillo con contorno oscuro; nada del FAQ tiene transición, así que abrir y cerrar no crea animaciones (CONT-11; UI Considerations: a11y-motion)"
    - "Desde 1024 px, `#faq` es una rejilla `minmax(0, 5fr) minmax(0, 7fr)` con gap de 48 px, el h2 a la izquierda (`align-self: start`) y la lista a la derecha; bajo 1024 px va el h2 y luego la lista (CONT-11)"
    - "El h2 de `#agenda` pasa a ser `agenda.title`, el titular del CTA final tal cual del doc de Ari ('¿Listo para que te encuentren cuando te estan buscando?', con la errata 'estan' sin tilde, que se reporta a Ari en el SUMMARY y no se corrige); conserva `id='agenda-title'`, `tabindex='-1'`, peso 700 y color blanco, y suma la clase `.section-title` (barra de 48x8 px) (CONT-13, COPY-01)"
    - "`#agenda` conserva la estructura de la fase 1 (`<section id='agenda' aria-labelledby='agenda-title' data-tone='purple'>`, rejilla 5fr/7fr desde 1024 px con el DOM igual al visual, intro, enlace de respaldo, tarjeta `.form-embed`, iframe con sus atributos y `min-height`, y `noscript`) y no lleva ningún `CtaLink`: la página tiene 4 CTA. Su padding vertical mide 64 px bajo 1024 px y 96 px desde 1024 px (CONT-13)"
    - "`AgendaCollage` (plan 02-10, cuya raíz es `div[data-collage='agenda']` con una capa svg y dos píldoras HTML decorativas, `team work` y `seo`, y no un svg) se monta en la columna izquierda de `#agenda`, después del enlace de respaldo: a 1024 y 1280 px es visible y el rectángulo de esa raíz (nada de lo pintado sale de su caja) no toca a `.form-embed` ni al iframe; a 320, 390 y 768 px está en `display: none`; no entra en el orden de tabulación; con él `/` suma dos píldoras a las once de 02-10 y unos 2 KB de HTML (CONT-13, DSGN-04)"
    - "El footer (`<footer data-tone='light'>` con borde superior de 3 px y padding vertical `--section-y`) se monta en `BaseLayout` y aparece en `/` y en `/privacidad`: logo horizontal (`Logo` de 02-09, variante `horizontal` y tono `light` como el footer, es decir la mesa oficial 06 sin recolor; sin enlace en `/` y con enlace a `/` en otras rutas), un `<nav aria-label>` con un bloque de contacto (correo) y otro de redes, y el enlace 'Política de privacidad' a `/privacidad/` (con `aria-current='page'` en esa ruta). Una columna bajo 640 px, dos de 640 a 1023 px y tres (`minmax(0, 1.2fr) 1fr 1fr`) desde 1024 px (CONT-12)"
    - "Correo y redes son `pending` con el texto 'FALTA CONFIRMAR' en texto plano; el correo pasa a enlace `mailto:` solo si el texto cumple una expresión estricta de correo. Los enlaces del pie miden 44 px o más de alto, van en `--link` con subrayado permanente y separados 8 px. Sin 'todos los derechos reservados', año ni texto legal ajeno al doc, y el footer no agrega encabezados (CONT-12, COPY-01)"
    - "`/privacidad` (`src/pages/privacidad.astro`) usa `BaseLayout`, lleva `noindex` en todo entorno (también con `PUBLIC_ENV=production`) y no lleva canonical; muestra un único h1 'Política de privacidad' (`privacy.title`) y el cuerpo `privacy.body` como 'FALTA CONFIRMAR' `pending`; no tiene h2 ni JavaScript nuevo (CONT-12)"
    - "En `/privacidad` el CTA del header apunta a `/#agenda` (en `/` sigue en `#agenda`), el segundo skip link se omite porque `#agenda` no existe allí, y todo `href='#...'` de esa página resuelve a un id existente. Cargar `/#agenda` desde ahí deja el foco en el h2 por el `cta-focus.ts` de la fase 1, sin cambios (CONT-12, CONT-13)"
    - "Orden de tabulación de `/` a 1280 px: skip 1, skip 2, CTA del header, CTA del hero, CTA de La solución, CTA de Casos, los resúmenes del FAQ (uno por pregunta, contados desde el YAML), el enlace de respaldo, el iframe y, tras el iframe en el DOM, el enlace de privacidad del pie. A 390 px es el mismo sin el CTA del header. `a11y-base.spec.ts` lo afirma sin `tabindex` positivos (CONT-11, CONT-12)"
    - "Sin JavaScript el FAQ abre con `<details>` nativo, todo el contenido de estas secciones y del footer es visible y el enlace a `/privacidad/` funciona. El único JS propio del build sigue siendo el script de foco (menos de 3 KB) y `/privacidad` no carga ninguno (CONT-11)"
    - "Con `prefers-reduced-motion: reduce` y con `no-preference`, `document.getAnimations().length` es 0 en `/` y en `/privacidad`: este plan no agrega movimiento, y el giro de 150 ms del icono del FAQ es del plan 07 (UI Considerations: a11y-motion)"
    - "Sin scroll horizontal a 320, 390, 768, 1024 y 1280 px en `/` y en `/privacidad`: hijos de rejilla con `min-width: 0` y `overflow-wrap: anywhere` en preguntas, títulos y enlaces; con espaciado de texto forzado (SC 1.4.12) nada se recorta ni se superpone (UI Considerations: long-text y overflow)"
    - "Toda ranura sin dato muestra el texto exacto 'FALTA CONFIRMAR' y nunca queda vacía ni oculta; todo texto del YAML se imprime como nodo de texto sin leer su estado; las pruebas derivan lo esperado del YAML con el criterio de `walkClaims` y no de listas escritas a mano (UI Considerations: empty; COPY-01)"
    - "`PUBLIC_ENV=production` sigue haciendo fallar la guarda de copy solo por reglas PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`); `npm run build` sin producción pasa; `npm run pending` regenera `PENDING-COPY.md` de forma determinista con las filas nuevas y `node scripts/list-pending.mjs --check` sale 0 (COPY-01)"
    - "El HTML de `/` pesa menos de 60 KB sin comprimir y las únicas peticiones a terceros de `/` y de `/privacidad` son forms.clickup.com y app-cdn.clickup.com; el iframe de ClickUp, su enlace de respaldo y su `noscript` no cambian (UI Considerations: loading y error)"
    - "Todo el diseño del plan pasó por `impeccable` (lote D con shape, layout, clarify y adapt; lote E con layout, colorize y harden) y `design-taste-frontend` (diales 7, 3 y 4) invocados con la herramienta Skill; `02-VISUAL-LOG.md` registra por lote y ronda los verbos, hallazgos, correcciones y capturas a 320, 390, 768, 1024 y 1280 px (base, movimiento reducido y sin JavaScript, con ClickUp bloqueado). Los lotes cierran cuando estas secciones cumplen los rasgos 6, 7 y 8 de la lista de vibra (DSGN-04)"
    - statement: "Estado abierto o cerrado del `<summary>` anunciado por un lector de pantalla, y rol conservado con `list-style: none` y `display: flex` (Safari con VoiceOver): comprobación manual, anotada en `02-VISUAL-LOG.md` como pendiente para Juan; la prueba automática solo confirma que Enter y Espacio alternan `open` (UI Considerations: interaction)"
      verification: backstop
    - "Supuesto explícito (UI Considerations: assets): el logo del footer es el `Logo` de 02-09 con su tono por defecto (`light`, mesa 06); el color de marca lo decidió Juan el 2026-09-19 (el azul violeta oficial del logo; Ari lo confirma después) y este plan no lo toca. Supuesto explícito (copy): las cadenas de estas secciones quedan mapeadas en la tabla 'Ranuras de copy'; el doc trae el titular y el cuerpo del CTA final y no trae nada de Para quién es, FAQ, footer ni privacidad"
  artifacts:
    - path: "src/content/landing.es.yaml"
      provides: "Claves `for_whom`, `faq`, `footer` y `privacy`, y el titular del CTA final en `agenda.title`"
      contains: "for_whom:"
    - path: "src/content.config.ts"
      provides: "Esquema estricto de `for_whom`, `faq` (5 a 6 ítems), `footer` y `privacy`"
      contains: "for_whom"
    - path: "scripts/lib/copy-rules.mjs"
      provides: "Regla INVERSION (rangos de inversión mensual y cifras en USD no anuales) exportada con `findInversion`"
      contains: "INVERSION"
    - path: "tests/guards/copy.test.mjs"
      provides: "Pruebas nuevas por mutación de la regla INVERSION, solo agregadas al final; la prueba 9b es de 02-03 y aquí solo se ejecuta y se reporta"
      contains: "INVERSION"
    - path: "src/components/sections/ForWhom.astro"
      provides: "Sección Para quién es (CONT-10): dos tarjetas con iconos, borde continuo y discontinuo"
      contains: "para-quien"
    - path: "src/components/sections/Faq.astro"
      provides: "Sección FAQ (CONT-11) con `<details>` nativos, sin JavaScript y sin transición"
      contains: "<details"
    - path: "src/components/SiteFooter.astro"
      provides: "Footer (CONT-12) con logo, contacto, redes y enlace de privacidad"
      contains: "<footer"
    - path: "src/components/AgendaSection.astro"
      provides: "`#agenda` con el titular del CTA final, la clase `.section-title` y `AgendaCollage` en la columna izquierda"
      contains: "AgendaCollage"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout con footer, prop `noindex` y ruta canónica por página"
      contains: "SiteFooter"
    - path: "src/pages/privacidad.astro"
      provides: "Ruta `/privacidad/` con h1, cuerpo 'FALTA CONFIRMAR' y `noindex`"
      contains: "noindex"
    - path: "src/components/SiteHeader.astro"
      provides: "CTA del header con `href` según la ruta (`#agenda` en `/`, `/#agenda` en las demás)"
      contains: "/#agenda"
    - path: "src/components/SkipLinks.astro"
      provides: "Segundo salto solo en `/`, donde existe `#agenda`"
      contains: "pathname"
    - path: "src/pages/index.astro"
      provides: "Composición con `ForWhom` y `Faq` antes de `AgendaSection`, en el orden canónico"
      contains: "ForWhom"
    - path: "tests/e2e/closing-sections.spec.ts"
      provides: "Pruebas de Para quién es, FAQ, `#agenda`, footer y `/privacidad`, y recortes de captura por sección"
      contains: "PRIVACY_PATH"
    - path: "tests/e2e/a11y-base.spec.ts"
      provides: "Orden de tabulación con los resúmenes del FAQ; el aserto de texto del bloque 'textos de Ari visibles' es de 02-03 y aquí solo se ejecuta y se reporta"
      contains: "summary"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada con las afirmaciones pending nuevas (Para quién es, FAQ, footer y privacidad)"
      contains: "faq.items[0].question"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Entradas de los lotes D (parte 06) y E con verbos, hallazgos, correcciones y capturas"
      contains: "Lote E"
  key_links:
    - from: "src/components/sections/Faq.astro"
      to: "src/content/landing.es.yaml"
      via: "`getLanding()`: `faq.title` y `faq.items[i].question` y `.answer` impresos con `.text`, sin leer el estado"
    - from: "src/components/sections/ForWhom.astro"
      to: "src/components/ui/SectionShell.astro"
      via: "`id='para-quien'`, `tone='light'` y `title` desde `for_whom.title`; las dos tarjetas van en el slot por defecto"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/SiteFooter.astro"
      via: "el footer se monta tras `</main>`; `privacidad.astro` pasa `noindex` y `path` al layout"
    - from: "src/components/SiteFooter.astro"
      to: "src/components/brand/Logo.astro"
      via: "`variant='horizontal'`, `tone` igual al `data-tone` del footer (`light`), `name` desde `brand.name.text` y `href` solo fuera de `/`"
    - from: "src/components/AgendaSection.astro"
      to: "src/components/collage/AgendaCollage.astro"
      via: "el collage (raíz `div[data-collage='agenda']` de 02-10) vive en `.agenda-copy`, después de `.agenda-fallback`, dentro de un contenedor `aria-hidden` que solo se muestra desde 64em"
    - from: "scripts/lib/copy-rules.mjs"
      to: "scripts/check-copy.mjs"
      via: "`checkCopy` agrega violaciones INVERSION al arreglo `content`, que `prebuild` bloquea en producción"
    - from: "tests/e2e/closing-sections.spec.ts"
      to: "src/content/landing.es.yaml"
      via: "lectura con `yaml`: los textos y las cantidades esperadas (resúmenes, ítems, ranuras) salen del YAML"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir ni reordenar el texto del doc de Ari (incluida la errata 'estan' del titular del CTA final) ni inventar el de una ranura que el doc no trae: títulos de Para quién es y FAQ, líneas de las columnas, preguntas y respuestas, correo, redes y cuerpo de privacidad muestran 'FALTA CONFIRMAR'"
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT leer el estado (`status`) de una afirmación en un componente ni ocultar una afirmación pending: las secciones imprimen `.text` siempre (grep sobre `src` sin `.status`)"
    - requirement_id: CONT-10
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT publicar rangos de inversión mensual (4 a 5k al mes, 1.5k al mes) ni cifras de inversión en Para quién es o en el FAQ: la regla INVERSION los marca si la afirmación no es `verified` y la ranura de inversión queda en 'FALTA CONFIRMAR'"
    - requirement_id: CONT-11
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT agregar JavaScript, `tabindex`, el atributo `name` de `<details>` (que los haría exclusivos), transiciones ni una pregunta de garantía al FAQ: son 5 o 6 `<details>` nativos"
    - requirement_id: CONT-13
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT cambiar el iframe de ClickUp (atributos, `min-height`, `loading`, `referrerpolicy`), su script, el enlace de respaldo, el `noscript` ni el borde, radio, sombra y `overflow` de `.form-embed`, ni sumar lógica de carga diferida del iframe (Juan la decide aparte); las pruebas de la fase 1 lo vigilan"
    - requirement_id: CONT-13
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT poner un `CtaLink` dentro de `#agenda`: un ancla a sí misma no hace nada y suma una parada de teclado (`#agenda a[data-cta]` da 0 y la página tiene 4 CTA)"
    - requirement_id: CONT-12
      category: seo
      status: resolved
      verification: test
      statement: "MUST NOT dejar `/privacidad` indexable mientras su cuerpo sea 'FALTA CONFIRMAR': `noindex` también con `PUBLIC_ENV=production` y sin canonical; la exclusión del sitemap es una nota para la fase 3, no de este plan"
    - requirement_id: CONT-12
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT agregar 'todos los derechos reservados', año, texto legal ni encabezados al footer, ni h2 en `/privacidad`"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar inyección de HTML en bruto, colores hex en `src/components` o `src/pages`, `outline: none`, `line-height` con `!important` ni `height` fijo en contenedores de texto: los colores salen de tokens y de `var(--color-brand-*)`"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: judgment
      statement: "MUST NOT modificar archivos de otros planes: `SectionShell`, `tokens.css`, `global.css` y `CtaLink` (02-01), `Logo` (02-09), sprite y collage (02-02 y 02-10), fotos (02-11), las secciones de 02-03 a 02-05, `cta-focus.ts`, `page-structure.spec.ts` ni `public/`"
    - requirement_id: DSGN-04
      category: motion
      status: resolved
      verification: test
      statement: "MUST NOT agregar animaciones ni transiciones: el icono del FAQ cambia de plus a minus sin transición (el giro de 150 ms es del plan 07) y `document.getAnimations().length` sigue en 0"
    - requirement_id: CONT-10
      category: accessibility
      status: resolved
      verification: judgment
      statement: "MUST NOT distinguir las columnas 'es' y 'no es' solo por color: cambian el icono (check o x), el trazo del borde (continuo o discontinuo) y el h3"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar el formulario de ClickUp, leer `.env` ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Cerrar la landing con las tres piezas que faltan antes de la iteración final: las secciones Para quién es (CONT-10) y FAQ (CONT-11), la sección `#agenda` convertida en el CTA final con el titular del doc de Ari y su collage (CONT-13), y el footer con la página `/privacidad` (CONT-12). Todo el texto sale del YAML tal cual del doc de Ari (COPY-01); donde el doc no trae texto (Para quién es, FAQ, footer, privacidad) la página muestra el relleno visible "FALTA CONFIRMAR", listado en `PENDING-COPY.md`. El diseño pasa por `impeccable` y `design-taste-frontend` (DSGN-04).

Purpose: que el visitante que llegó decidido a agendar no encuentre huecos raros antes del formulario, que sepa si el servicio es para él (sin rangos de inversión que Ari no aprobó), que resuelva dudas sin JavaScript y que el CTA final y el pie de página cierren el recorrido con la misma identidad. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: `ForWhom.astro`, `Faq.astro`, `SiteFooter.astro`, `privacidad.astro`, `#agenda` con titular nuevo y `AgendaCollage`, `BaseLayout`, `SiteHeader` y `SkipLinks` conscientes de la ruta, la regla INVERSION en la guarda de copy, las claves `for_whom`, `faq`, `footer` y `privacy` del YAML y su esquema, `closing-sections.spec.ts`, el orden de tabulación actualizado en `a11y-base.spec.ts` y las entradas de los lotes D y E en `02-VISUAL-LOG.md`.

Decisiones de CONTEXT.md que implementa (por nombre de sección): "Secciones informativas" (Para quién es sin rangos de inversión, FAQ de 5 a 6 preguntas con `<details>`, footer con contacto, redes y política de privacidad con cuerpo "FALTA CONFIRMAR"), "Textos pending" (relleno visible donde falta el dato, sin placeholders vacíos ni contenido oculto), "Hero, header y orden de la página" (orden de las 13 piezas y CTA con el mismo texto), "Excepciones del contrato de diseño aprobadas por Juan" (`#agenda` es el CTA final por sí misma, 96 px de padding desde 1024 px) y "Estilo visual, collage y movimiento" (`AgendaCollage` como collage estático, un `div` con capa svg y píldoras HTML decorativas según 02-10; método `impeccable` más `design-taste-frontend`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicarla con `grep -n '^## \|^### '` y leer con `offset` y `limit`). `PRODUCT.md` y `DESIGN.md` (raíz) los generó el plan 02-01: aquí solo se leen. Los contratos vigentes de `Logo`, `AgendaCollage`, tokens y `tests/e2e/lib/brand.ts` (`PURPLE_RGB`) los fijan 02-09 y 02-10 (que corren antes, con 02-11): léelos en sus SUMMARY y en su sección "Artifacts this phase produces"; si difieren de lo que dice este plan, manda el archivo.
</context>

## Artifacts this phase produces

Rutas relativas a la raíz del repositorio. Los nombres de abajo los reutiliza el plan 07 (movimiento e iteración de cierre) y la fase 3: no se renombran después.

- **YAML** (`src/content/landing.es.yaml`, esquema en `src/content.config.ts`): `for_whom` con `title`, `is_for` y `is_not_for` (cada una con `title` e `items` como lista de afirmaciones; las claves no son `yes` y `no` porque YAML 1.1 las lee como booleanos); `faq` con `title` e `items` (lista de objetos `{question, answer}`, de 5 a 6); `footer` con `nav_label`, `contact_label`, `email`, `social_label`, `social` y `privacy_link`; `privacy` con `title` y `body` (lista de afirmaciones, un `<p>` por elemento); y `agenda.title` con el titular del CTA final. Siguen la convención de 02-01 (una clave snake_case por sección, `title` como afirmación).
- **Componentes**: `src/components/sections/ForWhom.astro` (ganchos `.fw-card`, `.fw-is-for`, `.fw-is-not-for`), `src/components/sections/Faq.astro` (ganchos `.faq-item` en el `<details>`, `.faq-summary`, `.faq-answer`, `.faq-icon` para el svg y `.faq-icon-v` para la barra vertical del plus, que el plan 07 usa para el giro de 150 ms), `src/components/SiteFooter.astro` (`.site-footer`, `.footer-grid`, `.footer-nav`) y `src/pages/privacidad.astro`. `BaseLayout.astro` suma las props `noindex?: boolean` y `path?: string` (por defecto `/`).
- **Guarda de copy**: `INVERSION_MONTHLY_RE`, `INVERSION_CURRENCY_RE`, `INVERSION_PATH_PREFIXES` (`for_whom.` y `faq.`) y `findInversion(text)` exportados desde `scripts/lib/copy-rules.mjs`; la regla `INVERSION` de contenido dentro de `checkCopy`.
- **Pruebas**: `tests/e2e/closing-sections.spec.ts` (constante `PRIVACY_PATH` con `/privacidad/`; bloques de Para quién es, FAQ, `#agenda`, footer y `/privacidad`; un bloque `recorte` que solo corre con `PHASE2_BATCH` y guarda `test-results/phase2/<lote>-<id>-<ancho>.png` de una sección a 390 y 1280 px) y el orden de tabulación de `a11y-base.spec.ts` con `summary`.
- **Lotes de captura**: `D06` (Para quién es y FAQ) y `E06` (`#agenda`, footer y `/privacidad`), con la herramienta de 02-01 (`PHASE2_BATCH=<lote> ... page-structure.spec.ts -g "captura"`); se nombran así para no pisar las capturas `D` del plan 05.
- **Evidencia**: `02-06-SUMMARY.md` con el resultado de las pruebas, las erratas del doc ('estan' en el titular del CTA final), las afirmaciones nuevas para Ari y las desviaciones de abajo.
- **Notas para la fase 3** (no se hacen aquí): excluir `/privacidad/` del sitemap mientras siga `noindex` (`sitemap({ filter })` en `astro.config.mjs`) y quitar `noindex` de `privacidad.astro` cuando exista el texto legal; cuando Ari entregue las redes, el YAML de `footer.social` pasa a lista con las URLs en `config`.

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`. Casos (brief, regla de copy): **A** existe en el doc y pasa las guardas, se muestra tal cual; **B** nombre estructural tomado de CONTEXT o del UI-SPEC y no del doc, se muestra tal cual y `pending` para que Ari lo confirme; **C** falta en el doc, relleno visible 'FALTA CONFIRMAR' con `pending`, `confirm_by: Ari` y un `reason` que dice qué entregar; **D** microcopy estructural que nombra una zona de la interfaz y no afirma nada, `verified`.

| Ranura | Origen | Caso | Estado en el YAML |
|--------|--------|------|-------------------|
| `agenda.title` | Doc, `## 10. CTA final`, Headline: '¿Listo para que te encuentren cuando te estan buscando?' (errata 'estan' tal cual) | A | `verified`; reemplaza 'Agenda tu llamada' |
| `agenda.intro` | Doc, `## 10. CTA final`, Cuerpo | A | sin cambios (`pending` por depender de `call.duration`) |
| `agenda.fallback_lead`, `fallback_link`, `iframe_title`, `noscript` | Fase 1 | D | sin cambios |
| `for_whom.title` | El doc no trae la sección | C | `pending` |
| `for_whom.is_for.title` ('Para quién es') y `is_not_for.title` ('Para quién no es') | CONTEXT y UI-SPEC, tabla de copy | B | `pending`, `reason`: nombre tomado de CONTEXT |
| `for_whom.is_for.items[0..2]` y `is_not_for.items[0..2]` | El doc no trae la sección | C | `pending`; el `reason` de `is_for.items[0]` pide comunicar el perfil de USD 200k o más al año y todos piden no publicar rangos de inversión |
| `faq.title` | El doc no trae FAQ | C | `pending` |
| `faq.items[0..5].question` y `.answer` | El doc no trae FAQ; temas de CONTEXT | C | `pending`; el `reason` de cada par nombra su tema (qué es GEO, duración de la llamada, qué preparar, inversión, tiempos de resultados, si aplica a mi negocio) y el de inversión pide no publicar cifras |
| `footer.nav_label` ('Enlaces del pie de página'), `contact_label` ('Contacto'), `social_label` ('Redes') | Nombres de bloque de CONTEXT ('contacto, redes') | D | `verified` |
| `footer.email` y `footer.social` | El doc no trae correo ni redes | C | `pending` |
| `footer.privacy_link` ('Política de privacidad') y `privacy.title` (mismo texto, h1) | CONTEXT y REQUIREMENTS lo fijan | D | `verified` |
| `privacy.body[0]` | El doc no trae la política | C | `pending` |
| Línea de marca del footer | El doc no la trae | n/a | no se agrega (UI-SPEC: 'si el doc lo trae') |

Ninguna cadena nueva dispara VOSEO, DASH, AEO, VERIFICAR ni INVERSION (se comprueba en las Tareas 1 y 2). Filas nuevas en `PENDING-COPY.md`: 25 (9 de Para quién es, 13 de FAQ, 2 del footer, 1 de privacidad). La página muestra 'FALTA CONFIRMAR' en 22 ranuras de `/` (7 + 13 + 2) y en 1 de `/privacidad`.

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **Titular del CTA final con la errata del doc**: se muestra 'te estan buscando' sin tilde, tal cual el doc (regla del brief: no se corrige en silencio); REQUIREMENTS y el UI-SPEC lo escriben con tilde. Se reporta a Ari en el SUMMARY. Revertir: cambiar el texto de `agenda.title` a la forma con tilde cuando Ari corrija el doc.
2. **Títulos de sección y ranuras del FAQ en 'FALTA CONFIRMAR'**: el doc no trae título de Para quién es ni de FAQ, y 'Preguntas frecuentes' no aparece en CONTEXT ni en REQUIREMENTS, así que no se inventa. Los seis resúmenes y las seis respuestas se leen igual entre sí ('FALTA CONFIRMAR'); el tema de cada par vive en el `reason` de `PENDING-COPY.md`. Revertir o mejorar: cuando Ari entregue las preguntas, se reemplazan los textos y se corre `npm run pending`.
3. **Nombres de columna en `pending`** ('Para quién es', 'Para quién no es'): el UI-SPEC dice 'nombres de CONTEXT; confirmar con el doc'. Si Ari los confirma, pasan a `verified`.
4. **Claves `is_for` e `is_not_for`** en lugar de `yes` y `no`: evita la lectura booleana de YAML 1.1 en herramientas que no usan `yaml` 2.
5. **`Faq.astro` no usa `SectionShell`**: el shell de 02-01 pone el h2 encima de un cuerpo de una sola columna y el FAQ desde 1024 px lleva el h2 a la izquierda. Usa `<section id='faq'>` propio con la clase `.section-title` de `global.css` (que 02-01 dejó para este caso) y `padding-block: var(--section-y)`. `ForWhom.astro` sí usa `SectionShell` (h2 arriba y dos columnas debajo). Si el shell gana una variante de dos columnas, el FAQ puede migrar.
6. **Anillo de foco del `<summary>` interior**: el UI-SPEC dice anillo oscuro con offset 2 px; con el borde de 3 px de la tarjeta, un anillo exterior de 5 px deja solo 2 px visibles sobre el amarillo. Se dibuja dentro de la tarjeta blanca (offset negativo, color del tono `light` anidado) y se mide con estilos calculados. Revertir: `outline-offset: var(--focus-offset)` y tono amarillo en el `details`.
7. **Icono del FAQ sin giro**: el UI-SPEC lo lista en el presupuesto de movimiento (150 ms), que es de DSGN-05 y del plan 07; aquí el icono cambia de plus a minus al instante para no romper la cuenta de cero animaciones de 02-01.
8. **Segundo skip link omitido en `/privacidad`** (el UI-SPEC permite omitirlo o enlazar a `/#agenda`): un salto a otra página no salta contenido repetido y el CTA del header ya ofrece `/#agenda`. Revertir: renderizarlo con `href='/#agenda'`.
9. **Correo con `mailto:` solo si pasa una expresión estricta** (sin espacios, sin `?` ni `,`); si no, texto plano. Las redes quedan como una sola ranura de texto plano porque no hay datos (redes y URLs se modelan cuando Ari las entregue).
10. **Guarda INVERSION como regla de contenido, con excepción `verified` y alcance por ruta**: bloquea producción y solo avisa en otros entornos (como VOSEO y AEO); una afirmación `verified` la pasa, porque `verified` significa que Ari la aprobó (regla de estados del YAML). Solo mira las rutas `for_whom.*` y `faq.*` (constante `INVERSION_PATH_PREFIXES`): los casos de éxito del doc traen cifras en $ que son ventas y no inversión ('De $41K a $76K en ventas'), y una regla global daría falsos positivos en el plan 04. Ampliar el alcance es cambiar esa constante.
11. **Enlace del pie a `/privacidad/` con barra final**: formato de directorio de Astro y hosts estáticos, sin salto de redirección. `startsWith('/privacidad')` decide `aria-current`.
12. **`a11y-base.spec.ts` y `cta-focus.spec.ts`**: ninguno afirma el texto del h2 de `#agenda` (usan el id `agenda-title`, que no cambia), así que `cta-focus.spec.ts` no se edita y de `a11y-base.spec.ts` solo cambian el orden de tabulación (casos (a) de 1280 y 390 px), el tipo del YAML y el mapeo de paradas. El aserto que exigía que la página no contuviera 'FALTA CONFIRMAR' (bloque 'textos de Ari visibles') y la prueba 9b de `copy.test.mjs` son del plan 02-03, único editor de ambas (derivadas del YAML, se ajustan solas cuando se agregan rellenos): este plan solo las ejecuta y reporta su resultado, nunca las edita. Si fallan por las afirmaciones de este plan, se reporta el detalle al coordinador y no se toca ninguna.
13. **Padding de `#agenda`**: 02-01 ya subió `--section-y` a 96 px desde 1024 px y `AgendaSection` usa `padding-block: var(--section-y)`; este plan no lo redefine y lo vuelve a medir en 64, 768, 1023, 1024 y 1280 px.
14. **Footer fuera de `main`**: `BaseLayout` lo monta tras `</main>`, así que `page-structure.spec.ts` (que lee `main > section`) no lo cuenta y sus h2 siguen siendo los 11 del UI-SPEC.
15. **h1 de `/privacidad` en tamaño Heading** (no Display): es una página de texto; se usa `--text-heading` sin agregar un sexto tamaño.

## UI Considerations (lift del UI-SPEC para este plan)

- **covered** (ya en `must_haves.truths` como texto): empty (toda ranura sin dato muestra 'FALTA CONFIRMAR'), long-text y overflow (`overflow-wrap: anywhere`, `min-width: 0`, sin scroll horizontal a cinco anchos, espaciado de texto forzado), zero-one-many (FAQ de 5 a 6 con el esquema como tope), loading (iconos y collage en SVG en línea, sin recursos externos), error (iframe de ClickUp sin cambios), a11y-motion (cero animaciones con ambas preferencias) y a11y-color (tarjeta blanca sobre amarillo o blanco con borde oscuro de 3 px, iconos oscuros sobre amarillo con 10.22, columnas distinguibles sin color). Las listas estilizadas con `list-style: none` de Para quién es llevan `role='list'` (fila interaction de las listas), afirmado por prueba.
- **backstop** (en `must_haves.truths` como `{statement, verification: backstop}`): fila interaction del `<summary>` (estado anunciado y rol con `list-style: none`), que necesita lector de pantalla y queda anotada para Juan en `02-VISUAL-LOG.md`.
- **No aplican a este plan**: partial (MetricCard, plan 04), visual del `HeroCollage` a 320 px (plan 01). La fila visual de `AgendaCollage` se cubre con una prueba de no superposición con el formulario.
- **unresolved que este plan resuelve o asume**: copy (mapeada ranura por ranura en la tabla de arriba; el doc trae el CTA final y nada más de este plan) y assets (el footer usa el `Logo` de 02-09 en su tono `light`; el color de marca ya lo decidió Juan y Ari lo confirma después).

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Estado |
|--------|------|--------|
| GOAL (ROADMAP fase 2) | Landing completa con identidad de marca, de 320 a 1280 px, todo el copy tal cual del doc de Ari | Para quién es, FAQ, CTA final, footer y privacidad aquí; el resto en los planes 01 a 05 y 07 |
| REQ | CONT-10, CONT-11, CONT-12, CONT-13 | Tareas 2 (CONT-10, CONT-11), 1 y 3 (CONT-12), 3 (CONT-13) |
| REQ | COPY-01 | Todas las tareas (tabla de ranuras, erratas reportadas, ranuras en 'FALTA CONFIRMAR') |
| REQ | DSGN-04 | Tareas 2 y 3 (lotes D06 y E06 con `impeccable` y `design-taste-frontend`) |
| RESEARCH | No existe `02-RESEARCH.md` en esta fase; el UI-SPEC y `.planning/research/` son la fuente | Sin ítems pendientes |
| CONTEXT | Secciones informativas (Para quién es sin rangos de inversión, FAQ con `<details>`, footer y privacidad), textos pending, orden de la página, CTA final por sí misma, 96 px, `AgendaCollage` estático | Este plan |
| CONTEXT | Guarda contra rangos de inversión (hallazgo 6 del UI-SPEC: 'el planner agrega una guarda') | Tarea 2 (regla INVERSION) |
| UI-SPEC | Secciones 10, 11, 12 y 13, Interaction Contract (orden de tabulación, estados del `<summary>`), Responsive, Verification Hooks, Design Method (lotes D y E) | Este plan; el giro del icono del FAQ es del plan 07 |
| Fuera de este plan (no son huecos) | Tokens, `SectionShell`, `CtaLink` (02-01); sprite y estructura del collage (02-02); logo, colores y favicon (02-09); collage, píldoras y avatares (02-10); fotos (02-11); problema a cómo funciona (02-03 a 02-05); movimiento, `critique` de página completa y `polish` (02-07); sitemap, SEO y QA final (fase 3) | Diferido a esos planes |

## Presupuesto de contexto (17 archivos en `files_modified`, unos 12 con autoría real)

Cinco casi no tienen autoría: `PENDING-COPY.md` lo genera `npm run pending`, `index.astro`, `SkipLinks.astro` y `SiteHeader.astro` reciben ediciones de pocas líneas, y `02-VISUAL-LOG.md` es un registro breve. Estimación de 95k tokens (confianza baja, sin historial de calibración): Tarea 1 unos 25k, Tarea 2 unos 42k y Tarea 3 unos 28k. La iteración visual pesa menos que en 02-01 porque las piezas son tarjetas y listas sin collage nuevo: el ejecutor mira solo los recortes de 390 y 1280 px de cada sección (`recorte`) y comprueba 320, 768 y 1024 px con las mediciones de los specs, abriendo una captura solo si la medición falla.

Puntos de corte: cada tarea termina con el build verde y commiteada. Tras la Tarea 1 (tracer) y tras la Tarea 2, si el contexto ya pasó de 50 %, el ejecutor hace commit, escribe `02-06-SUMMARY.md` con `status: partial` (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. Si el checker exige partir el plan por tamaño (17 archivos), el corte limpio es el límite de la Tarea 2: **06a** = Tarea 2 (Para quién es, FAQ, guarda INVERSION y orden de tabulación; su tracer sería el FAQ con un solo ítem de punta a punta) y **06b** = Tareas 1 y 3 (privacidad, footer y `#agenda`), sin archivos compartidos de código salvo `landing.es.yaml`, `content.config.ts`, `PENDING-COPY.md` y `closing-sections.spec.ts`, así que irían en waves distintas.

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/`, `public/` o `astro.config.mjs`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; este plan no lo usa). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; `PUBLIC_ENV=production` falla solo por PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`).
- Las pruebas derivan los textos y las cantidades esperadas del YAML (lectura con `yaml`, mismo criterio de `walkClaims` y `fill`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- La fase 2 no cambia el comportamiento del iframe de ClickUp; las verificaciones humanas de la fase 1 siguen diferidas.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `landing.es.yaml` a HTML | Texto editado por Ari y el equipo (también desde la web de GitHub) entra a la página; debe salir escapado |
| YAML a `href` | El correo del footer se convierte en `mailto:` si el texto lo parece; un valor arbitrario podría emitir un esquema peligroso |
| Ruta `/privacidad` a buscadores | Página con cuerpo de relleno que no debe indexarse ni competir con la landing |
| Copy editable a publicación | Un rango de inversión escrito por descuido llegaría al visitante como promesa comercial |
| SVG en línea | Iconos escritos en el repositorio, sin datos externos |
| Navegador a terceros | El iframe y el script de ClickUp son las únicas peticiones externas de la página |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering | Textos del YAML impresos en Para quién es, FAQ, footer y privacidad | medium | mitigate | Astro escapa las expresiones de texto; los componentes imprimen `.text` como nodo de texto; criterio con grep: cero `set:html` en `src` |
| T-02-06-02 | Tampering | `mailto:` del footer construido desde `footer.email` | medium | mitigate | `SiteFooter.astro` solo emite el enlace si el texto cumple una expresión estricta de correo (sin espacios, `?`, `,` ni `;`); si no, texto plano; prueba con un valor inyectado en una copia temporal del YAML |
| T-02-06-03 | Information Disclosure | `/privacidad` indexada con el cuerpo 'FALTA CONFIRMAR' | medium | mitigate | `noindex` en cada entorno (también con `PUBLIC_ENV=production`) y sin canonical; comprobado en dos builds; la exclusión del sitemap queda como nota de la fase 3 y el build de producción sigue bloqueado por PENDING y MISSING |
| T-02-06-04 | Tampering | Rangos de inversión mensual publicados sin aprobación de Ari | high | mitigate | Regla INVERSION en `copy-rules.mjs` (bloquea producción, avisa en otros entornos) con pruebas por mutación; las ranuras de inversión nacen en 'FALTA CONFIRMAR' |
| T-02-06-05 | Information Disclosure | Nuevas peticiones a terceros (fuentes, analítica, CDN) desde las secciones nuevas | medium | mitigate | Iconos y collage en SVG en línea; criterio: los únicos hosts `https://` de `dist/index.html` y `dist/privacidad/index.html` son forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG |
| T-02-06-06 | Denial of Service | HTML o JS más pesados (Core Web Vitals) | low | mitigate | HTML de `/` por debajo de 60 KB sin comprimir y cero JS nuevo: la prueba (e) de `cta-focus.spec.ts` sigue pasando con un solo script propio de menos de 3 KB. Presupuesto compartido: 02-10 y 02-11 dejan `dist/index.html` en 42240 bytes como máximo, así que 02-04, 02-05 y 02-06 juntos disponen de unos 19 KB bajo el tope global de 61440; este plan suma `AgendaCollage` (4096 bytes como tope) y dos píldoras, mide y anota en el SUMMARY cuántos bytes suma y, si el tope global peligra, no lo sube: se detiene y reporta a Juan (opciones de la pregunta abierta 4 de 02-10: subir el tope, recortar píldoras o mover el sprite a un archivo estático) |
| T-02-06-07 | Tampering | Ancla o CTA que apunta a un id inexistente en `/privacidad` | low | mitigate | `SkipLinks` y `SiteHeader` deciden por ruta; prueba: todo `href='#...'` de `/privacidad` resuelve a un id existente y el CTA del header lleva `/#agenda` |
| T-02-06-08 | Repudiation | Copy `pending` que Ari no ve y aprueba a ciegas | low | mitigate | `npm run pending` regenera `PENDING-COPY.md` con las 25 filas nuevas y `--check` lo compara byte a byte |
| T-02-06-09 | Spoofing | Iframe de ClickUp (tercero) dentro de `#agenda` | low | accept | Sin cambios respecto a la fase 1: mismo origen y atributos (`referrerpolicy`, `title`); este plan no toca el iframe |

Este plan no instala paquetes (UI-SPEC, Registry Safety: sin dependencias nuevas), por lo que no hay fila de cadena de suministro.
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de /privacidad de punta a punta con el primer corte del footer y capas conscientes de la ruta</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, src/components/SiteFooter.astro, src/layouts/BaseLayout.astro, src/pages/privacidad.astro, src/components/SiteHeader.astro, src/components/SkipLinks.astro, tests/e2e/closing-sections.spec.ts</files>
  <precondition>Existen `src/components/CtaLink.astro` con la prop `href` (02-01) y `src/components/brand/Logo.astro` de 02-09 (props `name`, `href?`, `variant?`, `tone?` y `class?`) con el header ya conectado (`SiteHeader.astro` trae la constante `tone` que alimenta su `data-tone` y la prop `tone` del logo); si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 13. Footer y privacidad`, `## Copywriting Contract`, `## Interaction Contract` (orden de tabulación y foco a `#agenda`) y `## Page Architecture` (landmarks); ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: las notas del final (líneas 199 a 205), para confirmar que el doc no trae footer ni política de privacidad
    - src/layouts/BaseLayout.astro, src/components/SiteHeader.astro (estado tras 02-09), src/components/SkipLinks.astro, src/components/CtaLink.astro (prop `href` de 02-01), src/lib/site.ts, src/content.config.ts, src/content/landing.es.yaml
    - tests/e2e/a11y-base.spec.ts (líneas 1 a 66: lectura del YAML y `activeStop`) y tests/e2e/page-structure.spec.ts (solo leer: `PAGE_ORDER` y herramienta de capturas; no se edita)
    - scripts/lib/copy-rules.mjs (`MISSING_MARK`, `walkClaims`)
  </read_first>
  <action>
    Implementa CONT-12 en su recorte más fino, de punta a punta por todas las capas del plan (copy, esquema, guarda de pendientes, componente, layout, página nueva, cabecera, skip links y prueba de navegador). Las secciones, el resto del footer y la iteración visual son de las Tareas 2 y 3. Antes de editar, anota `git rev-parse HEAD` como BASE para los criterios de alcance.

    Skills primero (per DSGN-04, regla de Juan). Con la herramienta Skill invoca `impeccable` (carga `reference/craft-floor.md`, corre `context` y lee `PRODUCT.md` y `DESIGN.md` de la raíz, que generó 02-01) y `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4. Si esta tarea y las siguientes corren en ejecutores distintos, cada uno las vuelve a invocar. El pie y la página de privacidad son del lote E de `impeccable` (verbos layout, colorize y harden); aquí solo se levanta la estructura.

    Copy (per COPY-01 y la tabla de ranuras). En `src/content/landing.es.yaml` agrega `footer` con `nav_label` ('Enlaces del pie de página', `verified`) y `privacy_link` ('Política de privacidad', `verified`), y `privacy` con `title` ('Política de privacidad', `verified`) y `body` como lista de una afirmación con el texto exacto 'FALTA CONFIRMAR', `status: pending`, `confirm_by: Ari` y un `reason` que diga que el doc de Ari no trae la política y que Ari entrega el texto legal. En `src/content.config.ts` agrega `footer` con esas dos afirmaciones y `privacy` con `title` y `body` (`z.array(claim).min(1)`), como objetos estrictos. Corre `npm run pending` para regenerar `PENDING-COPY.md`.

    Footer, primer corte (per CONT-12; la Tarea 3 lo completa con logo, contacto y redes). Crea `src/components/SiteFooter.astro`: `<footer class="site-footer" data-tone="light">` con `.wrap` y un `<nav aria-label>` con `footer.nav_label` que contiene un enlace de clase `footer-link` con `href="/privacidad/"` y el texto de `footer.privacy_link`, con `aria-current="page"` cuando `Astro.url.pathname` empieza con `/privacidad`. Estilo local: `border-top: var(--border-pop)`, `padding-block: var(--section-y)`; el enlace en `inline-flex`, `min-height: var(--target-min)`, `color: var(--link)`, subrayado permanente de 2 px con `text-underline-offset: 4px` que engrosa a 3 px en hover. Sin encabezados, sin texto que no salga del YAML y sin JavaScript.

    Layout (per la ruta). En `src/layouts/BaseLayout.astro` agrega las props `noindex?: boolean` y `path?: string` (por defecto `/`); la etiqueta `robots` con `noindex` se emite cuando `!isProduction || noindex`; el `<link rel="canonical">` solo cuando `isProduction`, no es `noindex` y `canonicalUrl(path)` existe; monta `<SiteFooter />` justo después de `</main>`. No cambies nada más del layout (fuentes, skip links, header).

    Página (per CONT-12). Crea `src/pages/privacidad.astro`: `getLanding()`, `<title>` compuesto como `privacy.title` y `brand.name` separados por dos puntos y un espacio (mismo criterio que `meta.title_template`), y `<BaseLayout title noindex path="/privacidad/">` con una `<section id="privacidad" class="privacy" aria-labelledby="privacy-title" data-tone="light">` con `.wrap`, un único `<h1 id="privacy-title">` con `privacy.title` y un `<p>` por cada afirmación de `privacy.body`, impresos como nodos de texto. Estilo local: `padding-block: var(--section-y)` y el h1 en `--text-heading` con su interlineado (Heading, sin sexto tamaño). Deja en el frontmatter un comentario que diga que el `noindex` se quita cuando exista el texto legal (nota de la fase 3).

    Cabecera y saltos (per CONT-12, CONT-13). En `src/components/SiteHeader.astro` define `ctaHref` como `#agenda` cuando `Astro.url.pathname` es `/` y `/#agenda` en las demás rutas, y pásalo como `href` a `CtaLink`; conserva el Logo, la constante `tone` que 02-09 conecta al `data-tone` del header y a la prop `tone` del logo, y todo lo de 02-02. En `src/components/SkipLinks.astro` renderiza el segundo salto (`#agenda`) solo cuando `Astro.url.pathname` es `/`: en otras rutas el destino no existe y el CTA del header ya ofrece `/#agenda`.

    Prueba (per lección 3 y T-02-06-03/07). Crea `tests/e2e/closing-sections.spec.ts` con el patrón de lectura del YAML de `a11y-base.spec.ts` (parseo con `yaml`), la constante `PRIVACY_PATH` con `/privacidad/` y un bloque de `/privacidad` que afirme, a 1280x800: una respuesta 200; un solo h1 con el texto de `privacy.title` y ningún h2; un `<p>` por elemento de `privacy.body` con su texto del YAML; la etiqueta `robots` con `noindex` y ningún `link[rel=canonical]`; el CTA del header con `href="/#agenda"` (y en `/` con `#agenda`); un solo skip link (`#main`) frente a dos en `/`; que todo `a[href^="#"]` de la página apunte a un id existente; que el enlace `a[href="/privacidad/"]` del pie de `/` abra esa página y en ella lleve `aria-current="page"`; que el clic en el CTA del header lleve a `/#agenda` con `#agenda-title` enfocado; que no haya scroll horizontal a 320, 390, 768, 1024 y 1280 px; y que `document.getAnimations().length` sea 0 con `reduce` y con `no-preference`. Suma un orden de tabulación por subsecuencia en `/privacidad` (primero `a:#main`; el CTA del header antes que el enlace de privacidad), que la Tarea 3 endurece. Confirma que estas pruebas fallan antes de crear la página.

    Ciclo de ejecución de las reglas heredadas: build, preview en el puerto 4322, specs, `npx astro preview stop`. Después, un build de producción solo para comprobar `noindex` (ver `<verify>`) y un build normal para dejar `dist` coherente.
  </action>
  <verify>
    <automated>npm run pending && node scripts/list-pending.mjs --check && node --test tests/guards/*.test.mjs && npm run build</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0 (el archivo versionado difiere del que se generaría)</fails_when>
    <automated>PUBLIC_ENV=production PUBLIC_SITE_URL=https://loopsgrowth.example npx astro build && node -e "const fs=require('fs');const p=fs.readFileSync('dist/privacidad/index.html','utf8');const h=fs.readFileSync('dist/index.html','utf8');const noindex=/<meta[^>]*name=\"robots\"[^>]*noindex/;const canon=/rel=\"canonical\"/;process.exit(noindex.test(p)&&!canon.test(p)&&canon.test(h)&&!noindex.test(h)?0:1)" && npx astro build</automated>
    <fails_when>non-zero exit: en el build de producción `/privacidad` no lleva `noindex`, lleva canonical, o `/` pierde su canonical o gana `noindex`; el último `npx astro build` deja `dist` en modo no productivo para las pruebas de navegador</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/closing-sections.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts</automated>
    <fails_when>non-zero exit de Playwright o falla alguna prueba de `closing-sections.spec.ts` o de la fase 1 (estas dos deben pasar sin haber sido editadas); si `astro preview` no levanta porque el puerto 4322 está ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>`/privacidad/` es una página real con un solo h1, cuerpo 'FALTA CONFIRMAR' y `noindex` en todo entorno, alcanzable desde el enlace del pie de `/`; el header y los saltos se ajustan a la ruta; el layout monta el footer en ambas rutas; `/` sigue indexable en producción; las pruebas de la fase 1 siguen en verde sin editarse y la lista de pendientes trae las filas nuevas.</done>
  <acceptance_criteria>
    - `grep -c "^| privacy\.body\[0\]" PENDING-COPY.md` devuelve 1 y `node scripts/list-pending.mjs --check` sale 0; `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)` no lista violaciones estructurales ni reglas distintas de PENDING y MISSING en las rutas `footer.*` y `privacy.*`
    - `test -f dist/privacidad/index.html`, con exactamente un `<h1` y ningún `<h2` (contados sobre el HTML construido) y sin ninguna etiqueta `<script` propia
    - El build de producción con `PUBLIC_SITE_URL` deja `noindex` y ningún canonical en `dist/privacidad/index.html` y el canonical (sin `noindex`) en `dist/index.html`; el build final es no productivo
    - Compuerta de higiene sobre `src/components/SiteFooter.astro src/components/SiteHeader.astro src/components/SkipLinks.astro src/layouts/BaseLayout.astro src/pages/privacidad.astro`: `grep -n -E "set:html|outline: none|\.status\b|#[0-9a-fA-F]{6}\b" <archivos> | grep -v -E ":[0-9]+:\s*(//|/\*|\*|<!--)" | wc -l` devuelve 0
    - `git diff --name-only BASE` lista solo archivos de esta tarea (los de `<files>`, `PENDING-COPY.md` incluido) y no toca `src/components/AgendaSection.astro`, `src/scripts`, `src/styles`, `public`, `tests/e2e/page-structure.spec.ts` ni archivos de otros planes
    - `closing-sections.spec.ts` pasa con ClickUp bloqueado (`npm run test:e2e:isolated`): un skip link y CTA `/#agenda` en `/privacidad`, dos skip links y CTA `#agenda` en `/`, todo ancla interna con destino, cero animaciones con ambas preferencias, sin scroll horizontal a cinco anchos
    - `a11y-base.spec.ts` y `cta-focus.spec.ts` pasan sin haber sido editados (incluida la prueba (e): un solo script propio de menos de 3 KB)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Para quién es, FAQ con details nativos y guarda INVERSION</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, scripts/lib/copy-rules.mjs, tests/guards/copy.test.mjs, src/components/sections/ForWhom.astro, src/components/sections/Faq.astro, src/pages/index.astro, tests/e2e/closing-sections.spec.ts, tests/e2e/a11y-base.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <precondition>Existen `src/components/ui/SectionShell.astro` con las props `id`, `tone`, `title` y el slot por defecto, la clase `.section-title` en `global.css` (02-01) y la herramienta de capturas de `page-structure.spec.ts`; si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 10. Para quién es y para quién no`, `### 11. FAQ`, `## Reference Synthesis` (filas Para quién es y FAQ), `## Interaction Contract` (orden de tabulación y estados del `<summary>`), `## Responsive`, `## Design Method and Iteration Protocol` (con la lista de vibra) y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md: `### Secciones informativas`
    - src/components/ui/SectionShell.astro, src/styles/tokens.css y src/styles/global.css (estado tras 02-01: tonos, `--card-pad`, `--card-gap`, `--radius-card`, `.section-title`), DESIGN.md (regla de tarjeta blanca sobre amarillo o blanco)
    - src/pages/index.astro, src/content/landing.es.yaml y src/content.config.ts (estado tras 02-05)
    - scripts/lib/copy-rules.mjs (`checkCopy`, `matchesOf`, `cleanText`, `NOT_WORD_BEFORE`, `findMissingMark`) y tests/guards/copy.test.mjs (helpers `run`, `docWith` y `claimYaml`; las pruebas 9b y 10 se leen solo para ejecutarlas y verificarlas: la 9b es de 02-03)
    - tests/e2e/a11y-base.spec.ts (líneas 1 a 100 y 184 a 200: tipo `es` del YAML, `activeStop`, `tabUntilIframe` y orden de tabulación; el bloque 'textos de Ari visibles' con `checkTexts`, líneas 440 a 455, se lee solo para ejecutarlo y verificarlo: es de 02-03)
  </read_first>
  <behavior>
    - INVERSION marca, en `for_whom.*` y `faq.*` y con `status: pending`, los textos '4 a 5k al mes', '4-5k al mes', '1.5k al mes', '+1500 al mes', 'USD 1500' y 'desde $2,000'; en producción bloquea (código 1) y en otro entorno avisa (código 0)
    - INVERSION no marca la misma cadena con `status: verified`, ni en otra ruta (por ejemplo `cases.*` con 'De $41K a $76K en ventas'), ni 'USD 200k o más al año', 'facturación de 200k al año', '6-12 meses' ni '2 veces al mes'
    - Con el YAML real, la guarda no reporta INVERSION y las rutas nuevas solo reportan PENDING y MISSING
    - `#para-quien` muestra dos tarjetas (es, no es) y `#faq` muestra un `<details>` por elemento de `faq.items`, todos cerrados al cargar, que Enter y Espacio abren y cierran
  </behavior>
  <action>
    Implementa CONT-10 y CONT-11 con la guarda que pide el hallazgo 6 del UI-SPEC. Antes de editar, anota `git rev-parse HEAD` como BASE.

    Skills primero (per DSGN-04). Con la herramienta Skill invoca `impeccable` (lote D, parte 06: verbos shape, layout, clarify y adapt; carga `reference/craft-floor.md` y lee `PRODUCT.md` y `DESIGN.md`) y `design-taste-frontend` con la lectura declarada de la Tarea 1 y los diales 7, 3 y 4. Antes de escribir componentes, agrega a `02-VISUAL-LOG.md` la entrada "Lote D (parte 06), ronda 0" con la lectura y los verbos.

    Guarda primero (rojo, per CONT-10 y COPY-01). En `tests/guards/copy.test.mjs` agrega pruebas por mutación con los helpers existentes: cada cadena de la sección `<behavior>` marcada y no marcada, en producción y en local, más una prueba sobre el YAML real sin INVERSION. Solo agrega pruebas nuevas al final del archivo y no borres ni cambies ninguna existente: la prueba 9b (y la 10) son del plan 02-03, único editor de la 9b, que la deriva del YAML. Ejecútala junto con el resto y reporta su resultado; si falla por las afirmaciones de este plan, no la edites: anota la falla y su detalle para el SUMMARY y para el coordinador. Después, en `scripts/lib/copy-rules.mjs`, agrega y exporta `INVERSION_PATH_PREFIXES` (`for_whom.` y `faq.`), `INVERSION_MONTHLY_RE` (un monto con miles por punto o coma, decimales y sufijo `k` o `mil`, opcionalmente seguido de un segundo monto unido por `a`, `-`, `y` o `hasta`, y después, con moneda opcional, `al mes`, `por mes`, `mensual`, `mensuales` o `/mes`), `INVERSION_CURRENCY_RE` (`USD`, `US$` o `$` con un monto, salvo que le siga `al año` o `anual`, con `o más` opcional en medio) y `findInversion(text)`, que devuelve extractos con `matchesOf`. En `checkCopy`, para cada afirmación cuya ruta empiece con un prefijo y cuyo `status` no sea `verified`, agrega una violación `INVERSION` al arreglo `content`, con el texto limpio de las demás reglas. Documenta en el encabezado del archivo el motivo (hallazgo 6, CONTEXT) y el alcance, sin dar por hecho que Ari aprobó nada.

    Copy (per COPY-01 y la tabla de ranuras). En `src/content/landing.es.yaml`, en el orden de la página (tras las secciones de los planes 03 a 05 y antes de `agenda`), agrega `for_whom` con `title` ('FALTA CONFIRMAR'), `is_for` y `is_not_for` (cada una con `title`, 'Para quién es' y 'Para quién no es', y `items` de tres afirmaciones 'FALTA CONFIRMAR'), y `faq` con `title` ('FALTA CONFIRMAR') e `items` de seis objetos `{question, answer}` con el texto 'FALTA CONFIRMAR'. Todas `pending` con `confirm_by: Ari` y un `reason` que nombra la ranura y lo que debe entregar Ari: el primer ítem de `is_for` debe comunicar el perfil de USD 200k o más al año; los ítems de las dos columnas y la respuesta de inversión piden expresamente no publicar rangos ni cifras de inversión sin aprobación; el `reason` de cada pregunta y respuesta del FAQ nombra su tema en este orden: qué es GEO, duración de la llamada, qué preparar, inversión, tiempos de resultados y si aplica a mi negocio. Los dos nombres de columna llevan `reason` "Nombre tomado de CONTEXT; el doc de Ari no trae esta sección". No agregues pregunta de garantía. En `src/content.config.ts` agrega `for_whom` (con `is_for` e `is_not_for` de `title` e `items` de al menos una afirmación) y `faq` (`title` e `items` como `z.array(z.strictObject({ question: claim, answer: claim })).min(5).max(6)`). Corre `npm run pending`.

    Para quién es (per CONT-10 y la fila de la síntesis). Crea `src/components/sections/ForWhom.astro` con `SectionShell` (`id="para-quien"`, `tone="light"`, `title` desde `for_whom.title`) y en el slot por defecto un contenedor de rejilla con dos tarjetas en el orden del DOM 'es' y 'no es'. Cada una: un `<h3>` con id propio y su título, y un `<ul role="list" aria-labelledby>` cuyos `<li>` llevan un icono SVG en línea (check para 'es', x para 'no es'; 24 px, `aria-hidden="true"`, `focusable="false"`, trazo `currentColor`, mismo estilo que el icono externo de `AgendaSection`) y el texto del ítem impreso como nodo de texto. La tarjeta 'es' (clases `fw-card fw-is-for`, `data-tone="yellow"`) lleva fondo amarillo por el tono, `border: var(--border-pop)`, `box-shadow: var(--shadow-pop)` y `border-radius: var(--radius-card)`; la de 'no es' (`fw-card fw-is-not-for`, `data-tone="light"`) es blanca, con `border: 3px dashed var(--color-brand-dark)` y sin sombra. Padding `var(--card-pad)`. Rejilla de una columna bajo 40em y de dos `minmax(0, 1fr)` con `gap: 1.5rem` desde 40em, `align-items: stretch` para igualar la altura; hijos con `min-width: 0` y `overflow-wrap: anywhere`. Sin CTA, sin enlaces, sin hover, sin cursor de puntero y sin transición: las tarjetas no son interactivas.

    FAQ (per CONT-11 y la fila de la síntesis). Crea `src/components/sections/Faq.astro` con su propia sección (per la desviación 5 de Supuestos): `<section id="faq" aria-labelledby="faq-title" data-tone="yellow">` con `padding-block: var(--section-y)`, un `.wrap` en rejilla de una columna con `gap: 2rem` que desde 64em pasa a `minmax(0, 5fr) minmax(0, 7fr)` con `column-gap: 3rem`, el `<h2 id="faq-title" class="section-title">` con `faq.title` (a la izquierda, `align-self: start`) y una lista (`div` con `display: grid` y `gap: 1rem`) de un `<details class="faq-item" data-tone="light">` por elemento de `faq.items`, sin el atributo `name` y sin `open`. Cada uno: `<summary class="faq-summary">` con la pregunta en un `<span class="faq-question">` y un `<svg class="faq-icon">` de 24 px (`aria-hidden`, `focusable="false"`) con un disco `faq-icon-disc`, la barra horizontal y una barra vertical de clase `faq-icon-v` que `details[open]` oculta (plus cerrado, minus abierto); y `<div class="faq-answer"><p>` con la respuesta. Estilo: `.faq-item` blanco, `border: var(--border-pop)`, `border-radius: var(--radius-card)` (16 px) y `overflow: visible`; `.faq-summary` en `display: flex; justify-content: space-between; align-items: center; gap: 1rem`, `min-height: var(--target-min)`, padding 1rem (1.5rem desde 40em), `list-style: none`, el marcador nativo de WebKit oculto, `cursor: pointer`, pregunta en peso 700 con `overflow-wrap: anywhere` y `min-width: 0`; hover: el disco se rellena con `var(--color-brand-yellow)` (contorno oscuro); foco: anillo sólido de `var(--focus-width)` con `outline-offset` negativo igual a la suma del ancho y `var(--focus-offset)`, para que quede dentro de la tarjeta (desviación 6); `.faq-answer` con `border-top: var(--border-pop)` y el mismo padding. Sin JavaScript, sin transición en ninguna propiedad y sin cambiar el marcado nativo de `<details>`. Deja las clases `faq-item`, `faq-summary`, `faq-answer`, `faq-icon` y `faq-icon-v` como ganchos para el plan 07.

    Página (per CONTEXT, orden de las 13 piezas). En `src/pages/index.astro` importa `ForWhom` y `Faq` desde `../components/sections/` y móntalos, en ese orden, justo antes de `<AgendaSection />` y después de lo que dejó el plan 05. No cambies el resto de la composición.

    Pruebas del navegador (per lección 3, sobre `closing-sections.spec.ts`). Agrega bloques con expectativas derivadas del YAML. Para quién es: h2 y h3 con sus textos; ítems en orden; `role="list"` en ambas listas y un icono SVG por ítem; a 390 px una columna con 'es' primero, y a 640, 768, 1024 y 1280 px dos columnas con `column-gap` de 24 px y alturas que difieren 1 px o menos; estilos calculados (tarjeta 'es' `rgb(255, 198, 2)` con borde sólido de 3 px y sombra distinta de `none`; tarjeta 'no es' con borde discontinuo de 3 px y sombra `none`); cero enlaces, botones y `[data-cta]` dentro de `#para-quien`; ningún texto de la sección coincide con un patrón de inversión mensual. FAQ: número de `details` igual a `faq.items.length` (entre 5 y 6); textos de resumen y de respuesta iguales a los del YAML y en orden; todos cerrados al cargar y ninguno con `name`; Enter abre y Espacio cierra el primero; estilos del `summary` (`display: flex`, alto de 44 px o más, padding de 16 px a 390 px y de 24 px a 768 y 1280 px); del `details` (borde de 3 px sólido, radio de 16 px, fondo blanco, separación de 16 px entre tarjetas cerradas); icono de 24 px con `.faq-icon-v` visible cerrado y oculto abierto; anillo de foco tras `Tab` con estilo sólido, ancho de 3 px, `outlineOffset` negativo y contenido dentro del rectángulo de la tarjeta; a 1280 px las dos columnas en proporción 5 a 7 (tolerancia de 0,02), `column-gap` de 48 px y el h2 a la izquierda de la lista, y a 1023 px una columna; con `javaScriptEnabled: false`, Enter sobre el primer resumen agrega `open`; `document.getAnimations().length` es 0 con `reduce` y con `no-preference`, antes y después de abrir. Suma un bloque `recorte`, que solo corre con `PHASE2_BATCH` y guarda `test-results/phase2/<lote>-<id>-<ancho>.png` de `#para-quien` y `#faq` a 390 y 1280 px con lo que no es localhost abortado.

    Orden de tabulación (per Interaction Contract; en `tests/e2e/a11y-base.spec.ts` solo cambia esto y nada más). Suma `faq: { items: unknown[] }` al tipo del YAML y una constante con `faq.items.length`; sube el máximo por defecto de `tabUntilIframe` a 24; en el mapeo de paradas, una parada `SUMMARY` se nombra `summary`. En el caso (a) de 1280 px y en el de 390 px, lee el arreglo esperado tal como lo dejaron los planes 03 y 04 (con `cta:solucion` y `cta:casos` entre el CTA del hero y el enlace de respaldo) e inserta tantos `summary` como resúmenes haya, justo antes del enlace de respaldo; ajusta los títulos de los casos. El bloque 'textos de Ari visibles' (`checkTexts`) es del plan 02-03 y no se edita: se ejecuta junto con el resto y se reporta su resultado. Si falla porque la página ahora muestra el relleno de este plan, no lo toques: anota el aserto y el texto que lo dispara para el SUMMARY y para el coordinador.

    Ciclo del lote D06 (per protocolo del UI-SPEC). (1) Construir el lote completo desde el contrato. (2) Una ronda de captura en lote con `PHASE2_BATCH=D06` a los cinco anchos en base, movimiento reducido y sin JavaScript, con ClickUp bloqueado, más los recortes `recorte`; mirar solo los recortes de 390 y 1280 px y comprobar 320, 768 y 1024 px con las mediciones de los specs, mirando una captura solo si la medición falla. (3) `impeccable critique` del lote contra el contrato de las secciones 10 y 11, sus filas de la síntesis de referencias y los rasgos 6, 7 y 8 de la lista de vibra. (4) Corregir todo en un solo lote de cambios, con una ronda de confirmación como máximo; si un criterio de salida sigue fallando tras la confirmación, repetir el ciclo sin acumular retoques abiertos, con tope de tres ciclos: si no se cumple, detenerse y reportar a Juan con capturas. (5) Registrar en `02-VISUAL-LOG.md` la entrada "Lote D (parte 06), ronda N" con verbos, hallazgos, correcciones y rutas de capturas, y anotar el pendiente manual del `<summary>` con lector de pantalla (fila interaction). Al terminar: `npx astro preview stop`.
  </action>
  <verify>
    <automated>node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check && npm run build && node scripts/check-contrast.mjs</automated>
    <fails_when>non-zero exit, o el resumen de `node --test` muestra "# fail" mayor que 0 (incluidas las pruebas por mutación de INVERSION y la 9b, que es de 02-03 y solo se verifica: si falla, no se edita, se reporta), o `--check` sale distinto de 0, o el build falla por el esquema (`for_whom` o `faq` mal formados)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const mine=r.content.filter(v=>/^(for_whom|faq)\./.test(v.path));const ok=r.structural.length===0&&mine.length>0&&mine.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&r.content.every(v=>v.rule!=='INVERSION');process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING y MISSING en las rutas nuevas (voseo, guion largo, AEO, VERIFICAR o INVERSION), alguna violación INVERSION en todo el YAML, o no aparecen las rutas nuevas</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/closing-sections.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts tests/e2e/page-structure.spec.ts</automated>
    <fails_when>non-zero exit de Playwright o falla alguna prueba de `closing-sections.spec.ts`, de la fase 1 (con el orden de tabulación actualizado) o de `page-structure.spec.ts` (orden y tono de secciones, un solo h1, sin salto de encabezados, sin desborde a cinco anchos, cero animaciones, HTML por debajo de 60 KB); si `astro preview` no levanta porque el puerto 4322 está ocupado, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
    <automated>PHASE2_BATCH=D06 npm run test:e2e:isolated -- tests/e2e/page-structure.spec.ts -g "captura" && PHASE2_BATCH=D06 npm run test:e2e:isolated -- tests/e2e/closing-sections.spec.ts -g "recorte" && test "$(ls test-results/phase2/D06-*.png | wc -l)" -ge 19</automated>
    <fails_when>non-zero exit, o hay menos de 19 capturas del lote D06 (15 de página completa más 4 recortes de `#para-quien` y `#faq`)</fails_when>
  </verify>
  <done>Para quién es y el FAQ están en la página, en su orden y tono, con el relleno 'FALTA CONFIRMAR' donde el doc no trae texto, dos columnas distinguibles sin color y un FAQ de `<details>` nativos que se abre con teclado y sin JavaScript; la guarda INVERSION impide publicar rangos de inversión sin aprobación y está probada por mutación; el orden de tabulación de la fase 1 incluye los resúmenes; el lote D06 pasó por `impeccable` y `design-taste-frontend` y quedó registrado.</done>
  <acceptance_criteria>
    - `node --test tests/guards/*.test.mjs` termina con "# fail 0" e incluye al menos 8 pruebas nuevas de INVERSION (seis marcadas, no marcada con `verified`, no marcada en otra ruta, no marcada 'USD 200k o más al año' y sin INVERSION en el YAML real)
    - `grep -c "^| for_whom\." PENDING-COPY.md` devuelve 9, `grep -c "^| faq\." PENDING-COPY.md` devuelve 13 y `node scripts/list-pending.mjs --check` sale 0
    - `grep -o "<details" dist/index.html | wc -l` devuelve el número de elementos de `faq.items` (6) y `grep -c "<details[^>]* name=" dist/index.html` devuelve 0; `dist/index.html` pesa menos de 61440 bytes
    - Compuerta de higiene sobre `src/components/sections/ForWhom.astro src/components/sections/Faq.astro src/pages/index.astro`: `grep -n -E "set:html|outline: none|\.status\b|#[0-9a-fA-F]{6}\b|transition|animation" <archivos> | grep -v -E ":[0-9]+:\s*(//|/\*|\*|<!--)" | wc -l` devuelve 0
    - `git diff --name-only BASE` lista solo archivos de esta tarea (más `PENDING-COPY.md`) y no toca `src/components/ui`, `src/styles`, `src/components/collage`, `src/components/brand`, `page-structure.spec.ts` ni las secciones de otros planes
    - `closing-sections.spec.ts` pasa con ClickUp bloqueado: dos columnas iguales en altura y con gap de 24 px desde 640 px; tarjeta 'es' amarilla `rgb(255, 198, 2)` con borde sólido y sombra, tarjeta 'no es' con borde discontinuo y sin sombra; FAQ con `summary` de 44 px o más, padding de 16 y 24 px, tarjetas de 3 px y radio de 16 px separadas 16 px, anillo de foco de 3 px dentro de la tarjeta, proporción 5 a 7 desde 1024 px y Enter y Espacio alternando `open` con y sin JavaScript
    - `a11y-base.spec.ts` pasa con el orden `a:#main, a:#agenda, cta:header, cta:hero, cta:solucion, cta:casos, summary x N, a:<form_url>, iframe` a 1280 px y sin `cta:header` a 390 px, con N derivado del YAML, y el bloque 'textos de Ari visibles' se ejecuta y su resultado se reporta sin editarlo
    - Solo verificación de lo que es de 02-03: `D=$(git diff BASE -U0 -- tests/e2e/a11y-base.spec.ts) && printf '%s\n' "$D" | grep -c -E "FALTA CONFIRMAR|checkTexts"` devuelve 0, y `E=$(git diff BASE -U0 -- tests/guards/copy.test.mjs) && printf '%s\n' "$E" | grep -E "^-" | grep -v -E "^---" | wc -l` devuelve 0 (en `copy.test.mjs` solo se agregaron líneas: ninguna prueba existente, incluida la 9b, fue editada); ambas pruebas se ejecutan y su resultado (pasa o falla, con el detalle) va al SUMMARY y al coordinador
    - `PHASE2_BATCH=D06` dejó al menos 19 capturas en `test-results/phase2/` y `02-VISUAL-LOG.md` trae la entrada "Lote D (parte 06)" con verbos, hallazgos, correcciones, rutas de capturas y el pendiente manual del lector de pantalla
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: #agenda como CTA final con AgendaCollage, footer completo y cierre visual del lote E06</name>
  <files>src/components/AgendaSection.astro, src/components/SiteFooter.astro, src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, tests/e2e/closing-sections.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <precondition>Existen `src/components/collage/AgendaCollage.astro` (02-10: prop `class?`, raíz `div[data-collage="agenda"]`, `display: none` bajo 64em) y `src/components/brand/Logo.astro` (02-09: props `name`, `href?`, `variant?`, `tone?` y `class?`, con `tone` por defecto `light`); si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 12. CTA final y formulario`, `### 13. Footer y privacidad`, `## Interaction Contract` (estados de enlaces y foco), `## Responsive`, `## Reference Synthesis` (filas CTA final y Footer) y `## Design Method and Iteration Protocol` (lote E y lista de vibra); ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: `## 10. CTA final` (líneas 180 a 195), para copiar el titular tal cual
    - src/components/AgendaSection.astro, src/components/SiteFooter.astro (estado tras la Tarea 1), src/components/collage/AgendaCollage.astro (contrato de 02-10), src/components/brand/Logo.astro (contrato de 02-09), tests/e2e/lib/brand.ts (`PURPLE_RGB`), src/styles/global.css (`.section-title`) y src/styles/tokens.css (`--section-y`)
    - tests/e2e/closing-sections.spec.ts (estado tras la Tarea 2) y tests/e2e/a11y-base.spec.ts (bloque '#agenda: estructura', casos (d) y (g), solo lectura)
  </read_first>
  <behavior>
    - El h2 de `#agenda` muestra `agenda.title` tal cual del doc (con la errata), conserva `id="agenda-title"` y `tabindex="-1"` y trae la barra de 48x8 px
    - `#agenda` mide 64 px de padding vertical bajo 1024 px y 96 px desde 1024 px, no lleva ningún CTA y el collage nunca toca la tarjeta del formulario
    - El footer muestra logo, contacto, redes y el enlace de privacidad en una, dos o tres columnas según el ancho, con 'FALTA CONFIRMAR' en texto plano donde falta el dato, sin encabezados y sin texto legal ajeno al doc
    - En `/privacidad` el orden de tabulación es exacto: skip 1, logo del header, CTA del header, logo del footer y enlace de privacidad
  </behavior>
  <action>
    Implementa CONT-13 y completa CONT-12. Antes de editar, anota `git rev-parse HEAD` como BASE.

    Skills primero (per DSGN-04). Con la herramienta Skill invoca `impeccable` (lote E, parte 06: verbos layout, colorize y harden; carga `reference/craft-floor.md` y lee `PRODUCT.md` y `DESIGN.md`) y `design-taste-frontend` con la lectura declarada de la Tarea 1 y los diales 7, 3 y 4. Agrega a `02-VISUAL-LOG.md` la entrada "Lote E (parte 06), ronda 0" con la lectura y los verbos.

    Pruebas primero (rojo, per lección 3). En `closing-sections.spec.ts`, con expectativas derivadas del YAML, agrega: `#agenda`: el h2 con el texto de `agenda.title`, `id="agenda-title"`, `tabindex="-1"`, peso 700, color `rgb(255, 255, 255)` y `::before` de 48x8 px; padding vertical calculado de 64 px a 390, 768 y 1023 px y de 96 px a 1024 y 1280 px; ningún `a[data-cta]` dentro de `#agenda` y exactamente cuatro en la página (`header`, `hero`, `solucion`, `casos`); el iframe con `loading="lazy"`, `title` igual a `agenda.iframe_title` y `referrerpolicy`, la tarjeta `.form-embed` con borde de 3 px y el `noscript` presente; el collage (`#agenda .agenda-art [data-collage="agenda"]`, un `div` desde 02-10 y no un svg) con `aria-hidden="true"` y con un `svg.collage-layer` interior con `aria-hidden="true"` y `focusable="false"`, después de `.agenda-fallback` en el DOM, visible a 1024 y 1280 px sin que el rectángulo de esa raíz toque a `.form-embed` ni al iframe (la no superposición se mide sobre la raíz, no sobre el svg interior), con todo lo pintado dentro de su caja (cada `[data-trait]` y cada `[data-pill]` dentro del rectángulo de la raíz, tolerancia de 0.5 px), exactamente dos `[data-pill]` con palabras de `CHIP_WORDS` (importada de `../../src/components/collage/collage-rules.mjs`), sin elementos enfocables dentro, y con `display: none` a 320, 390 y 768 px. Footer: `data-tone="light"`, borde superior de 3 px sólido y padding vertical de `--section-y`; el logo con nombre accesible igual a `brand.name.text`, sin enlace en `/` y con `href="/"` en `/privacidad/` (el enlace del logo en `/privacidad` es el contrato de 02-02 y se conserva), con `data-artboard` `06` y `data-logo-tone` `light` igual al `data-tone` del `<footer>`; `nav` con `aria-label` igual a `footer.nav_label`; correo y redes con su texto del YAML en texto plano mientras sean el relleno (y un `mailto:` solo si el texto del YAML parece un correo); enlace de privacidad con el texto del YAML, `href="/privacidad/"`, alto de 44 px o más, subrayado y color igual a `PURPLE_RGB` (importado de `./lib/brand`, 02-09; nunca un literal rgb); separación de 8 px o más entre enlaces y etiquetas; columnas: una a 390 px, dos a 768 px y tres a 1024 y 1280 px con el bloque de marca 1,2 veces más ancho que cada uno de los otros dos (tolerancia de 0,03) y esos dos iguales; ninguna separación entre el logo y el bloque vecino menor que la altura del propio logo (área de salvado); cero encabezados dentro del `footer`; sin 'todos los derechos reservados' ni un año de cuatro dígitos en su texto. `/privacidad`: orden de tabulación exacto a 1280 px (skip 1, logo del header, CTA del header, logo del footer, enlace de privacidad), sin scroll horizontal a cinco anchos con el footer completo. Extiende el bloque `recorte` con `#agenda`, `footer` y la página `/privacidad/` completa a 390 y 1280 px (ClickUp bloqueado).

    Copy (per COPY-01 y la tabla de ranuras). En `src/content/landing.es.yaml`: `agenda.title.text` pasa a ser el titular del doc, carácter por carácter, '¿Listo para que te encuentren cuando te estan buscando?', conservando `status: verified` y sin corregir la errata (repórtala en el SUMMARY); en `footer` suma `contact_label` ('Contacto', `verified`), `email` ('FALTA CONFIRMAR', `pending`, `confirm_by: Ari`, `reason` que pide el correo de contacto), `social_label` ('Redes', `verified`) y `social` ('FALTA CONFIRMAR', `pending`, `confirm_by: Ari`, `reason` que pide las redes con su nombre y URL). En `src/content.config.ts` extiende `footer` con esas cuatro afirmaciones. Corre `npm run pending`.

    `#agenda` (per CONT-13 y el UI-SPEC sección 12). En `src/components/AgendaSection.astro`: agrega la clase `section-title` al h2 (conserva `id="agenda-title"`, `tabindex="-1"` y el texto desde `agenda.title`); después de `.agenda-fallback`, dentro de `.agenda-copy`, monta un contenedor `.agenda-art` con `aria-hidden="true"` que envuelve `<AgendaCollage class="agenda-collage" />`, en `display: none` por defecto y `display: block` con `margin-top: 2rem` desde 64em; el collage de 02-10 ya trae su propio `display` (`none` bajo 64em y `block` con `width: 100%` desde 64em), su `aspect-ratio` y su `container-type: inline-size` (las píldoras miden en `cqw` sobre esa raíz): no los sobrescribas, y este contenedor solo aporta el margen y, si hace falta, un `max-width`. Deja `padding-block: var(--section-y)` como está (02-01 ya lo sube a 96 px desde 1024 px), la rejilla, la intro, el enlace de respaldo, la tarjeta, el iframe con todos sus atributos, su script y el `noscript` sin cambios, sin lógica de carga diferida y sin ningún `CtaLink`. Actualiza el comentario de cabecera del componente.

    Footer completo (per CONT-12). En `src/components/SiteFooter.astro`, sobre lo de la Tarea 1: importa `Logo`; `homeHref` es `undefined` en `/` y `/` en las demás rutas; estructura `<footer class="site-footer" data-tone="light">` con `.wrap.footer-grid` que contiene `.footer-brand` (`Logo` con `variant="horizontal"`, `tone={tone}`, `name` desde `brand.name.text` y `href={homeHref}`; define `const tone = 'light'` y úsala también en el `data-tone` del `<footer>`, como hace `SiteHeader` desde 02-09, para que no se separen: el logo horizontal solo existe sobre `light` y `resolveLogo` rompe el build con un error en español si se le pide otro tono, y sobre `dark` o `purple` habría que usar la variante `apilado`) y `<nav class="footer-nav" aria-label>` con dos `.footer-block`: contacto (`<p class="footer-label">` con `footer.contact_label` y el correo) y redes (`<p class="footer-label">` con `footer.social_label`, `footer.social` en un `<p class="footer-value">` y el enlace de privacidad de la Tarea 1). El correo se emite como `<a class="footer-link" href="mailto:...">` solo si su texto (recortado) cumple una expresión estricta de correo (caracteres `A-Za-z0-9._%+-` antes de la arroba y un dominio con punto, sin espacios, `?`, `,`, `;` ni `:`); si no, va como `<p class="footer-value">` con el texto tal cual. Estilo local: `.footer-grid` de una columna con `--footer-gap: 2rem` como `gap`; desde 40em, dos columnas `minmax(0, 1fr)` con `.footer-nav` en la segunda; desde 64em, `minmax(0, 1.2fr) minmax(0, 1fr) minmax(0, 1fr)` con `.footer-nav` en `grid-column: span 2` y una rejilla interna de dos columnas `minmax(0, 1fr)` con el mismo `--footer-gap` (así los tres bloques quedan alineados con 1,2 a 1 a 1); `.footer-block` en rejilla con `gap: 0.5rem`; `.footer-label` en `--text-label` y peso 700; `.footer-value` y enlaces con `overflow-wrap: anywhere` y `min-width: 0`; enlaces con `min-height: var(--target-min)`, `--link` y subrayado permanente. Respeta el área de salvado del logo: la separación con los bloques vecinos no baja de la altura del logo. Sin encabezados, sin año, sin 'todos los derechos reservados', sin línea de marca inventada y sin JavaScript.

    Ciclo del lote E06 (per protocolo del UI-SPEC). (1) Construir el lote completo desde el contrato. (2) Una ronda de captura en lote con `PHASE2_BATCH=E06` a los cinco anchos en base, movimiento reducido y sin JavaScript, con ClickUp bloqueado, más los recortes `recorte`; mirar solo los recortes de 390 y 1280 px y comprobar 320, 768 y 1024 px con las mediciones de los specs, mirando una captura solo si la medición falla. (3) `impeccable critique` del lote contra las secciones 12 y 13 del contrato, las filas CTA final y Footer de la síntesis de referencias y los rasgos 2, 3, 7 y 8 de la lista de vibra. (4) `impeccable harden`: texto largo (una dirección de correo larga y 'FALTA CONFIRMAR' en el pie a 320 px, el titular largo del CTA final en 5fr, las rutas con espaciado de texto forzado), sin alturas fijas en texto. (5) Corregir todo en un solo lote de cambios, con una ronda de confirmación como máximo; si un criterio de salida sigue fallando, repetir el ciclo sin acumular retoques abiertos, con tope de tres ciclos: si no se cumple, detenerse y reportar a Juan con capturas. (6) Registrar en `02-VISUAL-LOG.md` la entrada "Lote E (parte 06), ronda N" con verbos, hallazgos, correcciones y rutas de capturas.

    Cierre del plan. Corre la suite completa de Chromium con ClickUp bloqueado (`npm run test:e2e:isolated`, todos los specs), los guardas, el contraste, `npm run pending` con `--check` y el build de producción (debe fallar solo por PENDING y MISSING). La prueba 9b de `copy.test.mjs` y el bloque 'textos de Ari visibles' de `a11y-base.spec.ts` son de 02-03: aquí solo se ejecutan; si fallan por afirmaciones de este plan, no se editan y se reporta el detalle. Prepara `02-06-SUMMARY.md` con: resultados (incluido el de esas dos verificaciones), la errata 'estan' del titular del CTA final y las demás del doc que aparezcan, las 25 afirmaciones nuevas para Ari, las desviaciones de Supuestos y las notas para la fase 3. Al terminar: `npx astro preview stop`.
  </action>
  <verify>
    <automated>node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check && npm run build && node scripts/check-contrast.mjs</automated>
    <fails_when>non-zero exit, o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0, o el build falla por el esquema de `footer` o `agenda`</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const mine=r.content.filter(v=>/^(for_whom|faq|footer|privacy|agenda)\./.test(v.path));const paths=mine.map(v=>v.path);const ok=r.structural.length===0&&mine.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&paths.includes('footer.email')&&paths.includes('footer.social')&&paths.includes('privacy.body[0]')&&!paths.includes('agenda.title')&&r.content.every(v=>v.rule!=='INVERSION');process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING y MISSING en las rutas de este plan, `agenda.title` aparece como violación (el titular del doc no debe disparar ninguna regla), faltan las rutas del footer o de privacidad, o hay INVERSION</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated</automated>
    <fails_when>non-zero exit de Playwright o falla cualquier spec de Chromium (`closing-sections`, `a11y-base`, `cta-focus`, `page-structure`, `brand-assets` y los de los planes 03 a 05; si falla una prueba que es de otro plan, como 'textos de Ari visibles', no la edites: reporta el detalle y detente); si `astro preview` no levanta porque el puerto 4322 está ocupado, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
    <automated>PHASE2_BATCH=E06 npm run test:e2e:isolated -- tests/e2e/page-structure.spec.ts -g "captura" && PHASE2_BATCH=E06 npm run test:e2e:isolated -- tests/e2e/closing-sections.spec.ts -g "recorte" && test "$(ls test-results/phase2/E06-*.png | wc -l)" -ge 21</automated>
    <fails_when>non-zero exit, o hay menos de 21 capturas del lote E06 (15 de página completa más 6 recortes de `#agenda`, `footer` y `/privacidad/`)</fails_when>
    <automated>node -e "const fs=require('fs');const hosts=new Set();for(const f of ['dist/index.html','dist/privacidad/index.html']){for(const m of fs.readFileSync(f,'utf8').matchAll(/https?:\/\/([a-z0-9.-]+)/gi))hosts.add(m[1].toLowerCase())}const allowed=new Set(['forms.clickup.com','app-cdn.clickup.com','www.w3.org']);const extra=[...hosts].filter(h=>!allowed.has(h));const size=fs.statSync('dist/index.html').size;process.exit(extra.length===0&&size<61440?0:1)"</automated>
    <fails_when>non-zero exit: aparece un host distinto de forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG en `dist/index.html` o `dist/privacidad/index.html`, o `dist/index.html` pesa 61440 bytes o más</fails_when>
  </verify>
  <done>`#agenda` cierra el recorrido con el titular del CTA final del doc de Ari, su collage estático a la izquierda desde 1024 px y el formulario intacto; el footer trae logo, contacto, redes y política de privacidad con el relleno visible donde falta el dato y respeta el área de salvado; `/privacidad` es navegable con teclado en el orden correcto; el lote E06 pasó por `impeccable` y `design-taste-frontend` y quedó registrado; la suite completa, las guardas y el build de producción (que falla solo por PENDING y MISSING) están como se pide.</done>
  <acceptance_criteria>
    - `closing-sections.spec.ts` y todo `npm run test:e2e:isolated` pasan con ClickUp bloqueado: h2 de `#agenda` con el texto del YAML, barra de 48x8 px, padding de 64 px a 390, 768 y 1023 px y de 96 px a 1024 y 1280 px, cero `a[data-cta]` dentro de `#agenda` y cuatro en la página, collage sin tocar la tarjeta ni el iframe y oculto bajo 1024 px, footer en una, dos y tres columnas con proporción 1,2 a 1 a 1, y orden de tabulación exacto en `/privacidad`
    - `D=$(git diff BASE -- src/components/AgendaSection.astro) && printf '%s\n' "$D" | grep -E "^[-+].*(iframe|noscript|form-embed|clickup|referrerpolicy|loading=)" | wc -l` devuelve 0 (el iframe, su script, el `noscript` y la tarjeta no cambian) y `grep -c "^| footer\." PENDING-COPY.md` devuelve 2
    - `grep -c 'href="mailto:' dist/index.html` devuelve 0 mientras el correo sea el relleno; la expresión estricta de correo aparece una sola vez en `SiteFooter.astro` y es la única fuente de `mailto:` (`grep -c "mailto:" src/components/SiteFooter.astro` devuelve 1); con dos builds temporales (correo `a@b.co?bcc=x@y.z` y correo `hola@loopsgrowth.example`, con copia de seguridad del YAML restaurada después) el primero no emite `mailto:` y el segundo emite exactamente `mailto:hola@loopsgrowth.example`
    - Compuerta de higiene sobre las líneas nuevas de `src/components/AgendaSection.astro` y `src/components/SiteFooter.astro` (la transición del enlace de respaldo de la fase 1 ya existía y no cuenta): `D=$(git diff BASE -U0 -- src/components/AgendaSection.astro src/components/SiteFooter.astro) && printf '%s\n' "$D" | grep -E "^\+" | grep -v -E "^\+\+\+" | grep -v -E "^\+\s*(//|/\*|\*|<!--)" | grep -E "set:html|outline: none|\.status\b|#[0-9a-fA-F]{6}\b|transition|animation" | wc -l` devuelve 0
    - `git diff --name-only BASE` lista solo archivos de esta tarea (más `PENDING-COPY.md`) y no toca `src/components/collage`, `src/components/brand`, `src/components/ui`, `src/styles`, `src/scripts`, `page-structure.spec.ts`, `public` ni archivos de otros planes
    - El build de producción con `PUBLIC_SITE_URL` falla solo por reglas PENDING y MISSING (según la segunda comprobación automática) y `dist/privacidad/index.html` conserva `noindex`
    - `PHASE2_BATCH=E06` dejó al menos 21 capturas en `test-results/phase2/` y `02-VISUAL-LOG.md` trae la entrada "Lote E (parte 06)" con verbos (layout, colorize, harden), hallazgos, correcciones y rutas de capturas
    - `02-06-SUMMARY.md` lista la errata 'estan' del titular del CTA final, las afirmaciones nuevas para Ari y las notas para la fase 3 (exclusión de `/privacidad/` del sitemap, retiro del `noindex` con el texto legal y modelado de las redes)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Al terminar las tres tareas, con `astro preview` levantado y ClickUp bloqueado:

1. `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check` salen 0.
2. `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)`: sin violaciones estructurales; en las rutas `for_whom`, `faq`, `footer`, `privacy` y `agenda` solo PENDING y MISSING; ninguna INVERSION; `agenda.title` sin violaciones. `PUBLIC_ENV=production PUBLIC_SITE_URL=https://loopsgrowth.example npm run build` falla y solo por PENDING y MISSING (afirmaciones pendientes y 'FALTA CONFIRMAR' en el YAML y en `dist`).
3. `npm run test:e2e:isolated` completo en verde: orden de tabulación con los resúmenes del FAQ, foco al h2 de `#agenda` con el texto nuevo, Para quién es en dos columnas iguales, FAQ 5 a 7 desde 1024 px, footer en tres columnas 1,2 a 1 a 1, `/privacidad` con `noindex` y navegable, sin scroll horizontal a cinco anchos, cero animaciones con ambas preferencias, único JS propio de menos de 3 KB. La prueba 9b y el bloque 'textos de Ari visibles' (de 02-03) solo se ejecutan y se reportan, sin editarlas.
4. `dist/index.html` por debajo de 61440 bytes y los únicos hosts externos son forms.clickup.com y app-cdn.clickup.com.
5. `02-VISUAL-LOG.md` con las entradas de los lotes D (parte 06) y E (parte 06); `git diff --name-only BASE` solo con archivos de `files_modified`.
</verification>

<success_criteria>
- El visitante ve si el servicio es para él (Para quién es sin rangos de inversión), resuelve dudas con un FAQ de `<details>` que funciona sin JavaScript y llega a un CTA final con el titular del doc de Ari, su collage y el formulario intacto (CONT-10, CONT-11, CONT-13).
- El footer con contacto, redes y política de privacidad existe en `/` y en `/privacidad/`, y esa página es real, navegable con teclado y con `noindex` (CONT-12).
- Todo texto sale del YAML tal cual del doc de Ari o muestra el relleno visible 'FALTA CONFIRMAR', listado en `PENDING-COPY.md`; la guarda INVERSION impide publicar rangos de inversión sin aprobación (COPY-01).
- Todo el diseño pasó por `impeccable` y `design-taste-frontend` con registro por lote y ronda (DSGN-04).
</success_criteria>

<output>
Al terminar crea `.planning/phases/02-secciones-marca-y-copy/02-06-SUMMARY.md` con: archivos creados y cambiados; resultado de las pruebas y las guardas; las erratas del doc detectadas ('estan' en el titular del CTA final); las 25 afirmaciones nuevas listadas para Ari; las desviaciones de Supuestos con lo que se eligió; el registro visual de los lotes D (parte 06) y E (parte 06) por ronda y las capturas; el pendiente manual del `<summary>` con lector de pantalla; y las notas para la fase 3 (sitemap, `noindex`, redes). Si el contexto pasa de 50 % entre tareas, escribe un SUMMARY parcial con `status: partial`.
</output>
