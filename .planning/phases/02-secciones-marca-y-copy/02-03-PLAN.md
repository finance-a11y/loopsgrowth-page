---
phase: 02-secciones-marca-y-copy
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/content/landing.es.yaml
  - src/content.config.ts
  - PENDING-COPY.md
  - tests/guards/copy.test.mjs
  - src/components/ui/SectionShell.astro
  - src/components/ui/PainCard.astro
  - src/components/ui/PillarCard.astro
  - src/components/sections/Problem.astro
  - src/components/sections/WhyNow.astro
  - src/components/sections/Solution.astro
  - src/pages/index.astro
  - tests/e2e/sections-problem-solution.spec.ts
  - tests/e2e/a11y-base.spec.ts
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04, COPY-01, DSGN-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`#problema` es `<section data-tone='dark'>` con un `<h2>` blanco (`problem.title`), tres `PainCard` dentro de `<ul role='list'>` (`problem.items[0..2]`, cada párrafo del doc tal cual como `<p>` en Title 600 y sin h3, por la regla del UI-SPEC para un solo texto por dolor) y, bajo la rejilla, la frase final `problem.closing`; sin CTA (CONT-02, COPY-01)"
    - "Cada `PainCard` es blanca (tono anidado `light`), con borde de 3 px oscuro, radio `--radius-card`, padding `--card-pad`, sombra dura naranja de 6 px sin difuminado tomada del tono padre (`--card-shadow`), numeral CSS morado (01, 02, 03) y una pegatina SVG de 64 px con aria-hidden; 3 columnas iguales de alto parejo desde 1024 px y 1 columna debajo, con el encabezado de tarjeta siempre en fila (CONT-02, DSGN-04)"
    - "`#por-que-ahora` es `<section data-tone='yellow'>` con h2 morado (`why_now.title`), un collage compacto de 160 px (lupa y ojos, tono yellow) y `<ul role='list'>` con una fila por cada oración del doc (`why_now.items`, seis), texto tal cual en Title 600, regla superior de 3 px oscura en cada fila y regla inferior además en la última; desde 1024 px dos columnas 5fr y 7fr con h2 y collage a la izquierda y la lista a la derecha; sin CTA (CONT-03, COPY-01)"
    - "`#solucion` es `<section data-tone='light'>` con h2 morado (`solution.title`, hoy 'FALTA CONFIRMAR' porque el titular del doc trae AEO y la guarda lo rechaza), lead `solution.lead` y cuatro `PillarCard` en el orden auditoría, estrategia, equipo dedicado y reportes, cada una con chip de collage propio y distinto (lupa, ojos, loop, clic), `<h3>` en Title 700 y cuerpo en Body; 1 columna bajo 640 px y 2 columnas desde 640 px con alto parejo por fila (CONT-04)"
    - "El Pilar 3 lista al equipo con el texto del doc en una lista de cuatro filas dentro de la tarjeta (Juan, Miguel, Verónica, Arianna, en ese orden) y la errata 'direcciôn' de la línea de Arianna se muestra tal cual (CONT-04, COPY-01)"
    - "El cuerpo del Pilar 4 (`solution.items[3].body`) muestra 'FALTA CONFIRMAR' y queda pending porque el doc trae una nota `[VERIFICAR: ...]`; el texto íntegro del doc queda en `reason` y sale en `PENDING-COPY.md`, y el título del pilar se muestra tal cual (COPY-01)"
    - "El CTA `location='solucion'` va a 48 px bajo la rejilla de pilares, alineado a la izquierda, con el mismo texto visible que los demás CTA (`ctaLabel(data)`), `href='#agenda'`, sin `aria-label`, con 48 px de alto; un clic o Enter deja el foco en `#agenda-title`; el orden de tabulación a 1280 px es skip 1, skip 2, CTA del header, CTA del hero, CTA de La solución, enlace de respaldo e iframe, y a 390 px el mismo sin el header (CONT-04)"
    - "Toda cadena de las tres secciones sale de `02-ARI-COPY-V2.md`: cada `text` que no es 'FALTA CONFIRMAR' es una subcadena literal del doc (lo comprueba un script) y las cuatro afirmaciones nuevas `pending` son `why_now.items[1]`, `solution.title`, `solution.items[2].list[2]` y `solution.items[3].body`; nada se humaniza, corrige ni reordena (COPY-01)"
    - "Con `PUBLIC_ENV=production` la guarda falla solo por reglas PENDING y MISSING (las rutas pending del YAML y las dos rutas con la marca), nunca por AEO, `[VERIFICAR]`, voseo ni guion largo; `npm run build` sin producción pasa; `npm run pending` es determinista y `node scripts/list-pending.mjs --check` sale 0 (COPY-01)"
    - "La prueba 9b de `tests/guards/copy.test.mjs` pasa a aceptar solo PENDING y MISSING sobre el YAML real, exige que toda reclamación con el texto 'FALTA CONFIRMAR' sea `pending` y deriva lo esperado del YAML con `walkClaims`; este plan es su único editor y el plan 07 solo la verifica, nunca la edita (COPY-01)"
    - "La aserción del caso k de `tests/e2e/a11y-base.spec.ts` ('textos de Ari visibles', con y sin JavaScript) que exigía que el texto visible no trajera la marca 'FALTA CONFIRMAR' se reemplaza en la tarea 1 por una aserción derivada del YAML: el número de apariciones de la marca en el texto visible de `/` es igual al número de reclamaciones cuyo `text` es la marca y cuya ruta no empieza por `privacy.`; este plan es el único que la reescribe y los planes 04 a 07 no la editan porque se ajusta sola con el YAML (COPY-01)"
    - "Las piezas de collage de estas secciones se prueban con los atributos que emite `CollagePiece` (plan 02): `svg[data-collage]` para todas y `data-collage-piece` con el nombre de la pieza (`sticker-*`, `chip-*`, `lupa`, `ojos-abajo`); nunca con el atributo de gancho de la composición del hero de 02-01, que es otro (COPY-01, DSGN-04)"
    - "Las erratas del doc en estas secciones (espacio doble antes de 'y' en `problem.items[2]`, 'direcciôn' con circunflejo y la línea de Arianna sin punto final) se muestran tal cual y se reportan en `02-03-SUMMARY.md` junto con el cargo distinto de Verónica frente a Quiénes somos (COPY-01)"
    - "Todo el diseño del plan pasó por `impeccable` (lote B: `shape`, `layout`, `colorize` y `bolder` si una sección se ve plana, más `critique` del protocolo) y `design-taste-frontend` (diales 7, 3 y 4) invocados con la herramienta Skill; `02-VISUAL-LOG.md` registra por ronda los verbos, hallazgos, correcciones y capturas a 320, 390, 768, 1024 y 1280 px (base, movimiento reducido y sin JavaScript, con ClickUp bloqueado) y el lote cierra cuando se cumplen los rasgos 2, 3, 7 y 8 de la lista de vibra (DSGN-04)"
    - "El texto largo no desborda: h2, filas, tarjetas y h3 llevan `overflow-wrap: anywhere`, los hijos de las rejillas `min-width: 0` y no hay scroll horizontal ni sección fuera del viewport a 320, 390, 768, 1024 y 1280 px (UI Considerations: long-text y overflow, resueltos explícitos)"
    - "Las cantidades fijas las impone el esquema: `problem.items` tiene exactamente 3 afirmaciones y `solution.items` exactamente 4; `why_now.items` admite 1 o más y la página imprime las que haya (UI Considerations: zero-one-many, resuelto explícito)"
    - "Toda ranura sin dato muestra el texto exacto 'FALTA CONFIRMAR' con el estilo de su ranura, nunca vacía ni oculta: hoy son dos, `solution.title` y `solution.items[3].body` (UI Considerations: empty, resuelto explícito)"
    - "El collage y las tarjetas son SVG en línea sin peticiones nuevas y el iframe de ClickUp, su enlace de respaldo y su `noscript` no cambian (UI Considerations: loading y error, resueltos explícitos)"
    - "Sin movimiento: `document.getAnimations().length` es 0 con `reduce` y con `no-preference`, y las tarjetas no tienen hover, cursor de puntero ni transform (UI Considerations: a11y-motion, resuelto explícito)"
    - "Color por tono: h2 blanco en dark y morado en yellow y light; texto oscuro sobre la tarjeta blanca (16.10), morado solo en numerales y h2 sobre claro o amarillo; pegatinas y chips con `tone='light'` porque van sobre tarjeta blanca y el collage de Por qué ahora con `tone='yellow'`, ambos validados por el build; toda tarjeta clara sobre superficie clara lleva borde oscuro de 3 px (UI Considerations: a11y-color, resuelto explícito)"
    - statement: "Las listas con `list-style: none` (`pain-grid`, `whynow-list`, `pillar-grid` y la del equipo) conservan la semántica de lista en Safari con VoiceOver; llevan `role='list'` y el ejecutor lo anota en `02-VISUAL-LOG.md` como comprobación de respaldo (UI Considerations: interaction, backstop)"
      verification: backstop
    - "Supuesto explícito (UI Considerations: copy, sin resolver en el UI-SPEC): este plan mapea cada ranura de las tres secciones a su cadena exacta del doc (tabla 'Ranuras de copy'); las cinco afirmaciones de mercado que el UI-SPEC suponía para Por qué ahora no existen en el doc, que trae seis oraciones en dos párrafos, y no se inventa ninguna. Supuesto explícito (assets): los chips, pegatinas y piezas vienen de `CollagePiece` (plan 02); este plan no modifica el collage ni el hero"
  artifacts:
    - path: "src/content/landing.es.yaml"
      provides: "Claves `problem`, `why_now` y `solution` con las 26 afirmaciones del doc de Ari (cuatro pending, dos con la marca de dato faltante)"
      contains: "why_now:"
    - path: "src/content.config.ts"
      provides: "Esquema estricto de las tres secciones (3 dolores, 4 pilares, lista de equipo opcional)"
      contains: "why_now"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada con las cuatro afirmaciones nuevas y el texto del doc en la columna de motivo"
      contains: "solution.items[3].body"
    - path: "tests/guards/copy.test.mjs"
      provides: "Prueba 9b que acepta PENDING y MISSING y exige pending en toda reclamación con la marca"
      contains: "MISSING"
    - path: "src/components/ui/SectionShell.astro"
      provides: "Variante `split` y slot `aside` compatibles hacia atrás (h2 y collage a la izquierda, cuerpo a la derecha desde 1024 px)"
      contains: "aside"
    - path: "src/components/ui/PainCard.astro"
      provides: "Tarjeta de dolor (tono anidado light, numeral CSS, pegatina, sombra del tono padre)"
      contains: "--card-shadow"
    - path: "src/components/ui/PillarCard.astro"
      provides: "Tarjeta de pilar (chip, h3, cuerpo y lista opcional) que reutilizan los demás planes si les sirve"
      contains: "chip-"
    - path: "src/components/sections/Problem.astro"
      provides: "Sección El problema (CONT-02)"
      contains: "id=\"problema\""
    - path: "src/components/sections/WhyNow.astro"
      provides: "Sección Por qué ahora (CONT-03) con collage compacto"
      contains: "por-que-ahora"
    - path: "src/components/sections/Solution.astro"
      provides: "Sección La solución (CONT-04) con el CTA `solucion`"
      contains: "location=\"solucion\""
    - path: "src/pages/index.astro"
      provides: "Composición con las tres secciones entre el hero y `#agenda`"
      contains: "Solution"
    - path: "tests/e2e/sections-problem-solution.spec.ts"
      provides: "Estructura, copy derivado del YAML, estilos calculados, columnas por ancho, CTA y foco de las tres secciones"
      contains: "solucion"
    - path: "tests/e2e/a11y-base.spec.ts"
      provides: "Orden de tabulación con el CTA de La solución a 1280 y a 390 px, y aserción de marcas visibles derivada del YAML (`walkClaims`) en el caso k, que reemplaza la que prohibía la marca"
      contains: "cta:solucion"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Rondas del lote B con verbos, hallazgos, correcciones y capturas"
      contains: "Lote B"
  key_links:
    - from: "src/components/sections/Problem.astro"
      to: "src/content/landing.es.yaml"
      via: "`getLanding()`: problem.title al `SectionShell`, problem.items y problem.closing impresos como nodos de texto, sin leer status, reason ni confirm_by"
    - from: "src/components/sections/Solution.astro"
      to: "src/components/CtaLink.astro"
      via: "`<CtaLink label={ctaLabel(data)} location=\"solucion\" />` en el slot `cta` de `SectionShell`"
    - from: "src/components/ui/PainCard.astro"
      to: "src/components/collage/CollagePiece.astro"
      via: "pegatina `sticker-clic`, `sticker-lupa` o `sticker-ojos` con `tone=\"light\"`"
    - from: "src/components/ui/PillarCard.astro"
      to: "src/components/collage/CollagePiece.astro"
      via: "chip `chip-lupa`, `chip-ojos`, `chip-loop` o `chip-clic` con `tone=\"light\"`"
    - from: "src/components/sections/WhyNow.astro"
      to: "src/components/ui/SectionShell.astro"
      via: "`split` y slot `aside` con el collage compacto"
    - from: "tests/e2e/a11y-base.spec.ts"
      to: "src/content/landing.es.yaml"
      via: "`walkClaims` cuenta las reclamaciones con la marca fuera de `privacy.` y el caso k exige el mismo número de apariciones en el texto visible de `/`"
    - from: "tests/guards/copy.test.mjs"
      to: "src/content/landing.es.yaml"
      via: "`walkClaims` deriva las rutas pending y las rutas con la marca que la guarda de producción debe reportar"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir ni reordenar el texto del doc de Ari (incluidas la errata 'direcciôn' y el espacio doble de `problem.items[2]`) ni recortar la nota `[VERIFICAR: ...]` para publicar el resto del cuerpo del Pilar 4: si una guarda rechaza algo, la afirmación queda pending con 'FALTA CONFIRMAR' como texto y el texto del doc en `reason`"
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT leer `status`, `reason` ni `confirm_by` en un componente ni ocultar una afirmación pending: los componentes imprimen solo `.text` como nodo de texto"
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT dejar la palabra AEO ni el texto `[VERIFICAR` en el HTML de `#problema`, `#por-que-ahora` ni `#solucion`: el titular con AEO y la nota del Pilar 4 viven solo en `reason`"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar inyección de HTML en bruto, colores hex dentro de `src/components` o `src/pages`, anulación del contorno de foco, `line-height` con `!important` ni alturas fijas en contenedores de texto: los colores salen de tokens y de `var(--color-brand-*)`"
    - requirement_id: DSGN-04
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT dar hover, cursor de puntero, transform, transición ni animación a las tarjetas ni a las filas, ni usar sombras con difuminado: las tarjetas no son interactivas y la sombra es dura (difuminado 0)"
    - requirement_id: DSGN-04
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT poner morado sobre oscuro, naranja o blanco como texto sobre amarillo, blanco sobre naranja ni amarillo como texto sobre claro: cada tarjeta y cada pieza usa el tono de la superficie donde realmente está (`light` sobre tarjeta blanca, `yellow` sobre amarillo)"
    - requirement_id: CONT-04
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT poner CTA en `#problema` ni en `#por-que-ahora` ni más de un CTA `solucion` en `#solucion`: el sitio tiene cuatro CTA (header, hero, solución, casos) y este plan aporta solo el de La solución"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT modificar `Hero.astro`, `HeroCollage.astro`, `SiteHeader.astro`, `BaseLayout.astro`, `AgendaSection.astro`, `SkipLinks.astro`, `CtaLink.astro`, `tokens.css`, `global.css`, `scripts/lib/contrast.mjs`, `src/components/collage/`, `src/components/brand/`, `src/scripts/` ni `public/`: son de los planes 01 y 02 y el iframe de ClickUp no cambia"
    - requirement_id: DSGN-04
      category: design
      status: resolved
      verification: judgment
      statement: "MUST NOT ampliar la escala aprobada por Juan: Title solo en h3, filas de lista y frases de cierre, `--section-y` solo con 64 y 96 px, sin tamaños, pesos ni pasos de espaciado nuevos"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar el formulario de ClickUp, leer `.env` ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Construir las tres primeras secciones de contenido después del hero, con el texto del doc de Ari tal cual: El problema (CONT-02, tono dark, tres tarjetas de dolor), Por qué ahora (CONT-03, tono yellow, lista de afirmaciones con collage compacto) y La solución (CONT-04, tono light, cuatro pilares con chip de collage y el CTA que sigue a la sección). Aplica la regla de copy de la fase (COPY-01: los textos que las guardas rechazan, "AEO" y `[VERIFICAR]`, no se editan: quedan pending y la página muestra "FALTA CONFIRMAR") y el método de diseño del lote B con `impeccable` y `design-taste-frontend` (DSGN-04).

Purpose: que un dueño de negocio se reconozca en el problema, entienda por qué la búsqueda cambió y vea qué hace el equipo, con un CTA a la vista al terminar la solución. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: claves `problem`, `why_now` y `solution` en el YAML con su esquema; `PainCard`, `PillarCard` y las secciones `Problem`, `WhyNow` y `Solution`; `SectionShell` con la variante `split`; el orden de tabulación con el CTA de La solución; la prueba 9b de la guarda de copy ajustada a la marca de dato faltante; `sections-problem-solution.spec.ts`; y las rondas del lote B en `02-VISUAL-LOG.md`.

Decisiones de CONTEXT.md que implementa (por nombre de sección; CONTEXT no numera D-XX): "Estilo visual, collage y movimiento" (referencias como guía de layout, fondos alternados, SVG estático, método `impeccable` más `design-taste-frontend`), "Textos pending" (el texto real se muestra tal cual y solo se lista; "FALTA CONFIRMAR" donde falta el dato y el build de producción sigue fallando), "Excepciones del contrato de diseño aprobadas por Juan" (Title como quinto tamaño para el h3 de tarjeta, 96 px de `--section-y`, el CTA tras La solución como uno de los cuatro) y "Specific Ideas" (si el doc trae AEO o `[VERIFICAR]` se reporta a Ari en lugar de editar en silencio).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicar con `grep -n '^## \|^### '` y leer con `Read` con `offset` y `limit`). Los planes 02-01 y 02-02 se leen solo en `## Artifacts this phase produces` y en sus decisiones de criterio.
</context>

## Artifacts this phase produces

Este plan produce (rutas relativas a la raíz del repositorio). Los nombres de abajo son los que reutilizan los planes 04 a 07: no se renombran después.

- **YAML** (`src/content/landing.es.yaml`, convención de 02-01): `problem` (`title`, `items` de 3 afirmaciones, `closing`), `why_now` (`title`, `items` de 1 o más afirmaciones) y `solution` (`title`, `lead`, `items` de 4 objetos con `title`, `body` y `list` opcional de afirmaciones). **Patrón de ranura sin dato o rechazada por una guarda**, que reutilizan los planes 04 a 06: `text` igual a `FALTA CONFIRMAR`, `status: pending`, `confirm_by: Ari` y `reason` con la causa y el texto literal del doc de Ari; el componente imprime solo `.text`.
- **Guarda de copy** (`tests/guards/copy.test.mjs`, prueba 9b): sobre el YAML real, la producción reporta solo PENDING y MISSING, PENDING en cada ruta pending y MISSING en cada ruta cuyo `text` lleve la marca, y toda reclamación con la marca debe ser `pending`. **Este plan es su único editor**: los planes 04 a 06 no la tocan y el plan 07 solo la verifica (la corre con las guardas), nunca la edita; se ajusta sola con el YAML.
- **Componentes**: `src/components/ui/PainCard.astro` (props `sticker`: `'clic' | 'lupa' | 'ojos'` y `text`; renderiza `<li class="pain-card" data-tone="light">`), `src/components/ui/PillarCard.astro` (props `chip`: `'lupa' | 'ojos' | 'loop' | 'clic'`, `title`, `body` y `list?`; renderiza `<li class="pillar-card">`), `src/components/sections/Problem.astro`, `WhyNow.astro` y `Solution.astro` (sin props). `SectionShell.astro` suma la prop opcional `split` y el slot `aside`; sin ellos se comporta como en 02-01.
- **Técnica de sombra heredada**: `--card-shadow` se declara en el contenedor de la rejilla (lee `--pop-shadow-color` del tono padre) y la tarjeta con tono anidado la usa, porque el tono anidado redefine `--pop-shadow-color`. Cualquier tarjeta blanca sobre dark o purple de los planes 04 a 06 usa la misma técnica.
- **Selectores para pruebas**: `#problema .pain-card`, `.pain-num`, `.pain-closing`; `#por-que-ahora .whynow-art`, `.whynow-list > li`; `#solucion .pillar-grid`, `.pillar-card`, `.pillar-team` y `a[data-cta="solucion"]`. Las piezas de collage se seleccionan con `svg[data-collage]` (todas) y `data-collage-piece` con el nombre de la pieza (`sticker-clic`, `sticker-lupa`, `sticker-ojos`, `chip-lupa`, `chip-ojos`, `chip-loop`, `chip-clic`, `lupa`, `ojos-abajo`), que es lo que emite `CollagePiece` (plan 02); el atributo de gancho del hero de 02-01 es otro y no se usa aquí.
- **Orden de tabulación** (`tests/e2e/a11y-base.spec.ts`, caso a): suma `cta:solucion` después de `cta:hero`; el plan 04 suma `cta:casos` y el 06 las paradas del FAQ.
- **Aserción de marcas visibles** (`tests/e2e/a11y-base.spec.ts`, caso k, "textos de Ari visibles", con y sin JavaScript): reemplaza la aserción que prohibía la marca de dato faltante por una derivada del YAML con `walkClaims` y `MISSING_MARK`: el número de apariciones de "FALTA CONFIRMAR" en el texto visible de `/` es igual al número de reclamaciones cuyo `text` es la marca y cuya ruta no empieza por `privacy.` (el cuerpo de la política vive en `/privacidad`). **Este plan es el único que la reescribe; los planes 04 a 07 NO la editan**, porque se ajusta sola cada vez que un plan agrega o resuelve una marca en el YAML.
- **Evidencia**: `02-03-SUMMARY.md` con el resultado de las pruebas, las erratas y hallazgos del doc, y las desviaciones de abajo.

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`, secciones `## 1. El problema`, `## 2. Por qué ahora` y `## 3. La solución` (líneas 47 a 104). Cada ranura declara cuál de los casos de la regla de copy aplica.

| Ranura (ruta en el YAML) | Origen en el doc | Caso que aplica | Estado |
|--------------------------|------------------|-----------------|--------|
| `problem.title` | 1. El problema, Headline | Existe en el doc y pasa las guardas: se muestra tal cual y no se lista | `verified` |
| `problem.items[0]` | Cuerpo, párrafo 1 (el costo por cliente sube) | Tal cual | `verified` |
| `problem.items[1]` | Cuerpo, párrafo 2 (el orgánico es la salida) | Tal cual | `verified` |
| `problem.items[2]` | Cuerpo, párrafo 3 (la lista que no se mueve) | Tal cual; trae espacio doble antes de "y" (errata: se conserva y se reporta) | `verified` |
| `problem.closing` | Cuerpo, párrafo 4 (la competencia aparece gratis) | Tal cual | `verified` |
| `why_now.title` | 2. Por qué ahora, Headline | Tal cual | `verified` |
| `why_now.items[0]` | Cuerpo, párrafo 1, oración 1 | Tal cual | `verified` |
| `why_now.items[1]` | Cuerpo, párrafo 1, oración 2 (los clientes le preguntan a ChatGPT o a Gemini) | Tal cual y se lista para Ari: afirmación de mercado con datos de EE.UU. (COPY-VERIFICATION 3b) | `pending` |
| `why_now.items[2]` | Cuerpo, párrafo 1, oración 3 | Tal cual | `verified` |
| `why_now.items[3]` a `[5]` | Cuerpo, párrafo 2, oraciones 1 a 3 | Tal cual | `verified` |
| `solution.title` | 3. La solución, Headline (trae "AEO") | Caso guarda AEO: la guarda lo rechaza, así que el texto guardado es `FALTA CONFIRMAR` y la página lo muestra; el titular literal del doc queda en `reason` | `pending` |
| `solution.lead` | Intro | Tal cual | `verified` |
| `solution.items[0].title` y `.body` | Pilar 1 | Tal cual | `verified` |
| `solution.items[1].title` y `.body` | Pilar 2 | Tal cual | `verified` |
| `solution.items[2].title` y `.body` | Pilar 3 (encabezado y la frase que termina en dos puntos) | Tal cual | `verified` |
| `solution.items[2].list[0]` y `[1]` | Pilar 3, viñetas de Juan y de Miguel | Tal cual | `verified` |
| `solution.items[2].list[2]` | Pilar 3, viñeta de Verónica | Tal cual y se lista para Ari: su cargo ("Gerente de Proyectos y Consultora SEO") difiere del de Quiénes somos ("Directora de Proyectos") | `pending` |
| `solution.items[2].list[3]` | Pilar 3, viñeta de Arianna | Tal cual con la errata "direcciôn" (circunflejo) y sin punto final (se reporta) | `verified` |
| `solution.items[3].title` | Pilar 4, título | Tal cual | `verified` |
| `solution.items[3].body` | Pilar 4, cuerpo con la nota `[VERIFICAR: ...]` | Caso VERIFICAR: el texto guardado es `FALTA CONFIRMAR` y la página lo muestra; el cuerpo íntegro del doc, con la nota, queda en `reason` | `pending` |
| CTA de La solución | `cta.label_template` con `call.duration` | Sin cambios: el mismo texto de los demás CTA | ya existente |
| Relleno de dato faltante | n/a | Dos ranuras: `solution.title` y `solution.items[3].body` | n/a |

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **Por qué ahora en seis filas, una por oración del doc.** El UI-SPEC pide una lista de afirmaciones cortas en Title 600, y el doc trae dos párrafos de tres oraciones cada uno; partir por oración conserva cada palabra, el orden y la puntuación, y deja a Ari confirmar solo la oración con datos de EE.UU. Revertir: unir el YAML en dos afirmaciones (una por párrafo, la primera `pending`); el componente imprime las filas que haya y las pruebas derivan del YAML.
2. **El problema con tres dolores y una frase final.** Los tres dolores son los párrafos 1 a 3 del doc, cada uno como un solo texto en `<p>` Title 600 y sin h3 (el respaldo que el propio UI-SPEC prevé cuando el doc no separa título y cuerpo); el párrafo 4 se muestra como frase final bajo la rejilla porque el contrato fija tres tarjetas. Revertir: subir `problem.items` a 4 y cambiar `.length(3)` a `.length(4)` en el esquema y las columnas de la rejilla; no se recomienda.
3. **`FALTA CONFIRMAR` como texto guardado en `solution.title` y `solution.items[3].body`** (regla de copy del brief, que manda sobre el "visible tal cual" del UI-SPEC para el hallazgo 1): los componentes no leen el estado, así que el relleno tiene que ser el propio `text`, y el texto del doc queda íntegro en `reason` y en `PENDING-COPY.md`. No se aplica la variante del UI-SPEC de publicar el Pilar 4 sin la nota porque eso edita el texto de Ari. Revertir: pegar el texto del doc en `text` (el "AEO" del titular volvería a disparar la regla AEO en producción).
4. **Hallazgo de guarda, no se arregla aquí.** `VERIFICAR_RE` en `scripts/lib/copy-rules.mjs` es `/\[VERIFICAR\]/`: no reconoce la forma real del doc, `[VERIFICAR: ...]`. Aquí no importa porque el Pilar 4 se guarda con la marca, pero el plan 07 (auditoría de guardas) debe ampliar la expresión; mientras tanto la prueba de este plan afirma que ninguna de las tres secciones muestra `[VERIFICAR`.
5. **Prueba 9b ajustada por primera vez con una marca real.** Mientras ninguna ranura tuviera el texto `FALTA CONFIRMAR`, 9b podía exigir que toda violación fuera PENDING; la primera ranura con la marca (esta) obliga a aceptar MISSING. El ajuste es más estricto en otro sentido: exige que toda reclamación con la marca sea `pending`. Este plan es el único editor de 9b; el plan 07 solo la verifica.
6. **`SectionShell` suma `split` y el slot `aside`** para que Por qué ahora ponga h2 y collage en la columna izquierda sin selectores `:global` frágiles hacia el interior del envoltorio. Es compatible hacia atrás; revertir: quitar la prop y el slot y hacer que `WhyNow` use el cuerpo apilado en todos los anchos.
7. **Pegatinas y chips (criterio propio, decorativos):** pegatinas de los dolores en el orden `sticker-clic`, `sticker-lupa`, `sticker-ojos` (anuncios pagados, búsqueda orgánica, la competencia que te ve); chips de los pilares en el orden `chip-lupa`, `chip-ojos`, `chip-loop`, `chip-clic` (el del UI-SPEC). Cambiar el orden es editar dos constantes.
8. **Verónica `pending`.** El cargo de la viñeta del Pilar 3 contradice el de la sección Quiénes somos del mismo doc; se lista para que Ari elija en vez de decidirlo aquí. Revertir: pasar la viñeta a `verified` y correr `npm run pending`.
9. **Las cinco afirmaciones del UI-SPEC no coinciden con el doc.** El UI-SPEC listaba cinco afirmaciones de mercado (incluidas "los ads se encarecen" y "el orgánico se abarata") según COPY-VERIFICATION; el Copy v2 no las trae y no se inventan (regla de Juan: si falta un texto se pide a Ari). Se reporta en el SUMMARY.
10. **`HeroCollage.astro` lo declaran 02-01 y 02-02 en la misma wave.** Este plan no lo toca y no depende de él (el Hero es de 02-01); el choque de `files_modified` lo resuelve el orquestador y se reporta en el SUMMARY.
11. **`a11y-base.spec.ts`: dos cambios, ambos de este plan.** (a) El CTA de La solución agrega una parada, así que el caso a (1280 y 390 px) se actualiza (lección 4 del brief). (b) El caso k ("textos de Ari visibles") afirmaba que el texto visible no trae la marca; con la primera marca real (el h2 y el Pilar 4) esa aserción se pondría roja desde esta wave, así que se reemplaza en la misma tarea que agrega las marcas por una derivada del YAML: apariciones de la marca en el texto visible de `/` igual a las reclamaciones con la marca fuera de `privacy.`. Por eso la tarea 1 imprime los cuatro pilares en HTML plano (h3, párrafo y lista del equipo) y ambas marcas ya son visibles al cerrar la tarea 1; la tarea 3 los reemplaza por `PillarCard`. El resto del archivo no cambia y los planes 04 a 07 no editan esa aserción.
12. **Sin `lang` en "Core Web Vitals" ni "link building".** Son un nombre propio y un término técnico (excepción de SC 3.1.2) y marcarlos exigiría partir el texto de Ari con marcado en línea, que la regla del proyecto no permite.

## UI Considerations (lift del UI-SPEC para este plan)

- covered (verdades ya en `must_haves.truths`): empty (marca en dos ranuras), long-text y overflow (`overflow-wrap: anywhere`, `min-width: 0`, cinco anchos), zero-one-many (esquema con 3 y 4 fijos), loading y error (SVG en línea, iframe intacto), a11y-motion (cero animaciones, tarjetas sin hover) y a11y-color (tono real de cada pieza, borde oscuro de 3 px).
- backstop (en `must_haves.truths` como `{statement, verification: backstop}`): las listas con `list-style: none` y VoiceOver en Safari; se añade `role="list"` y se anota la revisión en `02-VISUAL-LOG.md`.
- unresolved que este plan resuelve: fila copy, mapeada ranura por ranura en la tabla de arriba. No aplican a este plan: partial (MetricCard) y visual del `HeroCollage` a 320 px (planes 01 y 02); assets (plan 02).

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Estado |
|--------|------|--------|
| GOAL (ROADMAP fase 2) | Landing completa con secciones de contenido, identidad de marca y copy del doc de Ari, de 320 a 1280 px | Aquí: problema, por qué ahora y solución; el resto en los planes 04 a 07 |
| REQ | CONT-02 (tres dolores), CONT-03 (búsqueda en Google y en asistentes de IA), CONT-04 (cuatro pilares y CTA) | Cubierto: tareas 1 a 3 |
| REQ | COPY-01 (texto tal cual, si falta se pide a Ari) | Cubierto en todas las tareas; continúa en los planes 04 a 07 |
| REQ | DSGN-04 (skills `impeccable` y `design-taste-frontend`) | Cubierto: tareas 1 a 4, con el lote B en la tarea 4 |
| RESEARCH | No existe `02-RESEARCH.md`; el UI-SPEC y CLAUDE.md (sin islas, sin JS, contraste medido) son la fuente | Sin ítems pendientes |
| CONTEXT | Estilo visual y referencias, textos pending y regla de AEO y `[VERIFICAR]`, excepciones aprobadas por Juan (Title, 96 px, CTA tras La solución), fondos alternados | Cubierto |
| Fuera de este plan (no son huecos) | Resultados, casos (y su CTA), equipo, incluye, cómo funciona, para quién, FAQ, `#agenda`, footer y privacidad; movimiento y cierre; hero y collage | Planes 01, 02 y 04 a 07 |

## Presupuesto de contexto (14 archivos en `files_modified`, unos 9 con autoría real)

Cinco casi no tienen autoría: `PENDING-COPY.md` lo genera `npm run pending`, `SectionShell.astro`, `index.astro`, `content.config.ts` y `a11y-base.spec.ts` reciben ediciones pequeñas, y `02-VISUAL-LOG.md` solo suma entradas. La estimación es de 90k tokens (confianza baja, sin historial de calibración: factor 1): unos 20k la tarea 1, 22k la 2, 20k la 3 y 28k la 4. La iteración visual pesa más que el código, por eso el ejecutor mira solo las capturas de 390 y 1280 px y comprueba 320, 768 y 1024 px con las mediciones del spec, mirando una captura solo si la medición falla.

Puntos de corte: cada tarea termina con el build verde y commiteada. Tras la tarea 1 (tracer) y tras la tarea 2, si el contexto ya pasó de 50 %, el ejecutor hace commit, escribe `02-03-SUMMARY.md` con `status: partial` (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. El corte natural más importante es tras la tarea 3 (las tres secciones construidas y probadas); la tarea 4 no se parte a mitad de un ciclo de iteración: si al empezarla el contexto pasa de 50 %, se corta antes.

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/`, `public/` o `astro.config.mjs`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; este plan no lo usa). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; `PUBLIC_ENV=production` falla solo por PENDING y por la marca de dato faltante.
- Las pruebas derivan los textos esperados del YAML (lectura con `yaml`, `walkClaims` y `fill`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- La fase 2 no cambia el comportamiento del iframe de ClickUp; las verificaciones humanas de la fase 1 siguen diferidas.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de las tres secciones de punta a punta con el copy del doc y el CTA de La solución</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, tests/guards/copy.test.mjs, src/components/sections/Problem.astro, src/components/sections/WhyNow.astro, src/components/sections/Solution.astro, src/pages/index.astro, tests/e2e/a11y-base.spec.ts, tests/e2e/sections-problem-solution.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-01-PLAN.md y 02-02-PLAN.md: solo `## Artifacts this phase produces` y `## Supuestos y desviaciones` (o `## Decisiones de criterio`); ubicar con `grep -n '^## '` y leer con `offset` y `limit`. Los nombres de esas secciones (tonos, `SectionShell`, `CtaLink`, `PAGE_ORDER`, `CollagePiece`) se reutilizan tal cual
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol`, `### 2. El problema`, `### 3. Por qué ahora`, `### 4. La solución`, `## Copywriting Contract` (incluidos los hallazgos para Ari) y `## Interaction Contract` (orden de tabulación); ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: líneas 47 a 104 (El problema, Por qué ahora y La solución)
    - src/content.config.ts, src/content/landing.es.yaml, src/pages/index.astro, src/components/ui/SectionShell.astro, src/components/CtaLink.astro (estado tras los planes 01 y 02)
    - scripts/lib/copy-rules.mjs (`MISSING_MARK`, `walkClaims`, `findMissingMark`, las expresiones de las reglas), scripts/check-copy.mjs, scripts/list-pending.mjs y tests/guards/copy.test.mjs (la prueba 9b, alrededor de las líneas 262 a 290)
    - tests/e2e/a11y-base.spec.ts (líneas 1 a 27, el caso a de 1280 px y el caso a de 390 px, y `checkTexts` del caso k, alrededor de las líneas 440 a 452)
  </read_first>
  <action>
    Implementa CONT-02, CONT-03, CONT-04 y COPY-01 en su versión más fina que atraviesa todas las capas del plan (copy, esquema, guardas, componente, página, orden de tabulación y prueba). Las tarjetas, la lista, el collage y los pilares son de las tareas 2 y 3. Antes de editar, anota `git rev-parse HEAD` como BASE para los criterios de alcance y confirma que los planes 01 y 02 ya se ejecutaron (existen `src/components/ui/SectionShell.astro`, `src/components/sections/Hero.astro`, `src/components/collage/CollagePiece.astro` y `tests/e2e/page-structure.spec.ts`); si falta alguno, detente y repórtalo: este plan depende de ellos.

    Skills primero (per DSGN-04, regla de Juan). Invoca con la herramienta Skill `impeccable` (carga `reference/craft-floor.md`, corre `context` y el verbo `shape` del lote B a partir del UI-SPEC, de `PRODUCT.md` y de `DESIGN.md`; no repitas `init` ni `document`, son del plan 01) y `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4. Abre `02-VISUAL-LOG.md` con Read y agrega al final, con Edit y sin reescribir el archivo, la entrada "Lote B, ronda 0" con esa lectura, el brief de `shape` (foco de cada sección: problema es reconocerse, por qué ahora es el cambio de la búsqueda, solución es qué hace el equipo) y los verbos usados.

    Copy (per COPY-01 y la tabla `Ranuras de copy`). En `src/content/landing.es.yaml` agrega, después de `hero` y antes de `agenda`, las claves `problem`, `why_now` y `solution` con las 26 afirmaciones de la tabla, cada una como `{text, status}`, con el texto exacto, carácter por carácter, de `02-ARI-COPY-V2.md` líneas 47 a 104. Conserva el espacio doble de `problem.items[2]` antes de la "y", el circunflejo de la palabra "direcciôn", las comillas rectas, los dos puntos finales de la frase del Pilar 3 y la falta de punto final en la línea de Arianna; cada viñeta del Pilar 3 va sin el guion de lista. Estados según la tabla: pending `why_now.items[1]`, `solution.title`, `solution.items[2].list[2]` y `solution.items[3].body`; todo lo demás verified. Los dos casos con marca (`solution.title` y `solution.items[3].body`) llevan `text: FALTA CONFIRMAR`, `status: pending`, `confirm_by: Ari` y un `reason` que cita entre comillas el texto literal del doc y da la causa: para el titular, el de la línea 79 del doc y que la guarda rechaza la sigla de tres letras que trae, con Ari decidiendo qué término usar (hallazgos 1 y 4 del UI-SPEC); para el cuerpo del Pilar 4, el párrafo íntegro de la línea 104 con su nota entre corchetes y que la nota pide verificar el desglose por canal, con Ari respaldando con datos o suavizando. `why_now.items[1]` lleva `reason` "Afirmación de mercado del Copy v2 sobre clientes que preguntan a ChatGPT o a Gemini; misma familia que COPY-VERIFICATION 3b (datos de EE.UU.)" y `solution.items[2].list[2]` lleva `reason` "El cargo del Copy v2 en La solución ('Gerente de Proyectos y Consultora SEO') difiere del de Quiénes somos ('Directora de Proyectos'); Ari confirma cuál usar". No agregues ninguna ruta a `PLACEHOLDER_PATHS`: ninguna cadena lleva llaves. Si `npm run check:copy` marca en estas rutas una regla que la tabla no espera (voseo o guion largo en otra oración), no edites el texto: aplica a esa ranura el mismo patrón de marca y `reason` y repórtalo en el SUMMARY.

    Esquema (per COPY-01). En `src/content.config.ts`, dentro del `z.strictObject` de `landing`, agrega después de `hero`: `problem` con `title` (claim), `items` (`z.array(claim).length(3)`) y `closing` (claim); `why_now` con `title` y `items` (`z.array(claim).min(1)`); `solution` con `title`, `lead` y `items` (`z.array(z.strictObject({ title: claim, body: claim, list: z.array(claim).min(1).optional() })).length(4)`). Corre `npm run pending` para regenerar `PENDING-COPY.md`.

    Guarda (per las verdades de copy). En `tests/guards/copy.test.mjs` reescribe la prueba 9b y su título: importa `MISSING_MARK` y `findMissingMark` de `copy-rules.mjs`; sobre el YAML real en producción no hay violaciones estructurales; toda violación de contenido es de regla PENDING o MISSING; las rutas (ordenadas) de las violaciones PENDING son iguales a las de las reclamaciones pending derivadas con `walkClaims`; las rutas de las violaciones MISSING son iguales a las de las reclamaciones cuyo `text` contiene la marca según `findMissingMark`; toda reclamación con la marca tiene `status: pending`; el estado de salida es 1 si hay alguna violación y 0 si no; y el YAML no cambia (hash igual). Corre `node --test tests/guards/copy.test.mjs` y confirma que pasa con el YAML nuevo.

    Secciones del tracer (per CONT-02, CONT-03 y CONT-04). Crea `src/components/sections/Problem.astro`, `WhyNow.astro` y `Solution.astro`, cada una con `getLanding()` y un `SectionShell` de 02-01: `id="problema"` con `tone="dark"` y `title={data.problem.title.text}`; `id="por-que-ahora"` con `tone="yellow"` y `title={data.why_now.title.text}`; `id="solucion"` con `tone="light"`, `title={data.solution.title.text}` y `lead={data.solution.lead.text}`. Solo imprimen `.text` (sin `fill`, sin leer `status`, `reason` ni `confirm_by`, sin inyección de HTML en bruto). `Solution.astro` pone en el slot `cta` un `CtaLink` con `label={ctaLabel(data)}` y `location="solucion"`. Problem y WhyNow no llevan CTA. Además, `Solution.astro` imprime los cuatro pilares en HTML plano dentro del `SectionShell`: un `<ul role="list" class="pillar-grid">` con una `<li>` por pilar que lleva un `<h3>` con el título, un `<p>` con el cuerpo y, si existe `list`, un `<ul role="list" class="pillar-team">` con una `<li>` por texto; así las dos marcas del copy (el h2 y el cuerpo del Pilar 4) ya son visibles y la aserción derivada del YAML de `a11y-base.spec.ts` (ver abajo) cierra con igualdad desde esta tarea. Las tarjetas, las filas, el collage y los chips no se construyen aquí (tareas 2 y 3); esta tarea prueba la ruta completa: copy, esquema, guardas, página, pilares, CTA, foco y aserción de marcas.

    Página (per CONTEXT, "Hero, header y orden de la página"). En `src/pages/index.astro` importa las tres secciones y renderízalas entre el hero y `AgendaSection` en el orden Problem, WhyNow, Solution; no cambies el comentario del orden canónico ni ningún otro componente.

    Orden de tabulación y marcas visibles (per lección 4 del brief y COPY-01). En `tests/e2e/a11y-base.spec.ts` haces dos cambios y ninguno más. (a) Actualiza el caso a de 1280 px (orden nuevo: 'a:#main', 'a:#agenda', 'cta:header', 'cta:hero', 'cta:solucion', el enlace de respaldo y 'iframe') y el caso a de 390 px (igual sin 'cta:header'), con sus títulos al día. (b) En `checkTexts` del caso k ("textos de Ari visibles", que corre con y sin JavaScript) reemplaza la aserción que exige que el texto visible no contenga la marca de dato faltante por una derivada del YAML: cuenta las apariciones de la marca en `document.body.innerText` de `/` y exige que sean iguales al número de reclamaciones del YAML cuyo `text` es la marca y cuya ruta no empieza por `privacy.` (el cuerpo de la política vive en `/privacidad`). Deriva ese número con `walkClaims` y `MISSING_MARK` importados de `../../scripts/lib/copy-rules.mjs` (si Playwright no resuelve la importación del `.mjs`, usa un recorrido local equivalente: recorrer objetos y arreglos, tratar como reclamación el objeto con `text` o `status` e ignorar la clave `config`). Conserva las otras aserciones de `checkTexts` (llaves sueltas y palabras de borrador). Este plan es el único que reescribe esa aserción: se ajusta sola con el YAML y los planes 04 a 07 no la editan; déjalo dicho en un comentario breve sobre la propia aserción.

    Prueba (per lección 3). Crea `tests/e2e/sections-problem-solution.spec.ts` con el patrón de lectura del YAML de `a11y-base.spec.ts` (parseo con `yaml`) y un helper que normaliza los espacios al comparar texto. Subconjunto del tracer, a 1280x800 y a 390x844: las tres secciones existen con los ids `problema`, `por-que-ahora` y `solucion`, en ese orden relativo dentro de `main > section` y con `data-tone` dark, yellow y light; cada h2 tiene el texto de su `title` del YAML (el de La solución es la marca) y color calculado blanco en dark y `rgb(115, 24, 127)` en yellow y light; el lead de La solución tiene el texto del YAML; los cuatro `h3` de `#solucion` tienen el texto de `solution.items[i].title` y el cuerpo del cuarto pilar el texto guardado en el YAML (la marca); `#problema` y `#por-que-ahora` no contienen enlaces; `#solucion` contiene un solo `a[data-cta="solucion"]` con `href="#agenda"`, sin `aria-label`, con el mismo nombre accesible que el CTA del hero, alto de 48 px o más y su rectángulo completo por debajo del lead; un clic en ese CTA deja el hash en `#agenda` y el foco en `#agenda-title`. Ejecuta el ciclo de las reglas de ejecución: build, preview en el puerto 4322, specs y `npx astro preview stop`.
  </action>
  <verify>
    <automated>npm run build && node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0 (incluida la prueba 9b reescrita), o `--check` sale distinto de 0 porque `PENDING-COPY.md` difiere del que se generaría</fails_when>
    <automated>node --input-type=module -e "import {parse} from 'yaml';import {readFileSync} from 'node:fs';import {spawnSync} from 'node:child_process';import {walkClaims,MISSING_MARK} from './scripts/lib/copy-rules.mjs';const doc=readFileSync('.planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md','utf8');const nodes=walkClaims(parse(readFileSync('src/content/landing.es.yaml','utf8'))).filter(n=>n.kind==='claim'&&/^(problem|why_now|solution)[.\[]/.test(n.path));const marks=nodes.filter(n=>n.claim.text===MISSING_MARK).map(n=>n.path).sort();const bad=nodes.filter(n=>n.claim.text!==MISSING_MARK&&!doc.includes(n.claim.text));const r=JSON.parse(spawnSync('node',['scripts/check-copy.mjs','--json'],{encoding:'utf8',env:{...process.env,PUBLIC_ENV:'production'}}).stdout);const rules=[...new Set(r.content.map(v=>v.rule))].sort().join();const pend=r.content.filter(v=>v.rule==='PENDING').map(v=>v.path);const ok=nodes.length===26&&bad.length===0&&JSON.stringify(marks)===JSON.stringify(['solution.items[3].body','solution.title'])&&r.structural.length===0&&rules==='MISSING,PENDING'&&['why_now.items[1]','solution.title','solution.items[2].list[2]','solution.items[3].body'].every(p=>pend.includes(p));if(!ok)console.error(JSON.stringify({n:nodes.length,bad:bad.map(b=>b.path),marks,rules,pend}));process.exit(ok?0:1)"</automated>
    <fails_when>non-zero exit: no hay exactamente 26 afirmaciones en las tres claves, alguna `text` (salvo las dos con la marca) no es subcadena literal del doc de Ari, las rutas con la marca no son exactamente `solution.title` y `solution.items[3].body`, hay una violación estructural, aparece en producción una regla distinta de PENDING y MISSING (voseo, guion largo, sigla rechazada o nota de verificación), o falta alguna de las cuatro rutas pending nuevas</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/sections-problem-solution.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts tests/e2e/page-structure.spec.ts</automated>
    <fails_when>non-zero exit de Playwright, o falla alguna prueba del spec nuevo, del orden de tabulación actualizado, de la fase 1 o de `page-structure.spec.ts` (orden y tono de las secciones, desborde a cinco anchos, cero animaciones); si `astro preview` no levanta porque el puerto 4322 sigue ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>Las tres secciones aparecen entre el hero y `#agenda` con el tono y el h2 de su título; el copy completo de las tres pasa por esquema, guardas y lista de pendientes con cuatro pending y dos marcas; el CTA de La solución salta al formulario y deja el foco en el h2 de `#agenda`; la prueba 9b, el orden de tabulación y la aserción de marcas visibles del caso k (ahora derivada del YAML) quedaron actualizados; las demás pruebas de la fase 1 siguen en verde.</done>
  <acceptance_criteria>
    - El script de la segunda verificación sale 0: 26 afirmaciones, cada `text` (salvo las dos marcas) es subcadena literal de `02-ARI-COPY-V2.md`, las marcas están exactamente en `solution.title` y `solution.items[3].body`, la producción solo reporta PENDING y MISSING y las cuatro rutas pending nuevas están presentes
    - `grep -c "why_now.items\[1\]\|solution.title\|solution.items\[2\].list\[2\]\|solution.items\[3\].body" PENDING-COPY.md` devuelve 4 y `node scripts/list-pending.mjs --check` sale 0
    - `grep -o 'FALTA CONFIRMAR' dist/index.html | wc -l` devuelve 2 (el h2 de La solución y el cuerpo del Pilar 4, impreso en HTML plano en esta tarea) y `grep -o 'id="\(problema\|por-que-ahora\|solucion\)"' dist/index.html | wc -l` devuelve 3
    - `grep -o '\bAEO\b' dist/index.html | wc -l` devuelve 0 y `grep -c '\[VERIFICAR' dist/index.html` devuelve 0
    - `grep -c "findMissingMark" tests/guards/copy.test.mjs` devuelve al menos 1 y `node --test tests/guards/copy.test.mjs` pasa
    - `grep -c "cta:solucion" tests/e2e/a11y-base.spec.ts` devuelve 2 (1280 y 390 px)
    - La aserción de marcas visibles del caso k está derivada del YAML: `grep -c "not.toContain('FALTA CONFIRMAR')" tests/e2e/a11y-base.spec.ts` devuelve 0, `grep -c "walkClaims\|MISSING_MARK" tests/e2e/a11y-base.spec.ts` devuelve al menos 1, y el caso k pasa con y sin JavaScript con las dos marcas visibles (número de apariciones en `/` igual al de reclamaciones con la marca fuera de `privacy.`)
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0
    - `git diff --name-only BASE -- src/components/sections/Hero.astro src/components/collage src/components/brand src/components/SiteHeader.astro src/layouts src/components/AgendaSection.astro src/components/SkipLinks.astro src/components/CtaLink.astro src/styles scripts src/scripts public` no lista nada
    - `sections-problem-solution.spec.ts` pasa con ClickUp bloqueado (`npm run test:e2e:isolated`): tonos dark, yellow y light, h2 con el color esperado, un solo CTA `solucion` de 48 px o más con nombre igual al del hero, clic con foco en `#agenda-title`; `02-VISUAL-LOG.md` trae "Lote B, ronda 0"
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: El problema y Por qué ahora completos (PainCard, lista de afirmaciones, collage compacto y variante split)</name>
  <files>src/components/ui/PainCard.astro, src/components/sections/Problem.astro, src/components/sections/WhyNow.astro, src/components/ui/SectionShell.astro, tests/e2e/sections-problem-solution.spec.ts</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 2. El problema`, `### 3. Por qué ahora`, `## Reference Synthesis` (filas El problema, Por qué ahora y Ritmo global), `## Collage and Brand Assets Contract` (filas Lupa, Ojos, Pegatinas y las Reglas), `## Typography` (Reglas), `## Spacing Scale` (Reglas de ritmo), `## Interaction Contract` (fila Tarjetas de Estados) y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - src/components/ui/SectionShell.astro, src/components/collage/CollagePiece.astro y src/components/collage/collage-rules.mjs (`PIECES`, `ALLOWED_FILLS`, `assertToneSafe`), src/styles/tokens.css (los cuatro tonos y `--card-pad`, `--card-gap`, `--radius-card`), src/styles/global.css (`.section-title`, h3), DESIGN.md y PRODUCT.md
    - src/components/sections/Problem.astro y WhyNow.astro (estado tras la tarea 1) y tests/e2e/sections-problem-solution.spec.ts
  </read_first>
  <behavior>
    - `#problema` muestra tres `li.pain-card` con el texto de `problem.items` del YAML, en ese orden, y después la frase de `problem.closing`
    - Cada tarjeta: fondo blanco, texto `rgb(33, 33, 33)`, borde de 3 px `rgb(33, 33, 33)`, radio de 16 px, sombra `rgb(253, 105, 56) 6px 6px 0px 0px`, padding de 24 px bajo 640 px y 32 px desde 640 px, numeral `::before` "01", "02" y "03" en `rgb(115, 24, 127)` y pegatina SVG de 64 px con aria-hidden y sin título ni texto, las tres distintas entre sí
    - Rejilla de dolores: 3 columnas iguales de alto parejo desde 1024 px; 1 columna debajo
    - `#por-que-ahora` muestra una `li` por cada afirmación de `why_now.items` con el texto del YAML, peso 600, regla superior de 3 px oscura (y la última también inferior) y padding vertical de 16 px bajo 640 px y 24 px desde 640 px
    - El collage compacto mide 160 px de ancho, lleva dos piezas dentro de su caja y no cruza el h2 ni la lista; desde 1024 px el h2 y el collage quedan a la izquierda de la lista y bajo 1024 px el orden vertical es h2, collage, lista
    - Ninguna de las dos secciones contiene enlaces ni la marca de dato faltante; las tarjetas y filas no cambian con el hover (transform ninguno, cursor no pointer)
  </behavior>
  <action>
    Implementa CONT-02 y CONT-03 completos (COPY-01: solo se imprime `.text`) y la construcción del lote B de DSGN-04. Skills: invoca con la herramienta Skill `impeccable` (verbos `layout` y `colorize` del lote B; `bolder` solo si en la tarea 4 una sección se ve plana) y `design-taste-frontend` (diales 7, 3 y 4; sin degradados, sin vidrio, sin sombra difusa y sin tres tarjetas iguales sin variar). Patrón tomado de cada referencia (Reference Synthesis): El problema toma de skale.so las tres columnas sobre fondo oscuro con encabezado, título y párrafo, adaptadas a tarjetas blancas de tono anidado con sombra dura naranja, pegatina de collage y numeral CSS morado en lugar de hexágonos con ícono; Por qué ahora toma de rankingonai.com el bloque educativo de frases cortas y de m8l.com la barra de acento sobre el h2 (ya incluida en `.section-title`), con h2 y collage a la izquierda y las filas separadas por reglas de 3 px a la derecha.

    Pruebas primero (TDD, rojo). Extiende `sections-problem-solution.spec.ts` con las aserciones de la sección Behavior, a 1280x800 y a 390x844 (la matriz de cinco anchos es de la tarea 4), leyendo del YAML los textos, el número de filas y las marcas: los textos se comparan con espacios normalizados; el número de tarjetas y de filas sale de `problem.items` y `why_now.items`. Los estilos se leen con `getComputedStyle` (incluido el `::before` del numeral) y las posiciones con `getBoundingClientRect`. Confirma que fallan antes de construir los componentes.

    SectionShell (per la desviación 6). Edita `src/components/ui/SectionShell.astro`: prop opcional `split` (booleano, falso por defecto) que agrega la clase `section-shell--split`, y un slot `aside` que se imprime dentro de `.section-head`, después del h2 y del lead, solo si `Astro.slots.has('aside')`. Con `split`, desde 64em el `.wrap` pasa a rejilla `minmax(0, 5fr) minmax(0, 7fr)` con separación de 3rem y `align-items: start`, la cabecera en la columna 1 y el cuerpo en la columna 2, sin margen inferior de cabecera; bajo 64em nada cambia (flujo apilado: cabecera con su aside y luego el cuerpo). Sin `split` ni `aside` el componente se comporta exactamente como en 02-01.

    PainCard (per `### 2. El problema`). Crea `src/components/ui/PainCard.astro` con props `sticker` (`'clic' | 'lupa' | 'ojos'`) y `text`. Renderiza `<li class="pain-card" data-tone="light">` con un `.pain-head` en fila (`display: flex`, `justify-content: space-between`, `align-items: center`) que contiene `<span class="pain-num" aria-hidden="true">` (numeral por CSS: la rejilla hace `counter-reset` y la tarjeta `counter-increment`; el `::before` del span imprime el contador con formato de dos dígitos, en tamaño Heading, peso 700 y `var(--heading)`, que en el tono anidado es morado) y la pegatina `CollagePiece` con `name` compuesto como `sticker-` más la prop, `tone="light"` y `size="4rem"`; debajo, `<p class="pain-text">` en Title con peso 600, `var(--on-surface)` y `overflow-wrap: anywhere`. Estilo de la tarjeta: `background: var(--surface)` y `color: var(--on-surface)` del tono anidado, `border: var(--border-pop)`, `border-radius: var(--radius-card)`, `padding: var(--card-pad)`, `box-shadow: 6px 6px 0 var(--card-shadow)` (difuminado 0), `min-width: 0`, sin hover, cursor, transform ni transición. La variable `--card-shadow` no la define la tarjeta: la hereda del contenedor (ver Problem), porque el tono anidado redefine `--pop-shadow-color`.

    Problem (per CONT-02). Completa `Problem.astro`: dentro del `SectionShell`, un `<ul role="list" class="pain-grid">` con tres `PainCard` a partir de `data.problem.items`, con la constante local de pegatinas `clic`, `lupa`, `ojos` en ese orden (desviación 7). `.pain-grid` declara `--card-shadow: var(--pop-shadow-color)` (evaluada en el contenedor, que sigue en el tono dark y por eso resuelve a naranja), el `counter-reset` del contador, `list-style: none`, `padding: 0`, `margin: 0`, `display: grid`, `gap: var(--card-gap)` y una columna; desde 64em, `grid-template-columns: repeat(3, minmax(0, 1fr))` con `align-items: stretch`. Después de la rejilla, `<p class="pain-closing">` con `data.problem.closing.text` en Title con peso 700, `var(--on-surface)` (blanco sobre dark), separado 3rem de la rejilla, con `max-width` de 40ch y `overflow-wrap: anywhere`. Sin CTA y sin movimiento.

    WhyNow (per CONT-03). Completa `WhyNow.astro` con `SectionShell` y `split`: en el slot `aside`, un `<div class="whynow-art" aria-hidden="true">` de 10rem de ancho (160 px), `position: relative`, `aspect-ratio: 1` y `margin-top: 2rem`, con dos `CollagePiece` `tone="yellow"` (`lupa` a todo el ancho de la caja y `ojos-abajo` superpuesta en una esquina, sin salir de la caja) con los colores `a` y `b` tomados de `ALLOWED_FILLS.yellow` (blanco, morado, naranja u oscuro; nunca amarillo). En el cuerpo, `<ul role="list" class="whynow-list">` con una `<li>` por cada afirmación de `data.why_now.items`, imprimiendo solo `.text`. Filas: Title con peso 600, `var(--on-surface)`, `border-top: var(--border-pop)`, padding vertical de 1rem bajo 40em y 1.5rem desde 40em, `overflow-wrap: anywhere`; la última fila suma `border-bottom: var(--border-pop)`. Sin CTA, sin movimiento y sin marcado de idioma dentro de las frases.

    Ciclo de las reglas de ejecución tras cada cambio: build, preview en el puerto 4322, spec hasta verde y `npx astro preview stop` al final.
  </action>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/sections-problem-solution.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts</automated>
    <fails_when>non-zero exit de Playwright, o falla alguna aserción de las tarjetas (colores, borde, radio, sombra, padding, numeral, pegatina), de la lista (filas, reglas, peso), del collage (160 px, dentro de su caja, sin cruzar texto), de las columnas por ancho, del hover, de la estructura de página o de la fase 1; antes de construir los componentes las aserciones nuevas deben fallar por su contenido, nunca por un error de sintaxis del propio spec</fails_when>
    <automated>npm run build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluida la validación de tono de `CollagePiece`, que falla con un color no permitido), de las guardas o de la lista de pendientes</fails_when>
  </verify>
  <done>El problema muestra los tres dolores del doc en tarjetas blancas con sombra naranja, numeral y pegatina, más la frase final; Por qué ahora muestra cada oración del doc como una fila con reglas de 3 px y un collage compacto, con el h2 a la izquierda desde 1024 px; ninguna de las dos lleva CTA ni desborda.</done>
  <acceptance_criteria>
    - `sections-problem-solution.spec.ts` pasa con ClickUp bloqueado: a 1280 px las tres `.pain-card` comparten la misma posición superior y alto (diferencia de 1 px como máximo) y el mismo ancho; a 390 px comparten la misma posición izquierda (una columna)
    - Cada `.pain-card` mide en estilos calculados: fondo `rgb(255, 255, 255)`, texto `rgb(33, 33, 33)`, borde de 3 px `rgb(33, 33, 33)`, radio de 16 px, sombra `rgb(253, 105, 56) 6px 6px 0px 0px` y padding de 24 px a 390 px y de 32 px a 1280 px; el `::before` de `.pain-num` vale "01", "02" y "03" y su color es `rgb(115, 24, 127)`
    - Las tres pegatinas miden 64 px (1 px de tolerancia), llevan `aria-hidden="true"` y `focusable="false"`, no contienen `title` ni `text` y tienen tres valores distintos de `data-collage-piece` (`sticker-clic`, `sticker-lupa` y `sticker-ojos`, atributo que emite `CollagePiece`); `.pain-closing` tiene el texto de `problem.closing` con peso 700 y queda 48 px (2 px de tolerancia) por debajo de la rejilla
    - `.whynow-list > li` tiene tantas filas como `why_now.items` del YAML, con su texto en orden, peso 600, `border-top` de 3 px `rgb(33, 33, 33)` en todas y `border-bottom` de 3 px solo en la última; `.whynow-art` mide 160 px (1 px de tolerancia), contiene dos `svg[data-collage-piece]` (valores `lupa` y `ojos-abajo`) con sus rectángulos dentro del suyo y no cruza el h2 ni la lista; a 1280 px el h2 y `.whynow-art` quedan a la izquierda de la lista y comparten la posición izquierda, y a 390 px el orden vertical es h2, collage, lista
    - `#problema` y `#por-que-ahora` no contienen enlaces ni el texto 'FALTA CONFIRMAR'; con el puntero sobre una tarjeta y sobre una fila el transform calculado es 'none' y el cursor no es 'pointer'
    - `grep -c "Astro.slots.has('aside')" src/components/ui/SectionShell.astro` devuelve 1 y el archivo contiene `split`; `grep -c "card-shadow" src/components/sections/Problem.astro src/components/ui/PainCard.astro` suma al menos 2
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0
    - `git diff --name-only BASE -- src/components/sections/Hero.astro src/components/collage src/components/brand src/components/SiteHeader.astro src/layouts src/components/AgendaSection.astro src/components/SkipLinks.astro src/components/CtaLink.astro src/styles scripts src/scripts public` no lista nada
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: La solución completa (cuatro pilares con chip, equipo del Pilar 3, Pilar 4 con la marca y CTA)</name>
  <files>src/components/ui/PillarCard.astro, src/components/sections/Solution.astro, tests/e2e/sections-problem-solution.spec.ts</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 4. La solución`, `## Reference Synthesis` (fila La solución), `## Collage and Brand Assets Contract` (fila Chips de pilar y las Reglas), `## Typography` (Reglas del h3), `## Interaction Contract` (Estados y orden de tabulación) y `## Copywriting Contract` (hallazgos 1 y 3)
    - src/components/ui/SectionShell.astro (slot `cta`), src/components/collage/CollagePiece.astro y collage-rules.mjs (`PIECES` con los cuatro chips y su color fijo), src/styles/global.css (h3), DESIGN.md
    - src/components/sections/Solution.astro (estado tras la tarea 1), src/components/ui/PainCard.astro (para mantener el mismo criterio de tarjeta) y tests/e2e/sections-problem-solution.spec.ts
  </read_first>
  <behavior>
    - `#solucion` muestra cuatro `li.pillar-card` en el orden del YAML; cada una con un chip SVG de 64 px distinto de los demás, un `<h3>` con el texto de `solution.items[i].title` y un `<p>` con el de `body`
    - El Pilar 3 trae una lista de cuatro filas con el texto de `solution.items[2].list` en orden; los otros tres pilares no traen lista
    - El cuerpo del Pilar 4 es el texto guardado en el YAML (la marca 'FALTA CONFIRMAR'); en `#solucion` hay tantas apariciones de la marca como reclamaciones con la marca bajo `solution` (dos: el h2 y ese cuerpo)
    - Cada tarjeta: fondo blanco, borde de 3 px `rgb(33, 33, 33)`, radio de 16 px, sombra `rgb(33, 33, 33) 4px 4px 0px 0px`, padding de 24 px bajo 640 px y 32 px desde 640 px, `display: flex` en columna; h3 con peso 700, color `rgb(33, 33, 33)` y tamaño entre 20 y 24 px
    - Rejilla: 1 columna bajo 640 px y 2 columnas desde 640 px, con alto parejo entre las tarjetas de una misma fila
    - El CTA `solucion` queda a 48 px bajo la rejilla, alineado a la izquierda de la rejilla
    - Ninguna de las tres secciones muestra la palabra AEO ni el texto de la nota de verificación
  </behavior>
  <action>
    Implementa CONT-04 completo y COPY-01. Skills: invoca con la herramienta Skill `impeccable` (verbos `layout` y `colorize` del lote B) y `design-taste-frontend` (diales 7, 3 y 4; sin degradados ni sombras difusas; los cuatro pilares deben variar entre sí por su chip y por el contenido, no ser cuatro copias). Patrón por referencia (Reference Synthesis): la rejilla 2x2 de tarjetas de servicio de rankingonai.com, la numeración de capacidades de m8l.com solo como orden (sin numerales visibles) y el ritmo de bloques de skale.so, adaptados a `PillarCard` pop con chip de collage propio y el CTA debajo.

    Pruebas primero (TDD, rojo). Extiende `sections-problem-solution.spec.ts` con las aserciones de la sección Behavior a 1280x800 y a 390x844, con los textos, la lista del equipo y la cuenta de marcas leídos del YAML (el número de marcas bajo `solution` se deriva con el mismo criterio de `walkClaims` y `MISSING_MARK`, nunca escrito a mano) y una aserción de que el texto de las tres secciones no contiene la palabra rechazada por la guarda ni el inicio de la nota de verificación (con expresiones regulares en el spec, no en esta acción). Confirma que fallan antes de construir.

    PillarCard (per `### 4. La solución`). Crea `src/components/ui/PillarCard.astro` con props `chip` (`'lupa' | 'ojos' | 'loop' | 'clic'`), `title`, `body` y `list?` (arreglo de textos). Renderiza `<li class="pillar-card">` con el chip (`CollagePiece` con `name` compuesto como `chip-` más la prop, `tone="light"` y `size="4rem"`) arriba a la izquierda; `<h3>` con el estilo global de h3 (Title 700, `--on-surface`) a 1rem bajo el chip; `<p>` de cuerpo en Body a 0.5rem del h3; y, solo si hay `list`, `<ul role="list" class="pillar-team">` con una `<li>` por texto, sin viñetas nativas y con separación entre filas (la iteración de la tarea 4 decide si llevan regla de 3 px). Estilo: `display: flex` en columna, fondo blanco de marca (`var(--color-brand-white)`), `border: var(--border-pop)`, `border-radius: var(--radius-card)`, `box-shadow: var(--shadow-pop)` (4 px, dura), `padding: var(--card-pad)`, `min-width: 0`, `overflow-wrap: anywhere`, sin hover, cursor, transform ni transición. Imprime solo los textos recibidos (nunca `status`, `reason` ni `confirm_by`).

    Solution (per CONT-04). Reemplaza en `Solution.astro` el marcado plano de los pilares de la tarea 1: dentro del `SectionShell` (que ya recibe `title`, `lead` y el slot `cta`), el `<ul role="list" class="pillar-grid">` pasa a llevar cuatro `PillarCard` a partir de `data.solution.items`, con la constante local de chips `lupa`, `ojos`, `loop`, `clic` en ese orden (desviación 7) y `list` igual a los textos de `item.list` cuando exista. `.pillar-grid`: `list-style: none`, `padding: 0`, `margin: 0`, `display: grid`, una columna con `gap: var(--card-gap)`; desde 40em, `grid-template-columns: repeat(2, minmax(0, 1fr))` con `align-items: stretch`. El CTA de la tarea 1 queda a 48 px bajo la rejilla y alineado a la izquierda; si el `SectionShell` lo separa distinto, corrige el espaciado del slot `cta` solo para esta sección sin tocar el envoltorio. Los textos de las cuatro tarjetas salen tal cual del YAML: el cuerpo del Pilar 4 muestra la marca, porque así se guardó.

    Ciclo de las reglas de ejecución tras cada cambio: build, preview en el puerto 4322, spec hasta verde y `npx astro preview stop` al final.
  </action>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/sections-problem-solution.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts</automated>
    <fails_when>non-zero exit de Playwright, o falla alguna aserción de las tarjetas, de los chips (64 px, distintos, decorativos), de la lista del equipo, de la marca del Pilar 4, de las columnas, del CTA (48 px bajo la rejilla, alineado a la izquierda) o de la fase 1</fails_when>
    <automated>npm run build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluida la validación de tono de los chips), de las guardas o de la lista de pendientes</fails_when>
  </verify>
  <done>La solución muestra los cuatro pilares del doc en tarjetas pop con un chip distinto cada una, el Pilar 3 con la lista del equipo y la errata tal cual, el Pilar 4 con "FALTA CONFIRMAR" en su cuerpo y el CTA a 48 px bajo la rejilla; las tres secciones están completas y probadas.</done>
  <acceptance_criteria>
    - `sections-problem-solution.spec.ts` pasa con ClickUp bloqueado: cuatro `.pillar-card` en el orden del YAML; cada h3 y cada cuerpo con el texto del YAML; `ul.pillar-team` solo en el tercer pilar y con las cuatro filas del YAML en orden; el cuerpo del cuarto pilar igual al texto guardado
    - Los cuatro `svg[data-collage-piece^="chip-"]` tienen cuatro valores distintos de `data-collage-piece` (`chip-lupa`, `chip-ojos`, `chip-loop` y `chip-clic`; el atributo del hero de 02-01 no aplica aquí), miden 64 px (1 px de tolerancia), llevan `aria-hidden="true"` y `focusable="false"` y no contienen `title` ni `text`; el h3 queda a 16 px (2 px de tolerancia) bajo el chip y el cuerpo a 8 px (2 px) bajo el h3
    - Cada `.pillar-card` mide: fondo `rgb(255, 255, 255)`, borde de 3 px `rgb(33, 33, 33)`, radio de 16 px, sombra `rgb(33, 33, 33) 4px 4px 0px 0px`, padding de 24 px a 390 px y de 32 px a 1280 px, `display: flex` con `flex-direction: column`; h3 con peso 700, color `rgb(33, 33, 33)` y tamaño entre 20 y 24 px
    - A 390 px las cuatro tarjetas comparten la posición izquierda (1 columna) y a 1280 px hay dos columnas, con alto igual (1 px de tolerancia) entre las dos tarjetas de cada fila
    - El CTA `a[data-cta="solucion"]` queda a 48 px (2 px de tolerancia) bajo la rejilla y con la misma posición izquierda que ella; en `#solucion` el conteo de 'FALTA CONFIRMAR' es igual al número de reclamaciones con la marca bajo `solution` en el YAML
    - `grep -o 'FALTA CONFIRMAR' dist/index.html | wc -l` devuelve 2; `grep -o '<h3' dist/index.html | wc -l` devuelve 4 (a esta altura de la fase los pilares son los únicos h3); `grep -c 'direcciôn' dist/index.html` devuelve al menos 1 (la errata se muestra tal cual)
    - `grep -o '\bAEO\b' dist/index.html | wc -l` devuelve 0 y `grep -c '\[VERIFICAR' dist/index.html` devuelve 0
    - `grep -c "chip-" src/components/ui/PillarCard.astro` devuelve al menos 1; `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0
    - `git diff --name-only BASE -- src/components/sections/Hero.astro src/components/collage src/components/brand src/components/SiteHeader.astro src/layouts src/components/AgendaSection.astro src/components/SkipLinks.astro src/components/CtaLink.astro src/styles scripts src/scripts public` no lista nada
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Lote B de diseño (capturas, critique y correcciones) y verificación de 320 a 1280 px</name>
  <files>.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md, tests/e2e/sections-problem-solution.spec.ts, src/components/sections/Problem.astro, src/components/sections/WhyNow.astro, src/components/sections/Solution.astro, src/components/ui/PainCard.astro, src/components/ui/PillarCard.astro, src/components/ui/SectionShell.astro</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol` (Ciclo por lote y Lista de "vibra"), `## Reference Synthesis` (filas El problema, Por qué ahora, La solución y Ritmo global), `## Responsive`, `## Verification Hooks` y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (formato de las entradas de los lotes 0 y A y la ronda 0 del lote B), DESIGN.md y PRODUCT.md
    - tests/e2e/page-structure.spec.ts (la herramienta de capturas por lote `PHASE2_BATCH`: solo leerla, no cambiarla) y tests/e2e/sections-problem-solution.spec.ts (estado tras la tarea 3)
    - Los cinco componentes y las tres secciones de las tareas 2 y 3, y src/components/ui/SectionShell.astro
  </read_first>
  <action>
    Implementa DSGN-04 (lote B) y cierra CONT-02, CONT-03 y CONT-04 con la verificación de 320 a 1280 px. Skills: invoca con la herramienta Skill `impeccable` con los verbos del lote B (`shape`, `layout`, `colorize` y `bolder` solo si una sección se ve plana) y el `critique` del protocolo, y `design-taste-frontend` (diales 7, 3 y 4; sin degradados, sin vidrio, sin sombras difusas y sin animaciones infinitas). Anota cada verbo usado para el registro.

    Pruebas primero (rojo). Extiende `sections-problem-solution.spec.ts` con: (a) la matriz de cinco anchos, 320, 390, 768, 1024 y 1280 px: columnas esperadas (dolores en 3 desde 1024 px y en 1 debajo; pilares en 2 desde 640 px y en 1 debajo; Por qué ahora en dos columnas desde 1024 px y apilado debajo), `scrollWidth` de cada una de las tres secciones menor o igual al ancho del viewport, ningún rectángulo de tarjeta o fila fuera de `[0, innerWidth]` y `.whynow-art` sin cruzar el h2 ni la lista; (b) ritmo: padding superior e inferior de `#problema`, `#por-que-ahora` y `#solucion` de 64 px bajo 1024 px y de 96 px desde 1024 px; (c) espaciado de texto (SC 1.4.12): con `addStyleTag` (interlineado 1.5, separación de letras 0.12em, de palabras 0.16em y márgenes de párrafo de 2em, todo con prioridad forzada solo dentro del spec) ninguna `.pain-card`, `.pillar-card` ni `.whynow-list > li` recorta su contenido (`scrollHeight` menor o igual a `clientHeight` más 1) y no hay scroll horizontal a 320 px; (d) con `emulateMedia` en `reduce` y en `no-preference`, `document.getAnimations().length` es 0, y con `javaScriptEnabled: false` las tres secciones y el CTA `solucion` son visibles; (e) un bloque de capturas por sección que solo corre con `PHASE2_BATCH` definida (`test.skip` si no), con un título que contenga la palabra captura, que a 390 y a 1280 px guarda con `locator.screenshot()` los archivos `test-results/phase2/<lote>-<id>-<ancho>.png` de `problema`, `por-que-ahora` y `solucion` (seis archivos) con todo lo que no es localhost abortado. Confirma que (a) a (d) pasan o fallan por su contenido y corrige el código si fallan.

    Ciclo del lote B (per protocolo del UI-SPEC). (1) La construcción ya está hecha en las tareas 2 y 3. (2) Una ronda de captura en lote con `PHASE2_BATCH=B`: la herramienta de página completa de 02-01 (15 capturas: cinco anchos en base, movimiento reducido y sin JavaScript) más las seis de sección; mira solo las de sección a 390 y 1280 px y comprueba 320, 768 y 1024 px con las mediciones del spec, mirando una captura solo si la medición falla. (3) `impeccable critique` del lote contra el contrato de las tres secciones, las filas de la síntesis de referencias (El problema, Por qué ahora, La solución y Ritmo global) y los rasgos 2, 3, 7 y 8 de la lista de vibra: aire de 96 px y bloques de tono a ancho completo; tarjetas con borde de 3 px, sombra dura y radio parejo sin sombra difusa; cada sección con un foco claro y al menos un elemento de collage o un layout propio (pegatinas y numerales, collage con filas, chips en 2x2), ninguna con aspecto de plantilla; el CTA de La solución con el mismo texto y destino que los otros. (4) Corrige todo en un solo lote de cambios (verbos `layout`, `colorize` y, si una sección se ve plana, `bolder`), con una ronda de confirmación como máximo; si un criterio de salida sigue fallando, repite el ciclo sin acumular retoques abiertos, con tope de tres ciclos: si no se cumple, detente y reporta a Juan con capturas. Toda corrección respeta el contrato: solo tokens y `var(--color-brand-*)`, la escala aprobada, sombra dura y ningún movimiento. (5) Registra en `02-VISUAL-LOG.md` (Read y luego Edit al final) la entrada "Lote B, ronda N" con verbos usados, hallazgos, correcciones y rutas de las capturas, la nota de que las listas con `role="list"` se revisaron (comprobación de respaldo de VoiceOver) y qué rasgos de la lista de vibra quedaron cumplidos.

    Cierre. Corre el ciclo de las reglas de ejecución (build, preview en el puerto 4322, la suite completa y `npx astro preview stop`). Escribe `02-03-SUMMARY.md` con: el resultado de las pruebas; las erratas del doc detectadas en estas secciones (espacio doble en `problem.items[2]`, "direcciôn" con circunflejo y la línea de Arianna sin punto final) y la de "estan" del hero (plan 01); los hallazgos para Ari (el titular de La solución trae la sigla que la guarda rechaza y queda pending; la nota de verificación del Pilar 4 deja ese cuerpo pending; los dos cargos de Verónica; la afirmación de mercado `why_now.items[1]`; el posible lenguaje de promesa de "respondemos por el resultado", que solo se reporta); las cuatro rutas pending nuevas; la brecha de la guarda (la expresión de la regla de verificación no reconoce la nota con dos puntos: la resuelve el plan 07); el choque de `HeroCollage.astro` entre los planes 01 y 02; las desviaciones de Supuestos que se aplicaron y cualquier medición distinta de la estimada.
  </action>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated</automated>
    <fails_when>non-zero exit de Playwright, o falla cualquier prueba del proyecto `chromium` (fase 1, planes 01 y 02, o este plan: matriz de cinco anchos, ritmo de padding, espaciado de texto, cero animaciones, sin JavaScript); si `astro preview` no levanta porque el puerto 4322 sigue ocupado, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
    <automated>PHASE2_BATCH=B npm run test:e2e:isolated -- tests/e2e/page-structure.spec.ts tests/e2e/sections-problem-solution.spec.ts -g "captura" && test "$(ls test-results/phase2/B-*.png | wc -l)" -eq 21</automated>
    <fails_when>non-zero exit de los specs de capturas, o el número de capturas del lote B no es 21 (15 de página completa: 5 anchos por 3 variantes, más 6 de sección: 3 secciones por 2 anchos)</fails_when>
    <automated>npm run build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (con `prebuild` y `postbuild`), de las guardas de contraste y de copy, o de la lista de pendientes</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist --json || true) | node --input-type=module -e "import {parse} from 'yaml';import {readFileSync} from 'node:fs';import {walkClaims,MISSING_MARK} from './scripts/lib/copy-rules.mjs';let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const marks=walkClaims(parse(readFileSync('src/content/landing.es.yaml','utf8'))).filter(n=>n.kind==='claim'&&n.claim.text===MISSING_MARK).length;const ok=r.structural.length===0&&r.content.length===marks&&r.content.every(v=>v.rule==='MISSING');if(!ok)console.error(JSON.stringify({marks,found:r.content.length}));process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: el escaneo de `dist` en producción encuentra la marca en más o menos lugares que las reclamaciones con la marca del YAML (una ranura con la marca que no se muestra o una marca que no viene del YAML), o reporta una regla distinta de MISSING</fails_when>
  </verify>
  <done>Las tres secciones se ven y funcionan de 320 a 1280 px sin desborde ni recortes (también con el espaciado de texto forzado), sin movimiento y sin JavaScript; el lote B quedó registrado con capturas y cumple los rasgos 2, 3, 7 y 8 de la lista de vibra; el SUMMARY reporta las erratas y los hallazgos para Ari.</done>
  <acceptance_criteria>
    - `npm run test:e2e:isolated` pasa completo con ClickUp bloqueado: matriz de cinco anchos con las columnas esperadas, sin scroll horizontal y sin rectángulos fuera del viewport; padding de `#problema`, `#por-que-ahora` y `#solucion` de 64 px a 320, 390 y 768 px y de 96 px a 1024 y 1280 px; con espaciado de texto forzado ninguna tarjeta ni fila recorta su contenido; `document.getAnimations().length` es 0 con `reduce` y con `no-preference`; con JavaScript desactivado las tres secciones y el CTA `solucion` son visibles
    - Existen 21 capturas `test-results/phase2/B-*.png` (no versionadas); `02-VISUAL-LOG.md` trae "Lote B, ronda 0" y al menos una ronda más con verbos usados (incluido `critique`), hallazgos, correcciones, rutas de capturas, la nota de las listas con `role="list"` y los rasgos 2, 3, 7 y 8 marcados como cumplidos o, si no, la constancia de que el ejecutor se detuvo tras tres ciclos y reportó a Juan
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0; `grep -rEn "line-height[^;]*!important" src | wc -l` devuelve 0
    - `grep -oE "https?://[a-zA-Z0-9.-]+" dist/index.html | sort -u` no lista hosts distintos de forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG; `grep -o '\bAEO\b' dist/index.html | wc -l` devuelve 0 y `grep -c '\[VERIFICAR' dist/index.html` devuelve 0
    - `git diff --name-only BASE -- src/components/sections/Hero.astro src/components/collage src/components/brand src/components/SiteHeader.astro src/layouts src/components/AgendaSection.astro src/components/SkipLinks.astro src/components/CtaLink.astro src/styles scripts src/scripts public` no lista nada
    - `02-03-SUMMARY.md` existe y reporta las erratas y hallazgos listados en la acción, las cuatro rutas pending nuevas y las desviaciones aplicadas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `landing.es.yaml` a HTML | Texto editado por Ari y el equipo (también desde la web de GitHub) entra a la página; debe salir escapado |
| `reason` y `confirm_by` a `PENDING-COPY.md` | Metadatos que guardan el texto literal del doc (con la sigla y la nota que las guardas rechazan); nunca deben llegar al HTML |
| Props de componente a piezas SVG | `sticker`, `chip` y `tone` eligen piezas del sprite del plan 02; un valor fuera de la lista no debe compilar |
| Navegador a terceros | El iframe y el script de ClickUp siguen siendo las únicas peticiones externas de la página |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering | Texto de las tres secciones impreso desde el YAML | medium | mitigate | Astro escapa las expresiones de texto; los componentes imprimen `.text` como nodo de texto, sin inyección de HTML en bruto; criterio de aceptación con grep sobre `src` |
| T-02-03-02 | Information Disclosure | `reason` con el titular y la nota del doc llegando al HTML | medium | mitigate | Los componentes solo leen `.text`; criterios: cero apariciones de la sigla rechazada y de la nota de verificación en `dist/index.html` y aserción del spec sobre el texto de las tres secciones |
| T-02-03-03 | Repudiation | Afirmación pending que Ari no ve y aprueba a ciegas | medium | mitigate | `npm run pending` regenera `PENDING-COPY.md` y `--check` lo compara byte a byte; las cuatro rutas nuevas quedan listadas con su motivo y el texto literal del doc |
| T-02-03-04 | Tampering | Guarda debilitada al reescribir la prueba 9b | medium | mitigate | 9b acepta solo PENDING y MISSING, deriva las rutas del YAML con `walkClaims`, exige `pending` en toda reclamación con la marca y comprueba el hash del YAML |
| T-02-03-05 | Tampering | Pieza de collage con un color no permitido para el tono donde está | low | mitigate | `CollagePiece` (plan 02) valida tono y colores en el build y el build falla con un par no permitido; tonos reales: `light` en chips y pegatinas (sobre tarjeta blanca) y `yellow` en el collage compacto |
| T-02-03-06 | Information Disclosure | Peticiones nuevas a terceros (fuentes, analítica, CDN) | low | mitigate | Todo es texto y SVG en línea; criterio: los únicos hosts https de `dist/index.html` son forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG |
| T-02-03-07 | Denial of Service | HTML demasiado pesado (Core Web Vitals) | low | mitigate | `page-structure.spec.ts` afirma el HTML de `/` por debajo de 60 KB sin comprimir y las piezas reutilizan el sprite con `<use>` |
| T-02-03-08 | Tampering | Destino del CTA `solucion` alterado | low | mitigate | `CtaLink` restringe `href` a '#agenda' o '/#agenda' y ningún valor del YAML llega a `href`; el spec afirma `href="#agenda"` |

Este plan no instala paquetes (UI-SPEC, Registry Safety: sin dependencias nuevas), por lo que no hay fila de cadena de suministro.
</threat_model>

<verification>
Al cerrar las cuatro tareas, con el servidor preview levantado según las reglas de ejecución y detenido al final:

1. `npm run build` sale 0; `PUBLIC_ENV=production` falla solo por reglas PENDING y MISSING (el script de la Tarea 1 lo comprueba: 26 afirmaciones literales del doc, marcas solo en `solution.title` y `solution.items[3].body`, cuatro rutas pending nuevas).
2. `node scripts/check-contrast.mjs` sale 0 y `node --test tests/guards/*.test.mjs` no tiene fallos (con la prueba 9b reescrita).
3. `npm run test:e2e:isolated` pasa completo (fase 1, planes 01 y 02 y `sections-problem-solution.spec.ts`) con ClickUp bloqueado.
4. `npm run pending` es idempotente y `node scripts/list-pending.mjs --check` sale 0.
5. `02-VISUAL-LOG.md` trae el lote B y `02-03-SUMMARY.md` existe con las erratas y los hallazgos para Ari.
</verification>

<success_criteria>
Un dueño de negocio se reconoce en los tres dolores, entiende por qué la búsqueda dejó de ser solo Google, ve los cuatro pilares de lo que hace el equipo y encuentra el CTA al terminar la solución, todo con el texto del doc de Ari tal cual, verificado de 320 a 1280 px, con la sigla rechazada y la nota de verificación fuera de la página (quedan pending con "FALTA CONFIRMAR" y el texto del doc en la lista de Ari), con las guardas de copy y de contraste en verde y con el diseño pasado por `impeccable` y `design-taste-frontend` y registrado en el lote B.
</success_criteria>

<output>
Create `.planning/phases/02-secciones-marca-y-copy/02-03-SUMMARY.md` when done
</output>
