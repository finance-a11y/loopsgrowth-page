# Fase 2: registro visual (impeccable y design-taste-frontend)

Plan 02-01. Regla de Juan: todo trabajo de diseño web pasa por las skills `impeccable` y `design-taste-frontend`, invocadas con la herramienta Skill. Orden de mando (UI-SPEC, Design Method): primero el contrato del UI-SPEC y A11Y.md, luego las skills, que afinan dentro del contrato y no lo rompen.

Protocolo por lote: (1) construir el lote completo desde el contrato; (2) una ronda de capturas en lote con `PHASE2_BATCH=<lote>` (`test-results/phase2/<lote>-<ancho>[-reduce|-nojs].png`, no versionadas), a 320, 390, 768, 1024 y 1280 px con ClickUp bloqueado; (3) `critique` contra el contrato de la sección, la fila de la síntesis de referencias y la lista de "vibra"; (4) corregir todo en un lote, con una ronda de confirmación como máximo; (5) registrar aquí.

## Lote 0, ronda 0

- **Skills invocadas:** `impeccable` (context, luego el verbo `init`) y `design-taste-frontend`.
- **`impeccable context`:** sin PRODUCT.md ni DESIGN.md previos; implementación visual incumbente (Fase 1). Modo: Persuade (landing de captación).
- **`init`:** no hubo entrevista en vivo porque el ejecutor corre sin una persona que responda en esta sesión. Se generó `PRODUCT.md` en la raíz solo con hechos ya aprobados por Juan (`.planning/PROJECT.md`, `02-CONTEXT.md`, `02-UI-SPEC.md`, `.claude/CLAUDE.md`), y el archivo lo declara en su cabecera. No se dejó ninguna decisión visual dentro de PRODUCT.md.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin animaciones infinitas.
- **Conflictos resueltos a favor del contrato:** la skill desaconseja SVG dibujados a mano y exige generar imágenes; el UI-SPEC (Collage and Brand Assets Contract) fija el collage como SVG en línea de marca, sin peticiones, así que manda el contrato. La skill pide modo claro y oscuro; el contrato fija cuatro tonos de superficie por sección y A11Y.md, así que no se agrega un tema oscuro del sistema. La skill prohíbe el guion largo: el copy de Ari no se toca (regla del proyecto) y ningún texto propio de este plan lo usa.
- **Capturas:** ninguna (sin UI todavía).

## Lote 0, ronda 1

- **Skills invocadas:** `impeccable` (context, luego `document`, `typeset` y `colorize` aplicados al `tokens.css` real) y `design-taste-frontend` (diales 7, 3 y 4). El contrato del UI-SPEC manda sobre ambas.
- **Construido:** tonos `yellow` y `dark` completos, `--heading`, `--bar`, `--mark`, `--pop-shadow-color` y `--collage-stroke` por tono, escala Title (`--text-title`), `--card-pad`, `--card-gap`, `--radius-card`; `h3`, `.section-title` (barra de 48x8 px) y `section[id]` en `global.css`; `SectionShell` y `CtaLink` con cuatro ubicaciones, prop `href` y sombra por tono. Guarda de contraste con cuatro tonos, 11 pares aprobados y pares por tono (`--heading`, `--bar`, `--collage-stroke`), probada por mutación.
- **Medición** (página temporal `tone-smoke.astro`, borrada antes del commit; script ad hoc de Playwright fuera del repositorio, a 1280 px, ClickUp bloqueado):

| Tono | h2 | Barra (48x8 px) | Anillo de foco (3 px, sólido) | CTA (relleno / sombra) |
|------|----|-----------------|-------------------------------|------------------------|
| `light` | `rgb(115, 24, 127)` | `rgb(115, 24, 127)` | `rgb(115, 24, 127)` | naranja / oscuro |
| `yellow` | `rgb(115, 24, 127)` | `rgb(33, 33, 33)` | `rgb(33, 33, 33)` | naranja / oscuro |
| `dark` | `rgb(255, 255, 255)` | `rgb(253, 105, 56)` | `rgb(255, 198, 2)` | amarillo / naranja |
| `purple` | `rgb(255, 255, 255)` | `rgb(255, 198, 2)` | `rgb(255, 198, 2)` | amarillo / oscuro |

  Padding vertical de las cuatro secciones a 1280 px: 96 px arriba y abajo. Alto del CTA: 50.7 px (mínimo 48 px).
- **Capturas:** `test-results/phase2/0-390.png` y `test-results/phase2/0-1280.png` (no versionadas). Se miraron ambas.
- **Hallazgos (critique del lote contra el contrato):** ninguno que rompa el contrato. Se anota como observación que sobre `dark` la sombra naranja del CTA amarillo es sutil; el UI-SPEC la fija como decorativa (el relleno amarillo sobre oscuro mide 10.22 y define la forma), así que no se cambia. La tarjeta blanca sobre `dark` se distingue por relleno (16.10), sin borde, como manda el contrato.
- **Correcciones:** ninguna; no hizo falta segunda ronda.
- **Conflictos resueltos a favor del contrato:** la skill de gusto pide un tema único de página (Page Theme Lock) y `impeccable` desaconseja la sombra dura fuera de un mundo neobrutalista. El brandbook y el UI-SPEC fijan cuatro tonos alternados y la sombra dura de 4 px como rasgo de marca (ya presente en la Fase 1), y Juan aprobó ambos. Se mantienen.
- **`document`:** sin entrevista en vivo (mismo motivo que `init`). `DESIGN.md` describe lo extraído de los tokens y las decisiones ya aprobadas; no se generó el archivo lateral de tokens. Si Juan quiere afinar el lenguaje cualitativo, se corre `/impeccable document` de nuevo.

## Lote 0 (marca y collage, de 02-02), consolidado

Nota: consolidada desde 02-02-SUMMARY.md (decisión 9 de 02-02). Es la entrada "Registro visual, lote 0 (marca y collage)" de ese resumen, copiada sin cambios de fondo. No confundir con las rondas "Lote 0" de arriba, que son del plan 02-01 (tokens y tonos).

Skills invocadas con la herramienta Skill en la tarea 1: `design-taste-frontend` (lectura: landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, accesibilidad por encima de la estética; diales 7/3/4) e `impeccable` (verbos `shape` y `adapt` sobre el header; en las tareas 2 a 4 se aplicaron `shape`, `colorize` y `polish` en el mismo criterio, sin volver a cargar las skills). Sin `init` ni `document` (los hizo 02-01).

**Ronda 1, header (tarea 1).** Capturas `lote0-logo-320.png` y `lote0-logo-1280.png`. Hallazgos: (a) el logo de 32 px pesa poco al lado del h1 de 64 px y del CTA; (b) el logo quedaba pegado arriba frente al CTA porque el contenedor `.wordmark` heredaba una caja de línea de 24 px. Correcciones en un solo lote: 40 px desde 640 px (el header no cambia de altura, el CTA ya mide 44 px) y `.wordmark` como `flex` con centrado. Confirmación: logo centrado con el CTA a 1280 px; a 320 px se mantiene en 32 px.

**Ronda 2, identidad (tarea 2).** Capturas `lote0-marca-{320,390,768,1024,1280}.png`. `colorize` sobre los cuatro colores entregados, contrastes medidos: #4228d1 sobre blanco 8.55, sobre amarillo 5.43, sobre #212121 1.88 (falla: por eso el logo pasa a blanco sobre dark y purple) y sobre #73187f 1.13. El isotipo sobre dark se ve gracias al anillo crema y al iris, pero sus aros #4228d1 tienen 1.88 contra el fondo: no se debe usar sobre oscuro sin la variante de la mesa 17 del `.ai`. Favicon a 16 px (ampliado): los dos ojos y el mango se leen como formas separadas, pero el isotipo es apaisado (1.64:1) y ocupa poco alto del cuadrado; ver decisiones para Ari.

**Ronda 3, primitivas (tarea 3).** Capturas `lote0-collage-primitivas-{320,390,768,1024,1280}.png`. Hallazgo de fondo: el `loop` de anillos concéntricos con punto central se leía como diana (la misma lección del hero en 02-01). Corrección: dos anillos rellenos que se solapan (regla evenodd con contorno), sin punto central. También se igualó la altura de las celdas de la hoja para alinear los rótulos. Las variables `--cf-a`, `--cf-b` y `--cf-c` y `--collage-stroke` atraviesan el `<use>`: la misma lupa sale amarilla, blanca y naranja según el tono y con contorno oscuro o blanco. Confirmación: sin hallazgos nuevos.

**Ronda 4, composiciones (tarea 4).** Capturas `lote0-composiciones-{320,390,768,1024,1280}.png`. Hallazgos: en `AgendaCollage` el clic caía sobre los aros del loop y el loop tapaba el mango de la lupa. Corrección en un solo lote: loop detrás y a la derecha de la lupa, ojos arriba mirando a la derecha y clic debajo del loop. Confirmación: la lupa manda, cada pieza se ve completa y el trazo blanco de 3 px coincide con las primitivas; el clic queda cerca del lente pero no lo apunta con exactitud (se dejó así para no entrar en una tercera ronda). Punto 5 de la lista de vibra: los cuatro avatares se leen como set uniforme sobre círculos suaves, con los mismos ojos y proporciones. Punto 7: `AgendaCollage` es un elemento de collage propio de `#agenda` con el mismo trazo pop.

## Lote A, ronda 1

- **Skills invocadas:** `impeccable` (context y `critique` del lote; verbos del lote `shape`, `layout`, `typeset`, `colorize` y `adapt`, sin `animate`: el plan 07 anima la entrada) y `design-taste-frontend` (lectura de diseño: landing de captación B2B para dueños de e-commerce, collage pop de marca, accesibilidad por encima de la estética; diales 7, 3 y 4; sin degradados, sin vidrio, sin animaciones infinitas). El contrato del UI-SPEC manda sobre ambas.
- **Construido desde el contrato:** `Hero.astro` en rejilla (`minmax(0, 7fr) minmax(0, 5fr)` desde 64em, una columna antes; collage de 240 px de alto en A y 320 px en B) y `src/components/collage/HeroCollage.astro` (SVG en línea, seis piezas `loops`, `lupa`, `ojos`, `clic-a`, `clic-b`, `destellos` con `--i`, `--r` y `--r-from`, solo tokens de color, sin texto ni `title`). Sin movimiento: `document.getAnimations().length` es 0 y la opacidad del h1, el subtítulo y el CTA es 1 con `reduce` y con `no-preference`.
- **Capturas:** `PHASE2_BATCH=A`, 15 archivos en `test-results/phase2/A-{320,390,768,1024,1280}[-reduce|-nojs].png` (no versionadas), ClickUp bloqueado. Se miraron 390 y 1280 px; 320 (nota abajo), 768 y 1024 px se comprobaron con las mediciones del spec (sin desborde, `.hero-art` sin cruce con `.hero-copy`).
- **Hallazgos del `critique` (ronda de captura 1):**
  1. La lupa se leía como un huevo frito o una diana: tres aros concéntricos alrededor de la lente, disco amarillo dentro y el mango escondido bajo los aros. Rasgo 7 (un foco claro): incumplido.
  2. Ojos, clics y destellos quedaban pequeños frente al conjunto: la composición ocupaba menos de dos tercios del `viewBox`.
  3. El parche de puntos terminaba con un corte a mitad de punto en el borde.
  4. Sin hallazgos de estructura: h1 morado a 64 px y peso 700, CTA y collage completos en el primer pantallazo a 1280x800, collage a la derecha del texto, sin desborde en los cinco anchos.
- **Correcciones (un solo lote):** aros desplazados hacia arriba a la derecha y detrás de la lente; lente en dos capas (aro morado y cristal blanco con un brillo en arco) sin el disco amarillo; mango oscuro largo que sale de los aros; ojos a 1,4 de escala, sobre la lente; clics a 1,5 de escala apuntando al cristal, uno morado y otro blanco; destellos de 28 px; parche de puntos alineado a la cuadrícula del patrón (`x`, `y`, ancho y alto múltiplos de 18).
- **Ronda de confirmación (1 de 1):** 91 pruebas de Playwright pasan (`page-structure`, `a11y-base`, `cta-focus`) y las 15 capturas se regeneraron. Se miraron 1280 y 390 px otra vez: la lupa se lee como lupa, los ojos miran hacia el CTA y todo cabe en el primer pantallazo.
- **Lista de vibra:** rasgo 1 cumplido en el hero (titular enorme en negrita, un solo pill dominante y collage a la derecha, todo visible en 1280x800). Rasgo 7 cumplido para el hero (un foco claro y un collage propio, no se lee como plantilla). Los demás rasgos corresponden a secciones de planes posteriores.
- **Nota de 320 px (comprobación de respaldo):** se revisó `A-320.png`. El collage queda a 240 px de alto centrado bajo la descripción, sin desborde ni cruce con el texto. Con 320 px el texto del CTA se parte en dos líneas dentro del pill (reflujo permitido por A11Y.md, sin altura fija ni recorte). Es el CTA de la fase 1 y no se tocó.
- **Conflicto resuelto a favor del contrato:** `design-taste-frontend` desaconseja SVG dibujados a mano y pide generar imágenes; el UI-SPEC fija el collage como SVG en línea de marca sin peticiones ni raster. Manda el contrato. La skill también pide evitar el morado de IA; aquí el morado es el color de marca.

## Lote B, ronda 0

- **Plan 02-03.** Secciones El problema (dark), Por qué ahora (yellow) y La solución (light).
- **Skills invocadas:** `impeccable` (context y verbo `shape`; no se repiten `init` ni `document`, son del plan 01) y `design-taste-frontend`. El contrato del UI-SPEC y A11Y.md mandan sobre ambas.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animaciones infinitas.
- **Brief de `shape` (foco de cada sección):** El problema es reconocerse (tres dolores en tarjetas blancas sobre fondo oscuro, con la frase de cierre debajo); Por qué ahora es el cambio de la búsqueda (frases cortas con reglas de 3 px y un collage compacto de lupa y ojos); La solución es qué hace el equipo (cuatro pilares con chip propio y el CTA al final).
- **Verbos usados hasta aquí:** `shape`. Esta ronda es el tracer: las tres secciones con el copy completo, esquema, guardas y CTA, sin tarjetas ni collage (tareas 2 y 3).
- **Conflictos resueltos a favor del contrato:** la skill de gusto desaconseja tres tarjetas iguales y el UI-SPEC fija tres dolores en tres columnas iguales; se varía por pegatina y numeral. La skill prohíbe el guion largo y el copy de Ari no se edita (regla del proyecto).
- **Capturas:** ninguna todavía.

## Lote B, ronda 1

- **Plan 02-03, tarea 4.** Ronda de captura con `PHASE2_BATCH=B` (21 archivos: 15 de página completa y 6 de sección) sobre el sitio ya con el collage oficial, las pildoras y la foto de Por qué ahora de los planes 02-09 a 02-11. Se miraron solo las de sección a 390 y 1280 px; 320, 768 y 1024 px se comprobaron con las mediciones del spec (matriz de cinco anchos, ritmo de 64 y 96 px, SC 1.4.12 a 320 px), sin abrir capturas porque ninguna medición falló.
- **Referencia real:** `brand-inventory/moodboard.png` (recortes en blanco y negro sobre disco morado, pildoras "seo", "spy" y "team work" y superficie crema) y `ai_a.png`.
- **Skills y verbos usados:** `impeccable` con `critique` del lote y los verbos `layout` (collage de Por qué ahora y reglas del equipo) y `colorize` (alternancia blanco y crema en los pilares); `bolder` no hizo falta, ninguna sección quedó plana. `design-taste-frontend` con diales 7, 3 y 4 (sin degradados, sin vidrio, sin sombras difusas, sin animaciones infinitas). El contrato del UI-SPEC y A11Y.md mandaron sobre ambas: el desacuerdo con tres tarjetas iguales en El problema se resuelve, como en la ronda 0, con pegatina distinta, numeral y pildora por tarjeta.
- **Hallazgos del `critique` (1280 px):**
  1. Por qué ahora dejaba la columna izquierda vacía bajo un collage de 320 px frente a seis filas.
  2. Las cuatro tarjetas de La solución se leían uniformes.
  3. Las filas del equipo (Pilar 3) no tenían la regla de 3 px de la lista de Por qué ahora.
  4. El Pilar 4 tiene mucho aire bajo FALTA CONFIRMAR.
  5. El problema funciona: dos tarjetas medio vacías junto a la del párrafo largo son el alto parejo del contrato, no un defecto.
- **Correcciones (un solo lote, commit `149f172`):**
  1. `WhyNow`: el collage ocupa su columna desde 64em con tope de 26rem (416 px); bajo 64em sigue en 224 px. Se ajustaron `collage-language` y `collage-photos`, que fijaban 320 px.
  2. `PillarCard`: las tarjetas 2 y 4 van sobre crema (par oscuro sobre crema aprobado, 14.37), en tablero con las blancas 1 y 3. Los chips de amarillo se leen sobre crema.
  3. `PillarCard`: las filas del equipo llevan `border-top` de 3 px (variable `--border-pop`) y la última no duplica la del borde de la tarjeta.
  4. El aire del Pilar 4 se deja: viene de que el cuerpo es el marcador FALTA CONFIRMAR y de igualar el alto con el Pilar 3; se cierra cuando Ari entregue el texto. No se rompe el alto parejo por un marcador.
- **Ronda de confirmación:** 1 de 1 (tope de tres ciclos, no hizo falta otra). 1280 px de Por qué ahora y La solución y 390 px de La solución revisados tras la corrección.
- **Listas con `role="list"`:** `.pain-grid`, `.whynow-list`, `.pillar-grid` y `.pillar-team` se revisaron como respaldo de VoiceOver (el `list-style: none` de Safari quita la semántica sin el rol explícito); las cuatro llevan el rol. Queda la verificación manual con VoiceOver a cargo de una persona.
- **Rasgos de la lista de vibra:** 2 (recortes de media tinta: la foto de la lupa en Por qué ahora) y 3 (formas planas de color detrás: disco morado, chips amarillos y crema) cumplidos; 7 (cada sección con foco y layout propio: tarjetas numeradas, lista con collage, tablero 2x2) y 8 (aire de 96 px, bloques de tono a ancho completo, borde de 3 px, sombra dura y radio parejo, sin sombra difusa) cumplidos.
- **Capturas** (`test-results/phase2/`, no versionadas): `B-{320,390,768,1024,1280}` con `-reduce` y `-nojs`, y `B-{problema,por-que-ahora,solucion}-{390,1280}`.

## Lote M, ronda 0

- **Plan 02-09.** Marca oficial: morado #4228D1, crema #F4F3E0, las 17 mesas del .ai como logos por variante y tono, favicon de la mesa 18 y hoja de revisión `/marca/hoja/`. Tareas 1 a 3, construidas desde el contrato antes de la ronda de captura.
- **Skills invocadas:** `impeccable` (context sin entrevista, luego los verbos de cada tarea) y `design-taste-frontend`, ambas con la herramienta Skill. En la tarea 4 se volvieron a invocar (contexto nuevo, no se reutilizó nada) para el `critique`. El contrato del UI-SPEC y A11Y.md mandan sobre ambas.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animaciones infinitas.
- **Verbos usados en las tareas 1 a 3:** tarea 1 solo `impeccable context` (token, pares medidos y guardas, sin verbo de diseño); tarea 2 `shape` y `adapt` (mesas por variante y tono, alturas mínimas por ancho); tarea 3 ninguno (favicon medido con evidencia a 16 y 32 px, sin juicio visual todavía). El juicio visual completo es esta ronda.
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` desaconseja el morado "de IA" (regla LILA); el azul violeta es el color del BrandBook, del logo y del moodboard, y no se cambia. La skill pide generar imágenes y evitar SVG dibujados a mano; los logos son las mesas oficiales del .ai sin redibujar y el collage es SVG de marca por contrato. La skill prohíbe el guion largo: ningún texto propio lo usa y el copy de Ari no se toca.
- **Capturas:** ninguna en esta ronda (los cambios de las tareas 1 a 3 se miran en la ronda 1).

## Lote M, ronda 1

- **Skills invocadas:** `impeccable` (context y `critique`; `colorize` y `polish` como lente de la crítica, sin cambios que aplicar; `adapt` no hizo falta porque ningún logo se ve mal a 320 px) y `design-taste-frontend` (diales 7, 3 y 4).
- **Capturas:** `PHASE2_BATCH=M E2E_BLOCK_CLICKUP=1`, 20 archivos en `test-results/phase2/` (no versionadas): `M-{320,390,768,1024,1280}` con sus variantes `-reduce` y `-nojs` de la página y `M-hoja-{320,390,768,1024,1280}` de la hoja. Se miraron `M-1280`, `M-390` y `M-hoja-1280` completas, más recortes a escala real del hero, del header a 320 y 390 px (captura propia a 2x), de la identidad `light` y `dark`, del favicon y del collage de `#agenda` de la hoja. Se compararon con `ai_a.png`, `ai_b.png` y `moodboard.png`. 320, 768 y 1024 px se comprobaron con las mediciones de los specs (sin desborde y alto mínimo de cada logo).
- **Veredicto de los seis puntos:**
  - (a) Familia de azul violeta: cumple. Logo del header (`rgb(66, 40, 209)`), h1 (`rgb(66, 40, 209)`), numerales, subtítulo de La búsqueda dejó de ser solo Google y banda de `#agenda` son el mismo #4228D1. En el header hay un solo morado (medido a 320 y 390 px). Rasgo 1 del moodboard (azul violeta eléctrico dominante) presente y coherente con el crema y el naranja de acento.
  - (b) Hero: cumple. Con el morado nuevo, el aro de la lente y el cursor se leen contra los aros naranja y amarillo; la lupa se lee como lupa, con el mango oscuro saliendo de los aros y los ojos sobre ella. Un solo foco claro (rasgo 7).
  - (c) `#agenda`: cumple en la muestra de la hoja (`agenda-collage / purple`). Amarillo y blanco sobre morado se leen (5.43 y 8.55). El aro naranja y el mango se distinguen del fondo: el contorno blanco los separa y el naranja frente al morado es un contraste de matiz nítido a primer vistazo, así que NO se aplicó el respaldo (no se cambió `AgendaCollage.astro` ni `ALLOWED_FILLS.purple`). La página real aún no monta `AgendaCollage` en `#agenda` (lo monta el plan 02-06 con la pieza de 02-10); ese montaje se revisa allí.
  - (d) Logo del header a 32 px: cumple. Horizontal, 182 x 32 px a 320 y 390 px, trazo limpio, sin cortes ni tipografía pegada; el alto mínimo del catálogo (`MIN_HEIGHT_PX`) se respeta.
  - (e) Hoja contra las mesas: cumple. Apilado 01, 05, 08, 03; horizontal 06; imagotipo 07; emblema 10, 24 y 12; isotipo 13, 14, 16, 17; ojo 18, 19, 21 y 22 tienen el mismo dibujo y los mismos colores que `ai_a.png` y `ai_b.png`, sin fondo y con crema en los logos claros sobre oscuro y morado (mesas 08, 03 y 12). Cada uno se lee sobre su tono. Los dos isotipos sobre oscuro (mesas 17 y 22) se leen por el anillo crema; el aro morado sobre #212121 (1.88) queda como excepción de marca ya registrada.
  - (f) Favicon a 16 px: cumple con reserva. Sobre la muestra clara y la oscura se lee como un ojo con lupa (aro, pupila y mango); el brillo de la pupila ya no se distingue a 16 px, cosa esperable, y a 32 y 48 px se ve completo. La medición del SUMMARY (alto 14 y pupila 3.0 ráster, 3.7 vector) coincide con lo que se ve.
- **Correcciones:** ninguna. No hubo hallazgo de color ni de logos dentro del alcance de 02-09.
- **Ronda de confirmación:** no hizo falta (0 correcciones, 1 ciclo de 3 posibles).
- **Lista de vibra:** rasgo 1 cumplido en el hero a 1280 x 800 (titular enorme en negrita, un solo pill dominante, collage a la derecha, todo en el primer pantallazo). Rasgo 7 cumplido en hero, El problema (numerales con iconos), Por qué ahora (lupa con ojos) y La solución (chips propios); en `#agenda` queda pendiente del montaje de `AgendaCollage` (02-06 y 02-10), sin que se lea como plantilla porque lleva el titular del CTA y el formulario.
- **Notas de traspaso, fuera del alcance de 02-09 (no se corrigieron):**
  1. Rasgos 2 y 3 del moodboard (recortes en blanco y negro de media tinta y formas planas de color detrás del recorte): la página hoy no los tiene; es trabajo de los planes 02-10 (collage) y 02-11 (fotos en media tinta).
  2. `BaseLayout.astro` no declara `<link rel="icon">` y `src/layouts` está vetado en este plan: 02-07 debe enlazar `/favicon.svg` y `/favicon.ico`.
  3. En la captura de 1280 px el iframe de `#agenda` se ve vacío porque ClickUp está bloqueado a propósito; no es un defecto.
  4. El titular de La solución muestra `FALTA CONFIRMAR` (pendiente de Ari, listado en `PENDING-COPY.md`).
- **Conflicto resuelto a favor de la marca:** ver ronda 0 (regla LILA de `design-taste-frontend` contra el azul violeta del BrandBook).

## Lote C (collage de marca), ronda 0

- **Plan 02-10.** Loopy oficial de las mesas 13, 14, 18 y 19 del `.ai` en un mecanismo único de escenas (`scenes.mjs`, `assertScene` R1 a R11, `CollageScene`, `Pill`): hero, Por qué ahora, siete minis (tres pegatinas y cuatro chips), agenda y cuatro avatares. El nombre lleva la aclaración entre paréntesis para no confundirlo con el Lote C1 de 02-04 ni con el Lote C (equipo) de 02-05.
- **Skills invocadas** con la herramienta Skill: `impeccable` (context sin entrevista, porque el ejecutor corre sin una persona que responda; PRODUCT.md y DESIGN.md ya existían) y `design-taste-frontend`. En las tareas 1 a 3 se invocaron una vez (lectura de diseño de la tarea 1, reutilizada); en la tarea 4 se volvieron a invocar para el `critique`.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca con las piezas reales de Loopy y el moodboard, accesibilidad por encima de la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animaciones infinitas.
- **Verbos usados en las tareas 1 a 3:** `context` en cada tarea; el resto fue construcción directa desde el contrato (sin `shape` nuevo, porque la forma de las escenas ya venía fijada en el plan) y revisión visual rápida por captura de elemento. El juicio visual completo es la ronda 1 (`critique`, `colorize` y `polish`; `adapt` no hizo falta: nada se ve mal a 320 px).
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` desaconseja los SVG dibujados a mano (aquí la pieza principal es arte oficial del `.ai`, sin redibujar), el morado de IA (es el azul violeta del BrandBook), las tarjetas iguales (varían por escenario, por palabra y por escena) y el contorno grueso (las formas no llevan contorno; el trazo de 3 px es solo de los garabatos). Su regla de imágenes reales para el hero se cumple con las ranuras de foto (`PHOTO_SLOTS`) que llena el plan 02-11. La skill prohíbe el guion largo: ningún texto propio lo usa y el copy de Ari no se toca.
- **Capturas:** ninguna en esta ronda.

## Lote C (collage de marca), ronda 1

- **Skills invocadas:** `impeccable` (`critique`, con `colorize` y `polish` como lente) y `design-taste-frontend` (diales 7, 3 y 4).
- **Capturas** (`PHASE2_BATCH=C-collage E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts tests/e2e/brand-assets.spec.ts -g captura`, build servido en 4322, 55 pruebas): `C-collage-{320,390,768,1024,1280}` con sus variantes `-reduce` y `-nojs`, `C-collage-hoja-{320,390,768,1024,1280}` y las de elemento de la tarea 3, en `test-results/phase2/` (no versionadas). Se miraron enteras las de elemento de hero, whynow, sticker-lupa, chip-loop, avatar-ojos-morado, agenda a 1280, la hoja de avatares, y la franja del hero de la página a 320, 390 y 1280 px. 768 y 1024 px se juzgaron por las mediciones de los specs.
- **Los siete recortes lado a lado** (script de Pillow fuera del repositorio, `python3 compare.py` desde la raíz; referencia a la izquierda, nuestra captura a la derecha, a la misma altura, `C-collage-compare-<n>.png`). Cajas (izquierda, arriba, derecha, abajo): (1) ai_a.png (267, 801, 534, 1068) contra `ojos-purple`; (2) ai_b.png (534, 0, 801, 267) contra `lupa-purple`; (3) ai_a.png (801, 801, 1068, 1068) contra `ojos-yellow`; (4) ai_b.png (0, 267, 267, 534) contra `lupa-yellow`; (5) ai_a.png (0, 801, 267, 1068) y ai_b.png (267, 0, 534, 267) contra `ojos-light` y `lupa-light`; (6) moodboard.png (1100, 248, 1283, 362) para `spy` y (843, 790, 1065, 932) para `team work`, contra recortes de `pildoras-light` (celdas `spy / purple / cream`, `spy / cream / purple` y `team work / cream / purple`, cajas 96,420,236,490; 541,420,681,490 y 541,522,681,592); el recorte de banda completa era ilegible y se cambió por celdas; (7) moodboard.png (880, 130, 1300, 410) contra `hero-1280`. Las cajas del moodboard se ajustaron mirando el recorte.
- **Veredicto de los puntos a a g:**
  - (a) Fidelidad: cumple en los pares 1 a 5. Mismo dibujo, mismos colores (aro crema o morado, iris lila, pupila oscura, brillo crema), sin fondo. La única diferencia son las etiquetas de mesa ("13", "14", etc.) que trae el render de referencia.
  - (b) Lenguaje: cumple con reserva. Hero, Por qué ahora y agenda muestran el rasgo 1 (azul violeta dominante con acento amarillo y naranja y crema), 3 (disco plano detrás del Loopy), 4 (píldoras con palabra en negrita), 6 (flecha, destello, asterisco y garabato con solapes) y 7 (retícula de puntos y sombra dura). Se leen como el moodboard y no como clip art ni como paquete de stickers, salvo que falta el rasgo 2 (recortes fotográficos en blanco y negro): es el plan 02-11 y queda como pendiente, no se simuló. El rasgo 5 (wireframes de línea fina) queda fuera por decisión del plan.
  - (c) Hero: cumple tras la corrección. El Loopy de dos ojos es el foco; el panel crema con retícula vacío se ve intencional (una tarjeta con sombra dura, no un hueco). Hallazgo: la píldora `geo` era morada sobre el disco morado y su borde izquierdo se fundía con el disco. A 390 x 844 el primer pantallazo trae h1, subtítulo y CTA; el collage queda debajo del texto en una columna (contrato responsive, no es un defecto).
  - (d) Píldoras: proporción y peso, cumple con reserva. Contraste, palabra en negrita y sombra dura coinciden con `spy` y `team work` del moodboard (crema con morado 7.63, oscuro sobre amarillo 10.22); las del moodboard son mucho más grandes respecto a su composición. Hallazgo: las del hero eran pequeñas (13.8 px a 320 px). Ninguna tapa el Loopy ni el texto.
  - (e) Pegatinas y chips de 96 x 80 px: cumplen. Se leen a 320 y 390 px (palabra de 12 px, 10.5 px en `team work`, ambos decorativos y `aria-hidden`), alternan morado y amarillo y no compiten con el numeral ni con el h3 (medido por los specs).
  - (f) Avatares: cumplen. Cuatro discos de unos 100 px con la misma sombra dura y proporción, distintos entre sí por color (morado o amarillo) y por número de ojos (uno o dos); solo Loopy sobre disco, sin cara ni accesorio.
  - (g) Cantidad de píldoras: son 11 en `/` (dos en el hero, dos en Por qué ahora, tres pegatinas y cuatro chips; medido) y dos más cuando 02-06 monte la agenda. La crítica no las considera demasiadas en las composiciones grandes; en las minis son lo más pequeño de la pieza. No se quitó ninguna (el encargo pide el rasgo 4 en cada composición); pregunta para Juan en el SUMMARY.
- **Correcciones (un solo lote, commit `6368e97`):** `hero-pill-seo` y `hero-pill-geo` de fs 30 a 36 (16.6 px a 320 y 390 px, 27.9 px a 1280); `hero-pill-geo` pasa de morado con texto crema a crema con texto morado (7.63) y baja de y 352 a 394 para no tocar el mango de la lupa (el crema sobre el mango crema lo habría fundido); `hero-pill-seo` sube de y 168 a 164. `assertScene('hero')` pasa. La guarda R7 del hero muta ahora con la palabra `hoy` en lugar de `hola`: con la píldora más grande, `hola` chocaba antes (R5) con el destello.
- **Ronda de confirmación (1 de 1):** se regeneraron las 55 capturas y los siete recortes; se miraron el hero a 1280 y los recortes 7 y 6. Con la primera corrección de `geo` (y 350) el mango de la lupa se fundía con la píldora; se corrigió a y 394 en esa misma ronda y quedó despejada. Suite completa: 234 pasadas, 55 omitidas, 0 fallos; guardas 139 de 139.
- **Comprobación por mutación de las pruebas de Playwright de la hoja y las escenas** (pendiente de la tarea 3): se quitó `aria-hidden` de `Pill.astro` y de la raíz de `CollageScene.astro`; collage-language y brand-assets pasaron a 6 fallos. Se restauraron ambos archivos (sin diff).
- **Lista de vibra:** rasgo 1 cumplido en el hero a 1280 x 800 (titular enorme en negrita, un solo elemento dominante, collage a la derecha, todo en el primer pantallazo). Rasgo 7 cumplido en hero, Por qué ahora y agenda (Loopy oficial como foco, sin plantilla).
- **Rasgos del moodboard por composición:** hero 1, 3, 4, 6, 7 (2 pendiente, ranura vacía); whynow 1, 3, 4, 6, 7 (2 pendiente, ranura vacía); agenda 1, 3, 4, 6, 7 (sin ranura); pegatinas y chips 1, 3, 4, 6 (7 solo en la sombra dura); avatares 1 y 3.
- **Pendiente fuera de 02-10:** rasgo 2 (recorte en media tinta sobre las ranuras hero y whynow, plan 02-11).
- **Conflicto resuelto a favor de la marca:** ver ronda 0.

## Lote P (fotos), ronda 0

- **Plan 02-11, tarea 4.** Rasgo 2 del moodboard (recortes fotográficos en blanco y negro con granulado o media tinta) sobre las dos ranuras de foto de 02-10: hero (ojo tras una lupa, `hero-a`) y Por qué ahora (mano con un teléfono, `whynow-a`), con las candidatas `hero-b` y `whynow-b` en la hoja.
- **Skills invocadas** con la herramienta Skill, por primera vez en este plan (las tareas 1 a 3 siguieron el encargo del orquestador de usarlas solo aquí): `impeccable` (contexto sin entrevista, porque el ejecutor corre sin una persona que responda; PRODUCT.md y DESIGN.md ya existían; `critique` con `colorize` y `polish` como lente) y `design-taste-frontend`.
- **Crítica degradada:** `critique` pide dos sub agentes aislados (revisión de diseño y detector) y esta sesión no expone herramienta de sub agentes, así que la revisión de diseño y el detector corrieron en un solo contexto. El detector (`impeccable detect --json src/components/collage`) devolvió un solo aviso consultivo, `Pill.astro` con `border-radius: 0.45em` fuera de la escala de DESIGN.md (pieza de 02-10, no de las fotos); `CollagePhoto.astro` sin hallazgos. No se guardó instantánea de crítica.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca con recortes en media tinta, accesibilidad por encima de la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin sombras difusas, sin animación de las fotos (es de 02-07).
- **Conflictos resueltos a favor de las decisiones del proyecto:** `design-taste-frontend` desaconseja la fotografía de stock y pide imágenes generadas; Juan decidió stock con licencia y sin imágenes generadas (2026-09-19). La skill pide etiquetas sobre imágenes y pies de foto solo si son funcionales: las fotos son decorativas, sin pie ni etiqueta (el rótulo `code` de la hoja es herramienta interna). Su regla de sombra tintada al fondo cede a la sombra dura del lenguaje de marca. Las reglas de serif, guion largo y CTA no aplican a esta tarea.
- **Verbos usados:** `critique` (juicio de a a g), `colorize` (lente: par tinta y relleno, contraste 14.37 sobre crema y 16.10 sobre blanco, medido por la guarda, sin cambios) y `polish` (recorte y brillo del tratamiento). `adapt` no hizo falta: nada se desborda ni se cruza a 320 px.

## Lote P (fotos), ronda 1

- **Mediciones** (`PHASE2_BATCH=P-fotos`, bloque 'informe de fotos', ClickUp bloqueado, todo lo que no es localhost abortado; `test-results/phase2/P-fotos-medicion.json`). Con las fotos corregidas, a 390x844 y a 1280x800: LCP es `H1#hero-title` (172 ms y 184 ms en la corrida de confirmacion, 276 ms y 40 ms en la ultima; varia con la carga de la maquina), CLS 0, dos imágenes de 11812 y 3597 bytes (15409 en conjunto, tope 40960), 8 peticiones y 124109 bytes en total (documento 39497, hojas de estilo 26971, fuentes 42232, imágenes 15409). Antes de la corrección: 13006 y 3306 bytes, 16312 en conjunto. Decisión de carga: el LCP no es una img y el CLS es 0 en las dos medidas, así que el hero sigue eager.
- **Capturas** (`PHASE2_BATCH=P-fotos E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/collage-photos.spec.ts tests/e2e/page-structure.spec.ts -g "captura|informe de fotos"`, build servido en 4322, 23 pruebas): `P-fotos-{320,390,768,1024,1280}` con sus variantes `-reduce` y `-nojs`, `P-fotos-hero-<ancho>`, `P-fotos-whynow-<ancho>`, `P-fotos-hoja-<ancho>`, `P-fotos-foto-<id>-1280` de las cuatro candidatas y `P-fotos-frame-{hero,whynow}-1280-dpr{1,2}`, en `test-results/phase2/` (no versionadas). Se miraron enteras: los marcos con factor 2, hero y whynow a 1280, hero y whynow a 320 y 390, y la hoja por candidata. 768 y 1024 se juzgaron por las mediciones de los specs (marco igual a su ranura, sin cruces, sin scroll horizontal).
- **Los seis recortes lado a lado** (script de Pillow fuera del repositorio, `python3 compare.py` desde la raíz; referencia a la izquierda, nuestra captura a la derecha, a la misma altura, `P-fotos-compare-<n>.png`). Cajas del moodboard (izquierda, arriba, derecha, abajo): (1) moodboard.png (940, 132, 1120, 402) contra `frame-hero-1280-dpr2`; (2) moodboard.png (444, 640, 790, 938) contra la vista previa de `hero-a` sobre el papel (`treat.mjs --id hero-a --dry --preview`); (3) moodboard.png (880, 130, 1300, 410) contra `hero-1280`; (4) moodboard.png (713, 138, 947, 398) contra `frame-whynow-1280-dpr2`; (5) `foto-hero-a-1280` contra `foto-hero-b-1280`; (6) `foto-whynow-a-1280` contra `foto-whynow-b-1280`. Las cajas del moodboard se ajustaron mirando el recorte.
- **Veredicto de los puntos a a g** (sobre la primera captura, antes de corregir):
  - (a) Lenguaje: cumple con reserva. La media tinta en tinta oscura sobre crema se lee como recorte impreso en blanco y negro, no como filtro, y el sujeto es reconocible. Pero es más dura que el moodboard: el ojo de la referencia es gris continuo con grano fino sobre un disco plano, y el nuestro es binario con punto grueso (celda de 3 px de pantalla), más cerca de un grabado que de un granulado. El marco es una tarjeta con esquina redonda y sombra dura, no un recorte libre que sobresale del disco como en el moodboard (decisión 4 del plan). Los rostros en azul del moodboard (par 2) son de media tinta suave sobre color pleno: no se reproducen, la tinta es siempre la oscura.
  - (b) Legibilidad: en el tamaño grande se lee bien; en el chico (88 px de ancho del hero a 320 px) el ojo se lee pero el iris era pequeño porque el recorte incluía párpado y mejilla. Whynow a 320 px (73 px): la mano se lee y el teléfono es una mancha negra con ruido. Hallazgo corregido en parte (ver abajo).
  - (c) Moiré: no se ve moiré ni aliasing a los cinco anchos ni con factor 1 y 2; a 320 px la trama se funde en gris, que es lo que hace el navegador al reducir. No hizo falta un segundo tamaño con `srcset`.
  - (d) Integración: cumple. El marco se apila sobre el disco con relleno, radio y sombra de la ranura y no compite con Loopy, que sigue siendo el foco por tamaño y color. Hallazgo: la banda oscura de la ceja era la masa más pesada del collage; se aclaró.
  - (e) Cruces: cumple. Ninguna píldora, garabato ni marco tapa texto, CTA ni el sujeto de la foto a los cinco anchos (R12 y las pruebas de caja).
  - (f) Por qué ahora: cumple. La foto no cruza el h2 ni la lista y no desborda a 320 px (`scrollWidth` igual al ancho, medido a los cinco anchos).
  - (g) Candidatas (recomendación para Juan y Ari, no una decisión): hero, `hero-a` se lee como el ojo con lupa del moodboard y `hero-b` (mano con una lupa) es ilegible a tamaño de escena, se lee como una mancha de trama sobre el papel; se recomienda `hero-a`. Whynow, `whynow-b` (manos tecleando) tiene más detalle de línea y se reconoce mejor a 104 px, pero su fondo desenfocado no es liso; `whynow-a` (mano con teléfono) tiene la silueta más limpia como recorte. Empate razonable, se deja `whynow-a` elegida y la decisión es de ellos.
- **Correcciones (un solo lote, commit `483d6f2`, solo tratamiento de fotos):** `hero-a` recorte de left 0.12, top 0.2326, ancho 0.68, alto 0.588 a left 0.26, top 0.28, ancho 0.58, alto 0.5017 (el iris llena el marco) y brillo de 165 a 175 (cobertura de 0.495 a 0.437, PNG de 9387 bytes); `whynow-a` recorte de left 0.194, top 0.4675, ancho 0.5625, alto 0.3894 a left 0.24, top 0.565, ancho 0.46, alto 0.3185 (menos teléfono, mano más grande; cobertura de 0.247 a 0.32, PNG de 2886 bytes). No se tocaron escenas, componente, copy, tokens ni dependencias.
- **Ronda de confirmación (1 de 1):** se regeneraron capturas y recortes y se miraron los pares 1 a 4 y el hero y whynow a 320. El ojo de `hero-a` se lee ahora como ojo con iris, párpado y ceja también a 88 px. En `whynow-a` la mano es más grande y el gesto de tocar se lee; el teléfono sigue siendo una mancha oscura con ruido a 73 px: queda como observación abierta (pregunta para Juan y Ari, o elegir `whynow-b`). Guardas 162 de 162 tras la corrección.
- **Lista de vibra:** rasgo 2 cumplido en el hero y en Por qué ahora (recorte fotográfico en blanco y negro sobre una ranura, con sombra dura), con la reserva de (a). Todos los rasgos: hero 1, 2, 3, 4, 6, 7; whynow 1, 2, 3, 4, 6, 7; agenda 1, 3, 4, 6, 7.
- **Comprobación de las fotos:** las cuatro muestran manos o un ojo de personas reales y la fuente no registra permiso de modelo; se avisa a Ari en LICENSES.md y en el SUMMARY. Ninguna foto se publica en producción sin aprobación (puerta de `check-photos.mjs`).

## Lote C1 (Logramos juntos y Casos), ronda 0

- **Plan 02-04.** Lote C1: Lo que logramos juntos (`#resultados`, tono dark) y Casos de éxito (`#casos`, tono light). El equipo lo hace 02-05 con el nombre "Lote C (equipo)".
- **Skills invocadas** con la herramienta Skill: `impeccable` (`context` sin entrevista, porque el ejecutor corre sin una persona que responda; PRODUCT.md y DESIGN.md ya existían; verbo `shape` para el lote) y `design-taste-frontend`.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con las reglas de accesibilidad por encima de la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animaciones.
- **Verbos usados en esta ronda:** `context` y `shape` (construcción desde el contrato; la forma de las dos secciones ya venía fijada en el UI-SPEC).
- **Patrón tomado de cada referencia para Lo que logramos juntos:** afirmaciones de resultado en grande sobre fondo oscuro con discos de check (skale.so) y la rejilla 2x2 (rankingonai.com); adaptado con borde amarillo de 3 px, disco de check amarillo con marca oscura, sin fondo de tarjeta y con "FALTA CONFIRMAR" en el mismo estilo donde falta respaldo.
- **Patrón tomado de cada referencia para Casos de éxito (se construye en la tarea 2):** tarjeta con cifra dominante, marcador sobre la cifra y chip en la esquina (rankingonai.com); numerales grandes en el color de acento (m8l.com); líneas de resultado (skale.so). La tarjeta de Meta Ads es el bloque distinto, con la lupa del collage oficial que resalta la cifra.
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` desaconseja el morado de IA (es el azul violeta del BrandBook), los SVG dibujados a mano (el disco de check es una forma geométrica de dos trazos y el sprite oficial no trae un check) y las tarjetas iguales (la variación es la tarjeta ancha de Meta Ads y el cambio de tono entre las dos secciones). Prohíbe el guion largo: ningún texto propio lo usa y el copy de Ari no se toca.
- **Capturas:** ninguna en esta ronda (las de sección son de la tarea 3).

## Lote C1 (Logramos juntos y Casos), ronda 1

- **Pruebas primero.** Se sumaron (p) a (v) a `tests/e2e/results-cases.spec.ts`: (p) sin scroll horizontal y rectángulos dentro de `[0, innerWidth]` a 320, 390, 768, 1024 y 1280 px; (q) a 320 y 1280 px la cifra "+3.808%" y cada `dd` con el relleno dentro de su tarjeta y sin desborde; (r) padding vertical de 64 px a 390 px y 96 px a 1024 y 1280 px; (s) sin JavaScript, todos los textos del YAML y el CTA con `href="#agenda"`; (t) espaciado de texto forzado de SC 1.4.12 a 320 y 1280 px; (u) la cifra es lo más grande de cada tarjeta y el marcador es amarillo; (v) capturas por sección solo con `PHASE2_BATCH`. **Ninguna falló antes de iterar**: las 27 pruebas nuevas pasaron con la construcción de las tareas 1 y 2 (quedan como regresión); (r) y (u) se definen solo en los anchos que miden (sin `skip`), y (v) es el único bloque omitido sin lote.
- **Skills invocadas** con la herramienta Skill: `impeccable` (`context` sin entrevista, con PRODUCT.md y DESIGN.md existentes; verbos `critique`, `layout`, `typeset`, `colorize` y `adapt` como lente; sin `animate`) y `design-taste-frontend` (diales 7, 3 y 4).
- **Crítica degradada:** `critique` pide dos sub agentes aislados y esta sesión no expone herramienta de sub agentes, así que la revisión de diseño corrió en un solo contexto y no se guardó instantánea.
- **Capturas** (`PHASE2_BATCH=C1 E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts tests/e2e/results-cases.spec.ts -g captura`, 20 pruebas, 25 archivos): `C1-{320,390,768,1024,1280}` con `-reduce` y `-nojs` (15 de página completa) y `C1-resultados-<ancho>` y `C1-casos-<ancho>` (10 por sección), en `test-results/phase2/` (no versionadas). Se miraron `C1-resultados-390`, `C1-resultados-1280`, `C1-casos-390` y `C1-casos-1280` antes de corregir, y `C1-casos-1280` y `C1-casos-320` en la confirmación. 768 y 1024 px se juzgaron por las mediciones (rejilla, no cruce de la lupa, sin desborde).
- **Referencia de marca** (`brand-inventory/moodboard.png` y `ai_a.png`, con la herramienta Read): la tarjeta con cifra en azul violeta sobre bloque plano, sombra dura y aire generoso coincide con el moodboard (rasgos 1, 3 y 4); la lupa oficial es la del render de `ai_a.png`, sin recolorear.
- **Hallazgos de la crítica:**
  1. **Defecto real, chip de la tarjeta ancha estirado.** Al pasar a rejilla, el chip "Meta Ads" ocupaba todo el ancho de su columna (una pastilla larga, distinta de las cuatro tarjetas hermanas) a 390 y a 1280 px.
  2. **Punto señalado por el ejecutor anterior, lupa como elemento de rejilla.** La fila del chip mide 99 px por la lupa y dejaba un hueco bajo el chip a la izquierda (unos 67 px), con la cifra empujada hacia abajo. Evaluado: no se revierte a `position: absolute` (perdería la garantía de no cruce por construcción y las pruebas de no cruce y de apilado son reales); se resuelve con composición.
  3. Chip crema frente a la cifra: no compite. El chip es Label 600 de 2 rem de alto con borde de 3 px, y la cifra (Display 700, morado, marcador amarillo) sigue siendo lo más grande de cada tarjeta (prueba (u)). Sin cambio.
  4. "FALTA CONFIRMAR" en chip y `dd` a 320 px: se lee entera, sin recorte ni desborde (prueba (q)). El chip repetido en cuatro tarjetas y el `dd` de Canal es consecuencia del dato pendiente (supuesto 8 del plan), no de diseño. Sin cambio.
  5. Lo que logramos juntos: el 2x2 de borde amarillo de 3 px con disco de check se lee limpio sobre `dark`; la tarjeta del resultado 2 queda casi vacía por el dato pendiente, lo esperado. Sin collage en esta sección (el contrato solo pone la lupa en Casos). Sin cambio.
  6. `design-taste-frontend`: sin degradados de fondo, sin vidrio, sin sombras difusas; el marcador es una tonalidad plana. La variación contra "tres tarjetas iguales" es la tarjeta ancha con lupa y el cambio de tono dark a light. Sin cambio.
- **Correcciones (un solo lote, solo el estilo local de `MetricCard`):** en la variante ancha el chip pasa a `justify-self: start` (deja de estirarse) y a `align-self: center` frente a la lupa (fila de cabecera: chip a la izquierda, lupa a la derecha, sin hueco huérfano bajo el chip); desde 40 em las filas son `auto auto 1fr`, con la cifra y el `dl` anclados abajo (`align-self: end`) y la cabecera arriba. Ningún texto, token, componente de otro plan ni esquema cambió.
- **Ronda de confirmación (1 de 1):** se regeneraron las 25 capturas; a 320 y 390 px la cabecera es un chip con la lupa a la derecha y desaparece el hueco; a 1280 px el chip ya no se estira y la composición es diagonal (chip y lupa arriba, cifra abajo a la izquierda y datos abajo a la derecha). **Observación abierta:** a 1280 px la tarjeta ancha se estira a la altura de la cuarta y queda un vacío de unos 180 px entre la cabecera y la cifra; se lee como aire intencional del lenguaje del moodboard y se deja, pero conviene mirarlo con el copy real (si Ari da una línea de detalle para el caso 5, ocupa esa banda).
- **Revisión de la lupa frente a la cifra (comprobación de respaldo):** a 320 px la lupa (96 px) comparte fila con el chip y queda por encima de la cifra, sin tocar glifos ni el `dl`; a 1280 px queda en la esquina superior derecha, a más de 400 px de la cifra y sin cruzar el `dl`. El arte oficial no se recolorea ni se redibuja; `rotate={6}` se conserva. La prueba (l) confirma que no cruza chip, nombre, detalle ni `dl` y que la cifra queda por encima si hubiera solape.
- **Lista de vibra (numeración del plan):** rasgo 2 (aire de 96 px en escritorio) cumplido, medido por (r); rasgo 3 (tarjetas con borde de 3 px y sombra dura, sin sombras difusas) cumplido en Casos, y Lo que logramos juntos usa borde amarillo de 3 px sin sombra, según su contrato; rasgo 4 (cifra lo más grande con marcador amarillo) cumplido, medido por (u); rasgo 7 (foco claro y layout propio por sección) cumplido: 2x2 con discos de check sobre `dark` y rejilla 3 más ancha con lupa sobre `light`.
- **Suite:** 324 pasadas, 70 omitidas (capturas sin `PHASE2_BATCH`), 0 fallos; guardas 162 de 162; contraste 14 de 14.

## Lote C (equipo), ronda 0

- **Plan 02-05, tarea 1 (tracer).** Lote C (equipo): Quiénes somos (`#nosotros`, tono yellow). Las secciones Qué incluye y Cómo funciona son el lote D del mismo plan.
- **Skills invocadas** con la herramienta Skill: `impeccable` (`context` sin entrevista, porque el ejecutor corre sin una persona que responda; PRODUCT.md y DESIGN.md ya existían y no se repitió `init` ni `document`; verbo `shape`, que sin persona devuelve un brief con supuestos: el contrato de la sección 7 del UI-SPEC ya fija trabajo, evidencia, dirección y límites) y `design-taste-frontend`.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animación.
- **Brief de `shape` (supuestos):** el visitante es un dueño de negocio que baja a ver quién está detrás; necesita nombre, cargo y una señal de marca, no un currículum. Sin biografía, credencial, enlaces ni fotos (UI-SPEC sección 7 y STATE). Cuatro tarjetas pop blancas sobre amarillo con borde oscuro de 3 px y sombra dura, rejilla de 1, 2 y 4 columnas. Cada avatar es un Loopy oficial de 02-10 sobre un disco plano. Foco de la sección: el set de cuatro avatares como una familia.
- **Decisión de asignación de avatares:** por posición desde la constante `AVATARS` del componente (no desde el YAML, para no romper la guarda FND-02): Arianna `ojo-morado`, Verónica `ojo-amarillo`, Juan `ojos-morado`, Miguel `ojos-amarillo`. Solo cambian la cantidad de ojos y el color del disco; sin caras, accesorios ni parecido.
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` desaconseja los avatares genéricos y las tarjetas iguales; aquí el avatar es el Loopy oficial de la marca (arte de 02-10) y la rejilla de cuatro tarjetas la fija el contrato. Prohíbe el guion largo: ningún texto propio lo usa y el copy de Ari no se toca.
- **Capturas:** ninguna en esta ronda (las del lote C son de la tarea 3).

## Lote C (equipo), ronda 1

- **Plan 02-05, tarea 3.** Mediciones a cinco anchos primero (spec `team-includes-how.spec.ts`: desborde y rectángulos a 320, 390, 768, 1024 y 1280 px; espaciado de texto de SC 1.4.12 a 320 y 1280 px; contraste medido con `contrastRatio`; tarjetas pop y avatares a cinco anchos; movimiento con `reduce` y `no-preference`; peso; sin JavaScript; herramienta de capturas). Las 48 pruebas del archivo pasaron con la construcción de las tareas 1 y 2: ninguna medición falló, así que las correcciones de abajo salen de la crítica visual, no de una medición roja.
- **Skills invocadas** con la herramienta Skill: `impeccable` (contexto ya cargado en PRODUCT.md y DESIGN.md, sin entrevista; verbos `critique`, `layout`, `colorize`, `typeset` y `adapt` como lente) y `design-taste-frontend` (diales 7, 3 y 4). La crítica corrió en un solo contexto (sin sub agentes aislados).
- **Capturas** (`PHASE2_BATCH=C-equipo E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/team-includes-how.spec.ts -g captura`, 15 archivos): `test-results/phase2/C-equipo-{320,390,768,1024,1280}` con `-reduce` y `-nojs` (no versionadas). Se miraron `C-equipo-390` y `C-equipo-1280` antes de corregir y `C-equipo-1280` en la confirmación; 320, 768 y 1024 px se juzgaron por las mediciones.
- **Referencia de marca** (`brand-inventory/moodboard.png` y `ai_a.png`, con la herramienta Read): bloques planos de color pleno, sombra dura y aire generoso; el lote las cumple (amarillo pleno, tarjetas blancas con borde de 3 px y sombra dura, Loopy oficial sobre disco plano). `ai_a.png` (filas 13 a 16) muestra el mismo Loopy de doble ojo con anillo morado o crema sobre blanco, morado, naranja y amarillo: los cuatro avatares siguen esa misma gramática de color.
- **Hallazgos de la crítica:**
  1. **Defecto real, cargos desalineados a 1024 y 1280 px.** "Verónica Romero" baja a dos líneas en la tarjeta de 250 px y empuja su cargo 30 px más abajo que los otros tres.
  2. **Defecto real, avatar corrido.** El arte oficial trae unos 7 % de aire a la izquierda: el disco arrancaba 8 px a la derecha del nombre (7 px a 390 px).
  3. Rasgo 3 (borde de 3 px, sombra dura, radio parejo, sin sombra difusa): cumplido y medido a cinco anchos. Rasgo 5, set de cuatro avatares como familia uniforme: cumplido (mismo tamaño medido, mismo disco plano con sombra dura, solo cambian los ojos y el color; sin caras). Rasgo 7, foco claro: cumplido (el set de cuatro Loopy es el elemento de collage de la sección).
  4. Los cuatro nombres son `pending`: se muestran tal cual y siguen listados en `PENDING-COPY.md`.
- **Correcciones (un solo lote, solo `Team.astro`):** desde 64 em el `h3` reserva dos líneas con `min-height: 2lh` (sigue el interlineado, así que el espaciado de texto de SC 1.4.12 no lo rompe) y el avatar se compensa con `margin-inline-start: -6.7%` sobre el SVG (el ancho medido de 96 y 120 px no cambia). No se editó el arte de los avatares.
- **Ronda de confirmación (1 de 1):** se regeneraron las capturas. A 1280 px los cuatro cargos quedan a la misma altura y el disco de cada avatar arranca sobre el borde izquierdo del nombre. Costo asumido: en las tarjetas de nombre corto queda una línea de aire extra entre nombre y cargo. Las 48 pruebas siguen en verde.
- **Uniformidad del set de avatares (comprobación de respaldo):** los cuatro se leen como una familia (mismo círculo, misma sombra, misma lupa, uno o dos ojos y morado o amarillo). Se anota para Ari y para 02-08: las dos variantes amarillas comparten el color del fondo de la sección, y sobre la tarjeta blanca se separan solo por la sombra dura y la lupa morada; se leen bien.

## Lote D (incluye y cómo funciona), ronda 0

- **Plan 02-05, tarea 2 (construcción).** Lote D: Qué incluye (`#incluye`, tono light) y Cómo funciona (`#como-funciona`, tono dark). Skills invocadas con la herramienta Skill: `impeccable` (contexto sin entrevista; verbos `shape` y `layout`, brief por supuestos) y `design-taste-frontend` (diales 7, 3 y 4).
- **Lectura de diseño (design-taste-frontend):** landing de captación para dueños de e-commerce con lenguaje collage pop; Qué incluye abierto y sin tarjetas para variar respecto a Casos y Equipo; Cómo funciona oscuro con riel naranja y discos amarillos numerados, sin movimiento. Conflictos resueltos a favor de la marca y del contrato: `design-taste-frontend` desaconseja "three equal feature cards" y las etiquetas de paso genéricas; aquí la rejilla de seis es abierta y sin tarjeta, y los números de fase son el contador de la lista ordenada del contrato, no una etiqueta "Paso N". Ningún texto propio usa guion largo; el copy de Ari no se toca.
- **Capturas:** ninguna en esta ronda.

## Lote D (incluye y cómo funciona), ronda 1

- **Verbos usados:** `critique`, `layout`, `clarify` y `adapt` (`impeccable`) más la revisión de `design-taste-frontend` (diales 7, 3 y 4). Mismas mediciones que el lote C, todas en verde antes de iterar.
- **Capturas** (`PHASE2_BATCH=D-incluye-como`, 15 archivos): `test-results/phase2/D-incluye-como-{320,390,768,1024,1280}` con `-reduce` y `-nojs` (no versionadas). Se miraron 390 y 1280 px antes de corregir, y 1280 y 320 px en la confirmación; 768 y 1024 px por las mediciones.
- **Referencia de marca** (`moodboard.png` y `ai_a.png`, con la herramienta Read): fondos planos de tono lleno (blanco, negro `#212121`, naranja y amarillo en las tarjetas de logo de `ai_a.png`), acento naranja y amarillo, sin degradados; coincide con el sistema (oscuro con acentos amarillo y naranja, claro con morado).
- **Hallazgos de la crítica:**
  1. **Defecto real, Cómo funciona a 1024 y 1280 px.** "Llamada de conocimiento" baja a dos líneas y empuja su descripción; con descripciones de 4 y 3 líneas los chips de plazo quedaban a alturas distintas en las cuatro fases.
  2. Qué incluye respira frente a los bloques con tarjeta vecinos: ítems abiertos con borde superior de 3 px, sin tarjeta ni sombra (rasgo 3 en su variante abierta). Rejilla de 1, 1, 2, 3 y 3 columnas medida. Sin cambio.
  3. El chip de plazo de dos líneas no desborda a 320 px (medición de espaciado de texto y captura) y el conector discontinuo naranja no cruza texto: a 1280 px corre por detrás de los discos hasta el centro del siguiente, y en vertical corre por la columna del disco. Sin cambio.
  4. Contraste medido: texto de 4.5 o más y bordes de 3 o más (chip amarillo sobre oscuro, disco de check con borde oscuro sobre claro). Sin cambio.
  5. Nota para Ari (copy, no se toca): las descripciones de Qué incluye empiezan en minúscula con "para que..." y se leen como continuación del título; ya se anotó en el SUMMARY para que confirme si así se quiere.
- **Correcciones (un solo lote, solo `HowItWorks.astro`, desde 64 em):** `h3` con `min-height: 2lh`, `.step` con filas `auto 1fr`, `.step-body` en columna con el chip de plazo al pie (`margin-top: auto`) y descripción con `margin-bottom: 1rem`. El chip conserva `max-width: 100%` y `overflow-wrap: anywhere`.
- **Ronda de confirmación (1 de 1):** a 1280 px las cuatro descripciones arrancan a la misma altura (la fase 1 llena las dos líneas de título reservadas y las otras tres las reservan) y los cuatro chips quedan sobre la misma línea. A 320 px la columna con riel vertical se mantiene sin cambios. Las 48 pruebas siguen en verde.
- **Lista de vibra:** rasgo 3 (bordes de 3 px, sin sombra difusa) cumplido en Qué incluye y en el chip; rasgo 5 no aplica a estas dos secciones; rasgo 7 (foco claro y layout propio) cumplido: rejilla abierta de seis con discos de check sobre `light` y línea de cuatro fases con riel sobre `dark`.
- **Suite de cierre:** 372 pasadas, 85 omitidas (capturas sin lote), 0 fallos; guardas 168 de 168; contraste 14 de 14; `list-pending --check` sale 0 (36 pendientes); `PUBLIC_ENV=production npm run build` falla solo por PENDING (comprobación JSON: 0 estructurales, solo PENDING y MISSING).

## Lote D (parte 06: Para quién es y FAQ), ronda 0

- **Plan 02-06, tarea 2 (construcción).** Lote D, parte 06: Para quién es (`#para-quien`, tono light) y FAQ (`#faq`, tono yellow). Skills invocadas con la herramienta Skill antes de escribir los componentes: `impeccable` (contexto sin entrevista, PRODUCT.md y DESIGN.md ya existentes; verbos `shape`, `layout`, `clarify` y `adapt`, brief por supuestos) y `design-taste-frontend` (diales 7, 3 y 4). Esta entrada se anotó junto con la ronda 1, después de construir.
- **Lectura de diseño (design-taste-frontend):** landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin sombras difusas, sin animación ni transición.
- **Brief de `shape` (supuestos):** el visitante ya vio qué se hace y cómo se trabaja y ahora se pregunta "¿es para mí?" y "¿qué me falta saber?". Para quién es debe distinguirse sin color (icono, trazo del borde y h3); el FAQ debe leerse como filas grandes con borde e icono más/menos (rasgo 6). El texto de ambas secciones aún es "FALTA CONFIRMAR": el diseño se mide con ese relleno y con textos largos inyectados en una comprobación aparte.
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` desaconseja las tarjetas iguales y el encabezado partido (titular a la izquierda, contenido a la derecha); aquí las dos tarjetas de Para quién es difieren en tono, trazo, sombra e icono, y el FAQ parte h2 y lista en 5 a 7 porque el contrato de la sección 11 lo fija y la lista es contenido real. La sombra dura `4px 4px 0` es el lenguaje pop de la marca (moodboard), no una decoración añadida. Ningún texto propio usa guion largo; el copy de Ari no se toca.

## Lote D (parte 06: Para quién es y FAQ), ronda 1

- **Verbos usados:** `critique`, `layout`, `clarify` y `adapt` (`impeccable`) más la revisión de `design-taste-frontend` (diales 7, 3 y 4). La crítica corrió en un solo contexto (sin sub agentes aislados).
- **Mediciones primero** (`closing-sections.spec.ts`, ClickUp bloqueado): 44 pruebas verdes con la primera construcción, entre ellas columnas y gap de 24 px a 320, 390, 640, 768, 1024 y 1280 px, alturas iguales, estilos calculados de ambas tarjetas, FAQ con `summary` de 44 px o más y padding de 16 y 24 px, tarjetas de 3 px con radio de 16 px separadas 16 px, anillo de foco de 3 px con offset negativo dentro de la tarjeta, proporción 5 a 7 con gap de 48 px, Enter y Espacio con y sin JavaScript, cero animaciones con `reduce` y `no-preference` antes y después de abrir. Suite completa: 414 pasadas, 89 omitidas, 2 fallos ajenos a este lote (ver el resumen del plan: el conteo de "FALTA CONFIRMAR" visibles de 02-03 no descuenta las respuestas del FAQ cerradas).
- **Capturas** (`PHASE2_BATCH=D06 E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts tests/e2e/closing-sections.spec.ts -g "captura|recorte"`, 19 archivos): `test-results/phase2/D06-{320,390,768,1024,1280}` con `-reduce` y `-nojs` (15, no versionadas) más `D06-para-quien-{390,1280}` y `D06-faq-{390,1280}`. Se miraron los cuatro recortes de 390 y 1280 px; 320, 768 y 1024 px se juzgaron por las mediciones (ninguna falló).
- **Referencia de marca** (`brand-inventory/moodboard.png` y `ai_a.png`, con la herramienta Read): bloques planos de color pleno (amarillo, morado, negro, naranja), aire generoso, bordes y bloques sólidos sin degradado; las filas grandes con borde del FAQ y la tarjeta amarilla con sombra dura siguen esa gramática. El plus y el minus son un disco con trazo oscuro, del mismo peso que el borde de 3 px de las tarjetas.
- **Hallazgos de la crítica:**
  1. Para quién es se distingue sin color: tarjeta amarilla con borde sólido, sombra y checks frente a tarjeta blanca con borde discontinuo, sin sombra y con x. Sin cambio.
  2. FAQ a 1280 px: h2 a la izquierda y lista a la derecha dejan un vacío bajo el título; se lee como aire intencional del contrato (rasgo 2), no como defecto. Sin cambio.
  3. Con textos largos inyectados en una comprobación aparte (pregunta de cuatro líneas, respuesta de unas 400 palabras con una URL sin espacios), a 320 y 1280 px no hay desborde horizontal, el icono no se corta, el minus aparece al abrir y el disco del hover se rellena de amarillo (`rgb(255, 198, 2)`).
  4. Anillo de foco: morado del tono light dentro de la tarjeta blanca, a 2 px del borde oscuro; se distingue del borde y no se sale. Sin cambio.
  5. El relleno "FALTA CONFIRMAR" deja las dos tarjetas de Para quién es con poco contenido; es consecuencia del dato pendiente, no de diseño.
- **Correcciones:** ninguna. No hubo defecto medible ni visible que corregir, así que no hizo falta ronda de confirmación.
- **Lista de vibra:** rasgo 3 (borde de 3 px, sombra dura, radio parejo, sin sombra difusa) cumplido; rasgo 6 (FAQ de filas grandes con borde e icono más/menos) cumplido; rasgo 7 (foco claro y layout propio por sección: dos tarjetas contrastadas, y h2 con lista en 5 a 7) cumplido; rasgo 8 no aplica (ninguna de las dos secciones lleva CTA).
- **Pendiente manual (fila interaction):** probar el `<summary>` del FAQ con VoiceOver (macOS con Safari) y con NVDA o TalkBack: el anuncio de "contraído/expandido" y la lectura de la respuesta al abrir. Lo hace una persona; ningún ejecutor puede darlo por verificado.

## Lote E (parte 06), ronda 0

- **Plan 02-06, tarea 3 (construcción).** Lote E, parte 06: `#agenda` como CTA final (titular del doc de Ari y `AgendaCollage`), footer completo y `/privacidad` con ese footer. Skills invocadas con la herramienta Skill antes de escribir los componentes: `impeccable` (contexto sin entrevista, PRODUCT.md y DESIGN.md ya existentes, `craft-floor.md` cargado; verbos `layout`, `colorize` y `harden`) y `design-taste-frontend` (diales 7, 3 y 4).
- **Lectura de diseño (design-taste-frontend):** cierre de una landing de captación B2B para dueños de e-commerce, en lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética. Diales: DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4. Sin degradados, sin vidrio, sin animación ni transición nueva.
- **Conflictos resueltos a favor de la marca y del contrato:** `design-taste-frontend` pide no repetir CTA de la misma intención y evitar SVG dibujados a mano; aquí `#agenda` no lleva ningún CTA porque el formulario ya es el destino, y el collage usa el Loopy oficial de la marca (arte de Ari), no un icono dibujado a mano. También desaconseja los bloques de tarjeta idénticos: el footer no lleva tarjetas. El titular de Ari se muestra tal cual, con su errata 'estan'.
- **Colorize:** el footer sigue `light`: fondo blanco, tinta oscura, enlaces en `--link` (morado 9.69) con subrayado permanente y borde superior de 3 px. Sin acento nuevo: el morado y el amarillo ya viven en el logo y en el collage.
- **Capturas:** ninguna en esta ronda; ver la ronda 1.

## Lote E (parte 06), ronda 1

- **Verbos usados:** `critique`, `layout`, `colorize` y `harden` (`impeccable`) más la revisión de `design-taste-frontend` (diales 7, 3 y 4). La crítica corrió en un solo contexto (sin sub agentes aislados).
- **Mediciones primero** (`closing-sections.spec.ts`, ClickUp bloqueado): 37 pruebas nuevas, rojas antes de construir (20 fallaban) y verdes con la primera construcción: h2 con el texto del YAML y barra de 48x8 px, padding vertical de 64 px a 390, 768 y 1023 px y de 96 px a 1024 y 1280 px, cero `a[data-cta]` dentro de `#agenda` y cuatro en la página, collage después de `.agenda-fallback`, oculto bajo 1024 px y a 8 px o más de la tarjeta desde 1024 px, todo lo pintado dentro de la caja de su raíz, dos píldoras de `CHIP_WORDS`, footer en 1, 2 y 3 columnas con 1,2 a 1 a 1 y área de salvado del logo, enlace de privacidad de 44 px con subrayado y `PURPLE_RGB`, cero encabezados, y orden de tabulación exacto en `/privacidad` (skip, logo del header, CTA del header, logo del footer, privacidad).
- **Capturas** (`PHASE2_BATCH=E06 E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts tests/e2e/closing-sections.spec.ts -g "captura de lote|recorte"`, 25 archivos): `test-results/phase2/E06-{320,390,768,1024,1280}` con `-reduce` y `-nojs` (15) más `E06-agenda-{390,1280}`, `E06-footer-{390,1280}` y `E06-privacidad-{390,1280}` (6 recortes y 4 de sección; no versionadas). Se miraron los recortes de 390 y 1280 px; 320, 768 y 1024 px se juzgaron por las mediciones (ninguna falló). Nota de herramienta: Playwright vacía `test-results/` al empezar, así que los dos archivos de captura deben correr en el mismo comando.
- **Referencia de marca** (`brand-inventory/moodboard.png` y `ai_a.png`, con la herramienta Read): colores planos de tono lleno (morado, amarillo, naranja, crema), píldoras `seo`, `spy` y `team work` en negrita sobre plano, Loopy de doble ojo con lupa sobre disco de color; el collage de `#agenda` (disco amarillo con Loopy, píldoras `seo` y `team work`, trazos y retícula sobre el morado) y el logo horizontal oficial sobre blanco del footer (mesa 06 de `ai_a.png`) siguen esa gramática, sin degradados.
- **Hallazgos de la crítica:**
  1. **Defecto real, `/privacidad` con el footer a media ventana.** La página es corta y el footer terminaba a 380 px con un hueco blanco debajo hasta el borde de la ventana de 900 px. Rasgo de sitio, no de Ari: hay que pegarlo abajo.
  2. `#agenda` a 1280 px: titular de tres líneas a la izquierda, collage bajo el enlace de respaldo y tarjeta a la derecha; el collage no toca la tarjeta (separación de 8 px o más medida) y el titular con 'estan' se lee entero. Con ClickUp bloqueado la tarjeta se ve en blanco por diseño de la prueba, no por un defecto. Sin cambio.
  3. Footer a 1280 px en 1,2 a 1 a 1: logo alineado con la línea de las etiquetas, correo y redes con el relleno 'FALTA CONFIRMAR' y el enlace de privacidad a 8 px o más de cualquier texto. A 390 px una columna, con la separación de 32 px entre logo y nav (igual a la altura del logo). Sin cambio.
  4. Rasgo 3 (borde de 3 px, sin sombra difusa) cumplido en el footer y en la tarjeta; rasgo 8 (un solo CTA dominante) cumplido: ningún CTA nuevo en `#agenda`.
- **Harden (comprobación aparte con dos copias temporales del YAML, restaurado después):** correo `a@b.co?bcc=x@y.z` no emite `mailto:` y se muestra en texto plano; correo de 80 caracteres válido emite exactamente `mailto:` con ese valor y, junto con una URL de red social de más de 90 caracteres sin espacios, no produce desborde horizontal a 320 ni a 1280 px (el texto parte con `overflow-wrap: anywhere`); espaciado de texto forzado y el relleno 'FALTA CONFIRMAR' cubiertos por las mediciones. Sin alturas fijas en texto.
- **Correcciones (un solo lote):** `BaseLayout.astro` pasa el `body` a columna flexible con `min-height: 100dvh` y `main` con `flex: 1 0 auto` (prueba nueva: el footer de `/privacidad` llega al borde inferior con un hueco de 0.5 px o menos). No cambia la home, que ya es más alta que la ventana.
- **Ronda de confirmación (1 de 1):** el footer de `/privacidad` queda pegado al borde inferior a 1280 x 900; el orden de tabulación y el resto de mediciones siguen en verde.
- **Lista de vibra:** rasgo 2 (aire generoso) cumplido, rasgo 3 cumplido, rasgo 7 (foco claro y layout propio: titular con collage y tarjeta en `#agenda`, tres bloques en el footer) cumplido, rasgo 8 cumplido.
- **Pendiente manual (fila interaction), repetido del lote D06:** probar el `<summary>` del FAQ con VoiceOver (macOS con Safari) y con NVDA o TalkBack: el anuncio de contraído o expandido y la lectura de la respuesta al abrir. Lo hace una persona; ningún ejecutor puede darlo por verificado. También queda para una persona recorrer con teclado el iframe real de ClickUp (entrar y salir sin trampa de foco), fuera del alcance de estas pruebas con ClickUp bloqueado.

## Cierre, tracer (CIERRE0)

- **Lote:** `CIERRE0`, plan 02-08 tarea 1 (tracer de punta a punta: dependencia, prueba, build, captura, registro). Sin correcciones de `src/`.
- **Herramienta nueva** (`tests/e2e/phase-closing.spec.ts`, bloque 'hoja de contacto', solo con `PHASE2_BATCH`): `PHASE2_BATCH=CIERRE0 npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts -g "hoja de contacto"` genera 14 recortes por ancho (header, las 12 secciones de `main` y footer) a 1280 y 390 px en `test-results/phase2/CIERRE0-<id>-<ancho>.png` (28 archivos) y las hojas `CIERRE0-hoja-1280.png` (3 columnas) y `CIERRE0-hoja-390.png` (6 columnas). Con ClickUp bloqueado. No se versionan. Nota de herramienta: Playwright vacía `test-results/` al empezar cada corrida, así que las hojas se leen justo después de generarlas.
- **axe de humo** (`@axe-core/playwright` 4.13.0, etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa, sin iframe, ClickUp bloqueado): `/` y `/privacidad/` a 320, 390 y 1280 px con 0 violaciones en total (0 critical o serious, 0 moderate o minor). Se dejó la prueba en sus tres anchos y dos rutas desde el tracer; la tarea 2 no la amplía.
- **Primeras impresiones contra los 8 rasgos de vibra** (solo las dos hojas; ningún recorte abierto):
  1. Color plano de tono lleno y alternado (blanco, oscuro, amarillo, blanco, oscuro, blanco, amarillo, blanco, oscuro, blanco, amarillo, morado): se ve como lenguaje de marca, sin degradados.
  2. Bordes gruesos y sombra dura desplazada en tarjetas, píldoras y botón: consistente en las 12 secciones.
  3. Loopy (ojo o doble ojo con lupa) en el hero, Por qué ahora, La solución, equipo y `#agenda`, con píldoras `seo`, `geo`, `team work`: coincide con la gramática del moodboard.
  4. Fotos en media tinta (ojo en el hero, manos en Por qué ahora) dentro de un marco: presentes, pequeñas.
  5. Los cuatro CTA naranja son iguales; el primer pantallazo de 1280 px muestra titular, CTA y collage.
  6. Zonas con 'FALTA CONFIRMAR' (La solución, Para quién es, FAQ, footer) se leen como relleno claro, no como error de layout.
  7. Posibles puntos a mirar en CIERRE1: el hero a 1280 px tiene el texto largo de la descripción en cuerpo pequeño frente al titular; La solución muestra la tarjeta de equipo mucho más alta que su vecina de crema; la tarjeta blanca de `#agenda` queda vacía con ClickUp bloqueado (esperado).
  8. A 390 px las secciones mantienen una sola columna, sin recortes visibles.
- **Estado:** el tracer no encontró defectos que corregir. La matriz completa, el ciclo de `critique` y `polish` y la consolidación del lote 0 son de la tarea 2.

## Cierre, ronda CIERRE1 (plan 02-08 tarea 2)

- **Medición previa** (`tests/e2e/phase-closing.spec.ts`, commit 05919aa): matriz de desborde (`/` y `/privacidad/` a 320, 390, 640, 768, 1024 y 1280 px con `reduce` y `no-preference`: 24 pruebas), espaciado de texto SC 1.4.12 a 320 y 1280 px, sin JavaScript (12 secciones, `<details>` abre, 4 CTA), ClickUp bloqueado (12 secciones, footer, enlace de respaldo, cero `pageerror`), objetivos de 44 px, sombras y degradados, área de salvado del logo (header y footer a 390 y 1280 px) y peso y peticiones. Salieron en verde a la primera, con una sola precisión de definición: `MetricCard` pinta el marcador amarillo de la cifra con `linear-gradient(var(--mark), var(--mark))`, un relleno plano de un solo color, así que la prueba de degradados cuenta como degradado solo el `gradient` con más de un color (o que no sea lineal). No se tocó el CSS: el marcador no es un degradado a la vista.
- **Capturas:** `PHASE2_BATCH=CIERRE1` con `page-structure.spec.ts -g "captura"` (5 anchos por base, `reduce` y sin JavaScript, ClickUp bloqueado) y la hoja de contacto de la tarea 1 (14 recortes por ancho). Se miraron: hoja de 1280 y hoja de 390, `CIERRE1-390-nojs.png` y `CIERRE1-320-reduce.png` (partidas en columnas para leerlas), y solo dos recortes individuales: `inicio-1280` y `solucion-1280`. Referencias de marca vistas con la herramienta Read: `moodboard.png` y `ai_a.png`.
- **Skills invocadas con la herramienta Skill:** `impeccable` (setup con `impeccable context`, que cargó PRODUCT.md y DESIGN.md, y el verbo `critique` con su referencia leída) y `design-taste-frontend` (lectura declarada: landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética; DESIGN_VARIANCE 7, MOTION_INTENSITY 3, VISUAL_DENSITY 4; usada como lista final de antidefectos). `audit`, `polish`, `harden` y `adapt` se aplicaron con su criterio sobre la matriz medida de arriba, sin abrir sus referencias por separado. La `critique` corrió en un solo contexto porque este ejecutor no expone una herramienta de subagentes: DEGRADED single-context (sin subagente). El detector (`impeccable detect --json src`) sí corrió; la superposición en el navegador (`live-server`) no.
- **Detector:** 3 avisos "advisory" `design-system-radius` (0.25rem en el foco del logo, 0.45em en `Pill`, 3px en el resumen del FAQ), radios fuera de la escala de DESIGN.md. No se cambian: son formas aprobadas en lotes anteriores y cambiarlas rompería medidas ya verificadas. Los anota para Juan.
- **Hallazgos de la crítica (contra las 15 filas, los 8 rasgos y la brecha de marca de la sección 5 de 02-BRAND-INVENTORY.md):**
  1. **Defecto real (P2):** a 320 px la etiqueta del CTA de todas las ubicaciones caía a dos líneas (el botón medía 73 px de alto; el texto de 16.6 px con relleno de 24 por lado sumaba 289 px contra 288 de ancho útil). La skill de gusto lo prohíbe (CTA wrap ban).
  2. **Descripción del hero en cuerpo pequeño frente al titular (P3):** es el contrato del UI-SPEC (subtítulo y descripción en Body, máximo 55 ch) y una prueba lo fija (`a11y-base` (h): subtítulo en 400). No se cambia. Se anota como propuesta a Juan: subtítulo en Title (24 px) para subir la jerarquía entre el H1 de 64 px y el resto.
  3. **Tarjeta de equipo de La solución mucho más alta que su vecina de crema (P3):** es el contrato "alto parejo por fila" (UI-SPEC y prueba de rejilla). Cuando Ari entregue el cuerpo del pilar 4 la brecha se reduce; hoy la tarjeta de crema queda con relleno vacío bajo "FALTA CONFIRMAR". No se cambia.
  4. **Tarjeta de `#agenda` vacía con ClickUp bloqueado:** esperado; el enlace de respaldo y el aviso sin JavaScript se ven.
  5. **Footer escaso a 1280 px:** solo tres bloques con "FALTA CONFIRMAR"; es contenido pendiente de Ari, no un defecto de layout.
  6. **Fidelidad de marca:** se ve el lenguaje del moodboard (color plano, Loopy con lupa, píldoras `seo`, `geo`, `team work` y el recorte en media tinta del ojo). Las brechas ya conocidas de la sección 5 siguen siendo de Ari y Juan (píldoras `spy`, avatares Loopy, morado, fotos de stock), no de CSS.
- **Correcciones (un solo lote):** `CtaLink.astro`: `padding-inline: 1rem` bajo 22.5em (360 px). Prueba nueva en `phase-closing.spec.ts`: la etiqueta de cada `a[data-cta]` cabe en una línea a 320, 390 y 1280 px. Comprobado que la prueba distingue: con el relleno viejo el botón mide 73.2 px y con el nuevo 48 px. Commit 3d97955.

## Cierre, ronda CIERRE2 (confirmación)

- **Corrida:** `PHASE2_BATCH=CIERRE2` con `page-structure.spec.ts` y `phase-closing.spec.ts`: 107 pruebas pasan (incluida la hoja de contacto y las 15 capturas). `CIERRE2-320-reduce.png` (recorte superior) confirma el CTA en una línea a 320 px. No se abrió ningún otro recorte.
- **Suite de la tarea:** `npm run build` sale 0; Playwright chromium con `phase-closing`, `motion`, `page-structure` y `a11y-base`: 144 pasan y 17 en skip (las herramientas de captura y de hoja de contacto sin `PHASE2_BATCH`, como debe ser); `node --test tests/guards/*.test.mjs`: 207 pasan, 0 fallan; `node scripts/check-contrast.mjs` sale 0; `git diff d0ac3f7 -- src/content/landing.es.yaml` vacío (COPY-01).
- **Resultado:** la lista de vibra se cumple; no hizo falta `CIERRE3`.

## Cierre, los 8 rasgos de vibra (evidencia de CIERRE1 y CIERRE2, a 1280 x 800 salvo que se indique)

| # | Rasgo | Veredicto | Evidencia |
|---|-------|-----------|-----------|
| 1 | Hero con titular enorme, un pill dominante y collage a la derecha, todo en el primer pantallazo | Cumple | H1 de 64 px morado (top 147, bottom 377); CTA naranja único (490 a 541); collage a la derecha (289 a 692, bajo los 800 px de la ventana) |
| 2 | Bloques de tono a ancho completo con 96 px de aire desde 1024 px | Cumple | Las 12 secciones miden 96 px arriba y abajo a 1280 px (el hero, 64 px arriba y 96 abajo por contrato); alternancia blanco, oscuro, amarillo, blanco, oscuro, blanco, amarillo, blanco, oscuro, blanco, amarillo, morado (rgb 255,255,255 / 33,33,33 / 255,198,2 / 66,40,209) |
| 3 | Tarjetas con borde de 3 px, sombra dura y radio parejo, sin sombra difusa | Cumple | `pain-card` 3 px, radio 16 px, sombra `6px 6px 0 0` naranja; `pillar-card` y `metric-card` 3 px, radio 16 px, `4px 4px 0 0` oscuro; prueba: cero sombras con desenfoque y cero degradados en `/` y `/privacidad/` |
| 4 | La cifra es lo más grande de cada caso, con marcador amarillo | Cumple | 5 cifras de 64 px; el texto mayor del resto de cada tarjeta mide 24 px; marcador plano de un color |
| 5 | Cuatro avatares como set uniforme sobre círculos suaves | Cumple | 4 avatares de 120 x 120 px a 1280 px |
| 6 | FAQ como filas grandes con borde e icono más/menos | Cumple | 6 resúmenes de 77 px de alto (mínimo pedido 44 px), icono plus/minus, `<details>` que abre con y sin JavaScript |
| 7 | Cada sección con foco claro y un elemento de collage o layout propio | Cumple | Collage en hero, problema, por qué ahora, solución, equipo, casos (lupa de Meta Ads) y `#agenda`; layouts propios en incluye (ítems abiertos con regla), cómo funciona (fases con conector), para quién (par "es" y "no es") y footer (3 bloques). Hoja de contacto de 14 recortes: ninguno se lee como plantilla genérica |
| 8 | Cuatro CTA con el mismo texto y destino | Cumple | 4 `a[data-cta]` con "Agenda tu llamada de 30 minutos", `href="#agenda"`, relleno rgb(253,105,56); la etiqueta cabe en una línea a 320, 390 y 1280 px |

## Cierre, las 15 filas de la síntesis de referencias

| # | Fila | Se ve | Dónde |
|---|------|-------|-------|
| 1 | Header (logo y un pill de CTA) | Sí | `CIERRE1-header-1280.png`: logo a la izquierda, CTA naranja a la derecha; bajo 640 px solo el logo, como manda el UI-SPEC |
| 2 | Hero de 2 columnas | Sí | `CIERRE1-inicio-1280.png`: H1 grande y en negrita, un CTA, `HeroCollage` (lupa gigante, ojos, clics, aros y recorte del ojo) |
| 3 | El problema (3 columnas sobre oscuro) | Sí | Sección `problema`: 3 tarjetas blancas con sombra naranja, numeral morado y pegatina de collage |
| 4 | Por qué ahora (bloque educativo con barra) | Sí | Amarillo, collage a la izquierda y lista de 5 afirmaciones con reglas de 3 px |
| 5 | La solución (2x2) | Sí | 4 tarjetas pop con chip de collage; alto parejo por fila (ver hallazgo 3) |
| 6 | Lo que logramos juntos (bloques con check sobre oscuro) | Sí | 2x2 con borde amarillo y disco de check amarillo; "FALTA CONFIRMAR" con el mismo estilo |
| 7 | Casos de éxito (cifra dominante, chip, tarjeta ancha final) | Sí | 5 tarjetas con cifra de 64 px, chip de canal y `<dl>`; Meta Ads ancha con lupa |
| 8 | Quiénes somos (avatares sobre círculos) | Sí | 4 avatares Loopy con nombre y cargo en Outfit 700 |
| 9 | Qué incluye (ítems abiertos con regla) | Sí | 6 ítems con disco de check y regla superior de 3 px, sin tarjeta |
| 10 | Cómo funciona (fases en lista ordenada) | Sí | 4 fases sobre oscuro con numeral en disco amarillo, conector discontinuo y chip de plazo |
| 11 | Para quién es (par "es" y "no es") | Sí | Tarjeta amarilla de borde sólido con checks y tarjeta blanca de borde discontinuo con x |
| 12 | FAQ (filas grandes con icono) | Sí | 6 filas blancas sobre amarillo con icono más/menos, `<details>` nativo |
| 13 | CTA final y formulario | Sí | `#agenda` morada, h2 con collage a la izquierda y tarjeta blanca del iframe (vacía con ClickUp bloqueado, con enlace de respaldo) |
| 14 | Footer (contacto, redes, privacidad) | Sí | 3 bloques con el logo horizontal; contenido en "FALTA CONFIRMAR" hasta que Ari lo entregue |
| 15 | Ritmo global (96 px, alternancia, barra de acento) | Sí | Ver rasgo 2; barra de 48 x 8 px sobre cada h2 |

La fila 16 del UI-SPEC (Movimiento) no aplica a esta tabla: su presupuesto lo vigilan `motion.spec.ts` y `motion-budget.test.mjs` (en verde).

## Cierre, conteo de axe (`@axe-core/playwright` 4.13.0, etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa, sin iframe, ClickUp bloqueado)

| Ruta y ancho | Violaciones | critical o serious | moderate o minor |
|--------------|------------:|-------------------:|-----------------:|
| `/` a 320 px | 0 | 0 | 0 |
| `/` a 390 px | 0 | 0 | 0 |
| `/` a 1280 px | 0 | 0 | 0 |
| `/privacidad/` a 320 px | 0 | 0 | 0 |
| `/privacidad/` a 390 px | 0 | 0 | 0 |
| `/privacidad/` a 1280 px | 0 | 0 | 0 |

No quedan anotaciones `moderate` ni `minor` por listar. La compuerta completa de accesibilidad (revisión con lector de pantalla y verificador independiente) es de la fase 3.

## Cierre, medidas de peso y peticiones (`/`)

`dist/index.html`: 71 843 bytes crudos (tope 81 920) y 14 274 con gzip -9 (tope 25 600). Raíces `[data-collage]` (15, ninguna anidada) más el sprite (6 135 bytes): 24 392 bytes (tope 42 240). `dist` sin archivos `.js`. Hosts pedidos por `/` y `/privacidad/` (peticiones de la página y navegación de su iframe): solo localhost, forms.clickup.com y app-cdn.clickup.com.

## Cierre, pendientes para Juan (nadie más puede darlos por verificados)

1. **Movimiento:** mirar la página en un navegador real a 390 y 1280 px alternando la preferencia de movimiento reducido del sistema, y confirmar que la entrada del hero es sutil y no falta contenido (con `reduce` no hay animaciones: lo mide `motion.spec.ts`, pero la percepción es de una persona).
2. **VoiceOver en Safari** (y NVDA o TalkBack si se puede): que cada resumen del FAQ anuncie contraído o expandido y lea la respuesta al abrir. Recorrer con teclado el iframe real de ClickUp (entrar y salir sin trampa de foco).
3. **Avatares Loopy:** aprobar el set vigente (el Loopy oficial de 02-10: `ojo-morado`, `ojo-amarillo`, `ojos-morado`, `ojos-amarillo`, sin accesorios) y la asignación persona a variante. La versión anterior con lupa, auriculares, gafas y gorro ya no existe.
4. **Favicon:** aprobar la mesa 18 (un ojo con lupa), que es la que hoy publica `public/favicon.svg` (02-09); el isotipo de dos ojos de 02-02 quedó como alternativa.
5. **Variantes de logo por fondo:** el horizontal y el imagotipo solo van sobre blanco; el isotipo sobre oscuro usa la mesa 17 y el ojo sobre oscuro la 22 como excepción de marca; falta el emblema sobre oscuro.
6. **Morado:** confirmar #4228D1 (valor de los archivos del logo, decisión de Juan del 2026-09-19) frente al #73187F que rotula el BrandBook.
7. **Palabras de las píldoras:** `seo`, `geo` y `ads` salen del copy de Ari; `spy` y `team work` vienen del moodboard. Confirmar que se quedan.
8. **Fotos de stock:** elegir la foto de cada ranura en `/marca/hoja/` (hero y por qué ahora). Muestran manos u ojo de personas reales sin autorización de imagen registrada por la fuente, y la puerta `check-photos.mjs` bloquea producción hasta que se decida.
9. **Propuestas de la crítica (opcionales):** subtítulo del hero a tamaño Title (24 px) en lugar de Body; decidir si la tarjeta de equipo de La solución debe ocupar dos columnas para no dejar la tarjeta de crema con hueco; los tres radios fuera de la escala de DESIGN.md (0.25rem, 0.45em, 3px).
10. **Fase 3:** Lighthouse (rendimiento y SEO), la compuerta completa de accesibilidad y el dominio.
