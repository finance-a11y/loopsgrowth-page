---
phase: 02-secciones-marca-y-copy
plan: 04
type: execute
wave: 6
depends_on: ["02-03", "02-11"]
files_modified:
  - src/content.config.ts
  - src/content/landing.es.yaml
  - PENDING-COPY.md
  - src/components/ui/ResultItem.astro
  - src/components/ui/MetricCard.astro
  - src/components/sections/Results.astro
  - src/components/sections/Cases.astro
  - src/pages/index.astro
  - tests/e2e/results-cases.spec.ts
  - tests/e2e/a11y-base.spec.ts
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
autonomous: true
requirements: [CONT-05, CONT-06, COPY-01, DSGN-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`#resultados` es `<section id='resultados' data-tone='dark'>` armada con `SectionShell` (fondo `rgb(33, 33, 33)`, h2 blanco) y contiene el h2 con `results.title` (el titular del doc: 'Lo que cambia en 6 a 12 meses') y un `<ul role='list'>` de cuatro `ResultItem` (`<li>` con borde amarillo de 3 px, disco de check de 32 px, lead en Title 600 y cuerpo en Body 400) en el orden del doc; una columna bajo 640 px y 2x2 desde 640 px, con altura pareja por fila; sin h3 ni elementos enfocables dentro (CONT-05, DSGN-04)"
    - "El resultado del rango de presupuesto de ads no publica ninguna cifra: su cuerpo muestra el relleno 'FALTA CONFIRMAR' (`pending`), su lead se muestra tal cual y `pending`, y la frase del doc con su marca de verificación vive solo en `reason` (metadato que la página no imprime). El texto renderizado de `#resultados` no contiene un porcentaje de 30 ni de 50, ni cifra sustituta, ni versión suavizada (CONT-05, COPY-01)"
    - "El cuerpo del resultado 'Tu costo por cliente baja' (incluye 'el orgánico se abarata', inferencia sin respaldo literal, COPY-VERIFICATION 3d) se muestra tal cual y queda `pending` con `reason`; los resultados 3 y 4 se muestran tal cual y `verified` (el 4 tiene respaldo, COPY-VERIFICATION 3e) (CONT-05, COPY-01)"
    - "`#casos` es `<section id='casos' data-tone='light'>` (fondo blanco, h2 morado) con h2 igual a `cases.title` ('Casos de exito', el rótulo de sección del doc con su errata sin tilde, porque el doc no trae titular; `pending`) y un `<ul role='list'>` de exactamente cinco `<li>` con un `<article aria-labelledby>` cada uno (CONT-06)"
    - "Cada `MetricCard` lleva un chip de canal, un `<h3>` que contiene la cifra en Display morada con marcador amarillo (`--mark`) y la métrica del doc (nombre accesible '+85% en ventas'), una línea de detalle del doc (todas menos la quinta, que el doc no trae) y un `<dl>` con `<dt>` 'Sector', 'Plazo' y 'Canal' (`cases.labels`) y sus tres `<dd>`; las cifras son las del doc, '+85%', '+237%', 'x3', '+3.808%' y '+500%', sin logos, fotos ni gráficos (CONT-06, COPY-01)"
    - "Cada dato que el doc no da muestra 'FALTA CONFIRMAR' en su ranura, con estado `pending`, sin ocultar la tarjeta ni dejar hueco: el canal de los casos 1 a 4 (chip y `<dd>`) y el plazo del caso 5. Lo que el doc sí da se muestra tal cual y, si Ari debe confirmarlo (los cinco plazos, el sector y el canal del caso 5, la métrica del caso 5 con la errata 'trafico organico' y su encaje SEO/GEO), queda `pending` con `reason` (CONT-06, COPY-01)"
    - "La quinta tarjeta es el caso de Meta Ads en su propia tarjeta ancha: ocupa las dos columnas desde 640 px, en horizontal (cifra a la izquierda, detalle a la derecha) desde 640 px, y es la única con una lupa estática de 96 px (`CollagePiece name='lupa' tone='light'`, que desde 02-10 es el Loopy oficial de un ojo con lupa en esquema A y ya no admite colores por props; decorativa, detrás del texto, sin tapar la cifra ni el detalle) que resalta la cifra (CONT-06, DSGN-04)"
    - "Rejilla de casos medida con rectángulos: una columna bajo 640 px; dos columnas de 640 a 1023 px con la quinta ocupando ambas; tres columnas desde 1024 px con las tarjetas 1 a 3 en la primera fila, la 4 en la primera columna de la segunda y la 5 ocupando las columnas 2 y 3 (CONT-06)"
    - "El CTA `casos` es `a[data-cta='casos']` (`CtaLink location='casos'`, texto de `ctaLabel`, `href='#agenda'`, sin `aria-label`, alto de 48 px o más) en el slot `cta` de `SectionShell`, a 48 px bajo la rejilla; un clic o Enter lleva el foco a `#agenda-title`. El orden de tabulación de la fase 1 se actualiza en `a11y-base.spec.ts`: `cta:solucion` y luego `cta:casos` antes del enlace de respaldo y el iframe, a 1280 y a 390 px (CONT-06)"
    - "Todo el texto de ambas secciones vive en `landing.es.yaml` como afirmaciones `{text, status}` bajo `results` y `cases` (sin valores sueltos), copiado carácter por carácter de `02-ARI-COPY-V2.md`, secciones 4 y 5; un script de verificación exige que cada cadena, salvo 'FALTA CONFIRMAR' y las tres etiquetas de `cases.labels`, sea subcadena del doc. Las erratas 'trafico', 'organico' y 'exito' y la falta de punto final del caso 5 se muestran tal cual y se reportan en el SUMMARY (COPY-01)"
    - "Con `PUBLIC_ENV=production`, `check-copy` reporta para las rutas `results.` y `cases.` solo reglas PENDING (16) y MISSING (6) y ninguna VOSEO, DASH, AEO ni VERIFICAR; `npm run build` sin producción pasa; la producción falla solo por PENDING y MISSING; `npm run pending` regenera `PENDING-COPY.md` de forma determinista y `node scripts/list-pending.mjs --check` sale 0 (COPY-01)"
    - "Jerarquía de encabezados sin saltos: `#resultados` solo con su h2 y `#casos` con su h2 y cinco h3; `page-structure.spec.ts` sigue en verde con las dos secciones nuevas en su lugar canónico (después de La solución y antes de `#agenda`) y con su tono (CONT-05, CONT-06)"
    - "Sin scroll horizontal a 320, 390, 768, 1024 y 1280 px; a 320 px '+3.808%' y 'FALTA CONFIRMAR' quedan dentro de su tarjeta y del viewport; con espaciado de texto forzado (SC 1.4.12) no se recorta ni se superpone nada (UI Considerations: long-text, overflow, partial, empty y zero-one-many, resueltos explícitos)"
    - "Sin movimiento ni interacción falsa: `document.getAnimations().length` es 0 con `prefers-reduced-motion` en `reduce` y en `no-preference`; las tarjetas no cambian con hover, no usan cursor de puntero y no contienen elementos enfocables; el único enfocable de `#casos` es el CTA y `#resultados` no tiene ninguno (UI Considerations: a11y-motion, resuelto explícito)"
    - "Color medido: cifra en el morado de marca, que los specs leen como `PURPLE_RGB` de `tests/e2e/lib/brand.ts` (lo crea 02-09 a partir de `tokens.css`; ningún spec repite un literal rgb del morado), sobre blanco (8.55), marcador `rgb(255, 198, 2)`, texto de resultados `rgb(255, 255, 255)` sobre `rgb(33, 33, 33)` (16.10), borde y disco de resultados `rgb(255, 198, 2)` (10.22 sobre oscuro); ningún morado sobre oscuro ni amarillo como texto sobre claro; toda tarjeta blanca sobre `light` lleva borde oscuro de 3 px (UI Considerations: a11y-color, resuelto explícito)"
    - "El padding vertical de `#resultados` y `#casos` mide 64 px bajo 1024 px y 96 px desde 1024 px, y los dos tonos (`dark` y `light`) no repiten el de sus vecinas (DSGN-04)"
    - "Todo el diseño del plan pasó por `impeccable` (lote C1: `shape`, `layout`, `colorize`, `typeset` de las cifras y `adapt`) y `design-taste-frontend` (diales 7, 3 y 4) invocadas con la herramienta Skill; `02-VISUAL-LOG.md` registra por ronda los verbos, hallazgos, correcciones y capturas a 320, 390, 768, 1024 y 1280 px (base, movimiento reducido y sin JavaScript, con ClickUp bloqueado); el lote cierra cuando ambas secciones cumplen los rasgos 2, 3, 4 y 7 de la lista de vibra (DSGN-04)"
    - statement: "Composición de la lupa de la tarjeta 5 frente a la cifra a 320 y 1280 px: el aro y el mango no cruzan los glifos de la cifra ni el detalle; la comprobación se reevalúa con el Loopy oficial de un ojo de 02-10 (relación ancho a alto de 0.97, unos 96 px de ancho por 99 de alto, aro y mango morados con anillo crema), porque su geometría ya no es la de la lupa que dibujaron los agentes en 02-02; se revisa en las capturas de sección del lote C1 y se anota en `02-VISUAL-LOG.md`"
      verification: backstop
    - statement: "Con VoiceOver en Safari, las listas de resultados y de casos (con `list-style: none`) se anuncian como listas de 4 y de 5 elementos y el `<h3>` combinado se lee como '+85% en ventas'; el rol explícito y el nombre accesible los cubren las pruebas automáticas, la lectura con lector de pantalla queda como verificación manual diferida"
      verification: backstop
    - "Supuestos explícitos (UI Considerations, filas sin resolver): copy, resuelto aquí con la tabla 'Ranuras de copy' (cada cadena mapeada desde `02-ARI-COPY-V2.md`); assets, sin trabajo en este plan (no usa logo ni isotipo; la lupa sale de `CollagePiece`, reconstruido por 02-10 con el Loopy oficial). El check del disco de resultados se dibuja en línea porque el sprite de 02-10 (ocho símbolos `lg-`) no trae un símbolo de check"
  artifacts:
    - path: "src/content/landing.es.yaml"
      provides: "`results` (titular y cuatro resultados con lead y cuerpo) y `cases` (titular, etiquetas Sector, Plazo y Canal, y cinco casos con cifra, métrica, detalle, sector, plazo y canal), todo como afirmaciones del doc de Ari"
      contains: "cases:"
    - path: "src/content.config.ts"
      provides: "Esquema de `results` (cuatro ítems) y de `cases` (cinco ítems, `detail` opcional) reutilizando la constante `claim`"
      contains: "cases"
    - path: "src/components/ui/ResultItem.astro"
      provides: "`<li>` de resultado: borde amarillo de 3 px, disco de check en línea, lead en Title 600 y cuerpo en Body"
      contains: "result-lead"
    - path: "src/components/ui/MetricCard.astro"
      provides: "Tarjeta de métrica (`article` con chip, `h3` con cifra y métrica, detalle opcional y `dl`) con variante ancha que lleva la lupa"
      contains: "<dl"
    - path: "src/components/sections/Results.astro"
      provides: "Sección `#resultados` sobre `SectionShell` con tono `dark`"
      contains: "resultados"
    - path: "src/components/sections/Cases.astro"
      provides: "Sección `#casos` sobre `SectionShell` con tono `light`, cinco tarjetas y el CTA `casos` en el slot `cta`"
      contains: "location=\"casos\""
    - path: "src/pages/index.astro"
      provides: "Las dos secciones montadas después de La solución y antes de `#agenda`"
      contains: "Cases"
    - path: "tests/e2e/results-cases.spec.ts"
      provides: "Estructura, textos derivados del YAML, rejillas por ancho, lupa, CTA, sin desborde, sin movimiento y capturas de sección del lote C1"
      contains: "resultados"
    - path: "tests/e2e/a11y-base.spec.ts"
      provides: "Orden de tabulación de la fase 1 con `cta:casos` a 1280 y a 390 px"
      contains: "cta:casos"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada con las afirmaciones pendientes de `results.` y `cases.`"
      contains: "cases."
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Registro del lote C1 (Logramos juntos y Casos): rondas, verbos, hallazgos, correcciones y capturas"
      contains: "Lote C1"
  key_links:
    - from: "src/components/sections/Results.astro"
      to: "src/content/landing.es.yaml"
      via: "`getLanding()`: `results.title.text` y `results.items[i].lead.text` y `.body.text` pasados como cadenas a `ResultItem`, sin leer `status`"
    - from: "src/components/sections/Cases.astro"
      to: "src/components/ui/MetricCard.astro"
      via: "cada caso de `cases.items` se pasa como cadenas (`figure`, `metric`, `detail?`, `sector`, `period`, `channel`) más las etiquetas de `cases.labels`; el índice 4 marca la tarjeta ancha"
    - from: "src/components/sections/Cases.astro"
      to: "src/components/CtaLink.astro"
      via: "`<CtaLink slot='cta' location='casos' label={ctaLabel(data)} />` dentro de `SectionShell`; `cta-focus.ts` ya cubre todo `a[href='#agenda']`"
    - from: "src/components/ui/MetricCard.astro"
      to: "src/components/collage/CollagePiece.astro"
      via: "solo la variante ancha monta `<CollagePiece name='lupa' tone='light' />` (firma de 02-10: `name`, `tone`, `color?`, `size?`, `rotate?` y `class?`, sin `a`, `b` ni `c`); `assertToneSafe` valida el tono en el build"
    - from: "src/pages/index.astro"
      to: "tests/e2e/page-structure.spec.ts"
      via: "`PAGE_ORDER` de 02-01 ya fija `resultados` (dark) y `casos` (light); el spec compara el orden y el tono con `main > section` del build"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir, traducir ni reordenar el texto del doc de Ari (erratas 'trafico', 'organico' y 'exito', falta de punto final del caso 5, '$' sin moneda): si algo molesta a una guarda o a la lectura, se reporta en el SUMMARY"
    - requirement_id: CONT-05
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT publicar el rango de 30% a 50% ni una cifra sustituta ni una versión suavizada en `#resultados`: el relleno es 'FALTA CONFIRMAR' y la prueba e2e falla si el texto renderizado trae un porcentaje de 30 o de 50"
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT leer el estado (`status`) de una afirmación en un componente ni ocultar el texto de una afirmación `pending`: los componentes reciben cadenas ya extraídas con `.text`"
    - requirement_id: CONT-06
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT agregar logos, fotos, gráficos, franja de contadores, testimonios ni una cifra, sector, plazo o canal que el doc no traiga: el dato faltante es 'FALTA CONFIRMAR', nunca un valor inventado; ni `img` ni `svg` en `#casos` y `#resultados` salvo los discos de check y la lupa"
    - requirement_id: CONT-06
      category: design
      status: resolved
      verification: test
      statement: "MUST NOT poner lupa en otra tarjeta que la quinta: solo el caso de Meta Ads lleva lupa (UI-SPEC, sección 6)"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar inyección de HTML en bruto, colores hexadecimales dentro de `src/components`, anulación del contorno de foco ni alturas fijas en contenedores de texto: los colores salen de tokens y de `var(--color-brand-*)`"
    - requirement_id: DSGN-04
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT combinar los pares prohibidos (morado sobre oscuro, amarillo como texto sobre claro, blanco sobre naranja) ni dejar una tarjeta blanca sobre `light` sin borde oscuro de 3 px"
    - requirement_id: DSGN-04
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT agregar movimiento, hover, cursor de puntero ni opacidad menor a 1 en estas dos secciones: el movimiento es del plan 07 y las tarjetas no deben parecer clicables"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT modificar archivos de otros planes (`SectionShell.astro`, `CtaLink.astro`, `src/styles/`, `src/components/collage/`, `src/components/brand/`, el hero, las secciones y el YAML de 02-03, `AgendaSection.astro`, `SiteHeader.astro`, `BaseLayout.astro`, `public/`, `page-structure.spec.ts`, los scripts de guardas): el conjunto de archivos cambiados sale de `files_modified`"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar el formulario de ClickUp, leer `.env` ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Construir las secciones Lo que logramos juntos (`#resultados`, tono oscuro, cuatro resultados con disco de check) y Casos de éxito (`#casos`, tono claro, cinco tarjetas de métrica; la quinta es el caso de Meta Ads en tarjeta ancha con una lupa que resalta la cifra) con el texto del doc de Ari tal cual, el relleno visible "FALTA CONFIRMAR" donde el doc no da el dato (el rango de presupuesto de ads marcado para verificar, el canal de los casos 1 a 4 y el plazo del caso 5) y el CTA `casos` a 48 px bajo la rejilla (CONT-05, CONT-06, COPY-01, DSGN-04).

Purpose: que un dueño de negocio vea prueba social concreta (cifras de casos reales, sin logos ni fotos) y qué cambia en 6 a 12 meses, sin que la página afirme un dato que Ari no ha respaldado, y que llegue al CTA `casos` con la etiqueta idéntica a las de las otras tres ubicaciones. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: claves `results` y `cases` en `landing.es.yaml` y su esquema; `ResultItem`, `MetricCard`, `Results` y `Cases`; las dos secciones montadas en `index.astro`; `results-cases.spec.ts` (estructura, textos derivados del YAML, rejillas, lupa, CTA, sin desborde ni movimiento, capturas de sección); orden de tabulación de la fase 1 actualizado con `cta:casos`; `PENDING-COPY.md` regenerado; entrada "Lote C1" en `02-VISUAL-LOG.md`.

Decisiones de CONTEXT.md que implementa (por nombre de sección): "Prueba social" (las cifras `[VERIFICAR]` no se publican y muestran "FALTA CONFIRMAR"; casos como tarjetas de métrica sin logos ni fotos con cifra, sector anonimizado, plazo y canal; caso de Meta Ads en su propia tarjeta; dato faltante marcado `pending` sin ocultar la tarjeta), "Textos pending" (lo que el doc trae se muestra tal cual y solo se lista; el relleno solo donde falta el dato), "Estilo visual, collage y movimiento" (referencias como guía de layout, método `impeccable` más `design-taste-frontend`, lupa como SVG estático decorativo, fondos alternados y reglas de contraste), "Hero, header y orden de la página" (orden de las secciones y mismo texto en el CTA de casos) y "Excepciones del contrato de diseño aprobadas por Juan" (Title como quinto tamaño para el h3 y el lead, 96 px solo para `--section-y`, CTA de cuatro ubicaciones).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicar con `grep -n '^## \|^### '` y leer con `Read` usando `offset` y `limit` de 200 líneas como máximo). Los planes 02-01, 02-02 y 02-03 no se releen: solo se usan los nombres de la sección "Artifacts this phase produces" de cada uno. Los planes 02-09, 02-10 y 02-11 corren antes que este (orden de ejecución: 02-09, 02-10, 02-11, cierre de la tarea 4 de 02-03 y luego 02-04): sus SUMMARY y su sección "Artifacts this phase produces" fijan los nombres vigentes de tokens, `Logo`, `CollagePiece`, `tests/e2e/lib/brand.ts` (`PURPLE_RGB`) y los topes de peso; si difieren de lo que dice este plan, manda el archivo.
</context>

## Artifacts this phase produces

Nombres que reutilizan los planes 05 a 07 (rutas relativas a la raíz del repositorio). No se renombran después.

| Artefacto | Detalle | Lo usa |
|-----------|---------|--------|
| `landing.es.yaml`: `results` | `title` e `items[0..3]`, cada ítem con `lead` y `body` (afirmaciones) | 02-07 (auditoría de copy) |
| `landing.es.yaml`: `cases` | `title`, `labels.sector`, `labels.period` y `labels.channel`, e `items[0..4]` con `figure`, `metric`, `detail?`, `sector`, `period` y `channel` | 02-07 |
| `src/components/ui/ResultItem.astro` | Props `lead` y `body` (cadenas). Renderiza el `<li>`; clases `.result-item`, `.result-check`, `.result-lead` y `.result-body`. El disco de check en línea lo puede extraer 02-05 (Qué incluye) a un componente propio si lo necesita | 02-05 (referencia) |
| `src/components/ui/MetricCard.astro` | Props `id`, `figure`, `metric`, `detail?`, `sector`, `period`, `channel`, `labels` (`{sector, period, channel}`) y `wide?`. Renderiza `<article aria-labelledby>`; clases `.metric-card`, `.metric-chip`, `.metric-head`, `.metric-figure`, `.metric-mark`, `.metric-name`, `.metric-detail` y `.metric-facts`; atributo `data-wide` en la variante ancha | 02-07 (movimiento opcional) |
| `src/components/sections/Results.astro` y `Cases.astro` | Ids `resultados` (tono `dark`) y `casos` (tono `light`); `Cases` monta `CtaLink location='casos'` | `index.astro`, 02-07 |
| `tests/e2e/results-cases.spec.ts` | Herramienta de capturas por sección con `PHASE2_BATCH=C1`: `test-results/phase2/C1-resultados-<ancho>.png` y `C1-casos-<ancho>.png` (cinco anchos, variante base). Se suma a la de 02-01 (`page-structure.spec.ts`, página completa) sin cambiarla | 02-07 |

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`, secciones "4. Lo que logramos juntos" (líneas 106 a 119) y "5. Casos de exito" (líneas 121 a 127). Casos que aplican, según el brief: **A** existe y pasa las guardas, se muestra tal cual y no se lista; **B** existe, se muestra tal cual y se lista para que Ari lo confirme; **C** falta o trae `[VERIFICAR]`, se muestra "FALTA CONFIRMAR" y se lista; **D** errata del doc, se muestra tal cual y se reporta. Las claves `reason` no llevan el carácter de barra vertical ni saltos de línea (romperían la tabla de `PENDING-COPY.md`).

| Ranura | Origen en el doc | Caso | Estado en el YAML |
|--------|------------------|------|-------------------|
| `results.title` | Sección 4, Headline: "Lo que cambia en 6 a 12 meses" | A | `verified` |
| `results.items[0].lead` | "Tu costo por cliente baja." | A | `verified` |
| `results.items[0].body` | "Cada cliente que llega desde orgánico cuesta una fracción de lo que cuesta por ads, y esa diferencia crece con el tiempo (el orgánico se abarata, los ads se encarecen)." | B: inferencia "el orgánico se abarata" (COPY-VERIFICATION 3d) y "una fracción" sin respaldo propio | `pending`, con `reason` |
| `results.items[1].lead` | "Dependes menos de los ads." | B: resume el resultado cuyo rango no tiene respaldo; se muestra tal cual, sin cifra | `pending`, con `reason` |
| `results.items[1].body` | "Varios clientes bajan entre 30% y 50% su presupuesto de ads manteniendo el mismo tráfico. [VERIFICAR rango]" | C: `[VERIFICAR]` sin respaldo propio (COPY-VERIFICATION, detalle 2). Se muestra 'FALTA CONFIRMAR'; la frase del doc y su marca van en `reason` | `pending` (y MISSING) |
| `results.items[2].lead` y `.body` | "Apareces donde antes no estabas." y "En Google y en las respuestas de IA cuando tu cliente pide recomendaciones." | A. Lenguaje de promesa de visibilidad: no se reescribe, se reporta a Ari en el SUMMARY (hallazgo 7 del UI-SPEC) | `verified` |
| `results.items[3].lead` y `.body` | "El crecimiento se acumula." y "No es un pico de un mes. El contenido que rankea hoy sigue trayendo clientes el año que viene, sin volver a pagar por él." | A (COPY-VERIFICATION 3e, verificado) | `verified` |
| Nota de Ari sobre la fusión de outcomes | Cursiva al final de la sección 4 | No es copy de la página: no se muestra ni se guarda | n/a |
| `cases.title` | La sección 5 no trae Headline; se usa su rótulo: "Casos de exito" | B y D: errata 'exito' sin tilde; Ari confirma titular y ortografía | `pending`, con `reason` |
| `cases.labels.sector`, `.period`, `.channel` | Sin origen en el doc: microcopy estructural que fija el UI-SPEC ('Sector', 'Plazo', 'Canal') | A (nombra un campo de la interfaz y no afirma nada) | `verified` |

Casos de éxito, ranura por ranura (cada fila es una tarjeta; el orden es el del doc):

| Tarjeta | Se muestra tal cual y `verified` (A) | Se muestra tal cual y `pending` (B) | 'FALTA CONFIRMAR' y `pending` (C) |
|---------|--------------------------------------|--------------------------------------|-----------------------------------|
| 1 (ventas) | `figure` "+85%", `metric` "en ventas", `detail` "De $41K a $76K en ventas en 6 meses, con estrategia de contenido y optimización técnica.", `sector` "e-commerce de vapes (LATAM)" | `period` "6 meses" (sale del detalle; el UI-SPEC pide que Ari confirme los cinco plazos) | `channel`: el doc no nombra el canal |
| 2 (tráfico) | `figure` "+237%", `metric` "en tráfico", `detail` "De 1.267 a 4.230 clics mensuales en 6 meses, que se tradujeron en 552 inscripciones de calidad.", `sector` "academia de salud (EE.UU.)" | `period` "6 meses" | `channel` |
| 3 (conversiones) | `figure` "x3", `metric` "en conversiones", `detail` "+132% de tráfico orgánico y conversiones triplicadas en 6 meses.", `sector` "software de email marketing (SaaS B2B)" | `period` "6 meses" | `channel` (el detalle dice 'tráfico orgánico' pero no nombra el canal del caso) |
| 4 (clics) | `figure` "+3.808%", `metric` "en clics", `detail` "De cero a un posicionamiento sólido en 12 meses.", `sector` "app infantil" | `period` "12 meses" | `channel` |
| 5 (Meta Ads, ancha) | `figure` "+500%" | `metric` "en crecimiento de trafico organico" (errata D y encaje: el caso no es SEO/GEO), `sector` "marca personal referente en Meta Ads" (una sola frase en el doc; Ari confirma cómo se divide), `channel` "Meta Ads" (subcadena de esa frase) | `period` |

El caso 5 no trae línea de detalle en el doc: la clave `detail` se omite del YAML (el esquema la declara opcional) y el componente no imprime el párrafo, así que no queda hueco (el UI-SPEC no define esa ranura para la tarjeta ancha). Recuento esperado con `PUBLIC_ENV=production` para las rutas de este plan: 16 afirmaciones PENDING (3 en `results`, 13 en `cases`) y 6 MISSING (1 en `results`, 5 en `cases`).

## UI Considerations (lift del UI-SPEC para este plan)

- covered (verdades ya en `must_haves.truths`): empty y partial (ranura sin dato muestra 'FALTA CONFIRMAR', tarjeta completa), long-text y overflow (`overflow-wrap: anywhere`, hijos de grid con `min-width: 0`, sin scroll horizontal a cinco anchos), zero-one-many (el esquema fija 4 y 5 ítems; la quinta tarjeta ocupa el ancho sobrante), loading (SVG en línea, sin recursos externos ni peticiones nuevas), a11y-motion (cero animaciones), a11y-color (pares del UI-SPEC medidos en estilos calculados).
- backstop (en `must_haves.truths` como `{statement, verification: backstop}`): interaction (listas con `list-style: none` y VoiceOver; el `role='list'` explícito lo prueba el spec, la lectura con lector de pantalla queda diferida) y visual (composición de la lupa de la tarjeta 5 a 320 px, revisada en las capturas de sección).
- No aplican aquí: FAQ (`<details>`, plan 06), `HeroCollage` a 320 px (plan 01) y la fila assets (logo e isotipo, plan 02).
- copy (fila sin resolver del UI-SPEC): se resuelve para estas dos secciones con la tabla de ranuras de arriba; las demás secciones las mapea el plan que las construye.

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **La cifra va dentro del `<h3>`** ('+85%' en Display morado y 'en ventas' en Title, dos `span` con un espacio explícito entre ellos). El UI-SPEC lista la cifra y el h3 como elementos separados; con la cifra fuera, el encabezado de la tarjeta sería solo 'en ventas', que no sirve para navegar por encabezados ni como nombre de la tarjeta. El aspecto es el mismo. Revertir: sacar el `span` de la cifra del `<h3>` y ponerlo en un `<p>` antes.
2. **`metric` conserva la preposición del doc** ('en ventas', no 'ventas' como en los ejemplos del UI-SPEC): la frase del doc es '+85% en ventas' y quitar 'en' sería editarla. Leída con la cifra por encima, la tarjeta dice la frase completa de Ari.
3. **Se muestra la línea de detalle del doc** en las tarjetas 1 a 4 (por ejemplo 'De $41K a $76K en ventas en 6 meses...'). El UI-SPEC no la lista en `MetricCard`, pero es copy de Ari con las cifras concretas que respaldan el caso, y omitirla sería quitar texto del doc en silencio. La tarjeta 5 no la tiene porque el doc no la trae. Revertir: quitar el párrafo `.metric-detail` del render (el YAML puede conservarla).
4. **`ResultItem` separa lead y cuerpo**: lead en Title 600 y cuerpo en Body 400, ambos en blanco. El UI-SPEC dice 'el texto en Title 600' sin separar; el doc marca el lead en negrita y el resto en texto normal, y cuatro resultados de dos o tres frases enteros en Title serían un muro. Revertir: mostrar lead y cuerpo en un solo párrafo en Title 600.
5. **Resultado 2: el lead se muestra (`pending`) y el cuerpo dice 'FALTA CONFIRMAR'**, en vez de que todo el resultado diga 'FALTA CONFIRMAR' como escribe el UI-SPEC. El lead no lleva cifra ni marca `[VERIFICAR]` y es texto de Ari, no un suavizado nuestro; así Ari ve a qué resultado pertenece el dato que falta. Revertir: cambiar el `text` del lead a 'FALTA CONFIRMAR' (y la `reason` sigue igual).
6. **Titular de casos**: el doc no trae Headline para la sección 5; se usa su rótulo 'Casos de exito' (con la errata, `pending`). Alternativa si Ari prefiere otro: cambiar el `text` en el YAML.
7. **Plazos**: los de las tarjetas 1 a 4 se muestran como los da el doc ('6 meses', '12 meses') y quedan `pending`; el UI-SPEC los daba por faltantes porque se escribió sin poder leer el doc, y CONTEXT (Textos pending) limita el relleno a lo que falta. Revertir: cambiar los cuatro `text` a 'FALTA CONFIRMAR'.
8. **Chip y `<dd>` de Canal leen la misma ranura** (el UI-SPEC pide un chip de canal y un `<dl>` con Canal). Mientras el canal falte, 'FALTA CONFIRMAR' aparece en las dos partes de las tarjetas 1 a 4; es el estado que Ari debe resolver. Revertir: que el chip muestre otra ranura o se quite.
9. **Tarjeta 5**: el doc trae una sola frase ('marca personal referente en Meta Ads'); el sector muestra la frase completa y el canal la subcadena 'Meta Ads', ambos `pending`. No se parte la frase en silencio para dejar solo 'marca personal'.
10. **La tarjeta ancha se decide por índice** (la quinta, que en el doc es la de Meta Ads): el YAML no admite un booleano (un valor suelto rompe `check-copy` con BARE_STRING).
11. **El esquema fija los conteos** con `.length(4)` para `results.items` y `.length(5)` para `cases.items`: el layout de casos depende de que sean cinco.
12. **Disco de check en línea** (SVG dentro de `ResultItem`): el sprite de 02-10 tiene ocho símbolos `lg-` (dos Loopy, cinco garabatos y la retícula de puntos) y ninguno es un check. Son cuatro usos de unos 150 bytes. Si 02-05 o 02-06 lo necesitan, conviene extraerlo a un componente compartido en ese plan.
13. **Lote C partido**: este plan hace el lote C1 (Logramos juntos y Casos); el equipo lo hace el plan 02-05 con el nombre "Lote C (equipo)" en el mismo `02-VISUAL-LOG.md`. Las capturas de página completa reutilizan la herramienta de 02-01 con `PHASE2_BATCH=C1`, y este plan suma capturas recortadas por sección porque una captura de página completa a 390 px es demasiado alta para leerla.
14. **Orden de tabulación**: este plan edita `a11y-base.spec.ts` (caso a, a 1280 y a 390 px) para insertar `cta:casos` después de `cta:solucion`; supone que 02-03 ya insertó `cta:solucion` (si no está, la tarea se detiene y lo reporta).
15. **Punto de montaje**: `index.astro` monta `Results` y `Cases` justo después de `Solution` (02-03) y antes de `AgendaSection`. Verificado contra 02-03 al cerrar este plan: 02-03 ya suma `cta:solucion` al caso (a) de `a11y-base.spec.ts`, deja la guarda 9b de `copy.test.mjs` derivada del YAML con `walkClaims` (por eso este plan no toca `tests/guards/`) y anuncia que el plan 04 suma `cta:casos`.

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Plan |
|--------|------|------|
| GOAL | Landing completa con prueba social sin cifras sin respaldo, de 320 a 1280 px | Secciones 5 y 6 aquí; el resto en los planes 01 a 03 y 05 a 07 |
| REQ | CONT-05 (cuatro resultados sin cifras `[VERIFICAR]` sin respaldo), CONT-06 (casos como tarjetas de métrica sin logos, con Meta Ads en su tarjeta), COPY-01, DSGN-04 | Este plan (COPY-01 y DSGN-04 continúan en los planes 05 a 07) |
| RESEARCH | No hay `RESEARCH.md` en esta fase; el UI-SPEC y `.planning/research/COPY-VERIFICATION.md` son la fuente | n/a |
| CONTEXT | Prueba social (cifras `[VERIFICAR]` sin publicar, casos sin logos ni fotos, Meta Ads en su tarjeta, dato faltante `pending` sin ocultar), Textos pending, Estilo visual, orden de página y mismo CTA, tres excepciones de Juan | Este plan |
| UI-SPEC | Section Contracts 5 y 6, Reference Synthesis (filas Lo que logramos juntos y Casos de éxito), Copywriting Contract (estados y hallazgos), Interaction Contract (tarjetas no interactivas, orden de tabulación), UI Considerations | Este plan |
| CONTEXT | Equipo con avatares | Plan 05 |
| CONTEXT | CTA tras La solución | Plan 03 |
| CONTEXT | Movimiento bajo `motion-safe` | Plan 07 |

## Presupuesto de contexto (11 archivos en `files_modified`, unos 8 con autoría real)

Tres casi no tienen autoría: `PENDING-COPY.md` lo genera `npm run pending`, `02-VISUAL-LOG.md` es un registro breve e `index.astro` suma dos líneas. La estimación es de 90k tokens (T1 cerca de 24k, T2 cerca de 30k, T3 cerca de 36k), confianza baja (sin muestras de calibración, factor 1). El costo mayor es el ciclo visual de la Tarea 3: el ejecutor mira solo las capturas de sección de 390 y 1280 px (cuatro imágenes por ronda) y comprueba 320, 768 y 1024 px con las mediciones del spec, mirando una captura solo si la medición falla.

Puntos de corte (cada tarea termina con el build verde y commiteada):
1. Tras la Tarea 1 (tracer): la sección de resultados completa. Si el contexto ya pasó de 50 %, commit, `02-04-SUMMARY.md` con `status: partial` (hecho y pendiente) y la Tarea 2 corre en un ejecutor nuevo.
2. Tras la Tarea 2: ambas secciones y el CTA funcionan y están probadas, sin iteración visual. Es el corte natural más importante.
3. La Tarea 3 (ciclo visual) no se parte a mitad de un ciclo: si al empezarla el contexto pasa de 50 %, se corta antes y un ejecutor nuevo la toma leyendo el SUMMARY parcial y `02-VISUAL-LOG.md`.

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/`, `public/` o `astro.config.mjs`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; este plan no lo usa). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; `PUBLIC_ENV=production` falla solo por afirmaciones PENDING y por 'FALTA CONFIRMAR'.
- Las pruebas derivan los textos esperados del YAML (lectura con `yaml`, mismo patrón de `a11y-base.spec.ts`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- La fase 2 no cambia el comportamiento del iframe de ClickUp; las verificaciones humanas de la fase 1 siguen diferidas.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `landing.es.yaml` a HTML | Texto editado por Ari y el equipo (también desde la web de GitHub) entra a las tarjetas; trae `$`, `%`, `+` y paréntesis y debe salir escapado |
| Cifras de casos a la confianza del visitante | Resultados de clientes publicados como hechos: un dato sin respaldo daña la credibilidad de una agencia que vende confianza (rango de presupuesto de ads, plazos, canal) |
| Props de componente a `href` | El CTA `casos` reutiliza `CtaLink`; ningún valor del YAML llega a un `href` |
| SVG en línea | La lupa sale de `CollagePiece` (validado por tono en el build) y el disco de check se dibuja a mano, sin datos externos |
| Navegador a terceros | El iframe y el script de ClickUp son las únicas peticiones externas de la página; este plan no suma ninguna |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Tampering | Textos del YAML impresos en `ResultItem` y `MetricCard` | medium | mitigate | Astro escapa las expresiones de texto; los componentes imprimen `.text` como nodos de texto, sin inyección de HTML en bruto; criterio de aceptación con grep sobre `src`; el spec compara el `textContent` con el YAML carácter por carácter |
| T-02-04-02 | Repudiation | Rango de 30% a 50% y demás datos sin respaldo publicados como hechos | high | mitigate | El cuerpo del resultado 2 es 'FALTA CONFIRMAR' (`pending`); la frase del doc vive solo en `reason`; `PENDING-COPY.md` lista todo (`--check` compara byte a byte); la producción falla mientras haya PENDING o MISSING; el spec falla si `#resultados` muestra un porcentaje de 30 o de 50 |
| T-02-04-03 | Tampering | Cifras, sectores o frases de los casos retipeadas con cambios por el ejecutor | medium | mitigate | Script de verificación de la Tarea 1 y la 2: cada cadena de `results` y `cases` (salvo el relleno y las tres etiquetas) debe ser subcadena de `02-ARI-COPY-V2.md`; el SUMMARY lista las erratas conservadas |
| T-02-04-04 | Information Disclosure | Nuevas peticiones a terceros (fuentes, analítica, CDN) desde estas secciones | medium | mitigate | Solo SVG en línea y texto; criterio de aceptación: los únicos hosts `https://` de `dist/index.html` son forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG |
| T-02-04-05 | Denial of Service | HTML del documento demasiado pesado (Core Web Vitals) | low | mitigate | La lupa entra por `<use>` del sprite y el disco de check pesa unos 150 bytes por uso; el spec afirma `dist/index.html` por debajo de 60 KB sin comprimir. Presupuesto compartido: 02-10 y 02-11 dejan `dist/index.html` en 42240 bytes como máximo, así que 02-04, 02-05 y 02-06 juntos disponen de unos 19 KB bajo el tope global de 61440; este plan mide y anota en el SUMMARY cuántos bytes suma y, si el tope global peligra, no lo sube: se detiene y reporta a Juan (opciones de la pregunta abierta 4 de 02-10: subir el tope, recortar píldoras o mover el sprite a un archivo estático) |
| T-02-04-06 | Tampering | Un relleno de la lupa o del disco con color prohibido para su tono | low | mitigate | `CollagePiece` llama `assertToneSafe('light', ...)` y rompe el build si el color no está permitido; el disco usa solo `var(--color-brand-*)`; criterio de aceptación: cero hex en `src/components` |
| T-02-04-07 | Information Disclosure | Lectura de `.env` o de secretos durante la ejecución | low | mitigate | Regla de ejecución: solo `.env.example`; el plan no necesita ninguna variable secreta |

Este plan no instala paquetes (UI-SPEC, Registry Safety: sin dependencias nuevas), por lo que no hay fila de cadena de suministro.
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de Lo que logramos juntos de punta a punta con el texto del doc de Ari</name>
  <files>src/content.config.ts, src/content/landing.es.yaml, PENDING-COPY.md, src/components/ui/ResultItem.astro, src/components/sections/Results.astro, src/pages/index.astro, tests/e2e/results-cases.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 5. Lo que logramos juntos (CONT-05)`, `## Reference Synthesis` (filas Lo que logramos juntos, Casos de éxito y Ritmo global), `## Typography` (Reglas), `## Copywriting Contract` y `## Design Method and Iteration Protocol`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit` (200 líneas como máximo)
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: `## 4. Lo que logramos juntos` (líneas 106 a 119)
    - .planning/research/COPY-VERIFICATION.md: tabla resumen (líneas 12 a 22, filas 2, 3d y 3e)
    - src/content.config.ts, src/content/landing.es.yaml y src/pages/index.astro (estado tras 02-03), src/lib/content.ts (exports `getLanding`, `fill`, `textVars`, `ctaLabel`)
    - src/components/ui/SectionShell.astro (props y slots), src/styles/tokens.css (tonos, `--card-pad`, `--card-gap`, `--radius-card`, `--text-title`), DESIGN.md y PRODUCT.md (de 02-01)
    - scripts/lib/copy-rules.mjs (`walkClaims`, `MISSING_MARK`), tests/e2e/a11y-base.spec.ts (líneas 1 a 27: patrón de lectura del YAML) y tests/e2e/page-structure.spec.ts (solo lectura: `PAGE_ORDER` y herramienta de capturas)
  </read_first>
  <action>
    Implementa CONT-05 y COPY-01 en su versión más fina que atraviesa todas las capas del plan (copy en el YAML, esquema, componente de ítem, componente de sección, página, prueba y lista de pendientes). Los casos de éxito y la iteración visual son de las Tareas 2 y 3. Antes de editar, anota `git rev-parse HEAD` como BASE para los criterios de alcance y comprueba la dependencia con 02-03: `landing.es.yaml` ya trae las claves `problem`, `why_now` y `solution`, `index.astro` monta `Problem`, `WhyNow` y `Solution` (de `src/components/sections/`) y existen `src/components/ui/SectionShell.astro` (02-03 le sumó la prop `split` y el slot `aside`, compatibles hacia atrás), `CtaLink.astro` con `location` de cuatro valores, `src/components/collage/CollagePiece.astro` (con la firma de 02-10: `name`, `tone`, `color?`, `size?`, `rotate?` y `class?`; `name='lupa'` sobre `light` dibuja el Loopy de un ojo), `tests/e2e/lib/brand.ts` (02-09, exporta `PURPLE_RGB`) y `tests/e2e/page-structure.spec.ts` con `PAGE_ORDER`; si falta alguno, detente y repórtalo (dependencia de 02-01, 02-02, 02-03, 02-09, 02-10 o 02-11 no cumplida) en vez de recrearlo.

    Skills primero (per DSGN-04, regla de Juan). Antes de escribir un componente, invoca con la herramienta Skill `impeccable` (carga `reference/craft-floor.md`, corre `context` sobre el `PRODUCT.md` y el `DESIGN.md` que dejó 02-01 y el verbo `shape` para el lote C1) y `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4. Patrón tomado de cada referencia para Lo que logramos juntos (Reference Synthesis): afirmaciones de resultado en grande sobre fondo oscuro con discos de check, de skale.so, y la rejilla 2x2 de rankingonai.com; adaptado con borde amarillo de 3 px, disco de check amarillo, tono `dark` y "FALTA CONFIRMAR" en el mismo estilo donde falte respaldo. Para Casos de éxito (se construye en la Tarea 2): tarjeta con cifra dominante, resaltado sobre la cifra y chip en la esquina, de rankingonai.com; numerales grandes en el color de acento, de m8l.com; líneas de resultado, de skale.so. Agrega a `02-VISUAL-LOG.md` (ya existe, lo creó 02-01: solo agrega al final) la entrada "Lote C1, ronda 0" con esa lectura, los verbos usados y el patrón tomado de cada referencia para las dos secciones.

    Copy (per COPY-01 y CONTEXT, "Prueba social" y "Textos pending"). En `src/content/landing.es.yaml`, bajo `es`, agrega la clave `results` (convención de 02-01: una clave por sección en snake_case) con `title` e `items`: una lista de cuatro objetos con `lead` y `body`, todas afirmaciones `{text, status}` con el texto exacto, carácter por carácter, de `02-ARI-COPY-V2.md` sección 4 y los estados de la tabla "Ranuras de copy". El `body` del segundo resultado tiene `text` "FALTA CONFIRMAR" y `status: pending`, y su `reason` transcribe la frase del doc con su marca de verificación y remite a COPY-VERIFICATION, detalle 2 (así Ari ve qué dato falta); ninguna otra ranura de la sección lleva un porcentaje. Los `reason` de las otras dos afirmaciones `pending` siguen la tabla. No agregues la nota de Ari sobre la fusión de outcomes (no es copy de la página), ni llaves de plantilla, y no toques `PLACEHOLDER_PATHS`.

    Esquema. En `src/content.config.ts` agrega `results` al objeto estricto de la colección: `title` como `claim` e `items` como lista de exactamente cuatro objetos estrictos con `lead` y `body` (ambos `claim`). Reutiliza la constante `claim` y cualquier ayudante que 02-03 haya añadido; no cambies lo demás.

    ResultItem. Crea `src/components/ui/ResultItem.astro` con props `lead` y `body` (cadenas). Renderiza un `<li class="result-item">` con, en fila, un SVG en línea de 32 px (`class="result-check"`, `aria-hidden="true"`, `focusable="false"`, sin `<title>` ni `<text>`: un círculo relleno con `var(--color-brand-yellow)` y una marca de check con trazo de 3 px en `var(--color-brand-dark)`, definidos con clases del propio componente y sin colores literales) y una columna de texto con `<p class="result-lead">` y `<p class="result-body">` que imprimen las cadenas como nodos de texto (sin inyección de HTML en bruto). Estilo local con tokens: borde de 3 px sólido en `var(--color-brand-yellow)`, radio `var(--radius-card)`, padding `var(--card-pad)`, sin fondo, sin sombra, sin hover y sin cursor de puntero; lead en Title (`--text-title`, su interlineado, peso 600); cuerpo en Body (`--text-body`, peso 400, `margin-top` de 0.5rem); ambos con color `var(--on-surface)` (blanco en `dark`) y `overflow-wrap: anywhere`; el disco con `inline-size` de 2rem y `aspect-ratio` 1 (sin alto fijo) y sin encoger; el `<li>` con `min-width: 0` y sin viñeta.

    Results. Crea `src/components/sections/Results.astro`: obtiene `getLanding()` de `../../lib/content` y renderiza `SectionShell` con `id="resultados"`, `tone="dark"` y `title` igual a `results.title.text`, y dentro un `<ul role="list" class="results-grid">` con un `ResultItem` por cada elemento de `results.items` (`lead.text` y `body.text`). Estilo local de la rejilla: sin viñetas, `margin` y `padding` en 0, una columna con gap `var(--card-gap)` y, desde 40em, `repeat(2, minmax(0, 1fr))` con `align-items: stretch`. Los componentes nunca leen `status`.

    Página y pendientes. En `src/pages/index.astro` importa `Results` y móntalo justo después de `Solution` (el componente de La solución que dejó 02-03) y antes de `AgendaSection`; no cambies nada más (el comentario del orden canónico de 02-01 ya lista `resultados`). Corre `npm run pending` para regenerar `PENDING-COPY.md`.

    Prueba (per lección 3 del brief y DSGN-04). Crea `tests/e2e/results-cases.spec.ts` con el patrón de lectura del YAML de `a11y-base.spec.ts` (parseo con `yaml`; todo texto esperado sale del YAML; el CTA se resolverá en la Tarea 2 con `cta.label_template` y `call.duration`). Subconjunto del tracer, a 1280x800 y a 390x844: (a) `main > section#resultados` con `data-tone="dark"`, fondo calculado `rgb(33, 33, 33)`, un solo h2 con el texto de `results.title` y color `rgb(255, 255, 255)`, `aria-labelledby` que apunta a ese h2, y sin `h3`, `a`, `button` ni `[tabindex]` dentro; (b) un `ul[role="list"]` con tantos `li` como `results.items` y, en el mismo orden, el `textContent` de `.result-lead` y de `.result-body` igual al del YAML; (c) el texto completo de `#resultados` no contiene un porcentaje de 30 ni de 50 (expresión regular por palabra completa con espacio opcional antes del signo); (d) por `li`: borde de 3 px con color `rgb(255, 198, 2)`, lead de peso 600 y color `rgb(255, 255, 255)`, cuerpo de peso 400, y un SVG de 32 por 32 px con `aria-hidden="true"` y `focusable="false"`, sin `title` ni `text`; (e) a 390 px todos los `li` comparten `left` (una columna) y a 1280 px hay dos columnas (`li[0]` y `li[1]` con el mismo `top`, `li[2]` y `li[3]` con el mismo `top`) con alturas iguales por fila (1 px de tolerancia); (f) con `emulateMedia` en `reduce` y en `no-preference`, `document.getAnimations().length` es 0 y cada `li` tiene opacidad 1. Confirma que (a) a (e) fallan antes de crear los componentes. Ciclo de las reglas de ejecución: build, preview en el puerto 4322, spec, `npx astro preview stop`. En los comentarios del código no repitas los literales que buscan los criterios de aceptación con grep.
  </action>
  <verify>
    <automated>npm run build && node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0 (el archivo versionado difiere del que se generaría)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const mine=r.content.filter(v=>v.path.startsWith('results.'));const n=(rule)=>mine.filter(v=>v.rule===rule).length;const other=mine.filter(v=>v.rule!=='PENDING'&&v.rule!=='MISSING').length;process.exit(r.structural.length===0&&other===0&&n('PENDING')===3&&n('MISSING')===1?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING o MISSING en las rutas `results.` (voseo, guion largo, AEO o `[VERIFICAR]`), o el recuento de esas rutas no es 3 PENDING y 1 MISSING</fails_when>
    <automated>node -e "const {parse}=require('yaml');const fs=require('fs');const doc=fs.readFileSync('.planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md','utf8');const y=parse(fs.readFileSync('src/content/landing.es.yaml','utf8')).es;const bad=[];const walk=(n,p)=>{if(n&&typeof n==='object'&&'text' in n){if(n.text!=='FALTA CONFIRMAR'&&!doc.includes(n.text))bad.push(p)}else if(Array.isArray(n))n.forEach((c,i)=>walk(c,p+'['+i+']'));else if(n&&typeof n==='object')for(const [k,v] of Object.entries(n))walk(v,p+'.'+k)};walk(y.results,'results');console.log(bad.join('\n'));process.exit(bad.length?1:0)"</automated>
    <fails_when>non-zero exit: alguna cadena de `results` (salvo el relleno) no es subcadena literal de `02-ARI-COPY-V2.md`, es decir, se retipeó con cambios; el script imprime las rutas</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/results-cases.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts</automated>
    <fails_when>non-zero exit de Playwright, o falla alguna prueba de `results-cases.spec.ts`, de `page-structure.spec.ts` (orden y tono de `#resultados` contra `PAGE_ORDER`) o de la fase 1; si `astro preview` no levanta porque el puerto 4322 está ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>`#resultados` muestra el titular del doc y los cuatro resultados en un 2x2 de tarjetas con borde amarillo y disco de check sobre fondo oscuro; el resultado del rango de presupuesto de ads no publica ninguna cifra (su cuerpo dice "FALTA CONFIRMAR" y su lead se muestra tal cual); el copy nuevo pasa por esquema, guarda y lista de pendientes; el orden y el tono de la página se conservan y las pruebas de la fase 1 siguen en verde.</done>
  <acceptance_criteria>
    - `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)` lista, para las rutas `results.`, exactamente 3 reglas PENDING y 1 MISSING y ninguna VOSEO, DASH, AEO ni VERIFICAR; el script de subcadenas contra el doc sale 0
    - `grep -c "^| results\." PENDING-COPY.md` devuelve 3 (`results.items[0].body`, `results.items[1].lead` y `results.items[1].body`) y `node scripts/list-pending.mjs --check` sale 0
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0; `grep -nE "(^|[^-a-z])height: *[0-9]" src/components/ui/ResultItem.astro src/components/sections/Results.astro | wc -l` devuelve 0
    - `DIFF="$(git diff --name-only BASE)" && printf '%s\n' "$DIFF" | grep -Ev '^(src/content.config.ts|src/content/landing.es.yaml|PENDING-COPY.md|src/components/ui/ResultItem.astro|src/components/ui/MetricCard.astro|src/components/sections/Results.astro|src/components/sections/Cases.astro|src/pages/index.astro|tests/e2e/results-cases.spec.ts|tests/e2e/a11y-base.spec.ts|[.]planning/.*)$' | wc -l` devuelve 0
    - `results-cases.spec.ts` pasa con ClickUp bloqueado (`npm run test:e2e:isolated`): un solo h2 con el texto del YAML, cuatro `li` con lead y cuerpo del YAML en orden, sin porcentaje de 30 ni de 50 en el texto de la sección, borde de 3 px `rgb(255, 198, 2)`, una columna a 390 px y dos a 1280 px, cero animaciones con ambas preferencias; `page-structure.spec.ts` y `a11y-base.spec.ts` pasan sin haber sido editados
    - `grep -oE "https?://[a-zA-Z0-9.-]+" dist/index.html | sort -u` no lista hosts distintos de forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG
    - `02-VISUAL-LOG.md` trae la entrada "Lote C1, ronda 0"
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Casos de éxito con MetricCard, tarjeta ancha con lupa y CTA casos</name>
  <files>src/content.config.ts, src/content/landing.es.yaml, PENDING-COPY.md, src/components/ui/MetricCard.astro, src/components/sections/Cases.astro, src/pages/index.astro, tests/e2e/results-cases.spec.ts, tests/e2e/a11y-base.spec.ts</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 6. Casos de éxito (CONT-06)`, `## Collage and Brand Assets Contract` (fila Lupa y regla de color por tono), `## Interaction Contract` (Orden de tabulación y la fila Tarjetas de la tabla Estados) y `## Typography` (Reglas); ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: `## 5. Casos de exito` (líneas 121 a 127), copia literal; y la sección "Notas del doc para el proyecto" (erratas conocidas, líneas 199 a 205)
    - src/components/collage/CollagePiece.astro y src/components/collage/collage-rules.mjs (tras 02-10: props `name`, `tone`, `color?`, `size?`, `rotate?` y `class?`; `SCENE_PIECES` en lugar de `PIECES`; `assertToneSafe` y `loopyScheme`, que da el esquema A del Loopy sobre `light`; colores permitidos en `light`) y tests/e2e/lib/brand.ts (`PURPLE_RGB`, de 02-09)
    - src/components/ui/ResultItem.astro, src/components/sections/Results.astro, src/components/ui/SectionShell.astro (slot `cta`) y src/components/CtaLink.astro (props `label`, `location`, `href?`)
    - src/content/landing.es.yaml y src/content.config.ts (estado tras la Tarea 1), tests/e2e/results-cases.spec.ts (estado tras la Tarea 1) y tests/e2e/a11y-base.spec.ts (líneas 56 a 76 y 184 a 200: orden de tabulación)
    - tests/e2e/cta-focus.spec.ts (patrón de clic en un CTA y foco en `#agenda-title`)
  </read_first>
  <behavior>
    - Cada tarjeta se lee como un artículo cuyo encabezado es la frase completa del doc ('+85% en ventas') y trae un `dl` con Sector, Plazo y Canal
    - Todo dato que el doc no da muestra 'FALTA CONFIRMAR' en el `dd` y en el chip; ningún `dd` queda vacío y ninguna tarjeta se oculta
    - Solo la quinta tarjeta lleva la lupa; ocupa el ancho sobrante y no la tapa ni la cifra ni el detalle
    - Rejilla medida por rectángulos: 1 columna bajo 640 px, 2 con la quinta ancha de 640 a 1023 px, 3 desde 1024 px con la quinta en las columnas 2 y 3
    - El CTA de casos tiene la etiqueta idéntica a las otras tres ubicaciones y un clic lleva el foco a `#agenda-title`
    - Las tarjetas no son interactivas: sin hover, sin cursor de puntero y sin enfocables dentro; el orden de tabulación de la fase 1 suma `cta:casos` después de `cta:solucion`
  </behavior>
  <action>
    Implementa CONT-06 y COPY-01 para los casos de éxito y el CTA `casos` (per CONTEXT, "Prueba social" y "Hero, header y orden de la página"). Skills: si este ejecutor no cargó las de la Tarea 1, invoca de nuevo con la herramienta Skill `impeccable` (`reference/craft-floor.md` y el verbo `layout` para la construcción del lote C1) y `design-taste-frontend` con la misma lectura y los diales 7, 3 y 4. Patrón de las referencias para Casos de éxito, ya anotado en "Lote C1, ronda 0": tarjeta blanca con borde de 3 px, cifra morada con marcador amarillo, `<dl>` de sector, plazo y canal, sin logos, gráficos ni fotos, y la tarjeta de Meta Ads como bloque distinto con una lupa que resalta la cifra (versión estática del recuadro y la flecha de rankingonai.com).

    Pruebas primero (TDD, rojo). Extiende `tests/e2e/results-cases.spec.ts` con el bloque de casos, todo texto esperado desde el YAML: (g) `main > section#casos` con `data-tone="light"`, fondo `rgb(255, 255, 255)`, un solo h2 con el texto de `cases.title` y color igual a `PURPLE_RGB` (importado de `./lib/brand`, 02-09), `aria-labelledby` que apunta a ese h2; (h) un `ul[role="list"]` con cinco `li`, cada uno con un `article` cuyo `aria-labelledby` apunta a un `h3`; el `textContent` del h3 (espacios normalizados) es `figure` y `metric` del YAML separados por un espacio y `getByRole('heading', { level: 3, name })` lo encuentra por ese nombre; (i) por tarjeta: el chip tiene el texto de `channel`; el `dl` trae tres `dt` con los textos de `cases.labels` en el orden Sector, Plazo y Canal y tres `dd` con `sector`, `period` y `channel`; ningún `dd` está vacío; hay un párrafo `.metric-detail` si y solo si el YAML trae `detail` para ese caso; (j) por tarjeta, la cifra tiene color `PURPLE_RGB` y un fondo calculado que contiene `rgb(255, 198, 2)`; (k) rejilla por rectángulos de los `li` con 2 px de tolerancia: a 390 px una columna; a 640 y 768 px dos columnas con la quinta ocupando ambas (mismo `left` que la primera y `right` igual al de la segunda); a 1024 y 1280 px tres columnas con las tarjetas 1 a 3 en el mismo `top`, la cuarta con el `left` de la primera y la quinta con el `left` de la segunda y el `right` de la tercera; (l) la lupa: exactamente un `svg[data-collage="piece"][data-collage-piece="lupa"]` en `#casos` (la raíz que emite `CollagePiece`; el atributo `data-piece` pertenece a los grupos del hero), dentro de la quinta tarjeta, con `aria-hidden="true"`, `focusable="false"` y un solo `<use>` hacia el símbolo `lg-lupa` del sprite de 02-10, 96 px de ancho (1 px de tolerancia) y unos 99 px de alto (relación 0.97 del arte oficial), dentro del rectángulo de la tarjeta, sin cruzarse con los rectángulos de `.metric-detail` ni del `dl`, y con el texto de la cifra por encima de ella (`document.elementsFromPoint` en el centro del rectángulo del texto de la cifra: el primer elemento que pertenece al `h3` viene antes que la lupa o la lupa no aparece); ningún `img` en `#casos` y ningún `svg` fuera de la lupa; (m) el CTA: un solo `#casos a[data-cta="casos"]` con `href="#agenda"`, texto igual a `cta.label_template` resuelto con `call.duration`, sin atributo `aria-label` y de 48 px de alto o más; a 1280 px queda a 48 px bajo la rejilla (4 px de tolerancia); con ClickUp bloqueado, un clic deja el hash en `#agenda` y el foco en `#agenda-title`; (n) las tarjetas: cero elementos enfocables dentro de `#casos article`, cursor calculado distinto de `pointer` y, tras un `hover`, la `transform` y la `box-shadow` calculadas no cambian; (o) el HTML de `/` (con `page.request.get('/')`) pesa menos de 61440 bytes. Corre y confirma que fallan antes de construir los componentes.

    Copy (per COPY-01 y CONTEXT, "Prueba social"). En `src/content/landing.es.yaml`, bajo `es`, agrega la clave `cases`: `title`, `labels` (con `sector`, `period` y `channel`) e `items` (cinco objetos con `figure`, `metric`, `detail` cuando el doc la trae, `sector`, `period` y `channel`), todas afirmaciones `{text, status}` con el texto exacto, carácter por carácter, de `02-ARI-COPY-V2.md` sección 5 y los estados de las tablas de "Ranuras de copy". Las de la columna C llevan `text` "FALTA CONFIRMAR" y `status: pending`, y su `reason` dice qué falta y que se lo pide a Ari; las de la columna B llevan el `reason` que explica su motivo en la tabla. Conserva las erratas ("trafico organico", "exito") y la falta de punto final del caso 5; no corrijas nada ni agregues el signo de moneda que el doc omite. Las etiquetas "Sector", "Plazo" y "Canal" son `verified` (microcopy estructural que fija el UI-SPEC, no vienen del doc).

    Esquema. En `src/content.config.ts` agrega `cases`: `title` como `claim`, `labels` como objeto estricto con `sector`, `period` y `channel`, e `items` como lista de exactamente cinco objetos estrictos con `figure`, `metric`, `detail` opcional, `sector`, `period` y `channel` (todos `claim`).

    MetricCard. Crea `src/components/ui/MetricCard.astro` con props `id`, `figure`, `metric`, `detail?`, `sector`, `period`, `channel`, `labels` (objeto con `sector`, `period` y `channel`) y `wide?` (booleano, falso por defecto); todas cadenas salvo `wide`. Estructura, en este orden del DOM: `<article class="metric-card" aria-labelledby={id}>` (con `data-wide` solo en la ancha) que contiene `<p class="metric-chip">` con `channel`; `<h3 id={id} class="metric-head">` con `<span class="metric-figure"><span class="metric-mark">` que imprime `figure`, un espacio explícito escrito como expresión (la compresión de Astro elimina el espacio de un salto de línea entre elementos) y `<span class="metric-name">` con `metric`; si hay `detail`, `<p class="metric-detail">`; y `<dl class="metric-facts">` con tres grupos `<div>` con `<dt>` y `<dd>`: las etiquetas de `labels` y los valores `sector`, `period` y `channel`. Solo si `wide`, importa `CollagePiece` de `../collage/CollagePiece.astro` y monta `name="lupa"`, `tone="light"`, `size="6rem"` y una `rotate` de entre -8 y 8 grados que fija la iteración (desde 02-10 la pieza es el Loopy oficial de un ojo con lupa en esquema A: aro y mango morados y anillo crema salen de la mesa oficial, y la firma ya no acepta `a`, `b` ni `c`, así que no pases colores); decorativa y detrás del texto (el bloque del texto con `position: relative` y `z-index` mayor que el de la lupa; sin anular eventos de puntero para que la prueba de apilado sea real). Estilo local con tokens: fondo `var(--color-brand-white)`, `border: var(--border-pop)`, radio `var(--radius-card)`, `box-shadow: var(--shadow-pop)`, padding `var(--card-pad)`, columna con gap de 1rem, sin hover, sin transición y sin cursor de puntero; chip en Label 600 con borde de 3 px oscuro, radio `var(--radius-pill)`, `min-height` de 2rem, `max-width: 100%`, `overflow-wrap: anywhere` (admite dos líneas), alineado al inicio y con relleno decidido en la iteración dentro de los pares aprobados (texto oscuro sobre blanco o amarillo); h3 con `var(--on-surface)` y Title 700; `.metric-figure` en bloque con Display (`--text-display` y su interlineado, peso 700), color `var(--heading)` y `overflow-wrap: anywhere`; `.metric-mark` con un relleno plano `var(--mark)` detrás del tercio inferior de la cifra (una sola tonalidad, sin desenfoque ni degradado visible: el UI-SPEC lo describe como franja plana); `.metric-name` en bloque con Title 700; detalle en Body 400; cada grupo del `dl` en una fila de dos columnas (`dt` de ancho automático en Label 600, `dd` en Body con `margin: 0`, `min-width: 0` y `overflow-wrap: anywhere`). Sin alturas fijas: solo `min-height`; las ilustraciones con `inline-size` y `aspect-ratio`. En la variante ancha, desde 40em: `display: grid` de dos columnas (`minmax(0, 5fr)` y `minmax(0, 7fr)`), el chip ocupando ambas, el h3 en la primera columna a lo largo de las filas siguientes y el detalle y el `dl` en la segunda; la lupa con posición absoluta anclada a la esquina de la tarjeta sin desbordarla y sin cruzar el detalle ni el `dl`; bajo 40em la ancha es una tarjeta más, en columna.

    Cases. Crea `src/components/sections/Cases.astro`: obtiene `getLanding()` y `ctaLabel` de `../../lib/content` y renderiza `SectionShell` con `id="casos"`, `tone="light"` y `title` igual a `cases.title.text`; dentro, un `<ul role="list" class="cases-grid">` con un `<li class="case-item">` por caso (el índice 4 lleva `data-wide`) que contiene un `MetricCard` con `id` `caso-<n>-title` (n desde 1), las cadenas `.text` de `figure`, `metric`, `detail` (solo si existe), `sector`, `period` y `channel`, las etiquetas `cases.labels.*.text` y `wide` verdadero solo en el índice 4; y, en el slot `cta` de `SectionShell`, un `CtaLink` con `location="casos"` y `label={ctaLabel(data)}`. Estilo local de la rejilla: sin viñetas, `margin` y `padding` en 0, gap `var(--card-gap)`, una columna; desde 40em, `repeat(2, minmax(0, 1fr))` con la ancha en `grid-column: 1 / -1`; desde 64em, `repeat(3, minmax(0, 1fr))` con la ancha en `grid-column: span 2`; los `li` con `display: flex` y `min-width: 0`, y el `article` con `flex: 1` para que las alturas de una fila sean parejas. Ningún componente lee `status`.

    Página, pendientes y orden de tabulación. En `src/pages/index.astro` importa `Cases` y móntalo justo después de `Results` y antes de `AgendaSection`. Corre `npm run pending`. En `tests/e2e/a11y-base.spec.ts`, caso (a) de 1280 px y caso (a) de 390 px, inserta `'cta:casos'` justo después de `'cta:solucion'` en cada arreglo esperado, antes del enlace de respaldo (lección 4 del brief); si `cta:solucion` no está en esos arreglos (02-03 no lo dejó), no lo inventes: detente y repórtalo. No cambies nada más de ese archivo. Ciclo de las reglas de ejecución: build, preview en el puerto 4322, specs, `npx astro preview stop`. En los comentarios del código no repitas los literales que buscan los criterios de aceptación con grep.
  </action>
  <verify>
    <automated>npm run build && node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0 (el archivo versionado difiere del que se generaría)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const sc=(p)=>r.content.filter(v=>v.path.startsWith(p));const n=(p,rule)=>sc(p).filter(v=>v.rule===rule).length;const other=(p)=>sc(p).filter(v=>v.rule!=='PENDING'&&v.rule!=='MISSING').length;const ok=r.structural.length===0&&other('results.')===0&&other('cases.')===0&&n('results.','PENDING')===3&&n('results.','MISSING')===1&&n('cases.','PENDING')===13&&n('cases.','MISSING')===5;process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING o MISSING en las rutas `results.` o `cases.` (voseo, guion largo, AEO o `[VERIFICAR]`), o los recuentos no son 3 PENDING y 1 MISSING en `results.` y 13 PENDING y 5 MISSING en `cases.`</fails_when>
    <automated>node -e "const {parse}=require('yaml');const fs=require('fs');const doc=fs.readFileSync('.planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md','utf8');const y=parse(fs.readFileSync('src/content/landing.es.yaml','utf8')).es;const bad=[];const walk=(n,p)=>{if(n&&typeof n==='object'&&'text' in n){if(n.text!=='FALTA CONFIRMAR'&&!doc.includes(n.text))bad.push(p)}else if(Array.isArray(n))n.forEach((c,i)=>walk(c,p+'['+i+']'));else if(n&&typeof n==='object')for(const [k,v] of Object.entries(n))walk(v,p+'.'+k)};walk(y.results,'results');walk(y.cases.title,'cases.title');walk(y.cases.items,'cases.items');console.log(bad.join('\n'));process.exit(bad.length?1:0)"</automated>
    <fails_when>non-zero exit: alguna cadena de `results`, de `cases.title` o de `cases.items` (salvo el relleno) no es subcadena literal de `02-ARI-COPY-V2.md`; las etiquetas de `cases.labels` quedan fuera a propósito porque no vienen del doc</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/results-cases.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts</automated>
    <fails_when>non-zero exit de Playwright, o falla alguna prueba de `results-cases.spec.ts`, de `page-structure.spec.ts`, de `cta-focus.spec.ts` o de `a11y-base.spec.ts` (con `cta:casos` en el orden de tabulación); si `astro preview` no levanta porque el puerto 4322 está ocupado, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>`#casos` muestra cinco tarjetas de métrica con las cifras del doc (+85%, +237%, x3, +3.808% y +500%), chip, encabezado con cifra y métrica, detalle y lista de Sector, Plazo y Canal, con "FALTA CONFIRMAR" donde el doc no da el dato; la quinta es la tarjeta ancha de Meta Ads con la única lupa; la rejilla es de 1, 2 y 3 columnas según el ancho; el CTA `casos` lleva la misma etiqueta y salta a `#agenda`; el orden de tabulación de la fase 1 incluye `cta:casos`; todo pasa por esquema, guarda y lista de pendientes.</done>
  <acceptance_criteria>
    - `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)` lista 3 PENDING y 1 MISSING en `results.` y 13 PENDING y 5 MISSING en `cases.`, sin VOSEO, DASH, AEO ni VERIFICAR; el script de subcadenas contra el doc sale 0
    - `grep -c "^| cases\." PENDING-COPY.md` devuelve 13, `grep -c "^| results\." PENDING-COPY.md` devuelve 3 y `node scripts/list-pending.mjs --check` sale 0
    - `grep -c "trafico organico" src/content/landing.es.yaml` devuelve al menos 1 y `grep -c "Casos de exito" src/content/landing.es.yaml` devuelve al menos 1 (las erratas del doc se conservan tal cual)
    - `grep -c "cta:casos" tests/e2e/a11y-base.spec.ts` devuelve 2 y el resto de ese archivo no cambia (`git diff BASE -- tests/e2e/a11y-base.spec.ts` solo muestra las dos líneas del arreglo)
    - `grep -c 'location="casos"' src/components/sections/Cases.astro` devuelve 1 y `grep -c "CollagePiece" src/components/ui/MetricCard.astro` devuelve al menos 2 (importación y uso)
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0; `grep -nE "(^|[^-a-z])height: *[0-9]" src/components/ui/MetricCard.astro src/components/sections/Cases.astro | wc -l` devuelve 0
    - `test "$(wc -c < dist/index.html)" -lt 61440` sale 0 y `grep -oE "https?://[a-zA-Z0-9.-]+" dist/index.html | sort -u` no lista hosts distintos de forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG
    - `results-cases.spec.ts` pasa con ClickUp bloqueado: cinco `li` con `article`, h3 con la frase del doc como nombre, `dl` con las etiquetas y los valores del YAML, cifra morada con marcador amarillo, rejilla de 1, 2 y 3 columnas medida por rectángulos con la quinta en las columnas 2 y 3 desde 1024 px, una sola lupa de 96 px dentro de la quinta tarjeta sin cruzar el detalle ni el `dl`, CTA `casos` con la etiqueta del YAML y foco en `#agenda-title`, tarjetas sin enfocables ni hover, HTML de `/` menor a 61440 bytes
    - `DIFF="$(git diff --name-only BASE)" && printf '%s\n' "$DIFF" | grep -Ev '^(src/content.config.ts|src/content/landing.es.yaml|PENDING-COPY.md|src/components/ui/ResultItem.astro|src/components/ui/MetricCard.astro|src/components/sections/Results.astro|src/components/sections/Cases.astro|src/pages/index.astro|tests/e2e/results-cases.spec.ts|tests/e2e/a11y-base.spec.ts|[.]planning/.*)$' | wc -l` devuelve 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lote C1 de diseño (Logramos juntos y Casos), mediciones a cinco anchos y registro visual</name>
  <files>src/components/ui/ResultItem.astro, src/components/ui/MetricCard.astro, src/components/sections/Results.astro, src/components/sections/Cases.astro, tests/e2e/results-cases.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol` (Ciclo por lote y Lista de "vibra"), `## Reference Synthesis` (filas Lo que logramos juntos, Casos de éxito y Ritmo global), `### 5. Lo que logramos juntos` y `### 6. Casos de éxito`, `## Responsive`, `## Verification Hooks` y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - src/components/ui/ResultItem.astro, src/components/ui/MetricCard.astro, src/components/sections/Results.astro, src/components/sections/Cases.astro y tests/e2e/results-cases.spec.ts (estado tras la Tarea 2)
    - DESIGN.md, PRODUCT.md y .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (entrada "Lote C1, ronda 0")
    - tests/e2e/page-structure.spec.ts (solo lectura: herramienta de capturas por lote, que se reutiliza sin cambiarla) y tests/e2e/a11y-base.spec.ts (caso g: patrón de espaciado de texto a cada ancho)
  </read_first>
  <action>
    Implementa el cierre de DSGN-04 para el lote C1 y las mediciones de CONT-05 y CONT-06 a cinco anchos. Anota el SHA del commit de la Tarea 2 como BASE2 para el criterio de que ninguna corrección toca el copy. Skills: invoca con la herramienta Skill `impeccable` con los verbos del lote C del UI-SPEC limitados a estas dos secciones (`shape`, `layout`, `colorize`, `typeset` para las cifras y `adapt`; sin `animate`: el movimiento es del plan 07) y `design-taste-frontend` (diales 7, 3 y 4; sin degradados, sin vidrio, sin sombras difusas y sin tres tarjetas iguales sin variar: aquí la variación es la tarjeta ancha con lupa y el cambio de tono entre las dos secciones).

    Pruebas primero. Extiende `tests/e2e/results-cases.spec.ts` con: (p) a 320, 390, 768, 1024 y 1280 px, sin scroll horizontal (`scrollWidth` del documento menor o igual al ancho) y los rectángulos de `#resultados`, de `#casos` y de cada `li` dentro de `[0, innerWidth]`; (q) a 320 px, el texto de la cifra '+3.808%' (ubicado con el texto del YAML) y cada `dd` cuyo texto es el relleno quedan dentro del rectángulo de su tarjeta y sin desborde (`scrollWidth` menor o igual a `clientWidth` más 1); (r) padding vertical de `#resultados` y de `#casos` de 64 px a 390 px y de 96 px a 1024 y 1280 px; (s) con `javaScriptEnabled: false`, ambas secciones muestran todos los textos del YAML y el CTA `casos` lleva `href="#agenda"`; (t) espaciado de texto forzado de SC 1.4.12 (interlineado 1.5, párrafo 2x, letras 0.12em, palabras 0.16em, con una hoja de estilo inyectada como en `a11y-base.spec.ts`) a 320 y 1280 px sin desborde horizontal y sin cifras ni textos que se salgan de su tarjeta; (u) la cifra es lo más grande de cada tarjeta: el tamaño de fuente calculado de `.metric-figure` es mayor que el de `.metric-name`, `.metric-detail`, `dt`, `dd` y el chip, y el marcador `.metric-mark` tiene un fondo calculado que contiene `rgb(255, 198, 2)`; (v) el bloque de capturas por sección, que solo corre cuando `PHASE2_BATCH` está definida (`test.skip` si no) y cuyo título contiene la palabra "captura": por cada ancho abre un contexto nuevo con `browser.newContext`, aborta con `context.route` todo lo que no sea localhost y guarda `test-results/phase2/<lote>-resultados-<ancho>.png` y `<lote>-casos-<ancho>.png` con `locator.screenshot()` de cada sección (variante base). Las pruebas que ya pasen con la construcción de las Tareas 1 y 2 quedan como regresión; anota en `02-VISUAL-LOG.md` cuáles fallaron antes de iterar.

    Ciclo del lote C1 (per protocolo del UI-SPEC, "Ciclo por lote"). (1) Construir el lote completo desde el contrato: ya está hecho en las Tareas 1 y 2. (2) Una ronda de captura en lote con `PHASE2_BATCH=C1` (la herramienta de página completa de 02-01 más las capturas por sección de este spec) a los cinco anchos, con base, movimiento reducido y sin JavaScript, con ClickUp bloqueado; mira solo `C1-resultados-390.png`, `C1-resultados-1280.png`, `C1-casos-390.png` y `C1-casos-1280.png`, y comprueba 320, 768 y 1024 px con las mediciones del spec, mirando una captura solo si la medición falla. (3) `impeccable critique` del lote contra el contrato de cada sección, las filas de la síntesis de referencias y los rasgos 2, 3, 4 y 7 de la lista de vibra (aire de 96 px en escritorio, tarjetas con borde de 3 px y sombra dura sin sombras difusas, la cifra como lo más grande de cada tarjeta con marcador amarillo, y un foco claro con collage o layout propio por sección); revisa además la composición de la lupa frente a la cifra, que se reevalúa aquí con el Loopy oficial de 02-10 (el aro y el mango no cruzan los glifos ni el detalle; si el aro pasa sobre la cifra, ajusta posición, rotación o `size`: el arte oficial no se recolorea ni se redibuja), la legibilidad de "FALTA CONFIRMAR" dentro del chip y de los `dd` a 320 px, y el peso visual del chip frente a la cifra. (4) Corrige todo en un solo lote de cambios, con una ronda de confirmación como máximo; si un criterio de salida sigue fallando tras la confirmación, repite el ciclo con tope de tres ciclos: si no se cumple, detente y reporta a Juan con capturas. (5) Registra en `02-VISUAL-LOG.md` la entrada "Lote C1, ronda N" por ciclo, con verbos usados, hallazgos, correcciones, rutas de las capturas, la revisión de la lupa a 320 y a 1280 px (comprobación de respaldo) y los rasgos 2, 3, 4 y 7 marcados como cumplidos. Las correcciones tocan solo el estilo local y el marcado de `ResultItem`, `MetricCard`, `Results` y `Cases`; nunca tokens ni componentes de otros planes, y ninguna corrección cambia un texto del YAML. Al terminar: `npx astro preview stop`. En los comentarios del código no repitas los literales que buscan los criterios de aceptación con grep.
  </action>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated</automated>
    <fails_when>non-zero exit de Playwright: falla alguna prueba de `results-cases.spec.ts` (desborde a cinco anchos, cifra que no cabe a 320 px, padding de 64 y 96 px, sin JavaScript, espaciado de texto forzado, cifra más grande que el resto), de `page-structure.spec.ts`, de `a11y-base.spec.ts` o de `cta-focus.spec.ts`; si `astro preview` no levanta porque el puerto 4322 está ocupado, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
    <automated>PHASE2_BATCH=C1 npm run test:e2e:isolated -- tests/e2e/page-structure.spec.ts tests/e2e/results-cases.spec.ts -g "captura" && test "$(ls test-results/phase2/C1-*.png | wc -l)" -eq 25</automated>
    <fails_when>non-zero exit de los specs de capturas, o el número de capturas del lote C1 no es 25 (15 de página completa: cinco anchos por tres variantes, más 10 por sección: cinco anchos por dos secciones)</fails_when>
    <automated>npm run build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build, de las guardas, del contraste o de la lista de pendientes (el YAML no cambió en esta tarea, así que cualquier diferencia en `PENDING-COPY.md` indica que se tocó el copy)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const sc=(p)=>r.content.filter(v=>v.path.startsWith(p));const n=(p,rule)=>sc(p).filter(v=>v.rule===rule).length;const other=(p)=>sc(p).filter(v=>v.rule!=='PENDING'&&v.rule!=='MISSING').length;const ok=r.structural.length===0&&other('results.')===0&&other('cases.')===0&&n('results.','PENDING')===3&&n('results.','MISSING')===1&&n('cases.','PENDING')===13&&n('cases.','MISSING')===5;process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: cambió el estado o el texto de alguna afirmación de `results.` o `cases.`, o apareció una regla distinta de PENDING o MISSING en esas rutas</fails_when>
  </verify>
  <done>A 320, 390, 768, 1024 y 1280 px, Lo que logramos juntos y Casos de éxito se ven pulidos, sin scroll horizontal, sin texto recortado y sin movimiento: la cifra es lo más grande de cada tarjeta con su marcador amarillo, la tarjeta de Meta Ads es un bloque distinto con la lupa sin tapar texto, el padding vertical es de 64 y 96 px y "FALTA CONFIRMAR" se lee en cada ranura que lo lleva; el lote C1 quedó registrado con capturas y cumple los rasgos 2, 3, 4 y 7 de la lista de vibra.</done>
  <acceptance_criteria>
    - `npm run test:e2e:isolated` pasa completo con ClickUp bloqueado y `E2E_BLOCK_CLICKUP` activo; `results-cases.spec.ts` incluye las pruebas (p) a (v) y ninguna se saltó salvo el bloque de capturas cuando `PHASE2_BATCH` no está definida
    - Sin scroll horizontal a 320, 390, 768, 1024 y 1280 px; a 320 px '+3.808%' y cada `dd` con el relleno quedan dentro de su tarjeta; con espaciado de texto forzado a 320 y 1280 px no hay desborde
    - El padding vertical de `#resultados` y de `#casos` mide 64 px a 390 px y 96 px a 1024 y 1280 px; el fondo de `#resultados` es `rgb(33, 33, 33)` y el de `#casos` `rgb(255, 255, 255)`
    - En cada tarjeta el tamaño de fuente de `.metric-figure` es mayor que el de `.metric-name`, `.metric-detail`, `dt`, `dd` y el chip; el color es `PURPLE_RGB` (de `tests/e2e/lib/brand.ts`) y el marcador contiene `rgb(255, 198, 2)`
    - `ls test-results/phase2/C1-*.png | wc -l` devuelve 25 (15 de página completa y 10 por sección)
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components src/pages | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0; `grep -nE "(^|[^-a-z])height: *[0-9]" src/components/ui/ResultItem.astro src/components/ui/MetricCard.astro src/components/sections/Results.astro src/components/sections/Cases.astro | wc -l` devuelve 0
    - `git diff --name-only BASE2 -- src/content/landing.es.yaml src/content.config.ts PENDING-COPY.md src/styles src/components/collage src/components/brand src/components/ui/SectionShell.astro src/components/CtaLink.astro` no lista nada
    - `DIFF="$(git diff --name-only BASE)" && printf '%s\n' "$DIFF" | grep -Ev '^(src/content.config.ts|src/content/landing.es.yaml|PENDING-COPY.md|src/components/ui/ResultItem.astro|src/components/ui/MetricCard.astro|src/components/sections/Results.astro|src/components/sections/Cases.astro|src/pages/index.astro|tests/e2e/results-cases.spec.ts|tests/e2e/a11y-base.spec.ts|[.]planning/.*)$' | wc -l` devuelve 0 (BASE es el SHA anotado al empezar la Tarea 1)
    - `02-VISUAL-LOG.md` trae "Lote C1" con la ronda 0 y al menos una ronda de esta tarea, los verbos usados, los hallazgos, las correcciones, las rutas de las capturas y la nota de la revisión de la lupa a 320 y 1280 px; los rasgos 2, 3, 4 y 7 quedan marcados como cumplidos o, si no, el ejecutor se detuvo y reportó a Juan tras tres ciclos
    - `02-04-SUMMARY.md` reporta las erratas del doc ('trafico organico', 'exito' y la falta de punto final del caso 5), las afirmaciones nuevas de `PENDING-COPY.md`, los hallazgos para Ari, las desviaciones de "Supuestos y desviaciones" que se aplicaron y cualquier medición que difiera de la estimada
  </acceptance_criteria>
</task>

</tasks>

<verification>
Al cerrar las tres tareas, con el servidor preview levantado según las reglas de ejecución y detenido al final:

1. `npm run build` sale 0; `PUBLIC_ENV=production npm run build` falla solo por afirmaciones PENDING y por "FALTA CONFIRMAR" (`(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)` lista solo esas dos reglas en las rutas `results.` y `cases.`: 16 PENDING y 6 MISSING).
2. `node scripts/check-contrast.mjs` sale 0; `node --test tests/guards/*.test.mjs` sin fallos.
3. `npm run test:e2e:isolated` pasa completo (fase 1, `page-structure.spec.ts` y `results-cases.spec.ts`) con ClickUp bloqueado.
4. `npm run pending` es idempotente y `node scripts/list-pending.mjs --check` sale 0.
5. El script de subcadenas confirma que cada cadena de `results` y de `cases` (salvo el relleno y las tres etiquetas) es literal del doc de Ari.
6. `02-VISUAL-LOG.md` trae el lote C1 (ronda 0 y las rondas de la Tarea 3) y `02-04-SUMMARY.md` describe lo hecho.
</verification>

<success_criteria>
Las secciones Lo que logramos juntos y Casos de éxito están construidas con el texto del doc de Ari y sin ninguna cifra sin respaldo publicada (el rango de presupuesto de ads y los datos que el doc no da muestran "FALTA CONFIRMAR" y están listados para Ari); verificadas de 320 a 1280 px, con la tarjeta de Meta Ads como bloque distinto con lupa, el CTA de casos con la misma etiqueta que los otros tres y salto a `#agenda`, el orden de tabulación de la fase 1 actualizado y el diseño pasado por `impeccable` y `design-taste-frontend` y registrado en `02-VISUAL-LOG.md`.
</success_criteria>

<output>
Al terminar crea `.planning/phases/02-secciones-marca-y-copy/02-04-SUMMARY.md` con: archivos creados y cambiados; resultados de las pruebas y mediciones; el registro del lote C1 por ronda con rutas de capturas; las erratas del doc detectadas ('trafico', 'organico' y 'exito', y la falta de punto final del caso 5); los hallazgos para Ari (rango de presupuesto de ads sin respaldo; la inferencia "el orgánico se abarata"; el lenguaje de promesa de "Apareces donde antes no estabas"; el caso de Meta Ads y su encaje SEO/GEO; los cinco plazos; el canal de los casos 1 a 4; el sector y el canal del caso 5 como frase única; el caso 5 sin línea de detalle; el signo "$" de los casos sin moneda; "Casos de exito" como titular provisional); las desviaciones de "Supuestos y desviaciones" que se aplicaron; y la nota para 02-05 sobre el disco de check en línea. Si el contexto pasa de 50 % entre tareas, escribe un SUMMARY parcial con `status: partial`.
</output>

