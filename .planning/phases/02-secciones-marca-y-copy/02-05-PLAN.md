---
phase: 02-secciones-marca-y-copy
plan: 05
type: execute
wave: 7
depends_on: [02-04]
files_modified:
  - src/content.config.ts
  - src/content/landing.es.yaml
  - PENDING-COPY.md
  - src/components/sections/Team.astro
  - src/components/sections/Includes.astro
  - src/components/sections/HowItWorks.astro
  - src/pages/index.astro
  - tests/e2e/team-includes-how.spec.ts
  - tests/guards/copy-team-includes-how.test.mjs
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
autonomous: true
requirements: [CONT-07, CONT-08, CONT-09, COPY-01, DSGN-04]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`#nosotros` es `<section id='nosotros' data-tone='yellow'>` construida con `SectionShell`; su h2 sale de `team.title` y su cuerpo es un `<ul role='list'>` con cuatro `<li>` (TeamCard) en el orden Arianna Lupi, Verónica Romero, Juan Angulo, Miguel Pacheco; cada tarjeta lleva un `Avatar` SVG ilustrado con `aria-hidden='true'` y `focusable='false'`, un `<h3>` con el nombre (`team.members[i].name`) y un `<p>` con el cargo (`team.members[i].role`), todo tal cual del doc de Ari (CONT-07, COPY-01)"
    - "Los cuatro avatares son variantes distintas del mismo set Loopy oficial de 02-10 (`ojo-morado`, `ojo-amarillo`, `ojos-morado` y `ojos-amarillo`: un ojo o dos ojos con lupa sobre un disco plano morado o amarillo con sombra dura), sin caras, accesorios ni rasgos inventados, asignadas por posición desde la constante `AVATARS` del componente y no desde el YAML (una cadena suelta dentro de `es` rompe la guarda FND-02 con BARE_STRING); miden 96 px bajo 640 px y 120 px desde 640 px, todos del mismo tamaño, con 2560 bytes de HTML como máximo cada uno, y no son retratos (CONT-07)"
    - "Quiénes somos no muestra biografías, introducción, credenciales, enlaces ni redes: el UI-SPEC aprobado (sección 7) y STATE (consentimiento y credencial del equipo pendientes de Ari) lo excluyen; el texto de esas partes del doc queda solo en `02-ARI-COPY-V2.md` y sus erratas ('agil', 'traves', 'direccion', 'Coach SEO' pegado) se reportan en el SUMMARY (CONT-07, COPY-01)"
    - "Los cuatro `team.members[i].name` son `pending` (consentimiento del integrante para publicar su nombre y cargo, lo confirma Ari) y se muestran tal cual; los cuatro cargos y el h2 son `verified` porque existen en el doc y pasan las guardas (COPY-01)"
    - "`#incluye` es `<section id='incluye' data-tone='light'>` con seis IncludeItem en `<ul role='list'>`: cada uno con `border-top` de 3 px, disco de check de 32 px (amarillo, contorno oscuro, check oscuro, `aria-hidden`), `<h3>` con el título en negrita del doc y `<p>` con el resto de la frase; sin tarjeta, sin sombra, sin CTA y sin movimiento (CONT-08)"
    - "El título del primer entregable ('Auditoría SEO + AEO completa') contiene AEO y la guarda lo rechaza: el YAML guarda `includes.items[0].title` como `pending` con `text: FALTA CONFIRMAR` y el texto del doc dentro de `reason`; la página muestra 'FALTA CONFIRMAR' en ese h3 y la descripción del doc debajo. Los otros cinco títulos y las seis descripciones salen `verified` y tal cual, sin la coma que une el título con su descripción y con el inicio en minúscula que trae el doc (CONT-08, COPY-01)"
    - "`#como-funciona` es `<section id='como-funciona' data-tone='dark'>` con un `<ol role='list'>` de cuatro `<li>`: disco de 48 px (amarillo, número oscuro del 1 al 4 por contador CSS en `li::before`), `<h3>` con el título de la fase sin el prefijo 'Fase N:' ni el plazo, la descripción del doc y un chip de plazo (`.step-time`: texto amarillo, borde amarillo de 3 px, `max-width: 100%`, admite dos líneas); sin CTA ni movimiento (CONT-09)"
    - "Los plazos salen del doc y se muestran tal cual ('30 min', '2 semanas', '6-12 meses', 'mes 12+'), cada uno como afirmación `pending` con `reason`; títulos y descripciones de fase son `verified`; el orden 1 a 4 es el del doc (CONT-09, COPY-01)"
    - "Layouts por punto de quiebre medidos con `getBoundingClientRect`: equipo en 1, 2 y 4 columnas (320 y 390, 768, 1024 y 1280 px); incluye en 1, 2 y 3 columnas; fases en lista vertical con riel discontinuo de 3 px naranja hasta 1023 px y en cuatro columnas con conector horizontal desde 1024 px (CONT-07, CONT-08, CONT-09, DSGN-04)"
    - "Sin scroll horizontal a 320, 390, 768, 1024 y 1280 px; ninguna de las tres secciones se sale del viewport; hijos de rejilla con `min-width: 0`; nombres, cargos, títulos y chips con `overflow-wrap: anywhere` y sin recorte con el espaciado de texto de SC 1.4.12 a 320 y 1280 px; ninguna altura fija en contenedores de texto (UI Considerations: long-text y overflow, resueltos explícitos)"
    - "Toda combinación de color de las tres secciones está entre los pares aprobados y se mide con estilos calculados y `contrastRatio`: texto oscuro sobre tarjeta blanca, blanco sobre oscuro, chip y disco amarillos sobre oscuro (10.22) y número oscuro sobre el disco amarillo; las tarjetas blancas sobre amarillo llevan borde oscuro de 3 px (UI Considerations: a11y-color)"
    - "Las tres secciones no agregan paradas de teclado, enlaces ni botones; las tarjetas no tienen hover, cursor de puntero ni desplazamiento; `document.getAnimations().length` es 0 con `reduce` y con `no-preference` y ningún elemento de las secciones tiene transición (UI Considerations: a11y-motion; Interaction Contract, fila Tarjetas)"
    - "Con `PUBLIC_ENV=production` el contenido nuevo solo dispara reglas PENDING y MISSING (nueve rutas pendientes: cuatro nombres, `includes.items[0].title` y cuatro plazos); `npm run pending` las lista, `node scripts/list-pending.mjs --check` sale 0 y la prueba de guarda `copy-team-includes-how` deriva todo del YAML, sin cadenas ni listas de pending escritas a mano (COPY-01)"
    - "Todo el diseño del plan pasó por `impeccable` (lote C de equipo con `shape`, `layout`, `colorize`, `typeset` y `adapt`; lote D de incluye y cómo funciona con `shape`, `layout`, `clarify` y `adapt`; `critique` en ambos) y por `design-taste-frontend` (diales 7, 3 y 4), invocados con la herramienta Skill; `02-VISUAL-LOG.md` registra por lote y ronda los verbos, hallazgos, correcciones y capturas a 320, 390, 768, 1024 y 1280 px (base, `reduce` y sin JavaScript, con ClickUp bloqueado); los lotes cierran con los rasgos 3, 5 y 7 de la lista de vibra (DSGN-04)"
    - statement: "Semántica de lista con `list-style: none`: Safari con VoiceOver puede perder el rol de lista en el `<ol>` de fases y en los `<ul>` de equipo e incluye; el spec afirma `role='list'` y la comprobación con lector de pantalla queda en la revisión humana diferida de la fase 1 (UI Considerations: interaction, backstop)"
      verification: backstop
    - statement: "Uniformidad del set de cuatro avatares (rasgo 5 de la vibra; uniforme por construcción, con el mismo disco, la misma sombra y el mismo Loopy oficial, y distinto solo por cantidad de ojos y color del disco) y aspecto de los chips de plazo de dos líneas: el ejecutor revisa las capturas de 390 y 1280 px y lo anota en `02-VISUAL-LOG.md`; a 320, 768 y 1024 px lo cubre la medición automática"
      verification: backstop
    - "Supuestos explícitos (UI Considerations, fila copy: resuelta aquí solo para estas ranuras): el h2 de cada sección es el encabezado del doc sin el número; la cadena de cada ranura queda fijada en la tabla 'Ranuras de copy de este plan'; los ítems de la lista de pendientes de esta sección son nueve. Las demás secciones las mapea el plan que las construye"
  artifacts:
    - path: "src/content/landing.es.yaml"
      provides: "Claves `team`, `includes` y `how_it_works` con las cadenas del Copy v2 (nueve afirmaciones `pending`, el resto `verified`)"
      contains: "how_it_works:"
    - path: "src/content.config.ts"
      provides: "Esquema estricto de las tres claves con listas de 4, 6 y 4 elementos"
      contains: "how_it_works"
    - path: "src/components/sections/Team.astro"
      provides: "Sección Quiénes somos (CONT-07) con la constante `AVATARS` y cuatro TeamCard"
      contains: "AVATARS"
    - path: "src/components/sections/Includes.astro"
      provides: "Sección Qué incluye (CONT-08) con seis IncludeItem abiertos y disco de check"
      contains: "include-check"
    - path: "src/components/sections/HowItWorks.astro"
      provides: "Sección Cómo funciona (CONT-09) con `<ol>`, disco por contador CSS, chip de plazo y conector"
      contains: "step-time"
    - path: "src/pages/index.astro"
      provides: "Composición con Team, Includes y HowItWorks entre Casos y la sección de agenda, en el orden canónico"
      contains: "HowItWorks"
    - path: "tests/e2e/team-includes-how.spec.ts"
      provides: "Estructura, columnas por ancho, desborde, espaciado de texto, colores calculados, movimiento y herramienta de capturas recortadas por lote (`PHASE2_BATCH`)"
      contains: "PHASE2_BATCH"
    - path: "tests/guards/copy-team-includes-how.test.mjs"
      provides: "Guarda derivada del YAML: verified sin violaciones de contenido, pending con reason, texto AEO nunca en `text`, cantidades 4, 6 y 4"
      contains: "walkClaims"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada con las nueve rutas nuevas"
      contains: "how_it_works.steps[0].timeframe"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Entradas 'Lote C (equipo)' y 'Lote D (incluye y cómo funciona)' con rondas, verbos y capturas"
      contains: "Lote D"
  key_links:
    - from: "src/components/sections/Team.astro"
      to: "src/content/landing.es.yaml"
      via: "`getLanding()`: `team.title`, `team.members[i].name` y `.role` impresos como nodos de texto, sin `fill()` ni lectura del estado"
    - from: "src/components/sections/Team.astro"
      to: "src/components/collage/Avatar.astro"
      via: "`<Avatar variant={AVATARS[i]} />`; `AVATARS` es una unión literal de las cuatro variantes que fija 02-10 (`ojo-morado`, `ojo-amarillo`, `ojos-morado` y `ojos-amarillo`)"
    - from: "src/components/sections/HowItWorks.astro"
      to: "src/styles/tokens.css"
      via: "`data-tone='dark'` activa `--bar` (naranja) para el conector y `--on-surface` (blanco) para el texto; el disco y el chip usan `var(--color-brand-yellow)`"
    - from: "src/pages/index.astro"
      to: "src/components/sections/Team.astro"
      via: "Team, Includes y HowItWorks se renderizan en ese orden, después de Casos y antes de lo que siga (agenda o las secciones del plan 06)"
    - from: "tests/e2e/team-includes-how.spec.ts"
      to: "src/content/landing.es.yaml"
      via: "El spec lee el YAML con `yaml` y compara cada texto de la página con la afirmación de su ranura"
    - from: "tests/guards/copy-team-includes-how.test.mjs"
      to: "scripts/lib/copy-rules.mjs"
      via: "`walkClaims` y `checkCopy` sobre el YAML real: las afirmaciones verified no disparan reglas de contenido"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir ni reordenar el texto del doc de Ari: si una guarda lo rechaza (AEO en 'Auditoría SEO + AEO completa'), el texto del doc va en `reason` y la ranura dice FALTA CONFIRMAR; las erratas del doc se reportan en el SUMMARY sin corregirlas"
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT leer el estado de una afirmación en un componente ni ocultar una afirmación pending: los nombres del equipo y los plazos de las fases se muestran aunque estén pending"
    - requirement_id: CONT-07
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT mostrar biografía, introducción, credenciales, enlaces a redes, fotos ni retratos en Quiénes somos: solo avatar ilustrado, nombre y cargo (UI-SPEC sección 7 y STATE)"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar `set:html`, colores hex dentro de `src/components`, `outline: none`, `line-height` con `!important` ni `height:` fijo en contenedores de texto: los colores salen de tokens y de `var(--color-brand-*)`"
    - requirement_id: CONT-09
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT agregar CTA, enlaces, botones, `tabindex` ni paradas de teclado en Quiénes somos, Qué incluye ni Cómo funciona: el orden de tabulación de la fase 1 no cambia"
    - requirement_id: DSGN-03
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT combinar pares prohibidos (morado sobre oscuro, blanco o naranja como texto sobre amarillo, blanco sobre naranja, amarillo como texto sobre claro): el chip de plazo es amarillo sobre oscuro, el disco amarillo lleva número oscuro y toda tarjeta blanca sobre amarillo lleva borde oscuro de 3 px"
    - requirement_id: DSGN-04
      category: accessibility
      status: resolved
      verification: test
      statement: "MUST NOT animar ni hacer transiciones en estas secciones, ni dar hover, cursor de puntero o desplazamiento a las tarjetas: la fase 2 reserva el movimiento al plan 07 y estas secciones no figuran en su presupuesto"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: judgment
      statement: "MUST NOT modificar `tokens.css`, `global.css`, `SectionShell.astro`, `CtaLink.astro`, `Hero.astro`, `AgendaSection.astro`, `SiteHeader.astro`, `BaseLayout.astro`, `SkipLinks.astro`, `src/scripts/cta-focus.ts`, `public/`, los componentes de `src/components/collage/`, `page-structure.spec.ts` ni archivos de los planes 02-03 y 02-04: solo se edita lo listado en `files_modified`"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar el formulario de ClickUp, leer `.env`, instalar paquetes ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Construir el segundo bloque de credibilidad y proceso de la landing con el texto del doc de Ari: Quiénes somos (cuatro integrantes con avatar ilustrado, nombre y cargo; CONT-07), Qué incluye (seis entregables; CONT-08) y Cómo funciona (lista ordenada de cuatro fases con su plazo; CONT-09), con cada afirmación en `landing.es.yaml` como `{text, status}` y los textos sin editar (COPY-01), construido con `impeccable` y `design-taste-frontend` y verificado de 320 a 1280 px (DSGN-04).

Purpose: que el dueño del negocio vea quién está detrás, qué recibe y cómo se trabaja antes de llegar al formulario. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: claves `team`, `includes` y `how_it_works` en el YAML y en el esquema; `Team.astro`, `Includes.astro` y `HowItWorks.astro` montadas en `index.astro`; nueve afirmaciones nuevas en `PENDING-COPY.md`; `team-includes-how.spec.ts` y `copy-team-includes-how.test.mjs`; entradas de los lotes C (equipo) y D (incluye y cómo funciona) en `02-VISUAL-LOG.md`.

Decisiones de CONTEXT.md que implementa (por nombre de sección): "Prueba social" (equipo con avatar ilustrado SVG estático en estilo collage, nombre y cargo en texto real, avatar con `aria-hidden`), "Secciones informativas" ("Cómo funciona": cuatro fases en `<ol>`, cada una con plazo, plazos `pending`), "Textos pending" (el texto real del doc se muestra tal cual y solo se lista; el relleno FALTA CONFIRMAR solo donde el doc no sirve), "Estilo visual, collage y movimiento" (tonos alternados, contraste medido, método `impeccable` más `design-taste-frontend`, sin movimiento con `reduce`) y "Excepciones aprobadas por Juan" (Title como quinto tamaño para el h3 de tarjeta).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicar con `grep -n '^## \|^### '` y leer con `Read` usando `offset` y `limit` de 200 líneas o menos).
</context>

## Artifacts this phase produces

Rutas relativas a la raíz del repositorio. Los nombres de abajo son los que reutilizan el plan 02-06 (para quién es, FAQ, footer y privacidad) y el plan 02-07 (movimiento y cierre); no se renombran después. Reutiliza sin cambios lo que producen 02-01, 02-02, 02-09 y 02-10: `SectionShell` (props `id`, `tone`, `title`, `lead?`, `titleId?`), `Avatar` (prop `variant` con los valores de 02-10: `ojo-morado`, `ojo-amarillo`, `ojos-morado` y `ojos-amarillo`), los tokens (con el morado oficial de 02-09) `--surface`, `--on-surface`, `--bar`, `--card-pad`, `--card-gap`, `--radius-card`, `--border-pop`, `--shadow-pop`, la base global de `h3` (Title) y la herramienta `PHASE2_BATCH` con su convención de nombres.

- **YAML** (`src/content/landing.es.yaml`, insertado en orden de página justo antes de `agenda`): `team.title`, `team.members[0..3].name` y `.role`; `includes.title`, `includes.items[0..5].title` y `.description`; `how_it_works.title`, `how_it_works.steps[0..3].title`, `.description` y `.timeframe`. Sigue la convención de 02-01 (snake_case, `title` como afirmación, ítems como listas de objetos de afirmaciones). Nueve rutas `pending`: `team.members[0..3].name`, `includes.items[0].title` y `how_it_works.steps[0..3].timeframe`.
- **Esquema** (`src/content.config.ts`): `team` (`title` más `members` de exactamente 4 con `name` y `role`), `includes` (`title` más `items` de exactamente 6 con `title` y `description`) y `how_it_works` (`title` más `steps` de exactamente 4 con `title`, `description` y `timeframe`), todo `z.strictObject` sobre el `claim` existente.
- **Componentes**: `src/components/sections/Team.astro` (`section#nosotros`; clases `.team-grid`, `.team-card`, `.team-avatar`, `.team-role`; constante `AVATARS`), `sections/Includes.astro` (`section#incluye`; `.includes-grid`, `.include-item`, `.include-check`, `.include-desc`) y `sections/HowItWorks.astro` (`section#como-funciona`; `.steps`, `.step`, `.step-body`, `.step-desc`, `.step-time`; disco en `li.step::before` y conector en `li.step::after`). TeamCard, IncludeItem y StepItem son bloques de marcado dentro de cada sección, no archivos aparte.
- **Ids y tonos** (los fija `PAGE_ORDER` de 02-01, sin cambios): `nosotros` yellow, `incluye` light, `como-funciona` dark.
- **Pruebas**: `tests/e2e/team-includes-how.spec.ts` (estructura, columnas por ancho, desborde, espaciado de texto, colores calculados, movimiento y capturas recortadas con `PHASE2_BATCH=C-equipo` y `PHASE2_BATCH=D-incluye-como`) y `tests/guards/copy-team-includes-how.test.mjs` (guarda derivada del YAML).
- **Evidencia**: entradas "Lote C (equipo), ronda N" y "Lote D (incluye y cómo funciona), ronda N" en `02-VISUAL-LOG.md` y `02-05-SUMMARY.md` con resultados de pruebas, erratas del doc y hallazgos para Ari (abajo).

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`, secciones `## 6. Quiénes somos` (líneas 129 a 149), `## 7. Qué incluye` (151 a 158) y `## 8. Cómo funciona` (160 a 178). Los textos se copian carácter por carácter; solo se quitan los marcadores de formato del doc (`###`, `**`, los dos puntos que separan nombre y cargo, el prefijo "Fase N:" y el paréntesis del plazo), que son estructura y no texto de Ari. Todas las cadenas de la tabla se probaron contra `checkCopy`: solo la de AEO dispara una regla de contenido.

| Ranura | Origen en el doc | Caso que aplica | Estado en el YAML |
|--------|------------------|-----------------|-------------------|
| `team.title` | Encabezado "6. Quiénes somos" sin el número | Existe en el doc y pasa las guardas: se muestra tal cual y no se lista | `verified` |
| `team.members[0..3].name` | Encabezados de persona, antes de los dos puntos: Arianna Lupi, Verónica Romero, Juan Angulo, Miguel Pacheco | Existen y pasan; se muestran tal cual y se listan porque el consentimiento del equipo está por confirmar (STATE y UI-SPEC, "se muestran tal cual pero se listan") | `pending`, `reason` de consentimiento |
| `team.members[0..3].role` | Encabezados de persona, después de los dos puntos: Fundadora y estratega SEO, Directora de Proyectos, Director técnico, Especialista SEO | Existen y pasan; se muestran tal cual y no se listan | `verified` |
| Introducción de la sección 6 y las cuatro biografías | Líneas 131 a 149 (bajo "Intro" y bajo cada persona) | Existen en el doc pero no se muestran por diseño: el UI-SPEC aprobado (sección 7) excluye biografía y credencial hasta que Ari confirme credencial y consentimiento. Sin ranura en el YAML. Erratas del doc que quedan sin mostrar y se reportan: "agil", "traves", "direccion", "técnico.Coach SEO" pegado | n/a (ver Supuesto 1) |
| `includes.title` | Encabezado "7. Qué incluye" sin el número | Existe y pasa | `verified` |
| `includes.items[0].title` | Negrita "Auditoría SEO + AEO completa" | Existe pero la guarda rechaza AEO (brief): la afirmación se guarda `pending` con `text` igual a "FALTA CONFIRMAR" y el texto del doc dentro de `reason`; la página muestra FALTA CONFIRMAR | `pending` |
| `includes.items[0].description` | "para que sepas exactamente dónde ganar antes de gastar un peso." | Existe y pasa; se muestra tal cual (inicio en minúscula del doc) | `verified` |
| `includes.items[1].title` y `.description` | "Estrategia a 6-12 meses" y "para que sepas qué haremos, cuándo y cuándo esperar resultados." | Existen y pasan; tal cual (la repetición "cuándo y cuándo" es del doc, se reporta a Ari como observación) | `verified` |
| `includes.items[2].title` y `.description` | "Optimización técnica" y "(Core Web Vitals, velocidad, indexación, schema), para que Google y la IA puedan leerte y confiar en ti." | Existen y pasan; el paréntesis no está en negrita en el doc, por eso queda en la descripción | `verified` |
| `includes.items[3].title` y `.description` | "Contenido estratégico" y "para que rankees en Google y te citen los asistentes de IA con las mismas piezas." | Existen y pasan; tal cual | `verified` |
| `includes.items[4].title` y `.description` | "Link building y autoridad" y "para que te tomen en serio como fuente confiable." | Existen y pasan; tal cual | `verified` |
| `includes.items[5].title` y `.description` | "Reportes mensuales" y "para que veas el retorno en dinero, no en métricas de vanidad." | Existen y pasan; tal cual | `verified` |
| `how_it_works.title` | Encabezado "8. Cómo funciona" sin el número | Existe y pasa | `verified` |
| `how_it_works.steps[0..3].title` | Encabezados de fase sin "Fase N:" ni el paréntesis: Llamada de conocimiento, Auditoría y propuesta, Ejecución, Sistema funcionando | Existen y pasan; tal cual; el número lo pone el contador CSS del disco | `verified` |
| `how_it_works.steps[0..3].description` | Párrafos de las líneas 166, 170, 174 y 178 | Existen y pasan; tal cual | `verified` |
| `how_it_works.steps[0..3].timeframe` | Paréntesis de los encabezados: 30 min, 2 semanas, 6-12 meses, mes 12+ | Existen y pasan; CONTEXT los deja `pending`: se muestran tal cual en el chip y se listan (regla del brief); la fase 1 (30 min) además choca con los 20 min del embudo | `pending`, con `reason` |
| Nota de Ari bajo "8. Cómo funciona" | Línea 162 (cursiva sobre erratas corregidas) | Nota de proceso, no es copy de la página | n/a |
| Relleno de dato faltante | n/a | Solo `includes.items[0].title` lo necesita; ningún plazo falta porque el doc los trae | n/a |

## UI Considerations (lift del UI-SPEC para este plan)

- covered (verdades ya en `must_haves.truths`): empty (la única ranura sin texto utilizable, `includes.items[0].title`, muestra FALTA CONFIRMAR en el propio h3), long-text (nombres, cargos, títulos y chips con `overflow-wrap: anywhere` y chips de hasta dos líneas), overflow (hijos de rejilla con `min-width: 0`, prueba de sin scroll horizontal a cinco anchos), zero-one-many (4, 6 y 4 elementos fijos por esquema con `.length()`), a11y-motion (cero animaciones y cero transiciones), a11y-color (pares aprobados medidos con estilos calculados; borde oscuro de 3 px en tarjeta blanca sobre amarillo).
- backstop (en `must_haves.truths` como `{statement, verification: backstop}`): interaction (listas con `list-style: none` y `role="list"`, Safari con VoiceOver) y visual (uniformidad del set de avatares y chips de dos líneas).
- unresolved que este plan resuelve: fila copy, solo para las ranuras de esta sección (tabla de arriba). Loading y error no aplican: no hay recursos externos en estas secciones.

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **Sin biografías ni introducción en Quiénes somos.** El doc trae una introducción y una biografía por persona, pero el UI-SPEC aprobado por Juan (sección 7 y fila "Quiénes somos" de la síntesis) dice "sin biografía, sin credencial" y STATE deja pendientes la credencial y el consentimiento. CONTEXT y ROADMAP piden solo nombre, cargo y avatar. Revertir: agregar `bio` (y `team.intro`) como afirmaciones `pending` y un `<p>` por tarjeta; las erratas del doc se verían tal cual.
2. **h2 de las tres secciones = encabezado del doc sin el número** ("Quiénes somos", "Qué incluye", "Cómo funciona"): el doc no trae un titular aparte para estas secciones. Revertir: cambiar el `text` del YAML.
3. **Nombres `pending`, cargos `verified`**: cuatro filas nuevas en la lista de Ari por el consentimiento del equipo (STATE). Revertir: pasar los nombres a `verified` y correr `npm run pending`.
4. **Corte de los entregables**: título = negrita del doc; descripción = el resto de la frase sin la coma y el espacio que unen ambas partes; no se capitaliza ni se corrige nada (la descripción empieza en minúscula, como en el doc). En "Optimización técnica" el paréntesis no es negrita en el doc y queda en la descripción.
5. **Plazos mostrados tal cual y `pending`** (el UI-SPEC decía que el chip mostraría FALTA CONFIRMAR, pero fue escrito sin el doc a la vista; la regla de CONTEXT "Textos pending" y del brief manda mostrar el texto del doc y solo listarlo). Revertir: poner `text: FALTA CONFIRMAR` en las cuatro afirmaciones `timeframe` (dejando el plazo del doc en `reason`).
6. **Título de fase sin "Fase N:"**: el número lo muestra el disco (contador CSS) y el UI-SPEC prohíbe una etiqueta "STEP" inventada; no se reescribe texto, se separa la estructura. Sin etiqueta "Plazo" en el chip: el UI-SPEC no la define y el chip va justo después de la descripción; si la revisión con lector de pantalla la pide, se agrega como microcopy `verified`.
7. **Avatares asignados por posición desde `AVATARS`, no desde el YAML** (una cadena suelta dentro de `es` rompe FND-02): Arianna `ojo-morado`, Verónica `ojo-amarillo`, Juan `ojos-morado`, Miguel `ojos-amarillo`. 02-10 reemplazó los personajes con cara y accesorios de 02-02 por el Loopy oficial sobre un disco plano y sugiere esta asignación; es solo de color y de cantidad de ojos, sin caras ni rasgos inventados, y el set no busca parecido. Nota, sin trabajo aquí: si más adelante hay fotos del equipo, podrían reemplazar estos avatares por el tratamiento en media tinta de 02-11 (ranura nueva en una escena y `alt` confirmado por una persona). Revertir: reordenar la constante.
8. **Cargo del equipo en Body 600** (contrato de la sección 7) y no en Title (la tabla de Typography lo lista bajo Title): se elige el contrato de la sección por ser más específico y por dejar el nombre como jerarquía dominante. Ambas opciones usan tamaños ya aprobados.
9. **Conector de fases = línea discontinua CSS de 3 px** (contrato de la sección 9), sin pieza de collage aunque 02-02 anticipaba un `loop` (que 02-10 retiró junto con el sprite antiguo); si la crítica del lote D pide más collage en Cómo funciona (tono `dark`), se agrega un garabato decorativo de `CollagePiece` (`flecha`, `destello` o `asterisco`, en amarillo, que es su color por defecto sobre `dark`) y se vuelve a medir; Loopy (`ojos`, `lupa`) no puede ir directo sobre `dark` (`loopyScheme` lo rechaza) y aquí no se usa. El check de Qué incluye es un SVG en línea con geometría Tabler (el sprite de 02-10 trae ocho símbolos `lg-` y ninguno es un check).
10. **Capturas recortadas a las secciones del lote**, con su propia herramienta en `team-includes-how.spec.ts` (mismos nombres y variantes que la de 02-01, `<lote>-<ancho>[-reduce|-nojs].png`), en lugar de página completa: una captura completa a 1280 px se reduce tanto al leerla que las secciones no se distinguen. La herramienta de `page-structure.spec.ts` no se modifica.
11. **Tarjeta pop del equipo**: si 02-03 o 02-04 ya dejaron una clase o componente compartido de tarjeta (borde de 3 px, sombra dura, `--radius-card`), se reutiliza; si no, los estilos van en `Team.astro` con `--border-pop`, `--shadow-pop`, `--radius-card` y `--card-pad`, con tono anidado `light` como las demás tarjetas blancas.

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Plan |
|--------|------|------|
| GOAL (ROADMAP fase 2, criterios 2 y 3) | Los cuatro integrantes con nombre, cargo y avatar ilustrado; qué incluye (seis entregables) y cómo funciona (cuatro fases con plazos) | Este plan |
| REQ | CONT-07, CONT-08, CONT-09 | Este plan |
| REQ | COPY-01 y DSGN-04 | Este plan para estas secciones; continúan en 02-06 y 02-07 |
| RESEARCH | No hay `02-RESEARCH.md`; el UI-SPEC y `.planning/research/COPY-VERIFICATION.md` son la fuente | Sin ítems pendientes |
| CONTEXT | Equipo con avatar SVG estático `aria-hidden`; Cómo funciona en `<ol>` con plazos `pending`; textos pending tal cual del doc; tonos alternados, contraste y método `impeccable` más `design-taste-frontend`; Title como quinto tamaño | Este plan |
| CONTEXT | Hero, CTA, problema, por qué ahora, solución, resultados y casos | 02-01, 02-03 y 02-04 |
| CONTEXT | Para quién es, FAQ, CTA final, footer y privacidad | 02-06 |
| CONTEXT | Movimiento bajo `motion-safe` | 02-07 (estas secciones no figuran en su presupuesto de movimiento) |
| Deferred Ideas | Ninguna en CONTEXT | n/a |

## Presupuesto de contexto (10 archivos en `files_modified`, 6 con autoría real)

Con autoría real: `Team.astro`, `Includes.astro`, `HowItWorks.astro`, el spec, la guarda y el bloque del YAML. Casi sin autoría: `PENDING-COPY.md` (lo genera `npm run pending`), `index.astro` (tres importaciones y tres etiquetas), `content.config.ts` (tres claves), `02-VISUAL-LOG.md` (registro breve). Estimación de 84k tokens (Tarea 1 unos 22k, Tarea 2 unos 28k, Tarea 3 unos 34k; confianza baja, sin historial de calibración): el peso está en el ciclo visual de la Tarea 3, por eso el ejecutor mira solo las capturas de 390 y 1280 px de cada lote, y 320, 768 y 1024 px se comprueban con la medición del spec (mira una captura solo si la medición falla).

Puntos de corte: cada tarea termina con el build verde y commiteada. Tras la Tarea 1 (tracer) y tras la Tarea 2, si el contexto ya pasó de 50 %, el ejecutor hace commit, escribe `02-05-SUMMARY.md` con `status: partial` (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. La Tarea 3 no se parte a mitad de un ciclo de iteración: si al empezarla el contexto pasa de 50 %, se corta antes (el lote C y el lote D se pueden repartir en dos ejecutores; cada lote termina con su entrada en `02-VISUAL-LOG.md`).

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/` o `public/`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; este plan no lo usa). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; con `PUBLIC_ENV=production` el build falla solo por afirmaciones PENDING y por FALTA CONFIRMAR en `dist`.
- Las pruebas derivan los textos esperados del YAML (lectura con `yaml`, mismo criterio de `walkClaims` y `fill`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- La fase 2 no cambia el comportamiento del iframe de ClickUp; las verificaciones humanas de la fase 1 siguen diferidas.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `landing.es.yaml` a HTML | Texto editado por Ari y el equipo (también desde la web de GitHub) entra a la página; debe salir escapado |
| YAML a la prop `variant` de `Avatar` | Un valor arbitrario podría producir un SVG inválido o marcado inesperado |
| Datos personales del equipo a página pública | Nombre y cargo de cuatro personas se publican sin que el consentimiento esté confirmado |
| SVG en línea (check de Qué incluye) | Marcado escrito en el repositorio, sin datos externos |
| Navegador a terceros | El iframe y el script de ClickUp son las únicas peticiones externas de la página |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | Textos del YAML impresos en `Team`, `Includes` y `HowItWorks` | medium | mitigate | Astro escapa las expresiones de texto; los componentes imprimen `.text` como nodo de texto, sin inyección de HTML en bruto; criterio de aceptación con grep sobre `src` |
| T-02-05-02 | Tampering | Prop `variant` de `Avatar` | low | mitigate | El valor sale de la constante `AVATARS` (unión literal de las cuatro variantes) por posición; ningún valor del YAML llega a la prop; el YAML no admite cadenas sueltas (BARE_STRING) |
| T-02-05-03 | Information Disclosure | Nombre y cargo del equipo publicados sin consentimiento confirmado | medium | mitigate | Los cuatro nombres son `pending` con `reason` de consentimiento, quedan en `PENDING-COPY.md` y el build de producción falla mientras estén pendientes (COPY-02) |
| T-02-05-04 | Repudiation | Copy `pending` que Ari no ve y aprueba a ciegas | low | mitigate | `npm run pending` regenera `PENDING-COPY.md` y `--check` lo compara byte a byte; las nueve rutas nuevas quedan listadas |
| T-02-05-05 | Tampering | Editar el texto del doc para satisfacer una guarda (AEO) o cambiar un plazo | medium | mitigate | La prueba de guarda exige que toda afirmación cuyo `reason` mencione AEO tenga `text` igual a FALTA CONFIRMAR y que las `verified` no disparen reglas de contenido; el texto del doc queda en `reason` |
| T-02-05-06 | Information Disclosure | Nuevas peticiones a terceros (fuentes, analítica, CDN) | medium | mitigate | Avatares y check son SVG en línea; criterio de aceptación: los únicos hosts `https://` de `dist/index.html` son forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG |
| T-02-05-07 | Denial of Service | HTML de las tres secciones pesado (Core Web Vitals) | low | mitigate | El fragmento de `#nosotros`, `#incluye` y `#como-funciona` mide 20 KB como máximo y `dist/index.html` queda por debajo de 60 KB sin comprimir, comprobados en el spec. Presupuesto compartido: 02-10 y 02-11 dejan `dist/index.html` en 42240 bytes como máximo, así que 02-04, 02-05 y 02-06 juntos disponen de unos 19 KB bajo el tope global de 61440 (los cuatro avatares suman 10240 bytes como tope, 2560 cada uno); este plan mide y anota en el SUMMARY cuántos bytes suma y, si el tope global peligra, no lo sube: se detiene y reporta a Juan (opciones de la pregunta abierta 4 de 02-10: subir el tope, recortar píldoras o mover el sprite a un archivo estático) |

Este plan no instala paquetes (UI-SPEC, Registry Safety: sin dependencias nuevas), por lo que no hay fila de cadena de suministro.
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de Quiénes somos de punta a punta (YAML, esquema, Team con avatares, página, prueba y guarda)</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, src/components/sections/Team.astro, src/pages/index.astro, tests/e2e/team-includes-how.spec.ts, tests/guards/copy-team-includes-how.test.mjs, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 7. Quiénes somos (CONT-07)`, la fila "Quiénes somos" de `## Reference Synthesis`, la fila "Avatares" y las Reglas de `## Collage and Brand Assets Contract`, `## Typography` (Reglas), la fila Tarjetas de `## Interaction Contract` y `## Copywriting Contract`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: líneas 129 a 149 (sección 6)
    - src/components/collage/Avatar.astro (props, viewBox y tamaño; tras 02-10 la prop `variant` es ojo-morado, ojo-amarillo, ojos-morado u ojos-amarillo y la raíz es `svg[data-collage="avatar"][data-variant]` de 120x120), src/components/ui/SectionShell.astro (props y slots), src/components/sections/Hero.astro (patrón `getLanding()`), src/pages/index.astro, src/content.config.ts, src/content/landing.es.yaml, src/lib/content.ts
    - DESIGN.md y PRODUCT.md (los generó 02-01; regla de tarjeta blanca sobre amarillo) y .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (formato de las entradas)
    - scripts/lib/copy-rules.mjs (`walkClaims`, `checkCopy`, `MISSING_MARK`) y tests/e2e/a11y-base.spec.ts (líneas 1 a 27 y 216 a 248: lectura del YAML y patrón de desborde)
  </read_first>
  <action>
    Implementa CONT-07 y COPY-01 para Quiénes somos en su versión más fina que atraviesa todas las capas: copy, esquema, componente, página, prueba y guarda. Antes de editar, anota `git rev-parse HEAD` como BASE para los criterios de alcance y comprueba la dependencia con 02-10: `src/components/collage/Avatar.astro` acepta `variant` con `ojo-morado`, `ojo-amarillo`, `ojos-morado` y `ojos-amarillo`; si el archivo vigente usa otros valores (los cuatro personajes de 02-02 ya no existen), manda el archivo y lo anotas en el SUMMARY.

    Skills primero (per DSGN-04, regla de Juan). Antes de escribir el componente, invoca con la herramienta Skill `impeccable` (carga `reference/craft-floor.md`, corre `context` y el verbo `shape` sobre el contrato de la sección 7 y la fila de la síntesis de referencias, apoyado en `PRODUCT.md` y `DESIGN.md` que ya generó 02-01; no repitas `init` ni `document`) y `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4. Agrega a `02-VISUAL-LOG.md` la entrada "Lote C (equipo), ronda 0" con esa lectura, el verbo usado y la decisión de asignación de avatares.

    Pruebas primero (rojo). Crea el spec y la guarda de abajo y confirma que fallan (el spec porque la sección no existe, la guarda porque la clave `team` no existe) antes de agregar el YAML y el componente.

    Copy (per COPY-01 y la regla de copy del brief). En `src/content/landing.es.yaml`, justo antes de la clave `agenda`, agrega `team` con `title` y `members` (cuatro objetos con `name` y `role`) usando las cadenas exactas de la tabla "Ranuras de copy de este plan" (sección 6 del doc, encabezados de persona). `title` y los cuatro `role` llevan `status: verified`; los cuatro `name` llevan `status: pending` y `reason` "Consentimiento del integrante para publicar su nombre y cargo (STATE, Fase 2); Ari confirma". No agregues biografía, introducción, variante de avatar ni ninguna cadena suelta dentro de `es` (una cadena suelta dispara BARE_STRING). No agregues rutas a `PLACEHOLDER_PATHS`: ninguna cadena lleva llaves. En `src/content.config.ts`, suma `team` como `z.strictObject` con `title` como `claim` y `members` como lista de `z.strictObject({ name, role })` de tipo `claim` con `.length(4)`. Corre `npm run pending` para regenerar `PENDING-COPY.md`.

    Componente (per CONT-07). Crea `src/components/sections/Team.astro`. Frontmatter: `const data = await getLanding()` (mismo patrón que `Hero.astro`) y la constante `AVATARS`, tupla literal `'ojo-morado'`, `'ojo-amarillo'`, `'ojos-morado'`, `'ojos-amarillo'` en ese orden (Supuesto 7; una línea de comentario con el porqué). Marcado: `SectionShell` con `id="nosotros"`, `tone="yellow"` y el título de `data.team.title` (pasa el tipo que declare `SectionShell` para `title`: si espera cadena, `.text`); dentro, `<ul class="team-grid" role="list">` con un `<li class="team-card" data-tone="light">` por integrante, que contiene `<div class="team-avatar"><Avatar variant={AVATARS[i]} /></div>`, un `<h3>` con `member.name.text` y un `<p class="team-role">` con `member.role.text`, imprimiendo solo el texto como nodo de texto (sin `fill`, sin leer `status`, sin inyección de HTML en bruto). Estilo local con alcance del componente, solo tokens y `var(--color-brand-*)` (sin hex): lista sin viñetas ni márgenes; rejilla de 1 columna con `gap: var(--card-gap)`, 2 columnas desde 40em y `repeat(4, minmax(0, 1fr))` desde 64em; `.team-card` con `background: var(--surface)`, `color: var(--on-surface)`, `border: var(--border-pop)`, `box-shadow: var(--shadow-pop)`, `border-radius: var(--radius-card)`, `padding: var(--card-pad)`, `text-align: start` y `min-width: 0`, sin hover, sin cursor de puntero, sin transición ni transform; `.team-avatar` de 6rem de ancho con `aspect-ratio: 1` (7.5rem desde 40em) y el SVG a `width: 100%`; el `h3` con margen superior de 1rem y `overflow-wrap: anywhere`; `.team-role` con `font-weight: 600`, margen superior de 0.5rem y `overflow-wrap: anywhere`. Antes de escribir estilos de tarjeta propios, ejecuta `grep -rn "radius-card" src/components src/styles` y, si 02-03 o 02-04 dejaron una clase o componente compartido de tarjeta pop, reutilízalo (Supuesto 11).

    Página (per CONTEXT, "Hero, header y orden de la página"). En `src/pages/index.astro` importa `Team` desde `../components/sections/Team.astro` y renderízalo después de la última sección de contenido que exista (Casos, id `casos`) y antes de lo que siga (la sección de agenda). No cambies otros componentes de la página.

    Prueba (per lección 3 y DSGN-04). Crea `tests/e2e/team-includes-how.spec.ts` con el patrón de lectura del YAML de `a11y-base.spec.ts` (parseo con `yaml`, tipos mínimos para `team`, `includes` y `how_it_works`; todos los textos esperados salen del YAML). Bloque "Quiénes somos" a 1280x800 y a 390x844: `#nosotros` existe con `data-tone="yellow"` y su `aria-labelledby` apunta al único `h2` de la sección, cuyo texto es `team.title.text`; hay cuatro `h3` con los nombres del YAML en orden y cuatro `p.team-role` con los cargos del YAML en orden; ningún otro párrafo, enlace, botón ni `[tabindex]` dentro de la sección; cada tarjeta contiene un `svg[data-collage="avatar"]` con `aria-hidden="true"`, `focusable="false"` y sin `title`, con cuatro `data-variant` distintos cuyo conjunto es exactamente `ojo-morado`, `ojo-amarillo`, `ojos-morado` y `ojos-amarillo`, el mismo ancho medido, sin `[data-pill]` ni elementos SVG `text` dentro y con `outerHTML` de 2560 bytes o menos (medido con `TextEncoder`); el `ul` lleva `role="list"` y tiene cuatro `li`; la página sigue con un solo `h1` y ningún `h3` de la sección queda sin `h2` antes; `scrollWidth` del documento menor o igual a `clientWidth`.

    Guarda (per lección 3). Crea `tests/guards/copy-team-includes-how.test.mjs` con `node:test`: lee el YAML real con `yaml`, aplica `walkClaims` y `checkCopy` de `scripts/lib/copy-rules.mjs` y define la constante `SECTIONS` (hoy solo `team`; la Tarea 2 la extiende) y las cantidades del contrato (`team.members` con 4). Pruebas: (a) ninguna afirmación de esas secciones es una cadena suelta (`kind: 'bare'`); (b) toda afirmación `verified` queda fuera de las violaciones de contenido de `checkCopy`; (c) toda afirmación `pending` tiene `reason` no vacío; (d) toda afirmación cuyo `reason` mencione AEO tiene `text` igual a `MISSING_MARK`; (e) las cantidades del contrato. Ninguna lista de rutas pending escrita a mano.

    Ejecuta el ciclo de las reglas de ejecución: build, preview en el puerto 4322, specs y `npx astro preview stop`.
  </action>
  <verify>
    <automated>npm run build && node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0 (el archivo versionado difiere del que se generaría)</fails_when>
    <automated>node --input-type=module -e "import {parse} from 'yaml'; import {readFileSync} from 'node:fs'; const es=parse(readFileSync('src/content/landing.es.yaml','utf8')).es; const html=readFileSync('dist/index.html','utf8'); const list=readFileSync('PENDING-COPY.md','utf8'); const texts=[es.team.title.text, ...es.team.members.flatMap(m=>[m.name.text,m.role.text])]; const okHtml=texts.every(t=>html.includes(t)); const okList=es.team.members.every((m,i)=>list.includes('team.members['+i+'].name')); process.exit(okHtml&&okList&&es.team.members.length===4?0:1)"</automated>
    <fails_when>non-zero exit: falta en `dist/index.html` el h2, un nombre o un cargo tomado del YAML, falta un nombre del equipo en `PENDING-COPY.md` o `team.members` no tiene 4 elementos</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const p=r.content.filter(v=>v.rule==='PENDING').map(v=>v.path);const ok=r.structural.length===0&&r.content.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&[0,1,2,3].every(i=>p.includes('team.members['+i+'].name'));process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING y MISSING (voseo, guion largo, AEO o VERIFICAR) o algún nombre del equipo no figura como PENDING</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/team-includes-how.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts</automated>
    <fails_when>non-zero exit de Playwright o alguna prueba de `team-includes-how.spec.ts`, de `page-structure.spec.ts` o de la fase 1 falla; si `astro preview` no levanta porque el puerto 4322 está ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>La página muestra Quiénes somos en tono amarillo con las cuatro tarjetas (avatar Loopy distinto, nombre y cargo del doc de Ari) entre Casos y la agenda; los cuatro nombres quedan pending y listados; la guarda de copy de las secciones existe y pasa; el spec de estructura pasa junto con las pruebas de la fase 1 y de 02-01.</done>
  <acceptance_criteria>
    - `node --test tests/guards/copy-team-includes-how.test.mjs` sale 0 y salió distinto de 0 antes de agregar `team` al YAML
    - `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)` no trae violaciones estructurales ni reglas distintas de PENDING y MISSING, y lista `team.members[0..3].name` como PENDING (los cargos y el h2 no aparecen)
    - `grep -c "team.members\[" PENDING-COPY.md` devuelve 4 y `node scripts/list-pending.mjs --check` sale 0
    - `grep -o 'id="nosotros"' dist/index.html | wc -l` devuelve 1 y `grep -o 'data-collage="avatar"' dist/index.html | wc -l` devuelve 4 (los cuatro avatares están en el HTML sin JavaScript)
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components/sections | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0
    - `git diff --name-only BASE -- src/styles src/components/ui src/components/collage src/components/AgendaSection.astro src/components/SiteHeader.astro src/layouts src/components/SkipLinks.astro src/scripts public tests/e2e/page-structure.spec.ts` no lista nada
    - `team-includes-how.spec.ts` pasa con ClickUp bloqueado (`npm run test:e2e:isolated`): un h2, cuatro h3 con los nombres del YAML y cuatro cargos en orden, cuatro avatares `aria-hidden` con variantes distintas y del mismo ancho, ningún enlace, botón ni tabindex, sin scroll horizontal a 390 y 1280 px; `page-structure.spec.ts` y `a11y-base.spec.ts` siguen pasando sin haber sido editados
    - `02-VISUAL-LOG.md` trae la entrada "Lote C (equipo), ronda 0"
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qué incluye y Cómo funciona (copy, esquema, componentes, layouts por punto de quiebre y pruebas)</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, PENDING-COPY.md, src/components/sections/Includes.astro, src/components/sections/HowItWorks.astro, src/pages/index.astro, tests/e2e/team-includes-how.spec.ts, tests/guards/copy-team-includes-how.test.mjs</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### 8. Qué incluye (CONT-08)`, `### 9. Cómo funciona (CONT-09)`, las filas "Qué incluye" y "Cómo funciona" de `## Reference Synthesis`, `## Spacing Scale` (excepciones: tamaños de ilustración), `## Typography` (roles Label y Title, Reglas), `## Responsive` y la fila "Lista de fases" de `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: líneas 151 a 178 (secciones 7 y 8)
    - src/components/sections/Team.astro (patrón de la Tarea 1), src/components/ui/SectionShell.astro, src/styles/tokens.css (tono `dark`: `--bar`, `--on-surface`; `--color-brand-*`, `--text-title`, `--text-label`, `--radius-pill`) y src/styles/global.css (base de `h3`, `.section-title`)
    - scripts/lib/copy-rules.mjs, tests/guards/copy-team-includes-how.test.mjs y tests/e2e/team-includes-how.spec.ts (creados en la Tarea 1)
  </read_first>
  <behavior>
    - Qué incluye: seis `li` en el orden del YAML, cada uno con `h3` y `p.include-desc` del YAML; el `h3` del primero dice FALTA CONFIRMAR y el texto del doc con AEO no aparece en `dist`
    - Cómo funciona: `ol` con cuatro `li`, disco numerado del 1 al 4 por contador CSS, `h3`, descripción y chip de plazo con el texto del YAML
    - Columnas medidas: incluye 1 (320 y 390), 2 (768) y 3 (1024 y 1280); fases en columna hasta 1023 px y en cuatro columnas con el mismo `top` desde 1024 px; equipo sigue en 1, 2 y 4
    - Conector: discontinuo de 3 px naranja, vertical bajo 1024 px y horizontal desde 1024 px, ausente tras la última fase
    - La guarda cubre las tres secciones: cantidades 4, 6 y 4, `verified` sin violaciones, `pending` con `reason`, cualquier afirmación con AEO en `reason` tiene `text` igual a FALTA CONFIRMAR
  </behavior>
  <action>
    Implementa CONT-08, CONT-09 y COPY-01 para Qué incluye y Cómo funciona con el primer borrador completo de su contrato (protocolo del UI-SPEC, paso 1: construir el lote completo desde el contrato); la iteración visual es de la Tarea 3. Skills primero (per DSGN-04): invoca con la herramienta Skill `impeccable` (verbos `shape` y `layout` sobre las secciones 8 y 9 y sus filas de la síntesis de referencias) y `design-taste-frontend` (misma lectura declarada y diales 7, 3 y 4) antes de escribir los componentes.

    Pruebas primero (rojo). En la guarda, `SECTIONS` suma `includes` y `how_it_works` y las cantidades del contrato suman `includes.items` con 6 y `how_it_works.steps` con 4. En el spec agrega los dos bloques de abajo. Confirma que fallan antes de agregar YAML y componentes.

    Copy (per COPY-01 y la regla de copy del brief). En `landing.es.yaml`, entre `team` y `agenda`, agrega `includes` (`title` y `items` con seis objetos `title` y `description`) y `how_it_works` (`title` y `steps` con cuatro objetos `title`, `description` y `timeframe`) con las cadenas exactas de la tabla "Ranuras de copy de este plan". Estados: `includes.items[0].title` es `pending` con `text` exactamente "FALTA CONFIRMAR" y `reason` "Texto del Copy v2: 'Auditoría SEO + AEO completa'. La guarda rechaza AEO; Ari decide el término (SEO/GEO o AEO) y entrega el texto final" (el texto del doc con AEO va solo en `reason`, que la guarda no escanea, nunca en `text`); los otros cinco títulos, las seis descripciones, los títulos de sección y los títulos y descripciones de fase son `verified`; los cuatro `timeframe` son `pending` con `reason` (fase 1: "Plazo del Copy v2 (30 min); el embudo y el formulario hablan de 20 min (hallazgo 5 del UI-SPEC); Ari alinea con call.duration"; fases 2 y 4: "Plazo del Copy v2; los plazos de las cuatro fases quedan pending hasta que Ari confirme (CONTEXT)"; fase 3: "Plazo del Copy v2 (coincide con Estrategia a 6-12 meses de Qué incluye); Ari confirma (CONTEXT)"). Ninguna cadena lleva llaves. En `src/content.config.ts`, suma `includes` (`title` y `items` de `{ title, description }` con `.length(6)`) y `how_it_works` (`title` y `steps` de `{ title, description, timeframe }` con `.length(4)`). Corre `npm run pending`.

    Includes (per CONT-08). Crea `src/components/sections/Includes.astro` con el patrón de frontmatter de `Team.astro`: `SectionShell` con `id="incluye"`, `tone="light"` y el título de `data.includes.title`; dentro, `<ul class="includes-grid" role="list">` con un `<li class="include-item">` por entregable que contiene `<span class="include-check" aria-hidden="true">` con un `<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">` de un solo `<path d="M5 12l5 5l10 -10">` (geometría Tabler `check`, sin relleno, trazo `currentColor` de 3, extremos y uniones redondeados), un `<h3>` con `item.title.text` y un `<p class="include-desc">` con `item.description.text`; solo nodos de texto. Estilo local, solo tokens: lista sin viñetas; rejilla de 1 columna, 2 desde 40em y `repeat(3, minmax(0, 1fr))` desde 64em, con `column-gap: var(--card-gap)` y `row-gap: 2rem`; `.include-item` sin fondo, sin sombra y sin radio, con `border-top: 3px solid var(--on-surface)`, `padding-top: 1rem`, `min-width: 0` y una rejilla interna de dos columnas (`2rem` y `minmax(0, 1fr)`, `column-gap: 0.75rem`, `row-gap: 0.5rem`): el disco en la primera columna junto al `h3` y la descripción en la segunda columna; `.include-check` de 2rem de ancho con `aspect-ratio: 1`, `border-radius: 50%`, `background: var(--color-brand-yellow)`, `border: 3px solid var(--on-surface)`, `color: var(--on-surface)`, centrado, y su SVG de 1rem de ancho con `aspect-ratio: 1`; `h3` y `.include-desc` con `overflow-wrap: anywhere`. Sin hover, sin cursor de puntero, sin transición, sin CTA.

    Cómo funciona (per CONT-09). Crea `src/components/sections/HowItWorks.astro`: `SectionShell` con `id="como-funciona"`, `tone="dark"` y el título de `data.how_it_works.title`; dentro, `<ol class="steps" role="list">` con `counter-reset: step` y un `<li class="step">` por fase que contiene `<div class="step-body">` con `<h3>` (`step.title.text`), `<p class="step-desc">` (`step.description.text`) y `<p class="step-time">` (`step.timeframe.text`), solo nodos de texto, sin `fill` y sin leer `status`. Estilo local, solo tokens y `var(--color-brand-*)`: lista sin viñetas ni márgenes y rejilla de una columna con `gap: 2rem`; cada `li` con `position: relative`, `counter-increment: step`, `min-width: 0` y una rejilla de dos columnas (`3rem` y `minmax(0, 1fr)`, `column-gap: 1rem`); el disco es `li::before` con `content: counter(step)`, 3rem de ancho, `aspect-ratio: 1`, `border-radius: 50%`, `background: var(--color-brand-yellow)`, `color: var(--color-brand-dark)`, `font-weight: 700`, tamaño `var(--text-title)`, centrado, `position: relative` y `z-index: 1`; el conector vertical es `li:not(:last-child)::after`, decorativo, con `content: ""`, `position: absolute`, `top: 3rem`, `bottom: -2rem`, `left: calc(1.5rem - 1.5px)`, `border-left: 3px dashed var(--bar)` y sin alto declarado; desde 64em la lista pasa a `repeat(4, minmax(0, 1fr))` con `gap: var(--card-gap)`, cada `li` a una sola columna con el disco arriba (`row-gap: 1rem`) y el conector pasa a horizontal (`top: calc(1.5rem - 1.5px)`, `bottom: auto`, `left: 1.5rem`, `width: calc(100% + var(--card-gap))`, `border-left: 0`, `border-top: 3px dashed var(--bar)`) para cruzar por detrás de los discos; `h3` y `.step-desc` con `overflow-wrap: anywhere`; `.step-time` con `display: inline-block`, `max-width: 100%`, `min-height: 2rem`, `margin-top: 1rem`, `padding: 0.25rem 0.75rem`, `border: 3px solid var(--color-brand-yellow)`, `border-radius: var(--radius-pill)`, `color: var(--color-brand-yellow)`, `font-size: var(--text-label)`, peso 600 y `overflow-wrap: anywhere`. Sin hover, sin transición, sin CTA.

    Página (per CONTEXT, orden de la página). En `src/pages/index.astro` importa `Includes` y `HowItWorks` y renderízalos justo después de `Team`, en ese orden.

    Pruebas del spec (per lección 3, todos los textos salen del YAML). Bloque "Qué incluye": `#incluye` con `data-tone="light"` y `aria-labelledby` que apunta al único `h2` con `includes.title.text`; `ul[role="list"]` con seis `li.include-item`, cada uno con `h3` y `p.include-desc` iguales a los del YAML y en orden; un `svg[aria-hidden="true"][focusable="false"]` dentro de cada `.include-check`; cero `a`, `button` y `[tabindex]` en la sección; estilos calculados de `.include-item`: `border-top-width` 3px, `border-top-style` solid, `border-top-color` `rgb(33, 33, 33)`, `box-shadow` none y `cursor` distinto de pointer; `.include-check` de 32x32 px (tolerancia de 0.5) con `background-color` `rgb(255, 198, 2)`; columnas distintas por `left` de los `li`: 1 a 320 y 390 px, 2 a 768 px y 3 a 1024 y 1280 px. Bloque "Cómo funciona": `#como-funciona` con `data-tone="dark"`, `h2` con `how_it_works.title.text`, `ol[role="list"]` con cuatro `li.step`, cada uno con `h3`, `p.step-desc` y `p.step-time` iguales a los del YAML y en orden; `getComputedStyle(li, '::before')` con `width` y `height` de `48px`, `backgroundColor` `rgb(255, 198, 2)`, `color` `rgb(33, 33, 33)` y `content` que coincide con `counter(` o con un dígito entre comillas; `.step-time` con `color` `rgb(255, 198, 2)`, `border-top-width` 3px, `max-width` `100%`, `overflow-wrap` `anywhere` y `min-height` de 32 px o más; conector `getComputedStyle(li, '::after')` del primer `li` con `border-left-style` dashed, ancho 3px y color `rgb(253, 105, 56)` a 390 px, con `border-top-style` dashed, ancho 3px y el mismo color a 1280 px, y `content` `none` en el último `li`; los cuatro `li` comparten `left` (tolerancia de 1 px) y sus `top` crecen a 390 y 768 px, y comparten `top` con `left` creciente a 1024 y 1280 px; ningún `li` con `right` mayor que `innerWidth`; cero `a`, `button` y `[tabindex]`.

    Ejecuta el ciclo de las reglas de ejecución: build, preview en el puerto 4322, specs y `npx astro preview stop`.
  </action>
  <verify>
    <automated>npm run build && node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check</automated>
    <fails_when>non-zero exit del build (incluidos `prebuild` y `postbuild`), o el resumen de `node --test` muestra "# fail" mayor que 0, o `--check` sale distinto de 0 (el archivo versionado difiere del que se generaría)</fails_when>
    <automated>node --input-type=module -e "import {parse} from 'yaml'; import {readFileSync} from 'node:fs'; const es=parse(readFileSync('src/content/landing.es.yaml','utf8')).es; const html=readFileSync('dist/index.html','utf8'); const doc=es.includes.items[0].title.reason.match(/'([^']+)'/)[1]; const texts=[es.includes.title.text, ...es.includes.items.flatMap((it,i)=>[...(i===0?[]:[it.title.text]), it.description.text]), es.how_it_works.title.text, ...es.how_it_works.steps.flatMap(s=>[s.title.text,s.description.text,s.timeframe.text])]; const ok=texts.every(t=>html.includes(t))&&es.includes.items[0].title.text==='FALTA CONFIRMAR'&&!html.includes(doc)&&es.includes.items.length===6&&es.how_it_works.steps.length===4; process.exit(ok?0:1)"</automated>
    <fails_when>non-zero exit: falta en `dist/index.html` un texto tomado del YAML, el título del primer entregable no es FALTA CONFIRMAR, el texto del doc con AEO aparece en la página o las cantidades no son 6 y 4</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node --input-type=module -e "import {parse} from 'yaml'; import {readFileSync} from 'node:fs'; import {walkClaims} from './scripts/lib/copy-rules.mjs'; let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const mine=/^(team|includes|how_it_works)\./;const doc=parse(readFileSync('src/content/landing.es.yaml','utf8'));const want=walkClaims(doc).filter(n=>mine.test(n.path)&&n.claim?.status==='pending').map(n=>n.path).sort();const got=r.content.filter(v=>v.rule==='PENDING'&&mine.test(v.path)).map(v=>v.path).sort();const ok=r.structural.length===0&&r.content.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&want.length===9&&JSON.stringify(want)===JSON.stringify(got);process.exit(ok?0:1)})"</automated>
    <fails_when>non-zero exit: hay una violación estructural, una regla distinta de PENDING y MISSING (voseo, guion largo, AEO o VERIFICAR), las rutas pending de las tres secciones no son exactamente las del YAML o no son nueve</fails_when>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated -- tests/e2e/team-includes-how.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts</automated>
    <fails_when>non-zero exit de Playwright o alguna prueba de `team-includes-how.spec.ts`, de `page-structure.spec.ts` (orden y tono de secciones, un solo h1, jerarquía de encabezados, desborde) o de la fase 1 falla; si el puerto 4322 está ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
  </verify>
  <done>Qué incluye (seis entregables abiertos con disco de check) y Cómo funciona (lista ordenada de cuatro fases con disco numerado, chip de plazo y conector discontinuo) están en la página después de Quiénes somos, con el texto del doc de Ari; el título con AEO muestra FALTA CONFIRMAR; las nueve afirmaciones pending quedan listadas; las columnas por ancho, la semántica de lista y los estilos calculados pasan en el spec.</done>
  <acceptance_criteria>
    - `node --test tests/guards/copy-team-includes-how.test.mjs` sale 0 con las tres secciones y las cantidades 4, 6 y 4, y salió distinto de 0 antes de agregar el YAML
    - `grep -cE "^\| (team|includes|how_it_works)\." PENDING-COPY.md` devuelve 9, `grep -c "^| how_it_works.steps\[[0-3]\].timeframe" PENDING-COPY.md` devuelve 4, `grep -c "^| includes.items\[0\].title" PENDING-COPY.md` devuelve 1 y `node scripts/list-pending.mjs --check` sale 0
    - `grep -o 'id="incluye"' dist/index.html | wc -l` y `grep -o 'id="como-funciona"' dist/index.html | wc -l` devuelven 1 cada uno, y el orden en `src/pages/index.astro` es Team, Includes, HowItWorks
    - `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components/sections | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0; `grep -rn "line-height[^;]*!important" src | wc -l` devuelve 0
    - `grep -nE "(^|[^-[:alnum:]])height:" src/components/sections/Team.astro src/components/sections/Includes.astro src/components/sections/HowItWorks.astro | wc -l` devuelve 0 (sin alturas fijas: los discos, el SVG y el avatar usan `width` y `aspect-ratio`)
    - `git diff --name-only BASE -- src/styles src/components/ui src/components/collage src/components/AgendaSection.astro src/components/SiteHeader.astro src/layouts src/components/SkipLinks.astro src/scripts public tests/e2e/page-structure.spec.ts` no lista nada
    - `team-includes-how.spec.ts` pasa con ClickUp bloqueado: seis `li` con h3 y descripción del YAML en orden, 1, 2 y 3 columnas a 390, 768 y 1024 px; cuatro fases con disco de 48 px amarillo con número oscuro, chip amarillo con borde de 3 px, conector discontinuo naranja de 3 px (vertical a 390 y 768 px, horizontal a 1024 y 1280 px, ausente tras la última) y `role="list"` en el `ol` y en los `ul`; sin enlaces, botones ni `tabindex` en las tres secciones
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Iteración visual de los lotes C (equipo) y D (incluye y cómo funciona), medición a cinco anchos y cierre</name>
  <files>src/components/sections/Team.astro, src/components/sections/Includes.astro, src/components/sections/HowItWorks.astro, tests/e2e/team-includes-how.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (regla de Juan sobre diseño y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol` (verbos por lote, ciclo por lote y lista de "vibra"), las filas "Quiénes somos", "Qué incluye" y "Cómo funciona" de `## Reference Synthesis`, `## Verification Hooks`, `## Color` (Pares aprobados) y la fila Tarjetas de `## Interaction Contract`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (formato de las entradas de 02-01 y la entrada "Lote C (equipo), ronda 0")
    - src/components/sections/Team.astro, Includes.astro y HowItWorks.astro, y tests/e2e/team-includes-how.spec.ts (estado tras la Tarea 2)
    - scripts/lib/contrast.mjs (`contrastRatio`) y tests/e2e/a11y-base.spec.ts (líneas 216 a 248: patrón de desborde y espaciado de texto)
    - tests/e2e/page-structure.spec.ts: solo el bloque `PHASE2_BATCH` (patrón de contexto nuevo con todo lo que no es localhost abortado; solo lectura, no se modifica)
  </read_first>
  <action>
    Cierra el ciclo visual del UI-SPEC para los lotes C (equipo) y D (incluye y cómo funciona) y agrega las mediciones que protegen el resultado (per DSGN-04, CONT-07, CONT-08 y CONT-09). Skills: invoca con la herramienta Skill `impeccable` (lote C: `shape`, `layout`, `colorize`, `typeset` y `adapt`; lote D: `shape`, `layout`, `clarify` y `adapt`; `critique` en ambos) y `design-taste-frontend` (diales 7, 3 y 4) en cada ronda; antes de escribir CSS nuevo vuelve a cargar `reference/craft-floor.md`.

    Mediciones primero. Extiende `team-includes-how.spec.ts` con: (1) desborde a 320, 390, 768, 1024 y 1280 px: `scrollWidth` del documento menor o igual a `clientWidth`, el rectángulo de cada una de las tres secciones dentro de `[0, innerWidth]` y ningún descendiente de ellas con `right` mayor que `innerWidth`; (2) espaciado de texto de SC 1.4.12 a 320 y 1280 px con el patrón de `a11y-base.spec.ts` (interlineado 1.5, letras 0.12em, palabras 0.16em, párrafos 2em): sin scroll horizontal y con `scrollHeight - clientHeight` menor o igual a 1 en cada `h3`, `.team-role`, `.include-desc`, `.step-desc` y `.step-time`; (3) contraste medido con estilos calculados y `contrastRatio` de `scripts/lib/contrast.mjs`, subiendo por los ancestros hasta el primer `background-color` no transparente: 4.5 o más para h3, cargo, descripciones y chip (texto) y 3 o más para el borde del chip y el del disco de check contra su fondo; (4) tarjetas del equipo: `border` de 3 px sólido oscuro, `box-shadow` duro (desenfoque 0), `cursor` distinto de pointer, sin `transform` tras `hover()`, mismo radio en las cuatro, y avatares del mismo ancho medido (96 px a 390 px y 120 px a 768 y 1280 px); (5) movimiento: con `emulateMedia` en `reduce` y en `no-preference`, `document.getAnimations().length` es 0 y ningún elemento de las tres secciones tiene `transition-duration` distinto de `0s`; (6) peso: el `outerHTML` conjunto de `#nosotros`, `#incluye` y `#como-funciona` mide 20480 caracteres o menos y el HTML de `/` (`page.request.get('/')`) menos de 61440 bytes; (7) con `javaScriptEnabled: false`, las tres secciones y sus textos del YAML son visibles; (8) herramienta de capturas (Supuesto 10): un bloque que solo corre cuando `PHASE2_BATCH` está definida (`test.skip` si no), con un mapa de lotes (`C-equipo` recorta `#nosotros`; `D-incluye-como` recorta desde el borde superior de `#incluye` hasta el inferior de `#como-funciona`) que, por cada ancho y variante (base, `reduce`, `nojs`), abre un contexto nuevo con `browser.newContext`, aborta con `context.route` todo lo que no sea localhost y guarda `test-results/phase2/<lote>-<ancho>[-variante].png` con `page.screenshot({ fullPage: true, clip })`. Las mediciones (1) a (7) deben pasar; si alguna falla, se corrige el componente, no la prueba.

    Ciclo del lote C (equipo), per protocolo del UI-SPEC. (1) El lote ya está construido (Tarea 1). (2) Una ronda de captura en lote: con el preview arriba, `PHASE2_BATCH=C-equipo npm run test:e2e:isolated -- tests/e2e/team-includes-how.spec.ts -g "captura"`; mira solo las capturas de 390 y 1280 px en base y comprueba 320, 768 y 1024 px con las mediciones, mirando una captura solo si una medición falla. (3) `impeccable critique` contra el contrato de la sección 7, la fila "Quiénes somos" de la síntesis (nombre y cargo, sin biografía; su frase "avatares de trazo sobre círculos suaves" la reemplaza la fe de erratas de 02-10: el Loopy oficial sobre un disco plano con sombra dura, sin caras) y los rasgos 3, 5 y 7 de la lista de vibra (tarjetas con borde de 3 px, sombra dura y radio parejo sin sombra difusa; los cuatro avatares como un set uniforme; la sección con un foco claro y un elemento de collage o un layout propio). (4) Corrige todo en un solo lote de cambios, solo en `Team.astro` (los avatares son de 02-10 y no se editan: si el set no se lee uniforme, se anota como hallazgo para 02-08 y para Ari), con una ronda de confirmación como máximo; si un criterio sigue fallando, repite el ciclo con tope de tres ciclos por lote y, si no se cumple, detente y reporta a Juan con capturas. (5) Registra en `02-VISUAL-LOG.md` la entrada "Lote C (equipo), ronda N" con verbos usados, hallazgos, correcciones, rutas de las capturas y la revisión de uniformidad del set de avatares (comprobación de respaldo).

    Ciclo del lote D (incluye y cómo funciona), con los mismos pasos, `PHASE2_BATCH=D-incluye-como`, las filas "Qué incluye" y "Cómo funciona" de la síntesis y el contrato de las secciones 8 y 9. La crítica revisa además que Qué incluye respire frente a los bloques con tarjeta vecinos (ítems abiertos, sin tarjeta), que el chip de plazo de dos líneas no desborde a 320 px (medición) y que el conector discontinuo no cruce texto. La entrada del log se llama "Lote D (incluye y cómo funciona), ronda N".

    Cierre. Corre la suite completa con ClickUp bloqueado, las guardas, `npm run pending` con `--check` y `PUBLIC_ENV=production npm run build`, que debe fallar solo por afirmaciones PENDING y FALTA CONFIRMAR. Escribe `02-05-SUMMARY.md` con: resultado de las pruebas; erratas del doc detectadas ("agil", "traves", "direccion", "técnico.Coach SEO" pegado y "Coach SEO en aprendoclub" sin punto final, presentes en el doc y no mostradas por el Supuesto 1; "cuándo y cuándo" en "Estrategia a 6-12 meses", mostrada tal cual); hallazgos para Ari (el término AEO en "Auditoría SEO + AEO completa", el consentimiento de los cuatro integrantes, los plazos de las cuatro fases y el choque de "30 min" con los 20 min del embudo, si se muestran biografías e introducción, y las afirmaciones de resultado de las fases 2 y 4 para que las revise); y las desviaciones de este plan. Al terminar: `npx astro preview stop`.
  </action>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322 && npm run test:e2e:isolated</automated>
    <fails_when>non-zero exit de Playwright: falla cualquier medición de `team-includes-how.spec.ts` (desborde, espaciado de texto, contraste, tarjetas, movimiento, peso, sin JavaScript) o cualquier spec de la fase 1 o de 02-01; si el puerto 4322 está ocupado por un preview anterior, ejecutar `npx astro preview stop` y reintentar sin culpar al código</fails_when>
    <automated>PHASE2_BATCH=C-equipo npm run test:e2e:isolated -- tests/e2e/team-includes-how.spec.ts -g "captura" && PHASE2_BATCH=D-incluye-como npm run test:e2e:isolated -- tests/e2e/team-includes-how.spec.ts -g "captura" && test "$(ls test-results/phase2/C-equipo-*.png | wc -l)" -eq 15 && test "$(ls test-results/phase2/D-incluye-como-*.png | wc -l)" -eq 15</automated>
    <fails_when>non-zero exit: falta alguna de las 15 capturas de un lote (cinco anchos por base, `reduce` y `nojs`) o el preview de la verificación anterior ya no responde (en ese caso, `npx astro build && npx astro preview --port 4322` y reintentar)</fails_when>
    <automated>node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && npm run pending && node scripts/list-pending.mjs --check && npm run build && ((PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);process.exit(r.structural.length===0&&r.content.every(v=>v.rule==='PENDING'||v.rule==='MISSING')?0:1)})")</automated>
    <fails_when>non-zero exit: falla una guarda, el contraste, la lista de pendientes o el build, o la comprobación de producción encuentra una violación estructural o una regla distinta de PENDING y MISSING (voseo, guion largo, AEO o VERIFICAR)</fails_when>
  </verify>
  <done>Los lotes C y D cerraron con sus rondas registradas y con los rasgos 3, 5 y 7 de la vibra cumplidos en las tres secciones; las mediciones de desborde, espaciado de texto, contraste, tarjetas, movimiento, peso y contenido sin JavaScript pasan a cinco anchos; el SUMMARY deja a Ari la lista de decisiones y las erratas del doc.</done>
  <acceptance_criteria>
    - `team-includes-how.spec.ts` pasa completo con ClickUp bloqueado: sin scroll horizontal a 320, 390, 768, 1024 y 1280 px, sin recorte con el espaciado de texto a 320 y 1280 px, contraste de texto de 4.5 o más y de bordes de 3 o más con `contrastRatio`, tarjetas con borde de 3 px y sombra sin desenfoque, cero animaciones y cero transiciones con `reduce` y con `no-preference`, fragmento de las tres secciones de 20480 caracteres o menos, `/` menor a 61440 bytes y las tres secciones visibles sin JavaScript
    - `ls test-results/phase2/C-equipo-*.png | wc -l` devuelve 15 y `ls test-results/phase2/D-incluye-como-*.png | wc -l` devuelve 15
    - `grep -c "Lote C (equipo), ronda" .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md` devuelve 2 o más (ronda 0 más al menos una ronda de captura) y `grep -c "Lote D (incluye y cómo funciona), ronda" .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md` devuelve 1 o más; cada entrada nombra los verbos usados, los hallazgos y correcciones, las rutas de capturas y si los rasgos 3, 5 y 7 de la vibra se cumplen
    - `grep -nE "(^|[^-[:alnum:]])height:" src/components/sections/Team.astro src/components/sections/Includes.astro src/components/sections/HowItWorks.astro | wc -l` devuelve 0; `grep -rn "set:html" src | wc -l` devuelve 0; `grep -rEn "#[0-9a-fA-F]{6}\b" src/components/sections | wc -l` devuelve 0; `grep -rn "outline: none" src | wc -l` devuelve 0
    - `git diff --name-only BASE` lista solo archivos de `files_modified` y `02-05-SUMMARY.md` (ningún token, `SectionShell`, avatar, spec de otro plan ni archivo de 02-03 y 02-04)
    - Los únicos hosts `https://` de `dist/index.html` son forms.clickup.com, app-cdn.clickup.com y el espacio de nombres de SVG
    - `02-05-SUMMARY.md` existe con las secciones "Erratas del doc", "Hallazgos para Ari" y "Desviaciones", y no queda ningún servidor dev ni preview en marcha (`npx astro preview stop` ejecutado)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verificación de conjunto al terminar las tres tareas:

- CONT-07: `#nosotros` (tono yellow) muestra cuatro tarjetas con avatar Loopy distinto, nombre y cargo del doc, sin biografía ni enlaces; los cuatro nombres quedan pending y listados.
- CONT-08: `#incluye` (tono light) muestra seis entregables abiertos con disco de check; el primer título muestra FALTA CONFIRMAR y el texto del doc con AEO no está en `dist`.
- CONT-09: `#como-funciona` (tono dark) es un `<ol role="list">` de cuatro fases con disco numerado, descripción y chip de plazo del doc; los cuatro plazos quedan pending y listados.
- COPY-01: la guarda `copy-team-includes-how` y la comprobación de producción confirman que solo hay reglas PENDING y MISSING, que ninguna cadena del doc se editó y que las erratas del doc figuran en el SUMMARY.
- DSGN-04: `02-VISUAL-LOG.md` trae las rondas de los lotes C y D con los verbos de `impeccable`, la lectura de `design-taste-frontend` y las capturas a cinco anchos.
- Regresión: `npm run test:e2e:isolated` completo, `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build` y `node scripts/list-pending.mjs --check` en verde; `PUBLIC_ENV=production npm run build` falla solo por PENDING y FALTA CONFIRMAR.
</verification>

<success_criteria>
- Un visitante ve quién es el equipo, qué incluye el servicio y cómo se trabaja, con el texto del doc de Ari, entre Casos de éxito y el resto de la página, sin scroll horizontal de 320 a 1280 px y sin movimiento.
- Las tres secciones leen bien en tono amarillo, claro y oscuro con contraste medido, lista semántica, cero paradas de teclado nuevas y contenido completo sin JavaScript.
- Las nueve afirmaciones pending están listadas para Ari y el build de producción sigue bloqueado hasta que las confirme.
- Los cuatro avatares forman un set uniforme, cada sección tiene un foco claro y su propio layout, y todo pasó por `impeccable` y `design-taste-frontend` con registro en `02-VISUAL-LOG.md`.
</success_criteria>

<output>
Create `.planning/phases/02-secciones-marca-y-copy/02-05-SUMMARY.md` when done
</output>
