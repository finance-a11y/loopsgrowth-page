---
phase: 02-secciones-marca-y-copy
plan: 08
type: execute
wave: 10
depends_on: [02-07]
files_modified:
  - tests/e2e/phase-closing.spec.ts
  - package.json
  - package-lock.json
  - PENDING-COPY.md
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
  - .planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md
autonomous: true
requirements: [DSGN-03, DSGN-04, COPY-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La matriz de cierre pasa sobre lo que dejó 02-07: sin scroll horizontal a 320, 390, 640 (zoom 200 % de 1280), 768, 1024 y 1280 px en `/` y en `/privacidad/` con `reduce` y con `no-preference` (con la entrada del hero ya activa); con espaciado de texto forzado (SC 1.4.12) a 320 y 1280 px nada se recorta; sin JavaScript las 12 secciones se ven y el FAQ abre; con ClickUp bloqueado la página está completa y el enlace de respaldo se ve; todo enlace, resumen y botón visible mide 44 px o más de alto; ninguna sombra computada tiene desenfoque y no hay degradados (DSGN-03, DSGN-04)"
    - "axe de humo (`@axe-core/playwright` 4.13.0, etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa, sin el iframe) no reporta violaciones `critical` ni `serious` en `/` y en `/privacidad/` a 320, 390 y 1280 px; la compuerta completa de accesibilidad y Lighthouse son de la fase 3 (DSGN-03)"
    - "Ningún elemento de contenido invade el área de salvado `--logo-clear` (1 X) del logo del header ni del footer a 390 y 1280 px; el ancho de `--logo-clear` se resuelve con un elemento de prueba de `width: var(--logo-clear)` (DSGN-03, DSGN-04)"
    - "Peso y peticiones de `/`: HTML de menos de 61440 bytes sin comprimir, la suma de todos los `[data-collage]` de `/` (las raíces de composición, que desde 02-10 son `div` con capa svg y píldoras, más los `svg` de avatares y la pieza de `MetricCard`) y del sprite `.collage-sprite` de 42240 bytes o menos (el 40960 que traía este plan, subido en 1280 por las dos fotos de 02-11, que son los topes que 02-10 y 02-11 fijan para el HTML de `/`; los topes finos por pieza los vigilan las pruebas de 02-10 y 02-11), `dist` sin archivos `.js`, el script en línea de menos de 3072 bytes y solo se piden localhost, forms.clickup.com y app-cdn.clickup.com (DSGN-03)"
    - "Cierre visual: `critique` de página completa contra las 15 filas de 'Reference Synthesis' y los 8 rasgos de vibra con `impeccable`, seguido de `polish`, `harden`, `adapt` y `audit`, y la lista de antidefectos de `design-taste-frontend` (diales 7, 3 y 4), todos invocados con la herramienta Skill; `02-VISUAL-LOG.md` consolida el lote 0 de 02-02 y registra cada ronda `CIERRE` con verbos, hallazgos, correcciones, capturas y la tabla de los 8 rasgos con evidencia numérica; los 8 rasgos se cumplen en 3 ciclos como máximo o el plan se detiene y se muestra a Juan con capturas (DSGN-04)"
    - "Auditoría de copy y producción: `npm run pending` regenera `PENDING-COPY.md` de forma determinista y `node scripts/list-pending.mjs --check` sale 0; `npm run build` sin producción sale 0; con `PUBLIC_ENV=production` la guarda de copy falla solo por reglas PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`), y la puerta de fotos de 02-11 (`node scripts/check-photos.mjs`) también bloquea producción mientras la aprobación de Ari de las fotos elegidas siga `pendiente` o sobre alguna candidata, lo que se lista para Ari (COPY-01)"
    - "`02-ARI-FINDINGS.md` reúne todo lo que las guardas rechazaron y lo que los diez SUMMARY anteriores (02-01 a 02-07 y 02-09 a 02-11) reportaron: decisiones de alto impacto (el morado #4228D1 que Juan fijó el 2026-09-19 frente a la etiqueta #73187F de la página 8 del BrandBook, que Ari confirma; 20 frente a 30 minutos, AEO, licencia web de Hurme y aprobación de Outfit, consentimiento del equipo, avatares Loopy en lugar de caras, favicon de la mesa 18, variantes de logo por fondo, emblema sobre oscuro ausente, excepción de los isotipos sobre oscuro, imagotipo como candidato al header, crema como superficie, excepción del naranja sobre morado sin uso y fotos de stock de las ranuras hero y whynow pendientes de aprobación), texto que falta, texto que se muestra pero debe confirmarse (incluidas las palabras de las píldoras: `seo`, `geo` y `ads` salen del copy de Ari; `spy` y `team work` salen del moodboard y necesitan su visto bueno), erratas del doc, afirmaciones de mercado ausentes y cómo se aplica cada respuesta; cada primer segmento de ruta con afirmaciones `pending` aparece citado (COPY-01)"
    - "Suite completa en verde: `node --test tests/guards/*.test.mjs` (incluidas `motion-budget`, `phase2-static` y la prueba 9b de 02-03), `node scripts/check-contrast.mjs`, `npm run build`, `npx playwright test` (proyectos `chromium` y `live`, con el aserto de 'textos de Ari visibles' de 02-03 y `motion.spec.ts` de 02-07) y `npm run test:e2e:isolated`; si ClickUp no responde, `live` se reintenta más tarde sin culpar al código. Este plan solo ejecuta las aserciones de 02-03 y las guardas de 02-07, nunca las edita (DSGN-03, COPY-01)"
    - statement: "Sensación real del movimiento y estado abierto o cerrado del `<summary>`: una persona mira la entrada del hero con `reduce` y con `no-preference` en un navegador real y comprueba con VoiceOver en Safari que el FAQ anuncia su estado; es comprobación manual, anotada en `02-VISUAL-LOG.md` como pendiente para Juan (UI Considerations: interaction)"
      verification: backstop
    - statement: "Composición de `HeroCollage` a 320 px sin cortar piezas ni tapar el CTA: se comprueba con la captura de cierre a 320 px y con la medición del primer pantallazo de `page-structure.spec.ts` (UI Considerations: visual)"
      verification: backstop
    - "Supuesto explícito (UI Considerations: copy y assets, sin resolver aquí): el texto que falta, la confirmación del morado y de las variantes del logo, las palabras de las píldoras y la aprobación de las fotos dependen de Ari; este plan no los decide, los reúne en `02-ARI-FINDINGS.md`, y la página sigue mostrando 'FALTA CONFIRMAR' donde falta el dato"
  artifacts:
    - path: "tests/e2e/phase-closing.spec.ts"
      provides: "02-07 lo creó con 'conexiones de fase'; este plan lo extiende con axe de humo, matriz de desborde, espaciado de texto, sin JavaScript, ClickUp bloqueado, objetivos y área de salvado, peso y peticiones, y hoja de contacto por lote"
      contains: "AxeBuilder"
    - path: "package.json"
      provides: "`@axe-core/playwright` 4.13.0 como dependencia de desarrollo (la compuerta completa es de la fase 3)"
      contains: "@axe-core/playwright"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Registro consolidado: lote 0 de 02-02, rondas CIERRE, tabla de los 8 rasgos y de las 15 filas de síntesis"
      contains: "CIERRE1"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md"
      provides: "Lista única de decisiones, faltantes y erratas para Ari; cita el morado vigente y la etiqueta de la página 8 del BrandBook (02-09 documenta que debe citar ambos)"
      contains: "#4228D1"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada de forma determinista al cierre de la fase"
      contains: "faq.items"
  key_links:
    - from: "tests/e2e/phase-closing.spec.ts"
      to: "@axe-core/playwright"
      via: "`new AxeBuilder({ page })` con las etiquetas wcag2a a wcag22aa y `exclude('iframe')`"
    - from: "tests/e2e/phase-closing.spec.ts"
      to: "tests/e2e/page-structure.spec.ts"
      via: "la hoja de contacto recorre `PAGE_ORDER` y las capturas por lote reutilizan la herramienta `PHASE2_BATCH` de 02-01"
    - from: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      to: ".planning/phases/02-secciones-marca-y-copy/02-02-SUMMARY.md"
      via: "la entrada 'Lote 0' se copia al log con la nota 'consolidada desde 02-02-SUMMARY.md'"
    - from: ".planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md"
      to: "PENDING-COPY.md"
      via: "cada primer segmento de ruta con afirmaciones `pending` está citado en el archivo de hallazgos"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir ni reordenar el texto del doc de Ari, ni siquiera durante `polish`: la iteración visual cambia estilo y disposición, no palabras; `src/content/landing.es.yaml` no cambia en este plan"
    - requirement_id: COPY-01
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT aflojar la guarda de producción (`PUBLIC_ENV=production` sigue fallando por PENDING y MISSING) ni editar la prueba 9b de `copy.test.mjs` ni el aserto de 'textos de Ari visibles' de `a11y-base.spec.ts`, que son de 02-03: este plan solo las ejecuta"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: judgment
      statement: "MUST NOT saltarse `impeccable` ni `design-taste-frontend`: las tareas 1 y 2 los invocan con la herramienta Skill (verbos `critique`, `audit`, `polish`, `harden` y `adapt`) y registran en `02-VISUAL-LOG.md` lo que hicieron; la tarea 2 no se cierra con un rasgo de vibra fallando"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT modificar el bloque del formulario de ClickUp (iframe, atributos, `min-height`, script, enlace de respaldo, `noscript`, borde, radio, sombra y `overflow` de `.form-embed`), `cta-focus.ts`, la geometría o los colores de las piezas del collage, ni los tokens de color; `polish` solo toca archivos que ya existen en `src/components`, `src/styles` y `src/layouts`, sin dependencias nuevas ni JS nuevo"
    - requirement_id: DSGN-04
      category: motion
      status: resolved
      verification: test
      statement: "MUST NOT agregar ni cambiar movimiento en este plan: `polish` no puede sumar animaciones ni transiciones; el presupuesto lo vigilan `motion-budget.test.mjs` y `motion.spec.ts` de 02-07, que siguen en verde"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar inyección de HTML en bruto, colores hex en `src/components` o `src/pages`, `outline: none`, `line-height` con `!important` ni sombras difusas o degradados: `phase2-static.test.mjs` (02-07) y el bloque de la matriz de cierre lo vigilan"
    - requirement_id: DSGN-03
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT aceptar scroll horizontal, texto recortado con espaciado forzado ni elementos interactivos de menos de 44 px de alto en `/` o `/privacidad/`: se corrige el layout, no la prueba"
    - requirement_id: DSGN-03
      category: scope
      status: resolved
      verification: judgment
      statement: "MUST NOT correr Lighthouse ni la compuerta completa de accesibilidad (`REPORT.md`, `EXCEPTIONS.md`, lector de pantalla), ni tocar sitemap, JSON-LD o `noindex` de `/privacidad/`: son de la fase 3; axe aquí es solo una prueba de humo"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT instalar ninguna dependencia distinta de `@axe-core/playwright` 4.13.0 ni instalarla si `gsd-tools query package-legitimacy check --ecosystem npm @axe-core/playwright` (sin sufijo de versión) no da OK; MUST NOT enviar el formulario de ClickUp, leer `.env` ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Segunda mitad del cierre de la fase 2: iterar la página completa contra la vibra de las referencias de Ari a cinco anchos, con `impeccable` y `design-taste-frontend` (DSGN-03, DSGN-04), y auditar el copy para que Ari reciba una sola lista de decisiones y la suite de la fase quede en verde (COPY-01). Parte de lo que dejó el plan 02-07 (movimiento, guardas, conexiones y `tests/e2e/phase-closing.spec.ts`) y no reinventa ninguno de sus ganchos.

Purpose: que Juan y Ari tengan evidencia medida (capturas, anchos, axe de humo, pruebas verdes) de que la landing completa se ve y funciona de 320 a 1280 px, y una lista clara de lo que falta decidir, confirmar o corregir. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: `tests/e2e/phase-closing.spec.ts` extendido (axe de humo, matriz de desborde, espaciado de texto, sin JavaScript, ClickUp bloqueado, objetivos, sombras y área de salvado, peso y peticiones, hoja de contacto), `@axe-core/playwright` 4.13.0 en `devDependencies`, `02-VISUAL-LOG.md` consolidado, `02-ARI-FINDINGS.md` y la suite completa en verde.

Decisiones de CONTEXT.md que implementa (por nombre de sección; CONTEXT no numera D-XX): "Estilo visual, collage y movimiento" (`impeccable` con `critique`, `polish` y `adapt` más `design-taste-frontend`, iteraciones hasta la vibra, límites que no se negocian), "Textos pending" (relleno visible, `PENDING-COPY.md` determinista, el build de producción sigue fallando mientras haya `pending`), "Morado de marca y logo" (el morado #4228D1 lo fijó Juan el 2026-09-19; la etiqueta #73187F de la página 8 del BrandBook, las variantes de logo, las palabras de las píldoras y las fotos de stock se reportan a Ari) y "Excepciones del contrato de diseño aprobadas por Juan" (96 px de `--section-y` y 4 CTA, verificados en la matriz).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicarla con `grep -n '^## \|^### '` y leer con `offset` y `limit`). `PRODUCT.md` y `DESIGN.md` (raíz) los generó el plan 02-01: aquí solo se leen. Los SUMMARY de 02-01 a 02-07 se leen solo por sección (`grep -n -i` de erratas, hallazgos y desviaciones), nunca completos.
</context>

## Artifacts this phase produces

Rutas relativas a la raíz del repositorio. Los nombres de abajo los reutiliza la fase 3 (compuerta de accesibilidad, Lighthouse, SEO) y no se renombran después.

- **Pruebas de cierre**: `tests/e2e/phase-closing.spec.ts` (lo crea 02-07 con el bloque 'conexiones de fase'; este plan agrega 'axe de humo', 'matriz de desborde', 'espaciado de texto', 'sin JavaScript', 'ClickUp bloqueado', 'objetivos, sombras y área de salvado', 'peso y peticiones' y 'hoja de contacto'). La hoja de contacto solo corre con `PHASE2_BATCH`, guarda `test-results/phase2/<lote>-<id>-<ancho>.png` y las hojas `<lote>-hoja-1280.png` y `<lote>-hoja-390.png`. Lotes de captura: `CIERRE0` (tracer), `CIERRE1`, `CIERRE2` y, si hace falta, `CIERRE3`, con la herramienta de 02-01 (`PHASE2_BATCH=<lote> npx playwright test --project=chromium tests/e2e/page-structure.spec.ts -g "captura"`).
- **Dependencia**: `@axe-core/playwright` 4.13.0 (exacta) en `devDependencies`; la fase 3 la reutiliza para la compuerta.
- **Evidencia**: `02-VISUAL-LOG.md` consolidado, `02-ARI-FINDINGS.md` (una sola lista para Ari), `PENDING-COPY.md` regenerado y `02-08-SUMMARY.md` con el resultado de las pruebas, los cambios de `polish`, las decisiones de criterio de abajo y las verificaciones humanas que quedan para Juan.
- **Consumidos de 02-07 (no se renombran ni se editan aquí)**: `tests/e2e/lib/motion.ts`, `tests/e2e/motion.spec.ts`, `tests/guards/motion-budget.test.mjs`, `tests/guards/phase2-static.test.mjs` y el bloque 'conexiones de fase'. **De 02-03**: el aserto de 'textos de Ari visibles' de `a11y-base.spec.ts` y la prueba 9b de `copy.test.mjs`, que aquí solo se ejecutan. **De 02-09 a 02-11 (solo lectura)**: `tests/e2e/lib/brand.ts` (`PURPLE_RGB`), `02-BRAND-INVENTORY.md`, la hoja `/marca/hoja/`, `PHOTO_SLOTS`, `CHIP_WORDS` y `src/assets/photos/LICENSES.md` (fuente de las decisiones y pendientes para Ari) y las guardas `brand-palette`, `brand-assets`, `collage-scenes` y `photos`.
- **Notas para la fase 3** (no se hacen aquí): Lighthouse y Core Web Vitals, `REPORT.md`, `EXCEPTIONS.md` y la revisión con lector de pantalla, excluir `/privacidad/` del sitemap mientras siga `noindex`, JSON-LD (agregarlo a `RAW_HTML_ALLOW` de 02-07 con su razón) y la deriva de collage por scroll que el UI-SPEC marca opcional.

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`. Este plan no agrega ninguna ranura nueva ni edita `landing.es.yaml`: mide, pule estilo y reúne lo que los planes 01 a 07 dejaron `pending`. Casos del brief: **A** existe en el doc y pasa las guardas, **B** nombre estructural de CONTEXT o del UI-SPEC, **C** falta en el doc y se muestra 'FALTA CONFIRMAR', **D** microcopy estructural.

| Ranura | Origen | Caso | Estado en el YAML |
|--------|--------|------|-------------------|
| Toda ranura existente (A, B, C y D de los planes 01 a 07) | YAML tal como lo dejó 02-07 | sin cambios | La tarea 3 la audita: lista determinista, producción falla solo por PENDING y MISSING, y cada ruta `pending` queda citada en `02-ARI-FINDINGS.md` |

Las cadenas de `02-ARI-FINDINGS.md` no son copy de la página: son notas para Ari, en español neutro sin voseo ni guiones largos; las citas del doc van literales y con sus erratas.

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **axe entra ahora como dependencia de desarrollo** (la fase 3 la necesita de todos modos): versión exacta 4.13.0 como pide `STACK.md`, veredicto de legitimidad OK el 2026-09-19 (existe, unos 7,3 millones de descargas semanales, repositorio de dequelabs, sin postinstall). Revertir: quitar la dependencia y los bloques de axe; la fase 3 la instala.
2. **Hojas de contacto para leer la página entera.** Una captura de página completa a 1280 px es demasiado alta para leerla y 28 recortes cuestan demasiado contexto; la herramienta compone dos hojas (1280 y 390 px) y el ejecutor abre como máximo 4 recortes por ronda. Las capturas nunca se versionan (`test-results/`).
3. **640 px en la matriz** representa el zoom de 200 % sobre 1280 px (SC 1.4.4 y 1.4.10); 320 px representa el zoom de 400 %.
4. **`02-ARI-FINDINGS.md` es una nota para una persona, no copy de la página**: se escribe en español neutro con las reglas de estilo de Juan (sin guiones largos, sin voseo) y su prosa propia pasa por `humanizer`; las citas del doc de Ari van literales y no se tocan (COPY-01). Revertir: omitir la pasada de `humanizer`.
5. **El plan abre con un tracer estrecho** (regla de tracer primero del brief): axe instalado y medido sobre `/` a 1280 px, la primera hoja de contacto y la primera entrada del log, de punta a punta. La matriz completa, el ciclo de `critique` y `polish` y la consolidación son la expansión (tarea 2).
6. **Las aserciones de 02-03 son solo de ejecución**: el aserto de 'textos de Ari visibles' y la prueba 9b tienen un único dueño (02-03); aquí y en 02-07 solo se corren. Si una falla, se reporta como hallazgo, no se edita.
7. **Alcance de `polish`**: solo estilo y disposición de archivos que ya existen; nada de texto, tokens de color ni movimiento nuevo. Los cambios se listan en el SUMMARY como archivos modificados adicionales a `files_modified`.

## UI Considerations (lift del UI-SPEC para este plan)

- **covered** (ya en `must_haves.truths` como texto): loading (todo SVG en línea, cero `.js` propios), empty (toda ranura sin dato muestra 'FALTA CONFIRMAR' y la auditoría la cuenta), long-text y overflow (matriz a seis anchos con espaciado de texto forzado), error (ClickUp bloqueado con la página completa), a11y-motion (la matriz corre con `reduce` y con `no-preference` sin desbordes; el presupuesto lo protege 02-07), a11y-color (sin cambios de tokens; `check-contrast` sigue en verde) y zero-one-many (12 secciones y 11 h2 exactos, afirmados por 02-07).
- **backstop** (en `must_haves.truths` como `{statement, verification: backstop}`): interaction del `<summary>` (VoiceOver en Safari, anotado en `02-VISUAL-LOG.md` para Juan) y visual de `HeroCollage` a 320 px (captura de cierre y medición del primer pantallazo).
- **No aplican a este plan**: partial (MetricCard, plan 04; entra en la matriz de anchos).
- **unresolved que este plan asume**: copy y assets siguen abiertos para Ari (texto que falta, etiqueta del morado, variantes de logo, palabras de las píldoras, aprobación de las fotos, licencia de Hurme); se reúnen en `02-ARI-FINDINGS.md` y no se deciden aquí.

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Estado |
|--------|------|--------|
| GOAL (ROADMAP fase 2) | Landing completa con identidad de marca, de 320 a 1280 px sin scroll horizontal, todo el copy tal cual del doc de Ari | Cierre y evidencia aquí (tareas 1 a 3); movimiento, guardas y conexiones en 02-07; secciones y activos en 02-01 a 02-06 |
| REQ | DSGN-03 (320 a 1280 px sin scroll horizontal) | Tareas 1 y 2 (matriz, espaciado, objetivos, área de salvado, peso) y tarea 3 (suite completa) |
| REQ | DSGN-04 (skills `impeccable` y `design-taste-frontend`) | Tareas 1 y 2 (cierre con `critique`, `audit`, `polish`, `harden`, `adapt`) |
| REQ | COPY-01 (copy tal cual, se pide a Ari lo que falta) | Tarea 3 (auditoría, hallazgos y suite); las aserciones de 02-03 solo se ejecutan |
| REQ | DSGN-05 (movimiento desactivable con `reduce`) | 02-07; aquí la matriz lo confirma con ambas preferencias |
| RESEARCH | No existe `02-RESEARCH.md`; `STACK.md` (axe 4.13.0, Playwright 1.63.0) y el UI-SPEC son la fuente | Sin ítems pendientes |
| CONTEXT | Estilo visual y movimiento, textos pending, morado y logo, excepciones de Juan (96 px, 4 CTA) | Este plan |
| UI-SPEC | Design Method (fila Cierre, ciclo, lista de vibra, 3 ciclos máximo), Reference Synthesis, Verification Hooks (uno por uno en las tareas 1 a 3) | Este plan |
| Fuera de este plan (no son huecos) | Lighthouse, Core Web Vitals, compuerta completa de accesibilidad, sitemap, JSON-LD y `noindex` de `/privacidad/` (fase 3); deriva por scroll (opcional en el UI-SPEC); verificaciones humanas de la fase 1 (siguen diferidas) | Diferido a esos planes o a Juan |

## Presupuesto de contexto (6 archivos en `files_modified`, 3 con autoría real)

Con autoría real: `tests/e2e/phase-closing.spec.ts` (bloques nuevos), `02-VISUAL-LOG.md` y `02-ARI-FINDINGS.md`. Casi sin autoría: `package.json` y `package-lock.json` (una dependencia), `PENDING-COPY.md` (lo genera `npm run pending`). Además `polish` puede tocar archivos ya existentes de `src/`, que se declaran en el SUMMARY. Estimación de 50k tokens (confianza baja, sin historial de calibración, factor 1): tarea 1 unos 13k (tracer estrecho), tarea 2 unos 24k (dos skills, dos rondas de captura y las hojas de contacto) y tarea 3 unos 13k. El costo variable es la iteración visual: el ejecutor lee las dos hojas de contacto por ronda y como máximo 4 recortes, y las mediciones de los specs reemplazan a las capturas.

Puntos de corte: cada tarea termina con el build verde y commiteada. Tras la tarea 1 (tracer), si el contexto ya pasó de 50 %, el ejecutor hace commit, escribe `02-08-SUMMARY.md` con `status: partial` (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. La tarea 2 no se parte a mitad de un ciclo: si al empezarla el contexto pasa de 50 %, se corta antes. La tarea 3 puede correr sola en un ejecutor nuevo leyendo los SUMMARY parciales.

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/`, `public/` o `astro.config.mjs`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; antes, `curl -s -o /dev/null -w '%{http_code}' https://forms.clickup.com`, y si no da 200, reintentar más tarde sin culpar al código). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; `PUBLIC_ENV=production` falla solo por PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`).
- Las pruebas derivan los textos y las cantidades esperadas del YAML (lectura con `yaml`, mismo criterio de `walkClaims` y `fill`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- Antes de editar en cada tarea, anotar `git rev-parse HEAD` como BASE para los criterios de alcance (`git diff --name-only BASE` debe estar contenido en `files_modified`, más los archivos de `polish` que la tarea 2 declare en el SUMMARY).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante a sitio estático | HTML y CSS estáticos; sin entrada del visitante en este plan |
| Sitio a formulario de ClickUp | iframe y script de terceros intactos; este plan no los toca ni agrega peticiones nuevas |
| Equipo de desarrollo a registro npm | una dependencia de desarrollo nueva (`@axe-core/playwright`) entra a la máquina y a las pruebas, no al build |
| Repositorio a Ari | `02-ARI-FINDINGS.md` cita el doc de Ari y rutas del YAML; sin secretos ni datos de personas más allá de los nombres ya publicados en el copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Tampering | Cambios de `polish` que alteren el copy de Ari, el bloque del formulario o el presupuesto de movimiento | medium | mitigate | Regla de alcance con BASE: `git diff BASE -- src/content/landing.es.yaml` vacío; las pruebas de la fase 1 del iframe siguen corriendo; `motion-budget.test.mjs` de 02-07 sigue en verde; el copy se imprime sin leer el estado |
| T-02-08-02 | Information Disclosure | Capturas de `test-results/` o `02-ARI-FINDINGS.md` con datos indebidos | low | mitigate | Las capturas se toman con ClickUp bloqueado (sin formulario) y no se versionan; el archivo de hallazgos cita solo copy del doc y rutas del YAML; no se lee `.env` |
| T-02-08-03 | Denial of Service | Peticiones a terceros nuevas o peso extra de la página | low | mitigate | Prueba de peticiones (solo localhost, forms.clickup.com y app-cdn.clickup.com), HTML de menos de 61440 bytes, `[data-collage]` y sprite de 42240 bytes o menos y cero `.js` en `dist` |
| T-02-08-04 | Repudiation | Iteración visual sin evidencia (nadie sabe qué se cambió ni por qué) | low | mitigate | `02-VISUAL-LOG.md` registra verbos, hallazgos, correcciones y capturas por ronda; cada edición de `polish` se anota con archivo y medida |
| T-02-08-05 | Information Disclosure | Envío accidental del formulario de ClickUp durante las pruebas | low | accept | Ninguna prueba lo envía (regla heredada de la fase 1); los specs solo miden y navegan |
| T-02-08-SC | Tampering | Instalación npm de `@axe-core/playwright` 4.13.0 (y `axe-core` transitivo) | high | mitigate | Verificador de legitimidad (`package-legitimacy check --ecosystem npm @axe-core/playwright`, sin sufijo de versión) con veredicto OK el 2026-09-19; la tarea 1 lo vuelve a correr y se detiene si no da OK; versión exacta, solo `devDependencies`, sin postinstall, fuera del build de producción |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de cierre: axe instalado y medido de punta a punta sobre `/`, primera hoja de contacto y primera entrada del log</name>
  <files>package.json, package-lock.json, tests/e2e/phase-closing.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md</files>
  <precondition>02-07 está ejecutado y commiteado: existen `tests/e2e/phase-closing.spec.ts` con el bloque 'conexiones de fase', `src/styles/motion.css`, `tests/e2e/lib/motion.ts` y las guardas `motion-budget.test.mjs` y `phase2-static.test.mjs`; `npx astro build` pasa y `node ~/.claude/gsd-core/bin/gsd-tools.cjs` responde. Si falta algo, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### Ciclo por lote (acotado, por regla de impeccable)` y `### Lista de "vibra"` de `## Design Method and Iteration Protocol`, y `## Verification Hooks`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - tests/e2e/page-structure.spec.ts (herramienta de capturas `PHASE2_BATCH` y `PAGE_ORDER`), tests/e2e/phase-closing.spec.ts (02-07) y el bloque `recorte` de tests/e2e/closing-sections.spec.ts (patrón de recortes por sección)
    - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (entradas de los lotes A a E)
    - package.json (`devDependencies`) y .planning/research/STACK.md (líneas de `@axe-core/playwright`)
  </read_first>
  <action>
    Implementa DSGN-03 y DSGN-04 en su recorte más fino, de punta a punta por todas las capas: dependencia, prueba, build, captura y registro. La matriz completa, el ciclo con las skills y la consolidación del log son de la tarea 2. Antes de editar anota `git rev-parse HEAD` como BASE.

    Dependencia (per T-02-08-SC). Corre `node ~/.claude/gsd-core/bin/gsd-tools.cjs query package-legitimacy check --ecosystem npm @axe-core/playwright`, sin sufijo de versión (con `@4.13.0` pegado el verificador responde `does-not-exist`). El veredicto debe ser OK (el 2026-09-19: existe, unos 7,3 millones de descargas semanales, repositorio de dequelabs, sin postinstall); si no lo es, detén la tarea y repórtalo. Si lo es, instala solo `@axe-core/playwright` con versión exacta 4.13.0 como dependencia de desarrollo y ninguna otra.

    Pruebas del tracer (rojo). En `tests/e2e/phase-closing.spec.ts` agrega dos bloques. (1) 'axe de humo': `AxeBuilder` con las etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa y `exclude('iframe')` sobre `/` a 1280 px con ClickUp bloqueado (`context.route` que aborta todo lo que no sea localhost): cero violaciones `critical` o `serious`; las `moderate` y `minor` (`id` y cantidad de nodos) se adjuntan como anotaciones de la prueba para el log. (2) 'hoja de contacto' (solo con `PHASE2_BATCH`, `test.skip` si no): recortes de `header`, de cada sección de `PAGE_ORDER` y de `footer` a 1280 y 390 px en `test-results/phase2/<lote>-<id>-<ancho>.png` (14 por ancho) y dos hojas, `<lote>-hoja-1280.png` (3 columnas) y `<lote>-hoja-390.png` (6 columnas), que se componen abriendo una página en blanco con una rejilla CSS de `<img>` con datos en base64 rotuladas con su id y capturándola. Confirma que el bloque de axe falla antes de instalar la dependencia (el import no resuelve).

    Ronda del tracer (per el ciclo del UI-SPEC). Build, preview en el puerto 4322 y `PHASE2_BATCH=CIERRE0 npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts -g "hoja de contacto"`. Mira solo las dos hojas de contacto (no abras recortes en el tracer). En `02-VISUAL-LOG.md` agrega la primera entrada, 'Cierre, tracer (CIERRE0)': lote, capturas generadas, resultado de axe en `/` a 1280 px y primeras impresiones contra los 8 rasgos de vibra, sin correcciones todavía. Si axe reporta una violación `critical` o `serious` en el HTML propio, es un defecto real: anótalo y corrígelo en la tarea 2, no afloje la prueba.

    Ciclo de ejecución de las reglas heredadas: build, preview en el puerto 4322, specs, `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - `package.json` lista `@axe-core/playwright` en `devDependencies` con versión exacta `4.13.0` y `git diff BASE -- package.json` no muestra ninguna otra dependencia nueva; el veredicto OK de legitimidad está anotado en el SUMMARY.
    - El bloque 'axe de humo' pasa: `/` a 1280 px con ClickUp bloqueado tiene cero violaciones `critical` o `serious`; las demás quedan como anotaciones.
    - `PHASE2_BATCH=CIERRE0` produce 14 recortes por ancho (header, 12 secciones y footer) a 1280 y 390 px y las dos hojas, `CIERRE0-hoja-1280.png` y `CIERRE0-hoja-390.png`, no vacías.
    - `02-VISUAL-LOG.md` tiene la entrada 'Cierre, tracer (CIERRE0)' con lote, capturas, resultado de axe y primeras impresiones.
    - El tracer no corrige nada de `src/`: `git diff --name-only BASE` está contenido en `files_modified` y no incluye archivos de `src`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts && PHASE2_BATCH=CIERRE0 npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts -g "hoja de contacto"</automated>
    <fails_when>salida distinta de 0, axe reporta una violación `critical` o `serious`, el bloque 'hoja de contacto' queda en `skip` con `PHASE2_BATCH` definida, o una prueba de 02-07 del mismo archivo falla</fails_when>
    <automated>node -e "const fs=require('fs');const p=require('./package.json');const l=fs.readFileSync('.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md','utf8');const sheets=['1280','390'].every(w=>{const f='test-results/phase2/CIERRE0-hoja-'+w+'.png';return fs.existsSync(f)&&fs.statSync(f).size>0});process.exit(p.devDependencies['@axe-core/playwright']==='4.13.0'&&sheets&&/CIERRE0/.test(l)?0:1)"</automated>
    <fails_when>la dependencia no está fijada en 4.13.0, falta alguna de las dos hojas de contacto o el log no tiene la entrada del tracer</fails_when>
  </verify>
  <done>axe corre sobre la página real con ClickUp bloqueado, la herramienta de hojas de contacto genera dos imágenes legibles de toda la página y el log tiene su primera entrada; el tracer está commiteado.</done>
</task>

<task type="auto">
  <name>Task 2: Matriz completa, ciclo de critique y polish con confirmación, y log consolidado</name>
  <files>tests/e2e/phase-closing.spec.ts, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md, y los archivos ya existentes de src/components, src/styles y src/layouts que `polish` toque (se listan en el SUMMARY)</files>
  <precondition>La tarea 1 está commiteada (axe instalado y el bloque 'axe de humo' en verde) y `npx astro build` pasa; si falta algo, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol` (verbos por lote, fila Cierre, ciclo, lista de vibra), `## Reference Synthesis` (las 15 filas), `## Verification Hooks` y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - tests/e2e/phase-closing.spec.ts (tarea 1 y el bloque 'conexiones de fase' de 02-07), tests/e2e/page-structure.spec.ts (herramienta `PHASE2_BATCH` y `PAGE_ORDER`)
    - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (entradas de los lotes A a E, M, C (collage de marca), P (fotos) y la del tracer; los lotes M, C y P ya están en el log y no se repiten) y 02-02-SUMMARY.md (la entrada del lote 0: `grep -n -i "lote 0"`)
    - .planning/phases/02-secciones-marca-y-copy/02-BRAND-INVENTORY.md (rasgos del moodboard de la sección 4 y brecha de la sección 5: lectura obligatoria del verificador visual, per 02-09) y, solo si un hallazgo lo pide, recortes de `brand-inventory/` (moodboard.png, ai_a.png y ai_b.png, sin versionar)
    - PRODUCT.md y DESIGN.md (raíz)
  </read_first>
  <action>
    Implementa DSGN-03 y DSGN-04 en su cierre: medir la página completa, mirarla con las skills, corregir en un solo lote y confirmar. Antes de editar anota `git rev-parse HEAD` como BASE.

    Medición primero (per DSGN-03). Extiende `tests/e2e/phase-closing.spec.ts` con estos bloques, con expectativas derivadas del YAML o de las variables computadas y no de listas escritas a mano: (1) 'matriz de desborde': `/` y `/privacidad/` a 320, 390, 640, 768, 1024 y 1280 px con `reduce` y con `no-preference`; el ancho de desplazamiento del documento es menor o igual que su ancho de cliente y ningún rectángulo de `header`, `main > section` o `footer` cae fuera de `[0, innerWidth]`; (2) 'espaciado de texto': a 320 y 1280 px inyecta `line-height` 1.5, párrafos a 2 veces el cuerpo, `letter-spacing` 0.12em y `word-spacing` 0.16em (SC 1.4.12), sin scroll horizontal y con cero elementos con `overflow` oculto o recortado cuyo alto de contenido supere el alto de su caja en más de 1 px y que contengan texto; (3) 'sin JavaScript': las 12 secciones tienen alto mayor que 0 y son visibles, un `<details>` abre con clic y hay 4 CTA; (4) 'ClickUp bloqueado': con `context.route` abortando todo lo que no sea localhost, las 12 secciones, el footer y el enlace de respaldo de `#agenda` se ven y no hay `pageerror`; (5) amplía 'axe de humo' a `/` y `/privacidad/` a 320, 390 y 1280 px con ClickUp bloqueado; (6) 'objetivos, sombras y área de salvado': en `/` a 390 y 1280 px cada `a`, `summary` y `button` visible (salvo skip links fuera de pantalla y el iframe) mide 44 px o más de alto; ninguna sombra computada de `/` ni de `/privacidad/` tiene desenfoque y ningún `background-image` computado contiene un degradado; y por cada `.brand-logo` (header y footer, a 1280 y 390 px) resuelve `--logo-clear` con un elemento de prueba de `width: var(--logo-clear)` y comprueba que ningún elemento de contenido (`a`, `p`, `h1` a `h6`, `li`, `button`, `summary`, `svg` no decorativo) distinto del propio logo y de sus ancestros toca el rectángulo del logo ampliado por esa medida en los cuatro lados; (7) 'peso y peticiones': HTML de `/` de menos de 61440 bytes, suma de `outerHTML` de todos los `[data-collage]` de `/` (las raíces de composición, que desde 02-10 son `div` con capa svg, píldoras y, en el hero y en Por qué ahora, el marco de foto de 02-11, más los `svg` de avatares y la pieza de `MetricCard`) más el `outerHTML` del sprite `.collage-sprite` de 42240 bytes o menos (los topes por pieza los vigilan `collage-language.spec.ts` y `collage-photos.spec.ts`), `dist` sin archivos `.js`, el script en línea de `/` de menos de 3072 bytes, y los hosts pedidos por `/` y `/privacidad/` (evento `request`) contenidos en localhost, forms.clickup.com y app-cdn.clickup.com. Corre los bloques: lo que falle es un defecto real del layout y se corrige en el ciclo de abajo, no se afloja la prueba.

    Ciclo de cierre (per la fila Cierre del UI-SPEC; una ronda de captura es un pase en lote). (1) Ronda `CIERRE1`: corre la herramienta de 02-01 (`PHASE2_BATCH=CIERRE1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts -g "captura"`: cinco anchos por base, `reduce` y sin JavaScript, con ClickUp bloqueado) y la hoja de contacto de la tarea 1. Mira las dos hojas y, de las capturas de página completa, solo `CIERRE1-390-nojs.png` y `CIERRE1-320-reduce.png`; abre como máximo 4 recortes individuales donde un rasgo falle. (2) Con la herramienta Skill invoca `impeccable` (carga `reference/craft-floor.md`, corre `context`, lee `PRODUCT.md` y `DESIGN.md`) y corre `critique` de página completa contra las 15 filas de 'Reference Synthesis', los 8 rasgos de vibra, los contratos de sección y la brecha de marca de `02-BRAND-INVENTORY.md` (sección 5: color, Loopy, píldoras, recortes en media tinta, crema, logo por fondo y avatares); luego `audit` (accesibilidad y rendimiento de la página entera, sin Lighthouse, que es de la fase 3). Invoca `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4, y úsala como lista final de antidefectos (sin degradados, sin vidrio, sin tres tarjetas iguales seguidas sin variar, sin animaciones infinitas). (3) Aplica todas las correcciones en un solo lote con `polish`, `harden` (texto largo, relleno 'FALTA CONFIRMAR', `<details>`) y `adapt` (320 a 1280 px, zoom 200 %, espaciado de texto). Reglas del lote: solo archivos que ya existen en `src/components`, `src/styles` y `src/layouts`; ningún cambio de texto (COPY-01), del bloque del iframe, de tokens de color sin par aprobado en `node scripts/check-contrast.mjs`, de movimiento (lo vigila `motion-budget.test.mjs`), ni JavaScript o dependencias nuevas; cada edición se anota en el log con archivo, motivo y medida. (4) Reconstruye, repite los bloques de medición y corre la ronda de confirmación `CIERRE2` (una como máximo por ciclo). Si un rasgo de vibra sigue fallando, repite el ciclo con `CIERRE3`; tras 3 ciclos de página completa sin cumplir la lista, detente y muestra a Juan con capturas (regla del UI-SPEC).

    Registro (per DSGN-04). En `02-VISUAL-LOG.md`: (i) primero, la entrada 'Lote 0' que 02-02 dejó en su SUMMARY, copiada con la nota 'consolidada desde 02-02-SUMMARY.md' (decisión 9 de 02-02); (ii) por cada ronda `CIERRE`, los verbos usados, los hallazgos, las correcciones y las capturas; (iii) una tabla de los 8 rasgos de vibra con veredicto y evidencia numérica (primer pantallazo a 1280x800 de `page-structure.spec.ts`, padding de sección de 96 px desde 1024 px, bordes de 3 px y cero sombras difusas, avatares del mismo tamaño, cuatro CTA iguales) o, para los visuales, la captura y la fila de síntesis que los respalda; (iv) una tabla de las 15 filas de la síntesis con sí o no y dónde se ve; (v) los conteos de axe por impacto y las anotaciones `moderate` y `minor`; (vi) los pendientes para Juan: mirada real al movimiento con `reduce` y `no-preference`, VoiceOver sobre `<details>` en Safari, aprobación del set de avatares Loopy, del favicon (la mesa 18, decidida con evidencia en 02-09), de las variantes de logo por fondo y del morado, y la elección de las fotos de stock de cada ranura en `/marca/hoja/`.

    Ciclo de ejecución de las reglas heredadas: build tras cada cambio, preview en el puerto 4322, specs, `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - Matriz: `/` y `/privacidad/` sin scroll horizontal y sin rectángulos fuera de `[0, innerWidth]` a 320, 390, 640, 768, 1024 y 1280 px con ambas preferencias; con espaciado forzado a 320 y 1280 px hay cero elementos con texto recortado.
    - Sin JavaScript: 12 secciones visibles y el FAQ abre; con ClickUp bloqueado: 12 secciones, footer y enlace de respaldo visibles y cero `pageerror`.
    - axe: cero violaciones `critical` o `serious` en `/` y `/privacidad/` a 320, 390 y 1280 px; las demás quedan listadas en el log.
    - Cero elementos interactivos visibles de menos de 44 px de alto en `/` a 390 y 1280 px; cero sombras con desenfoque y cero degradados computados; cero intrusiones en el área de salvado del logo del header y del footer.
    - `/` pesa menos de 61440 bytes, los `[data-collage]` y el sprite suman 42240 bytes o menos, `dist` no tiene `.js` y los únicos hosts pedidos son localhost, forms.clickup.com y app-cdn.clickup.com.
    - `02-VISUAL-LOG.md` tiene el lote 0 consolidado, las rondas `CIERRE1` y `CIERRE2` (y `CIERRE3` si hizo falta) con verbos, hallazgos, correcciones y capturas, la tabla de los 8 rasgos con veredicto de cumple en todos, la tabla de las 15 filas, el conteo de axe y la lista de pendientes para Juan; el log deja constancia de haber invocado `impeccable` y `design-taste-frontend` con la herramienta Skill.
    - `git diff BASE -- src/content/landing.es.yaml` queda vacío, las pruebas de la fase 1 del iframe y las de 02-07 (`motion.spec.ts`, `motion-budget`, `phase2-static`) siguen en verde y `node scripts/check-contrast.mjs` sale 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts tests/e2e/motion.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs</automated>
    <fails_when>salida distinta de 0, una prueba de navegador queda en `skip` (salvo el bloque 'hoja de contacto' sin `PHASE2_BATCH`), axe reporta una violación `critical` o `serious`, o el resumen de `node --test` muestra "# fail" mayor que 0</fails_when>
    <automated>node -e "const l=require('fs').readFileSync('.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md','utf8');process.exit(/CIERRE1/.test(l)&&/CIERRE2/.test(l)&&/Lote 0/i.test(l)?0:1)" && git diff --quiet HEAD -- src/content/landing.es.yaml</automated>
    <fails_when>el log no tiene el lote 0 consolidado y las rondas `CIERRE1` y `CIERRE2`, o `landing.es.yaml` tiene cambios sin commitear (antes del commit de la tarea, `HEAD` es BASE)</fails_when>
    <human-check>Con `workflow.human_verify_mode=end-of-phase`: Juan abre la página en un navegador real a 390 y 1280 px, alterna la preferencia de movimiento reducido del sistema y confirma que la entrada del hero es sutil y que no falta contenido; además comprueba con VoiceOver en Safari que cada resumen del FAQ anuncia su estado; el resultado se anota en `02-VISUAL-LOG.md`.</human-check>
  </verify>
  <done>La página entera cumple la matriz de anchos, la prueba de humo de axe y los 8 rasgos de vibra con evidencia; el log consolidado y las hojas de contacto documentan cada ronda; ningún texto, formulario ni token de color cambió; commit hecho y verificaciones humanas anotadas para Juan.</done>
</task>

<task type="auto">
  <name>Task 3: Auditoría de fase, hallazgos consolidados para Ari y suite completa</name>
  <files>.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md, PENDING-COPY.md</files>
  <precondition>Las tareas 1 y 2 de este plan y las de 02-07 están commiteadas y existen `02-01-SUMMARY.md` a `02-07-SUMMARY.md` y `02-09-SUMMARY.md` a `02-11-SUMMARY.md`; si falta alguno, la fuente de sus hallazgos es el texto de su plan y se marca en el archivo de hallazgos con la nota 'fuente: plan, sin SUMMARY'.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (regla de copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md: `### Textos pending`, `### Morado de marca y logo` y `<specifics>` (pendientes de Ari)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### Ranuras que muestran "FALTA CONFIRMAR" hoy`, `### Ítems que se muestran tal cual pero se listan para que Ari los confirme` y `### Hallazgos para Ari`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-01-SUMMARY.md a 02-07-SUMMARY.md y 02-09-SUMMARY.md a 02-11-SUMMARY.md: solo las secciones de erratas, hallazgos, desviaciones y decisiones o preguntas para Ari (`grep -n -i 'errata\|hallazgo\|desviaci\|decisi\|pregunta\|Ari'` y `offset` y `limit`), nunca completos
    - src/assets/photos/LICENSES.md (solo las secciones 'Estado' y 'Registro': aprobación y nota de cada foto) y .planning/phases/02-secciones-marca-y-copy/02-BRAND-INVENTORY.md (sección 3, hallazgo de color)
    - PENDING-COPY.md (sin leerlo entero: `grep -c '^|'` y el conteo por primer segmento de ruta), scripts/list-pending.mjs (uso de `--check`) y `.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md` (pendientes para Juan de la tarea 2)
  </read_first>
  <action>
    Implementa COPY-01 en su cierre (auditoría y lista única para Ari) y confirma la fase completa. Antes de editar anota `git rev-parse HEAD` como BASE.

    Lista de pendientes (per el brief, lección 2). Corre `npm run pending`, anota el hash de `PENDING-COPY.md`, córrelo otra vez y comprueba que el hash es idéntico (determinista), y corre `node scripts/list-pending.mjs --check` (debe salir 0). Si `PENDING-COPY.md` difiere del versionado porque algún plan lo dejó viejo, commitea el archivo regenerado.

    Producción (per COPY-02 y COPY-01). Con `PUBLIC_ENV=production` corre `node scripts/check-copy.mjs --json`: sale 1, `structural` está vacío y el conjunto de reglas de `content` es solo PENDING y MISSING; anota cuántas hay de cada una. Corre `npm run build` sin `PUBLIC_ENV` (sale 0) y `PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist` (sale 1 y nombra `index.html` y `privacidad/index.html` por el relleno 'FALTA CONFIRMAR'). Corre `PUBLIC_ENV=production npm run build`: debe salir distinto de 0 en `prebuild` sin reconstruir `dist` (compara la fecha de modificación de `dist/index.html` antes y después). Corre además `PUBLIC_ENV=production node scripts/check-photos.mjs` (02-11): mientras la aprobación de las fotos elegidas siga `pendiente` o exista un raster de una candidata no elegida, sale 1 con los pasos para cerrar la elección; sin `PUBLIC_ENV` sale 0 y solo avisa. Conclusión que debe quedar escrita en el SUMMARY: en cuanto al copy, el build de producción falla solo por PENDING y MISSING; en cuanto a las fotos, la puerta de 02-11 bloquea hasta que Ari apruebe la foto elegida de cada ranura y se cierre la elección (`LICENSES.md`).

    Hallazgos para Ari (per COPY-01 y CONTEXT). Crea `.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md` con un encabezado (fecha, número de filas de `PENDING-COPY.md`, fase y plan) y seis secciones numeradas, de las que Ari puede responder por partes. Cada ítem dice qué ve hoy el visitante, qué se necesita de Ari y la ruta del YAML que se edita (en comillas invertidas); los textos del doc se citan literales, con sus erratas, y no se tocan. Toma el contenido de los diez SUMMARY anteriores (02-01 a 02-07 y 02-09 a 02-11; solo sus secciones de erratas, hallazgos, desviaciones, decisiones y preguntas para Ari), de `PENDING-COPY.md`, de `src/assets/photos/LICENSES.md` y de la lista de abajo; lo que un SUMMARY reporte y no esté en las secciones fijas va en la sección 6. (1) Decisiones de alto impacto: el morado de marca (la página usa #4228D1 por decisión de Juan del 2026-09-19, que es el color del logo, del isotipo, de la muestra del BrandBook y del moodboard; solo el texto de la página 8 del BrandBook dice #73187F, R115 G24 B127, Pantone 248 U, junto a un círculo dibujado en #4228D1, y Ari confirma cuál es el oficial: si dijera otro valor se cambia el token, las razones medidas de `scripts/lib/contrast.mjs` y los rellenos de los SVG oficiales con `scripts/brand/extract-artboards.mjs`); las variantes de logo (el logo horizontal y el imagotipo existen solo sobre blanco, así que sobre oscuro y sobre morado se usa el logo apilado, mesas 08 y 03; el emblema sobre oscuro no existe porque la mesa 25 trae texto morado sobre oscuro, 1.88, y se pide a Ari una versión con texto crema; los isotipos sobre oscuro, mesas 17 y 22, son una excepción de marca porque su aro morado mide 1.88 y la figura se lee por el anillo crema; y el imagotipo es un candidato a logo del header); el favicon, decidido con evidencia a 16 px como la mesa 18, un ojo con lupa; el set de avatares, que ahora son el Loopy oficial (un ojo o dos ojos con lupa sobre un disco plano) y no caras, porque el `.ai` no trae personas y el equipo no tiene fotos; el crema #F4F3E0 como superficie de píldoras y paneles y no como tono de sección; la excepción del naranja sobre morado, que sigue registrada pero sin uso; las fotos de stock de las ranuras hero y whynow, dos candidatas por ranura con fuente, autor, licencia y nota del sujeto en `src/assets/photos/LICENSES.md`, que esperan la aprobación de Ari (mientras siga `pendiente` el build de producción se bloquea); la duración de la llamada, 20 o 30 minutos (`call.duration`, y todos los textos que la usan); la terminología SEO/GEO frente a "AEO" (el titular de La solución y "Auditoría SEO + AEO completa" muestran 'FALTA CONFIRMAR' porque la guarda rechaza "AEO"); la licencia web de Hurme Geometric Sans 3 (mientras tanto se usa Outfit, una desviación de marca que Ari debe aprobar por escrito); y el consentimiento del equipo para publicar nombres y, si se quieren, biografías. (2) Texto que falta y la página muestra como 'FALTA CONFIRMAR': Para quién es (título, nombres de columna y tres líneas por columna, con el perfil de USD 200k o más al año y sin rangos de inversión mensual), FAQ (título y seis pares de pregunta y respuesta con sus temas), footer (correo y redes), política de privacidad, las dos marcas `[VERIFICAR...]` del doc (Pilar 4 y resultado 2 de la sección 4), el canal de los casos 1 a 4, los plazos y el sector que falten. (3) Texto que se muestra tal cual pero Ari debe confirmar (`pending` con texto del doc): las dos afirmaciones de mercado y la credencial del hero, los plazos de resultados y de fases, los nombres del equipo, las cifras de los casos, el cargo de Verónica en el Pilar 3 frente al de Quiénes somos, los rótulos usados como titular y las palabras de las píldoras del collage (`seo`, `geo` y `ads` salen del copy de Ari; `spy` y `team work` salen del moodboard del BrandBook y necesitan su visto bueno, que se aplica en `CHIP_WORDS` de `src/components/collage/collage-rules.mjs` y en la palabra de cada escena de `src/components/collage/scenes.mjs`). (4) Erratas del doc que la página muestra tal cual: cada una con su ruta ('estan' en `hero.description[1]` y en `agenda.title`, 'direcciôn' con circunflejo y sin punto final en el Pilar 3, el espacio doble de `problem.items[2]`, 'trafico', 'organico' y 'exito' en Resultados y Casos, la repetición 'cuándo y cuándo', la falta de punto final del caso 5 y el '$' sin moneda). (5) Afirmaciones que el UI-SPEC daba por existentes y el doc no trae ("los ads se encarecen", "el orgánico se abarata", entre las cinco de COPY-VERIFICATION): preguntar si Ari las quiere. (6) Cómo se aplica cada respuesta: se edita el `text` en `src/content/landing.es.yaml` y se cambia `status` a `verified` cuando Ari lo aprueba, se corre `npm run pending`, y `PUBLIC_ENV=production npm run build` pasa cuando la lista queda vacía y, para las fotos, cuando la aprobación de las elegidas queda registrada y la elección cerrada con los pasos de `LICENSES.md`. La prosa propia del archivo va en español neutro sin voseo ni guiones largos y pasa por `humanizer` con la herramienta Skill; las citas del doc no (per COPY-01 y la desviación 15).

    Suite completa (per el brief, lección 2; incluye la 9b y el aserto de 'textos de Ari visibles' de 02-03 y las guardas y specs de 02-07, que solo se ejecutan y nunca se editan). En este orden: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npx astro preview --port 4322`, `npx playwright test --project=chromium`, `npm run test:e2e:isolated` y, si `curl -s -o /dev/null -w '%{http_code}' https://forms.clickup.com` da 200, `npx playwright test --project=live`; si no da 200, anota 'live no ejecutado: ClickUp no responde' y reintenta más tarde sin culpar al código. Al terminar `npx astro preview stop` y detén todo servidor dev. Escribe `02-08-SUMMARY.md` con el resultado de cada comando, los cambios de `polish`, las decisiones de criterio de este plan, lo que queda para Juan y lo que queda para Ari.
  </action>
  <acceptance_criteria>
    - `npm run pending` ejecutado dos veces produce el mismo hash de `PENDING-COPY.md` y `node scripts/list-pending.mjs --check` sale 0.
    - Con `PUBLIC_ENV=production`, `check-copy --json` sale 1 con `structural` vacío y reglas solo PENDING y MISSING (conteos anotados); `npm run build` sin producción sale 0; `--dist dist` sale 1 nombrando `index.html` y `privacidad/index.html`; `PUBLIC_ENV=production npm run build` sale distinto de 0 sin cambiar `dist/index.html`; `PUBLIC_ENV=production node scripts/check-photos.mjs` sale distinto de 0 mientras la aprobación de las fotos elegidas siga `pendiente` (y sale 0 solo si Ari ya aprobó y la elección está cerrada: el SUMMARY anota cuál de los dos casos es), y sin `PUBLIC_ENV` sale 0.
    - `02-ARI-FINDINGS.md` existe con seis secciones numeradas, el encabezado con la cuenta de filas de `PENDING-COPY.md`, el morado #4228D1 vigente frente a la etiqueta #73187F de la página 8, las variantes de logo (horizontal e imagotipo solo sobre blanco, emblema sobre oscuro ausente, isotipos sobre oscuro como excepción, imagotipo como candidato al header), el favicon de la mesa 18, los avatares Loopy en lugar de caras, el crema como superficie, la excepción del naranja sobre morado sin uso, las fotos de stock pendientes de aprobación (`LICENSES.md`), las palabras de las píldoras (`spy` y `team work` del moodboard), 20 frente a 30 minutos, AEO, Hurme y Outfit, el consentimiento del equipo y las erratas con su ruta; todo primer segmento de ruta con afirmaciones `pending` o con el relleno aparece citado en comillas invertidas; las citas del doc son literales.
    - La suite completa sale en verde: guardas (66 de la fase 1 más las nuevas), contraste, build, `chromium`, `test:e2e:isolated` y `live` (o su reintento anotado).
    - `git diff --name-only BASE` está contenido en `files_modified`; ningún servidor dev o preview queda corriendo.
  </acceptance_criteria>
  <verify>
    <automated>npm run pending && H1=$(shasum PENDING-COPY.md) && npm run pending && test "$H1" = "$(shasum PENDING-COPY.md)" && node scripts/list-pending.mjs --check && npm run build && (PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist; test $? -eq 1)</automated>
    <fails_when>los dos hashes difieren (la lista no es determinista), `--check` sale distinto de 0, el build sin producción falla, o `--dist dist` no sale 1 (el relleno no llegó a `dist` o la guarda no lo ve)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const f=require('fs').readFileSync('.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md','utf8');const q=String.fromCharCode(96);const hits=r.content.filter(v=>v.rule==='PENDING'||v.rule==='MISSING');const segs=new Set(hits.map(v=>v.path.split(/[.[]/)[0]));const miss=[...segs].filter(x=>!f.includes(q+x));const secs=(f.match(/^## [1-6]\./gm)||[]).length;const ok=r.structural.length===0&&r.content.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&segs.size>0&&miss.length===0&&secs===6&&f.includes('#73187F')&&f.includes('#4228D1')&&f.includes('LICENSES.md')&&f.includes('team work')&&f.includes('mesa 18');process.exit(ok?0:1)})"</automated>
    <fails_when>`structural` no está vacío, `content` trae una regla distinta de PENDING y MISSING, el archivo de hallazgos no tiene las seis secciones numeradas, le faltan los dos morados, la nota de las fotos (`LICENSES.md`), la de las píldoras (`team work`) o la del favicon (`mesa 18`), o algún primer segmento de ruta con afirmaciones pendientes no está citado</fails_when>
    <automated>node -e "const {spawnSync:s}=require('child_process');const fs=require('fs');const t=fs.statSync('dist/index.html').mtimeMs;const r=s('npm',['run','build'],{env:{...process.env,PUBLIC_ENV:'production'},encoding:'utf8'});process.exit(r.status!==0&&fs.statSync('dist/index.html').mtimeMs===t?0:1)"</automated>
    <fails_when>el build de producción sale 0 (la guarda no bloquea) o reconstruyó `dist` (el bloqueo no ocurrió en `prebuild`)</fails_when>
    <automated>node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && npx playwright test --project=chromium && npm run test:e2e:isolated</automated>
    <fails_when>salida distinta de 0, el resumen de `node --test` muestra "# fail" mayor que 0 o un spec de navegador falla o queda en `skip` (con el preview de 4322 en marcha y `dist` reconstruido tras el último cambio)</fails_when>
  </verify>
  <done>La lista de pendientes es determinista, producción falla solo por PENDING y MISSING, Ari tiene un único archivo con todo lo que debe decidir, confirmar o corregir, y la suite completa de la fase está en verde (o `live` reintentado y anotado); commit hecho y SUMMARY escrito.</done>
</task>

</tasks>

<verification>
1. `node --test tests/guards/*.test.mjs` sale 0: las 66 pruebas de la fase 1 más las de 02-07 (VERIFICAR 3b a 3e, `motion-budget`, `phase2-static`); la 9b y el aserto de 'textos de Ari visibles' (de 02-03) pasan sin editarse aquí.
2. `node scripts/check-contrast.mjs` sale 0 y `npm run build` sin `PUBLIC_ENV` sale 0 (con `prebuild` y `postbuild`).
3. `npm run pending` es determinista y `node scripts/list-pending.mjs --check` sale 0.
4. `PUBLIC_ENV=production node scripts/check-copy.mjs --json` sale 1 con `structural` vacío y solo reglas PENDING y MISSING; `--dist dist` sale 1 por el relleno; `PUBLIC_ENV=production npm run build` falla en `prebuild` sin reconstruir `dist`.
5. Con `npx astro build && npx astro preview --port 4322`: `npx playwright test --project=chromium` (incluidos `motion.spec.ts` y `phase-closing.spec.ts`), `npm run test:e2e:isolated` y `npx playwright test --project=live` si ClickUp responde; `npx astro preview stop` al final.
6. axe de humo sin violaciones `critical` ni `serious` en `/` y `/privacidad/` a 320, 390 y 1280 px; la matriz de seis anchos pasa con `reduce` y con `no-preference`.
7. `git diff --name-only BASE` de cada tarea está contenido en `files_modified` (más los archivos de `polish` declarados en el SUMMARY) y `git diff BASE -- src/content/landing.es.yaml` queda vacío.
8. Verificaciones humanas para Juan, anotadas en `02-VISUAL-LOG.md`: mirada real al movimiento con ambas preferencias, VoiceOver sobre `<details>` en Safari y aprobación de avatares, favicon, variantes de logo, palabras de las píldoras y fotos.
</verification>

<success_criteria>
- La página completa pasa la matriz de seis anchos con ambas preferencias, el espaciado de texto forzado, sin JavaScript, con ClickUp bloqueado, el área de salvado del logo, el peso y las peticiones, y el axe de humo sin violaciones críticas ni serias, y cumple los 8 rasgos de vibra con evidencia en `02-VISUAL-LOG.md` (DSGN-03, DSGN-04).
- `impeccable` (`critique`, `audit`, `polish`, `harden`, `adapt`) y `design-taste-frontend` se invocaron con la herramienta Skill y el log lo registra por ronda, con tres ciclos como máximo (DSGN-04).
- Ari tiene un único archivo, `02-ARI-FINDINGS.md`, con decisiones, faltantes, confirmaciones y erratas; `PENDING-COPY.md` es determinista y el build de producción falla solo por lo pendiente (COPY-01).
- La suite completa está en verde, con las aserciones de 02-03 y las guardas de 02-07 ejecutadas sin editarse, y la fase 2 queda lista para la verificación y para la fase 3.
</success_criteria>

<output>
Create `.planning/phases/02-secciones-marca-y-copy/02-08-SUMMARY.md` when done
</output>
