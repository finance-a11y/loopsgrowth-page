---
phase: 02-secciones-marca-y-copy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/brand/logo-horizontal.svg
  - src/assets/brand/isotipo.svg
  - src/components/brand/Logo.astro
  - src/components/SiteHeader.astro
  - public/favicon.svg
  - public/favicon.ico
  - src/components/collage/collage-rules.mjs
  - src/components/collage/CollageSprite.astro
  - src/components/collage/CollagePiece.astro
  - src/components/collage/AgendaCollage.astro
  - src/components/collage/Avatar.astro
  - src/pages/marca/[sheet].astro
  - tests/e2e/brand-assets.spec.ts
  - tests/guards/brand-assets.test.mjs
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "El logo horizontal sale del vectorial de Ari (LOGO_LOOPSGRWOTH.ai, mesa 6 de 32) como SVG en línea en el header, con role='img', nombre accesible igual a brand.name del YAML ('Loops Growth'), sin aria-hidden, sin enlace en `/` y con enlace a `/` en cualquier otra ruta (CONTEXT, Estilo visual: logo e isotipo tomados del Drive de Ari; UI-SPEC, Surface Inventory: Logo)."
    - "El área de salvado se leyó del BrandBook (página 7): X en los cuatro lados, con X igual a la altura del logo o del isotipo. Se fija como `--logo-clear` en Logo.astro y corrige la suposición de media altura del UI-SPEC (decisión 14). Ningún elemento de contenido entra en esa zona a 320, 640, 768 y 1280 px."
    - "Alturas mínimas con `max()`: 32 px el logo horizontal y 24 px el isotipo, aunque un consumidor cambie `--logo-h`."
    - "El isotipo Loopy (mesa 13 del .ai, ojos con lupa) existe como SVG y reemplaza `public/favicon.svg` y `public/favicon.ico`; el `.ico` trae 16, 32 y 48 px."
    - "El sprite `CollageSprite` se monta una sola vez por página, dentro de SiteHeader (BaseLayout es de 02-01 en la misma wave), fuera de flujo, con aria-hidden='true' y nunca con `display: none`. Los símbolos usan el prefijo `cs-` y las piezas los invocan con `<use href>`."
    - "Toda pieza de collage es un `<svg>` en línea con aria-hidden='true' y focusable='false', sin `<title>`, sin texto, sin raster, sin degradados, sin filtros ni sombras difusas, con contorno de 3 px no escalable en `var(--collage-stroke)` y rellenos solo con `var(--color-brand-*)` (CONTEXT, Estilo visual: SVG estático en línea)."
    - "Las reglas de color por tono se aplican al construir con `assertToneSafe`: sobre dark y purple el contorno es blanco y no hay relleno morado; sobre light y yellow el contorno es oscuro; nunca amarillo sobre yellow ni oscuro sobre dark ni sobre purple."
    - "Sin movimiento por defecto: ninguna pieza declara animation, transition, @keyframes, `<animate>` ni `<set>`; `document.getAnimations().length` es 0 con prefers-reduced-motion en reduce y en no-preference. Las piezas de `CollagePiece` exponen `data-collage-piece` y la variable `--r` (rotación estática); los ganchos `data-piece`, `data-pupil` y `--r-from` del movimiento del hero son de HeroCollage, que crea 02-01."
    - "Presupuesto de peso medido sobre el HTML construido: sprite hasta 10 KB, AgendaCollage hasta 4 KB, cada Avatar hasta 2,5 KB, colección de la hoja hasta 28 KB (con los 8 KB de HeroCollage de 02-01 queda bajo los 40 KB del UI-SPEC) y `dist/index.html` menor a 60 KB."
    - "Todo se ve con JavaScript desactivado; este plan no agrega JS propio ni dependencias."
    - "La hoja de revisión `/marca/hoja/` existe solo fuera de producción (getStaticPaths vacío con PUBLIC_ENV=production), lleva noindex por BaseLayout y sus rótulos son ids de pieza, no copy de la landing."
    - "Suposición explícita (UI Considerations, fila assets, resuelta con evidencia el 2026-09-19): el vectorial y el BrandBook estaban en el Drive público de Ari, así que no se redibuja nada. Hallazgo para Ari y Juan: los colores del vectorial (#4228D1 principal, #6C61DB iris, #F4F3E0 crema, #1E1E1E pupila) no coinciden con el morado #73187F del texto del BrandBook ni con tokens.css. El logo se entrega con los colores del archivo de Ari; alinearlo a la paleta es editar dos assets."
    - "Prohibido: editar tokens.css, global.css, BaseLayout.astro, index.astro, HeroSkeleton.astro, HeroCollage.astro, AgendaSection.astro, landing.es.yaml o a11y-base.spec.ts (los toca o crea 02-01 en la misma wave)."
    - "Prohibido: set:html, hex en src/components (los hex del logo viven solo en src/assets/brand/*.svg y public/favicon.svg), texto o `<title>` dentro de SVG decorativo, versionar los originales .ai y .pdf del Drive, ejecutar impeccable init o document (los hace 02-01) y leer .env."
    - statement: "Los cuatro avatares se leen como un set uniforme sobre círculos suaves (lista de vibra, punto 5)"
      verification: backstop
  artifacts:
    - path: src/assets/brand/logo-horizontal.svg
      provides: "Logo horizontal 'loopsgrowth' recortado a la caja del arte, sin width ni height"
      contains: "viewBox"
    - path: src/assets/brand/isotipo.svg
      provides: "Isotipo Loopy (ojos con lupa) con los colores del vectorial"
      contains: "viewBox"
    - path: src/components/brand/Logo.astro
      provides: "Logo e isotipo con --logo-clear, --logo-h y alturas mínimas"
      contains: "--logo-clear"
    - path: src/components/collage/CollageSprite.astro
      provides: "Sprite inline con los símbolos cs-*"
      contains: "cs-lupa"
    - path: src/components/collage/CollagePiece.astro
      provides: "Envoltorio de una pieza con validación de tono en build"
      contains: "assertToneSafe"
    - path: src/components/collage/AgendaCollage.astro
      provides: "Composición 480x480 de #agenda, oculta bajo 1024 px"
      contains: "data-collage"
    - path: src/components/collage/Avatar.astro
      provides: "Cuatro avatares Loopy 120x120"
      contains: "variant"
    - path: src/pages/marca/[sheet].astro
      provides: "Hoja de revisión visual solo fuera de producción"
      contains: "getStaticPaths"
  key_links:
    - from: src/components/SiteHeader.astro
      to: src/components/brand/Logo.astro
      via: "<Logo name={data.brand.name.text} href={homeHref} /> dentro del contenedor .wordmark"
      pattern: "Logo"
    - from: src/components/brand/Logo.astro
      to: src/assets/brand/logo-horizontal.svg
      via: "import de SVG como componente de Astro"
      pattern: "logo-horizontal.svg"
    - from: src/components/SiteHeader.astro
      to: src/components/collage/CollageSprite.astro
      via: "montaje único del sprite antes del header"
      pattern: "CollageSprite"
    - from: src/components/collage/CollagePiece.astro
      to: src/components/collage/collage-rules.mjs
      via: "assertToneSafe(tone, fills) en el frontmatter"
      pattern: "assertToneSafe"
    - from: src/pages/marca/[sheet].astro
      to: src/lib/site.ts
      via: "isProduction vacía getStaticPaths"
      pattern: "isProduction"
---

<objective>
Entregar los activos de marca de la fase 2: el logo horizontal y el isotipo Loopy tomados del vectorial de Ari con su área de salvado (DSGN-02), y las primitivas, la composición de #agenda y los avatares del collage pop como SVG estático en línea (DSGN-01), todo construido con las skills `impeccable` y `design-taste-frontend` (DSGN-04). El HeroCollage no es de este plan: lo crea 02-01 como composición autocontenida y lo monta en el Hero.

Purpose: los planes de secciones (02-03 a 02-06) consumen estas piezas por nombre; el CTA final depende del collage, el header del logo y el equipo de los avatares. Este plan corre en la wave 1 en paralelo con 02-01 y no comparte archivos con él.

Output: `Logo.astro` en el header (tracer), isotipo y favicon, `CollageSprite` con `CollagePiece`, `AgendaCollage`, cuatro `Avatar`, la hoja `/marca/hoja/` para revisar todo a 5 anchos y las pruebas `brand-assets`.

Discovery (nivel 1): la importación de un `.svg` como componente de Astro se confirmó con Context7 (`/withastro/docs`, guía de imágenes: `import Logo from './logo.svg'` y atributos nativos por props). Sin paquetes nuevos: SVGO ya está instalado como dependencia de Astro (`node_modules/.bin/svgo`) y no se agrega al `package.json`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@src/components/SiteHeader.astro
@src/styles/tokens.css

<!-- 02-UI-SPEC.md pesa 73 KB: NO se carga entera. Cada tarea indica en read_first los encabezados a leer con grep -n '^## ' y Read con offset y limit. -->
<!-- Lo que ya se investigó al planificar (no repetir la investigación):
     - Drive público de branding (carpeta 1byOfW_MgbJ5YrX8UY2uwgek2EyZsKlhe): LOGO_LOOPSGRWOTH.ai (id 1fBrYK6dJJwBRERVetXYePqSSUIMrcZ0C) y BrandBook_loopgrowth.pdf (id 1qxC5Tow2cx4fAAW-Kt0tHVw-E6szyVtK). Se bajan con curl -L 'https://drive.google.com/uc?export=download&id=<id>'.
     - El .ai es un PDF de 32 mesas de 800x800 pt. Mesa 1 logo apilado, mesa 6 logo horizontal 'loopsgrowth' sobre blanco (11 paths de letras, sin fondo ni clip), mesa 7 imagotipo con ojos en las oes, mesa 13 isotipo Loopy (ojos con lupa, 11 paths), mesa 18 un ojo con lupa, mesas 14 a 17 el isotipo sobre color.
     - BrandBook página 7 (ÁREA DE SALVADO): X en los cuatro lados. Logo horizontal e isotipo: X igual a la altura del arte. Logo apilado: el arte mide 2X y el margen X. Página 6 explica la construcción del isotipo Loopy (ojos, ojos con lupa, un ojo con lupa).
     - Colores de las mesas: rgb(25.9%,15.7%,82.0%) = #4228D1, #6C61DB, #F4F3E0 y #1E1E1E. El texto del BrandBook (página 8) dice Purblue #73187F, pero su propia muestra se dibuja en #4228D1.
-->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Logo horizontal de punta a punta, del vectorial de Ari al header, con área de salvado</name>
  <files>src/assets/brand/logo-horizontal.svg, src/components/brand/Logo.astro, src/components/SiteHeader.astro, tests/e2e/brand-assets.spec.ts</files>
  <precondition>command -v pdftocairo curl y test -x node_modules/.bin/svgo (herramientas locales; no se instala nada)</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: encabezados `## Collage and Brand Assets Contract` (viñetas de Logo e isotipo, Área de salvado y Favicon), `### 0 y 1. Header y Hero` (bloque Header) y `## Design Method and Iteration Protocol`. Localízalos con `grep -n '^## \|^### '` y lee con Read offset y limit de 60 líneas como máximo.
    - src/components/SiteHeader.astro y src/styles/tokens.css (variables `--target-min`, `--focus-width`, `--focus-offset`, `--focus-ring`, `--color-brand-white`).
    - src/content/landing.es.yaml (bloque `brand.name`) y src/lib/content.ts (`getLanding`).
    - tests/e2e/a11y-base.spec.ts: caso (a) del orden de tabulación y casos (h) y (e); solo se leen, no se editan (02-01 los toca).
    - Skills a invocar con la herramienta Skill: `design-taste-frontend` e `impeccable`.
  </read_first>
  <action>
    Per CONTEXT (Estilo visual: logo e isotipo en SVG con su área de salvado, tomados del Drive de Ari) y UI-SPEC (Collage and Brand Assets Contract, Header).
    1. Fuente. En un directorio temporal fuera del repo baja el vectorial con `curl -sL 'https://drive.google.com/uc?export=download&id=1fBrYK6dJJwBRERVetXYePqSSUIMrcZ0C' -o logo.ai` y el BrandBook con el id `1qxC5Tow2cx4fAAW-Kt0tHVw-E6szyVtK` (`file` debe decir PDF document). Ninguno se versiona. Si la descarga falla, detente y repórtalo; no redibujes el logo (CONTEXT lo permite solo si falta el vectorial, y el 2026-09-19 estaba).
    2. Conversión. `pdftocairo -svg -f 6 -l 6 logo.ai logo6.svg`: la mesa 6 es el logo horizontal 'loopsgrowth' sobre blanco (confirma con `pdftocairo -png -r 40 -f 6 -l 6` y mirando la imagen). Debe salir un SVG de 800x800 con 11 elementos path y sin fondo.
    3. Limpieza. Usa `node_modules/.bin/svgo` sin agregarlo al package.json: quita width y height de la raíz, xmlns:xlink, metadatos y atributos redundantes de relleno, precisión 2, conserva el viewBox. Mide la caja real del arte en Chromium (getBBox de la raíz, con Playwright) y recorta el viewBox a esa caja sin holgura interna: el área de salvado es CSS, no arte. Sube el color a la raíz: atributo `fill` con el hex entregado en minúsculas en el elemento svg (los paths lo heredan); es el único lugar con el hex del logo. Guarda como `src/assets/brand/logo-horizontal.svg`.
    4. Componente. Crea `src/components/brand/Logo.astro` con props `name: string` (obligatoria, viene de `brand.name.text`), `href?: string`, `variant: 'horizontal'` (la unión crece a `'isotipo'` en la tarea 2) y `class?: string`. Importa el SVG como componente de Astro (`import LogoHorizontal from '../../assets/brand/logo-horizontal.svg'`); si este build no lo soporta, pega los paths en la plantilla con aria-labelledby y un elemento de título, sin insertar HTML crudo. Contenedor `<span class="brand-logo" data-variant="horizontal">`; con `href` un `<a class="brand-logo__link">` envuelve el SVG, sin `href` no hay enlace. El SVG lleva `role="img"`, `aria-label={name}` y `focusable="false"`, sin aria-hidden. Estilos en el `<style>` del componente y `:global(svg)` para el interior: `--logo-min: 32px`, `--logo-h: 2rem`, `--logo-clear: max(var(--logo-h), var(--logo-min))` (la X de la página 7 del BrandBook), alto `max(var(--logo-h), var(--logo-min))`, ancho auto, `display: block`. El enlace es `inline-flex` con `min-height: var(--target-min)` y foco con `outline: var(--focus-width) solid var(--focus-ring)` y `outline-offset: var(--focus-offset)`. Regla de prevención: bajo `:where([data-tone="dark"], [data-tone="purple"])` el relleno del SVG pasa a `var(--color-brand-white)`. Sin hex, sin animación ni transición. Comentario de cabecera en español que explique `--logo-clear` y su origen.
    5. Header. En `src/components/SiteHeader.astro` importa `Logo`, define `const homeHref = Astro.url.pathname === '/' ? undefined : '/';` y reemplaza el texto del wordmark por `<Logo name={data.brand.name.text} href={homeHref} />` dentro de `<span class="wordmark">`. Conserva la clase y su regla de peso 700: la aserción (h) de a11y-base sigue en verde sin editar ese archivo, que es de 02-01 (queda obsoleta; 02-07 puede retirarla). No toques el CTA, el padding ni los breakpoints. Actualiza el comentario de cabecera.
    6. Skills (DSGN-04). Con la herramienta Skill invoca `design-taste-frontend` (lectura de diseño declarada: 'landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética'; diales 7/3/4) e `impeccable` (verbos `shape` y `adapt` sobre el header; no ejecutes `init` ni `document`, los hace 02-01). Una ronda: captura `test-results/phase2/lote0-logo-320.png` y `lote0-logo-1280.png` (no versionadas), critica alineación y tamaño contra el contrato y corrige en un solo lote.
    7. Pruebas. Crea `tests/e2e/brand-assets.spec.ts` con `test.describe('logo horizontal en el header')`. El nombre esperado sale de `es.brand.name.text` leyendo el YAML con `yaml.parse`, como hace a11y-base (nunca una cadena escrita a mano). Casos: (a) a 320, 640, 768 y 1280 px el SVG del header tiene role img, nombre accesible igual al del YAML, sin aria-hidden, focusable false, no está dentro de un enlace en `/`, alto de 32 px o más y proporción igual a la del viewBox con 2 % de tolerancia; (b) a esos anchos ningún elemento de contenido (con texto propio, a, button, svg, img o iframe, excluyendo ancestros del logo y el sprite) entra en el rectángulo del logo ensanchado en `--logo-clear` (lee la variable asignándola al ancho de un elemento sonda); (c) `documentElement.scrollWidth <= innerWidth` a 320 y 1280 px; (d) con `javaScriptEnabled: false` el logo es visible con caja no vacía; (e) el relleno computado del primer path no es none ni negro puro.
    Protocolo de navegador (lección de la fase 1): `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio de código y `npx astro preview stop` al final. No leas `.env` ni envíes el formulario de ClickUp.
  </action>
  <acceptance_criteria>
    - `src/assets/brand/logo-horizontal.svg` existe, pesa menos de 5120 bytes, tiene `viewBox`, y su raíz no trae width ni height: `! head -c 300 src/assets/brand/logo-horizontal.svg | grep -Eq '<svg[^>]*(width|height)='`.
    - El SVG no trae metadatos ni superficie activa: `! grep -Eq '<(metadata|script|image|foreignObject)|xlink|[[:space:]]on[a-z]+=' src/assets/brand/logo-horizontal.svg`.
    - `grep -c -e '--logo-clear' -e '--logo-min' src/components/brand/Logo.astro` da 2 o más; `grep -c 'set:html' src/components/brand/Logo.astro` da 0; ningún hex en `Logo.astro` ni en `SiteHeader.astro`: `! grep -nE '#[0-9a-fA-F]{3}([^0-9a-zA-Z_-]|$)|#[0-9a-fA-F]{6}([^0-9a-zA-Z_-]|$)' src/components/brand/Logo.astro src/components/SiteHeader.astro`.
    - Con el build servido, a 320, 640, 768 y 1280 px el SVG del header mide 32 px o más de alto, su nombre accesible es 'Loops Growth' (del YAML), no hay elemento de contenido dentro de `--logo-clear` y no hay scroll horizontal a 320 ni a 1280 px.
    - En `/` el logo no es enlace y la aserción de orden de tabulación de a11y-base (caso a) y la de pesos (caso h) siguen verdes.
    - `git diff --name-only -- src/styles src/layouts src/pages/index.astro src/components/HeroSkeleton.astro src/components/AgendaSection.astro src/content tests/e2e/a11y-base.spec.ts` no lista nada.
    - Existen las capturas `test-results/phase2/lote0-logo-320.png` y `lote0-logo-1280.png`.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts tests/e2e/a11y-base.spec.ts; s=$?; npx astro preview stop; exit $s</automated>
    <fails_when>El logo se renderiza como imagen o fondo CSS; tiene aria-hidden; su nombre accesible no viene del YAML; es enlace en `/`; mide menos de 32 px; un elemento de contenido entra en la zona de `--logo-clear` a algún ancho; el SVG conserva width o height de 800 pt; hay scroll horizontal; el peso de `.wordmark` deja de ser 700 o cambia el orden de tabulación de a11y-base.</fails_when>
  </verify>
  <done>El logo horizontal del Drive de Ari se ve en el header a 320 y 1280 px, es accesible, no es enlace en `/`, respeta `--logo-clear`, no rompe ninguna prueba de la fase 1 y queda en un commit.</done>
</task>

<task type="auto">
  <name>Task 2: Isotipo Loopy, favicon y primera versión de la hoja de revisión (identidad)</name>
  <files>src/assets/brand/isotipo.svg, public/favicon.svg, public/favicon.ico, src/components/brand/Logo.astro, src/pages/marca/[sheet].astro, tests/e2e/brand-assets.spec.ts</files>
  <read_first>
    - src/components/brand/Logo.astro y tests/e2e/brand-assets.spec.ts (de la tarea 1).
    - 02-UI-SPEC.md: viñetas Logo e isotipo, Área de salvado y Favicon de `## Collage and Brand Assets Contract`.
    - src/lib/site.ts (`isProduction`) y src/layouts/BaseLayout.astro (el `noindex` fuera de producción y el slot).
    - Páginas 6, 7 y 10 del BrandBook (`pdftocairo -png -r 70 -f 6 -l 7` sobre el PDF ya descargado) para la construcción del isotipo y su zona de salvado.
  </read_first>
  <action>
    Per CONTEXT (Estilo visual) y UI-SPEC (favicon: reemplazar los de Astro por el isotipo).
    1. Isotipo. Del mismo `logo.ai` convierte la mesa 13 (`pdftocairo -svg -f 13 -l 13`): isotipo Loopy, ojos con lupa, 11 paths con cuatro rellenos entregados. Limpia como en la tarea 1, pero cada path conserva su `fill` en hex (es multicolor), viewBox recortado a la caja del arte, menos de 4 KB. Guarda `src/assets/brand/isotipo.svg`.
    2. Logo.astro. Amplía `variant` a `'horizontal' | 'isotipo'` e importa el SVG del isotipo. Para `[data-variant="isotipo"]`: `--logo-min: 24px` y `--logo-h: 2.5rem`; `--logo-clear` sigue siendo el alto efectivo (X del BrandBook, página 7). Sin variante oscura del isotipo: el comentario de cabecera indica que la mesa 17 del .ai es la versión sobre oscuro si algún día hace falta.
    3. Favicon. `public/favicon.svg` es el isotipo como archivo suelto: `xmlns`, viewBox cuadrado (centra el arte con margen simétrico de al menos 4 % por lado), sin script ni referencias externas, menos de 3 KB; los hex se permiten aquí y en los assets. `public/favicon.ico`: renderiza ese SVG con Chromium (Playwright, fondo transparente) a 16, 32 y 48 px y empaqueta un ICO con las tres imágenes, con un script local de una sola vez en el directorio temporal (Pillow si está disponible o un empaquetado manual de PNG dentro de ICO con Node). No agregues nada al repositorio ni al package.json. Mira el 16 px: si los dos ojos no se leen, usa la mesa 18 (un ojo con lupa, paso 3 de la construcción del BrandBook, página 6) solo para el favicon y márcalo para aprobación de Ari en el SUMMARY. BaseLayout no enlaza íconos y es de 02-01: el navegador pide `/favicon.ico` por convención; anota en el SUMMARY que enlazar el SVG con un link de tipo icon queda para 02-07 o quien toque BaseLayout.
    4. Hoja de revisión. Crea `src/pages/marca/[sheet].astro`: `export function getStaticPaths()` devuelve una lista vacía si `isProduction` (de `../../lib/site`) y `[{ params: { sheet: 'hoja' } }]` en otro caso; usa `BaseLayout` con un `title` propio (el aviso de ruta vacía en producción es esperado). Esta versión solo trae identidad: logo horizontal e isotipo sobre los tonos `light` y `dark`, cada uno dentro de un contenedor con `padding: var(--logo-clear)` y borde punteado que dibuja la zona de salvado. Los tonos `yellow` y `dark` los agrega 02-01 en paralelo: la hoja declara en su `<style>` un respaldo con `:where([data-tone="dark"])` y `:where([data-tone="yellow"])` (especificidad 0) para `--surface`, `--on-surface` y `--collage-stroke` con `var(--color-brand-*)`, de modo que los tokens reales ganen cuando existan y la hoja sirva antes de fusionar. Rótulos: solo ids de pieza en elementos code, sin frases; es herramienta interna, no copy de la landing.
    5. Skills e iteración (DSGN-04). `impeccable` (`shape` del isotipo en contexto y `colorize` para contrastar los cuatro colores entregados con la paleta) y `design-taste-frontend`. Captura la hoja en `test-results/phase2/lote0-marca-{320,390,768,1024,1280}.png` con reduce, sin JavaScript y con ClickUp bloqueado (`E2E_BLOCK_CLICKUP=1`); una ronda de crítica y una de confirmación como máximo.
    6. Pruebas (edita brand-assets.spec.ts). En la hoja, a los 5 anchos: isotipo de 24 px o más de alto, logo de 32 px o más; sobre el tono dark el relleno computado del logo horizontal es blanco; sin scroll horizontal. Archivos: `/favicon.ico` responde 200, empieza con los bytes 00 00 01 00, trae tres entradas de 16, 32 y 48 px y pesa más de 655 bytes; `/favicon.svg` es texto con viewBox, sin script ni referencias http.
    Cierre de la tarea: comprueba la exclusión de producción con un build directo (sin los hooks de npm, que fallan por afirmaciones pending) y reconstruye en modo normal al terminar.
  </action>
  <acceptance_criteria>
    - `src/assets/brand/isotipo.svg` pesa menos de 4096 bytes, tiene viewBox, sin width ni height en la raíz, sin metadatos, script, image, foreignObject ni xlink, y usa exactamente 4 valores de fill distintos.
    - `public/favicon.svg` pesa menos de 3072 bytes y su viewBox es cuadrado (ancho igual a alto); `public/favicon.ico` supera 655 bytes, empieza con 00 00 01 00 y declara 3 imágenes de 16, 32 y 48 px.
    - `dist/marca/hoja/index.html` existe en el build normal y `dist/marca` no existe en el build con `PUBLIC_ENV=production`.
    - En la hoja, el isotipo mide 24 px o más y el logo 32 px o más a 320, 390, 768, 1024 y 1280 px; sobre dark el logo horizontal se pinta con `rgb(255, 255, 255)`; no hay scroll horizontal.
    - Ningún hex en `src/components` ni `src/pages`: `! grep -rnE '#[0-9a-fA-F]{3}([^0-9a-zA-Z_-]|$)|#[0-9a-fA-F]{6}([^0-9a-zA-Z_-]|$)' src/components src/pages`.
    - Existen las capturas `lote0-marca-{320,390,768,1024,1280}.png` y el SUMMARY registra la ronda de crítica y el hallazgo de colores (#4228D1 frente a #73187F).
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && test -f dist/marca/hoja/index.html && npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts; s=$?; npx astro preview stop; PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build >/dev/null 2>&1; test ! -e dist/marca; p=$?; npx astro build >/dev/null 2>&1; [ $s -eq 0 ] && [ $p -eq 0 ]</automated>
    <fails_when>`favicon.ico` sigue en 655 bytes o le faltan tallas; el isotipo pierde un color entregado o gana metadatos; la hoja se genera con PUBLIC_ENV=production; el isotipo mide menos de 24 px; el logo sobre dark no cambia a blanco; la hoja desborda a 320 px; aparece un hex en src/components o src/pages.</fails_when>
  </verify>
  <done>Isotipo Loopy y favicon salen del vectorial de Ari, la hoja `/marca/hoja/` existe solo fuera de producción con la identidad a 5 anchos, y todo queda en un commit con la fase 1 en verde.</done>
</task>

<task type="auto">
  <name>Task 3: Sprite y primitivas de collage con validación de tono en el build</name>
  <files>src/components/collage/collage-rules.mjs, src/components/collage/CollageSprite.astro, src/components/collage/CollagePiece.astro, src/components/SiteHeader.astro, src/pages/marca/[sheet].astro, tests/guards/brand-assets.test.mjs, tests/e2e/brand-assets.spec.ts</files>
  <read_first>
    - 02-UI-SPEC.md: `## Collage and Brand Assets Contract` completo (tabla de piezas y Reglas de color por tono, Sin texto, Peso), `## Color` (lista cerrada de acento y Reglas derivadas de los tonos nuevos) y `### Presupuesto de movimiento`. Localiza con `grep -n '^## \|^### '` y lee con offset y limit de 60 líneas como máximo.
    - src/components/SiteHeader.astro, src/components/brand/Logo.astro y src/pages/marca/[sheet].astro (de las tareas 1 y 2); src/styles/tokens.css para los nombres `--color-brand-*`.
    - tests/guards/contrast.test.mjs (solo para imitar el estilo de las pruebas de guarda y de mutación).
    - Skills a invocar con la herramienta Skill: `design-taste-frontend` e `impeccable`.
  </read_first>
  <action>
    Per CONTEXT (Estilo visual: lupas, ojos y clics como SVG estático en línea, aria-hidden y focusable false, movimiento solo bajo motion-safe) y UI-SPEC (Collage and Brand Assets Contract).
    1. `src/components/collage/collage-rules.mjs` (ESM plano con JSDoc, lo importan Astro y `node --test`). Exporta `TONES` (light, yellow, dark, purple), `BRAND_COLORS` (yellow, orange, purple, white, dark) con `fillVar(color)` que devuelve `var(--color-brand-<color>)`, `ALLOWED_FILLS` (light: yellow, white, purple, orange, dark; yellow: white, purple, orange, dark; dark: yellow, orange, white; purple: yellow, orange, white), `PIECES` (nombre a `{ symbol, w, h, fixed? }`: lupa 240x240; ojos-izq, ojos-der y ojos-abajo 120x64; clic 80x80; loop 160x160; destello 24x24; puntos 96x72; sticker-ojos, sticker-clic y sticker-lupa 64x64; chip-lupa con fixed yellow, chip-ojos con fixed orange, chip-loop con fixed purple y chip-clic con fixed white, todos 64x64) y `assertToneSafe(tone, colors)`, que lanza un Error en español con pieza, tono y color si algún color usado (a, b, c y fixed) no está en `ALLOWED_FILLS[tone]`, y devuelve el contorno esperado (white en dark y purple, dark en light y yellow).
    2. `CollageSprite.astro`, sin props. Un solo `<svg class="collage-sprite" aria-hidden="true" focusable="false" width="0" height="0">` con 15 `<symbol id="cs-...">`: cs-lupa, cs-ojos-izq, cs-ojos-der, cs-ojos-abajo, cs-clic, cs-loop, cs-destello, cs-puntos, cs-sticker-ojos, cs-sticker-clic, cs-sticker-lupa, cs-chip-lupa, cs-chip-ojos, cs-chip-loop, cs-chip-clic, con los viewBox de `PIECES`. Posición fuera de flujo (absoluta, tamaño 0, overflow hidden); no lo ocultes con la propiedad display, porque el contrato pide que siga renderizado. Un `<style is:global>` define las clases de relleno `cs-a`, `cs-b` y `cs-c` (leen `--cf-a`, `--cf-b`, `--cf-c` con respaldo `var(--color-brand-yellow)`, `var(--color-brand-orange)` y `var(--color-brand-white)`), `cs-w` (blanco) y `cs-k` (oscuro, para pupilas), y la clase de contorno `cs-o`: color `var(--collage-stroke, var(--color-brand-dark))`, ancho 3px, `vector-effect: non-scaling-stroke`, uniones y remates redondeados. Geometría según el contrato: formas planas de color de marca, sin degradados, sin filtros SVG, sin texto ni elemento de título. lupa: aro y lente rellena (cs-a) con mango en otro color (cs-b) a 45 grados; ojos: dos óvalos blancos con contorno y pupilas oscuras, con la mirada a la izquierda, a la derecha o abajo; clic: cursor en flecha con contorno y ráfaga de 3 a 5 trazos; loop: anillos concéntricos rellenos con regla evenodd y contorno (no líneas sueltas); destello: marca más de 4 puntas; puntos: rejilla de círculos rellenos sin contorno; pegatinas: ojos, clic y lupa mini con rotación fija entre -4 y 6 grados dentro del símbolo; chips: círculo con contorno y glifo (lupa sobre amarillo, ojos sobre naranja, loop morado con trazos blancos, clic sobre blanco). Deja unos 2 px de margen interior para que el contorno no se corte. En los comentarios describe estas reglas con otras palabras (por ejemplo 'sin movimiento propio'), porque las pruebas rechazan esos literales en el archivo.
    3. `CollagePiece.astro`. Props: `name` (clave de `PIECES`), `tone` (obligatoria), `a?`, `b?`, `c?` (colores de marca), `size?` (ancho CSS), `rotate?` (grados) y `class?`. El frontmatter llama `assertToneSafe(tone, ...)` y traduce los colores con `fillVar` a las variables `--cf-a`, `--cf-b`, `--cf-c` en el atributo style (solo `var(--color-brand-*)`, nunca hex); `rotate` fija `--r` (transform estático). Renderiza `<svg class="collage-piece" data-collage data-collage-piece={name} viewBox aria-hidden="true" focusable="false"><use href="#cs-..." /></svg>` con `max-width: 100%`, `height: auto`, `display: block`, `overflow: visible`. El atributo se llama `data-collage-piece` y no `data-piece` para no chocar con los ganchos `data-piece` de la composición del hero que crea 02-01 (02-07 selecciona esos). Sin título, sin texto.
    4. Sprite en la página. En `SiteHeader.astro` importa `CollageSprite` y renderízalo como primer nodo, antes del `<header>`: queda una sola vez por página, fuera del landmark. BaseLayout es de 02-01 en esta wave; mover esa línea a BaseLayout después es trivial y se anota en el SUMMARY. No cambies nada más del header.
    5. Hoja (edita `[sheet].astro`). Agrega la sección de primitivas: para cada tono (light, yellow, dark, purple) y cada pieza de `PIECES`, renderiza `CollagePiece` con combinaciones permitidas (elige a y b dentro de `ALLOWED_FILLS[tone]`, sin repetir el color del fondo), en rejilla con `min-width: 0` y rótulo en elemento code.
    6. Guarda `tests/guards/brand-assets.test.mjs` (node:test, ESM). (i) Política independiente de la tabla: pares prohibidos (purple en dark, purple en purple, yellow en yellow, dark en dark, dark en purple) no están en `ALLOWED_FILLS`, y `assertToneSafe` lanza en cada uno y pasa en los permitidos con el contorno correcto. (ii) Mutación: una copia de la tabla con un par prohibido agregado es detectada por la misma comprobación (prueba de que no es vacía). (iii) Cada `PIECES[*].symbol` tiene exactamente un `<symbol id>` en CollageSprite.astro y viceversa. (iv) En una lista explícita `OWNED` de los archivos de este plan (`CollageSprite.astro`, `CollagePiece.astro`, `collage-rules.mjs` y `Logo.astro`; la tarea 4 agrega `AgendaCollage.astro` y `Avatar.astro`; no barras el directorio entero, porque el archivo de la composición del hero es de 02-01 y trae sus propias reglas): sin hex, sin inserción de HTML crudo, sin animaciones ni transiciones declaradas, sin elementos de animación SMIL, sin degradados, sin filtros, sin texto, sin raster, sin foreignObject, sin script y sin elemento de título; todo `<svg` de collage lleva aria-hidden true y focusable false, y el de Logo lleva role img; más una mutación por cada regla sobre cadenas de prueba. (v) Sobre `dist` (falla con mensaje 'ejecuta npx astro build' si falta): `dist/index.html` trae exactamente un `class="collage-sprite"`, pesa menos de 61440 bytes y el sprite menos de 10240; en `dist/marca/hoja/index.html` cada `<use href="#cs-...">` resuelve a un `<symbol id>` de esa página.
    7. Pruebas de navegador (edita brand-assets.spec.ts), sobre la hoja: (a) todo `svg[data-collage]` y `.collage-sprite` con aria-hidden true y focusable false; (b) dentro de ellos ni title, text, image, foreignObject, animate, animateTransform, set, linearGradient, radialGradient ni filter; (c) cada svg de collage tiene caja no vacía y su `<use>` resuelve; (d) el contorno: el ancho computado del primer path de `#cs-lupa` es 3px y el color de `--collage-stroke` (léelo asignándolo a `color` de un elemento sonda dentro de la sección de cada tono) es `rgb(33, 33, 33)` en light y yellow y `rgb(255, 255, 255)` en dark y purple; (e) `document.getAnimations().length` es 0 con reducedMotion reduce y con no-preference; (f) sin scroll horizontal a 320, 390, 768, 1024 y 1280 px; (g) con JavaScript desactivado todas las piezas se pintan; (h) capturas `test-results/phase2/lote0-collage-primitivas-{ancho}.png`.
    8. Skills e iteración (DSGN-04). Invoca `design-taste-frontend` (diales 7/3/4) e `impeccable` (`shape` de cada primitiva con estilo pop de línea gruesa, `colorize` para elegir rellenos permitidos por tono, `polish`). Ciclo acotado: construir, capturar la hoja a 5 anchos con reduce, sin JavaScript y con ClickUp bloqueado, `critique` contra el contrato, corregir en un solo lote y como máximo una ronda de confirmación. Registra en el SUMMARY, sección 'Registro visual, lote 0 (marca y collage)'.
    Protocolo de navegador: `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio y `npx astro preview stop` al final. No leas `.env` ni envíes el formulario.
  </action>
  <acceptance_criteria>
    - `node --test tests/guards/brand-assets.test.mjs` y `node --test tests/guards/*.test.mjs` pasan (los 66 previos más los nuevos); la mutación de la tabla es detectada.
    - `grep -c '<symbol' src/components/collage/CollageSprite.astro` da exactamente 15 y cada id `cs-*` de `PIECES` aparece una sola vez.
    - `dist/index.html` trae un solo `collage-sprite`, pesa menos de 61440 bytes y el sprite menos de 10240.
    - Sin construcciones prohibidas en los archivos de este plan: `! grep -nE 'set:html|@keyframes|animation|transition|<animate|<set[[:space:]]|Gradient|<filter|filter[[:space:]]*:|<text|<image|foreignObject|<script|<title' src/components/collage/CollageSprite.astro src/components/collage/CollagePiece.astro src/components/collage/collage-rules.mjs src/components/brand/Logo.astro` y `! grep -rnE '#[0-9a-fA-F]{3}([^0-9a-zA-Z_-]|$)|#[0-9a-fA-F]{6}([^0-9a-zA-Z_-]|$)' src/components`.
    - En la hoja: todas las piezas con aria-hidden true y focusable false, contorno de 3px, `--collage-stroke` resuelto a oscuro en light y yellow y a blanco en dark y purple, cero animaciones con ambas preferencias, sin scroll horizontal a 5 anchos y visibles sin JavaScript.
    - Existen las capturas `lote0-collage-primitivas-*.png` y el SUMMARY registra rondas, verbos y hallazgos.
    - `git diff --name-only -- src/styles src/layouts src/pages/index.astro src/components/HeroSkeleton.astro src/components/AgendaSection.astro src/content tests/e2e/a11y-base.spec.ts` no lista nada.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts; s=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ]</automated>
    <fails_when>`assertToneSafe` acepta morado sobre dark o purple, amarillo sobre yellow u oscuro sobre dark o purple; el guarda pasa con la tabla mutada; un símbolo queda sin uso o duplicado; el sprite se monta cero o dos veces; una pieza trae título, texto, raster, degradado, filtro, animación o hex; el contorno no mide 3px o no cambia con el tono; hay animaciones activas con cualquiera de las dos preferencias; algún ancho desborda; el sprite pesa más de 10 KB o `dist/index.html` más de 60 KB.</fails_when>
  </verify>
  <done>Sprite montado una vez por página, 15 símbolos y `CollagePiece` con validación de tono en build, la hoja muestra cada pieza en los cuatro tonos, las guardas y pruebas pasan y todo queda en un commit.</done>
</task>

<task type="auto">
  <name>Task 4: Composición AgendaCollage y cuatro avatares Loopy</name>
  <files>src/components/collage/AgendaCollage.astro, src/components/collage/Avatar.astro, src/pages/marca/[sheet].astro, tests/e2e/brand-assets.spec.ts, tests/guards/brand-assets.test.mjs</files>
  <read_first>
    - 02-UI-SPEC.md: filas AgendaCollage y Avatares de `## Collage and Brand Assets Contract`; `## Reference Synthesis` (fila del equipo y de `#agenda`); `### Presupuesto de movimiento`; `### Lista de "vibra"`; `## Design Method and Iteration Protocol`.
    - src/components/collage/CollagePiece.astro, CollageSprite.astro y collage-rules.mjs (de la tarea 3) y src/pages/marca/[sheet].astro.
    - Skills a invocar con la herramienta Skill: `design-taste-frontend` e `impeccable`.
  </read_first>
  <action>
    Per CONTEXT (Estilo visual: collage pop en SVG estático; Equipo: avatar ilustrado SVG estático en estilo collage con aria-hidden) y UI-SPEC (Collage and Brand Assets Contract).
    Alcance: la composición del hero no es de este plan, la crea 02-01 como pieza autocontenida y la monta en el Hero. No la crees, no la importes ni la renderices en la hoja; si la hoja necesita una vista previa del lenguaje del hero, muestra solo las primitivas de la tarea 3.
    1. `AgendaCollage.astro` (prop `class?`). viewBox `0 0 480 480`, `data-collage="agenda"`, tono `purple` (`assertToneSafe('purple', ...)`: contorno blanco, rellenos amarillo, naranja o blanco, nunca morado): lupa, ojos mirando a la derecha (hacia el formulario) y un clic. Regla del propio componente: oculto por debajo de 64em y visible desde 64em (responsivo, no movimiento). Peso máximo 4 KB.
    2. `Avatar.astro`. Props `variant: 'lupa' | 'auriculares' | 'gafas' | 'gorro'` y `class?`. viewBox `0 0 120 120`, `data-collage="avatar"` y `data-variant`, tono `light` (van dentro de tarjetas blancas). Set uniforme 'Loopy': círculo de fondo con el color dominante de la variante (lupa amarillo, auriculares naranja, gafas morado, gorro oscuro), cabeza redonda blanca con contorno de 3 px, los mismos ojos del sistema en la misma posición en las cuatro y un accesorio distinto por variante. No son retratos ni buscan parecido (decisión 10 del UI-SPEC; el set lo aprueba Ari). La asignación persona a variante la decide 02-05; sugerida sin obligación: Arianna con lupa. Peso máximo 2,5 KB cada uno.
    3. Hoja (edita `[sheet].astro`). Agrega AgendaCollage sobre purple (visible con la ventana a 1024 px o más, oculto por debajo) y los cuatro avatares en fila dentro de una tarjeta blanca con borde oscuro de 3 px sobre fondo yellow.
    4. Skills e iteración (DSGN-04). `design-taste-frontend` (diales 7/3/4) e `impeccable` (`shape`, `colorize`, `polish` y `bolder` si AgendaCollage se ve plana). Ciclo del UI-SPEC acotado: construir el lote completo, capturar la hoja con Playwright a 320, 390, 768, 1024 y 1280 px con reduce, sin JavaScript y con ClickUp bloqueado (`test-results/phase2/lote0-composiciones-{ancho}.png`), `critique` contra el contrato de collage, la fila de Reference Synthesis del equipo y la lista de vibra, corregir en un solo lote y una ronda de confirmación como máximo. Criterio de salida de este plan: punto 5 de la lista (los cuatro avatares como set uniforme sobre círculos suaves) y, del punto 7, que AgendaCollage sea un elemento de collage propio de `#agenda` con el mismo trazo pop que las primitivas. Si tras 2 ciclos no se cumplen, detente y muestra las capturas a Juan. Registra rondas, verbos, hallazgos, correcciones y capturas en el SUMMARY (sección 'Registro visual, lote 0 (marca y collage)'); 02-07 lo consolida en `02-VISUAL-LOG.md`, que se comparte y no se crea aquí. En los comentarios de los archivos describe las reglas con otras palabras, porque las pruebas rechazan ciertos literales.
    5. Pruebas. En brand-assets.spec.ts, sobre la hoja: bytes del HTML de AgendaCollage hasta 4096, cada Avatar hasta 2560 y toda la colección de la hoja (sprite, AgendaCollage, avatares y una instancia de cada pieza) hasta 28672; cada elemento de AgendaCollage queda dentro de su viewBox con 4 px de margen (getBBox); AgendaCollage con display none a 320, 390 y 768 px y visible a 1024 y 1280; los 4 avatares con `data-variant` distintos, mismo tamaño y alto igual a ancho; cero animaciones con ambas preferencias; sin scroll horizontal a 5 anchos; contorno por tono. En el guarda (edita brand-assets.test.mjs): AgendaCollage y Avatar llaman `assertToneSafe` y se agregan a la lista `OWNED` del detector de fuentes prohibidas.
    Protocolo de navegador: `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio y `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - En el HTML construido de la hoja: AgendaCollage 4096 bytes o menos, cada Avatar 2560 o menos y la colección completa 28672 o menos; `dist/index.html` sigue por debajo de 61440 bytes.
    - Los elementos de AgendaCollage quedan dentro de su viewBox con 4 px de margen y ninguna pieza tapa a otra de forma que se pierda la lupa o los ojos (revisión en captura).
    - Este plan no crea ni renderiza la composición del hero: `! grep -rn 'HeroCollage' src/components/collage/AgendaCollage.astro src/components/collage/Avatar.astro src/components/collage/CollagePiece.astro src/components/brand src/components/SiteHeader.astro src/pages/marca` sale limpio.
    - AgendaCollage está oculto a 320, 390 y 768 px y visible a 1024 y 1280 px; los 4 avatares tienen variantes distintas y el mismo tamaño.
    - `node --test tests/guards/*.test.mjs` y `node scripts/check-contrast.mjs` pasan; el barrido `! grep -nE 'set:html|@keyframes|animation|transition|<animate|<set[[:space:]]|Gradient|<filter|filter[[:space:]]*:|<text|<image|foreignObject|<script|<title' src/components/collage/CollageSprite.astro src/components/collage/CollagePiece.astro src/components/collage/AgendaCollage.astro src/components/collage/Avatar.astro src/components/collage/collage-rules.mjs src/components/brand/Logo.astro` sale limpio, sin hex en `src/components`.
    - `document.getAnimations().length` es 0 con ambas preferencias de movimiento y sin scroll horizontal a los 5 anchos; todo se pinta con JavaScript desactivado.
    - Existen las capturas `lote0-composiciones-*.png`; el SUMMARY registra el ciclo de crítica, el cumplimiento del punto 5 de la lista de vibra (y del 7 en lo que toca a AgendaCollage) y las decisiones para Ari (colores del logo, set de avatares, isotipo del favicon).
    - `git diff --name-only -- src/styles src/layouts src/pages/index.astro src/components/HeroSkeleton.astro src/components/collage/HeroCollage.astro src/components/AgendaSection.astro src/content tests/e2e/a11y-base.spec.ts` no lista nada.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium; s=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ]</automated>
    <fails_when>Una pieza de AgendaCollage queda cortada por el viewBox; algún peso supera su tope; AgendaCollage se ve por debajo de 1024 px; los avatares no comparten tamaño o variante distinta; hay animaciones activas; una composición usa un color prohibido para su tono; cualquier prueba de la fase 1 se rompe.</fails_when>
  </verify>
  <done>AgendaCollage y los cuatro avatares existen dentro de presupuesto, se ven bien en la hoja a 5 anchos, la crítica visual cumple el punto 5 de la vibra, toda la suite de la fase 1 sigue en verde y queda en un commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive público de Ari a repo (solo al autorar) | El .ai y el PDF se bajan una vez con curl y se convierten; el contenido es de terceros hasta que se limpia con SVGO y se revisa el diff |
| landing.es.yaml a Logo.astro | `brand.name.text` llega como atributo aria-label, escapado por Astro; es el único texto dinámico de este plan |
| build a producción | La hoja `/marca/hoja/` es herramienta interna y no debe publicarse |
| navegador a SVG en línea | SVG propio: sin scripts, sin referencias externas, sin peticiones nuevas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering | SVG convertidos del .ai (logo, isotipo, favicon) | medium | mitigate | SVGO más guarda estática y de aceptación: sin script, image, foreignObject, xlink, manejadores on*, metadatos ni referencias http; revisar el diff del SVG antes del commit |
| T-02-02-02 | Tampering | Componentes de marca y hoja | medium | mitigate | `brand-assets.test.mjs` prohíbe la inserción de HTML crudo y hex en `src/components`; el único texto dinámico es `name`, escapado como atributo |
| T-02-02-03 | Information Disclosure | Hoja `/marca/hoja/` publicada por error | low | mitigate | `getStaticPaths` vacío con PUBLIC_ENV=production, noindex fuera de producción; comprobación por build directo en la tarea 2 |
| T-02-02-04 | Information Disclosure | Metadatos del .ai o del PDF (autor, rutas) en SVG e ICO | low | mitigate | SVGO los elimina; la guarda rechaza metadatos; los originales .ai y .pdf no se versionan |
| T-02-02-05 | Denial of Service | Peso del HTML por el collage (Core Web Vitals) | low | mitigate | Topes medidos en el HTML construido: sprite 10 KB, AgendaCollage 4 KB, avatar 2,5 KB, colección 28 KB (más los 8 KB de HeroCollage de 02-01 queda bajo los 40 KB del UI-SPEC), `dist/index.html` menor a 60 KB |
| T-02-02-06 | Spoofing | Peticiones nuevas de terceros | low | mitigate | Todo va en línea y local; el acceso a Drive ocurre solo al autorar, no en el sitio; las pruebas corren con ClickUp bloqueado |
| T-02-02-07 | Repudiation | Licencia de assets de marca | low | accept | Los vectoriales son de Ari; el logo son contornos y no hay fuente que licenciar; la licencia web de Hurme sigue pendiente desde la fase 1 |
| T-02-02-SC | Tampering | Instalación de paquetes npm, pip o cargo | low | accept | Este plan no instala nada: SVGO ya viene con Astro y Pillow es una herramienta local que no entra al repo ni al package.json; no aplica auditoría de legitimidad |
</threat_model>

<verification>
Al cerrar el plan, en este orden y con las lecciones de la fase 1 (el preview se va a segundo plano dentro de un agente; reconstruir tras cada cambio; detenerlo al final):
1. `npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check`: las 66 guardas previas más las de `brand-assets` en verde, contraste intacto (este plan no toca tokens) y `PENDING-COPY.md` sin cambios.
2. `npx astro preview --port 4322`, luego `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium` (todo el proyecto chromium sin depender de ClickUp) y `npx astro preview stop`. El proyecto `live` no se toca: el iframe de ClickUp no cambia y las verificaciones humanas de la fase 1 siguen diferidas.
3. Exclusión de producción: `PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build` (directo, sin los hooks de npm) y `test ! -e dist/marca`; después `npx astro build` en modo normal y `npm run build` con sus hooks.
4. `git status --short` y `git diff --name-only` solo muestran archivos de `files_modified` (y el SUMMARY); ningún archivo de 02-01.
5. Revisión humana no bloqueante: Juan y Ari abren `/marca/hoja/` (con `npm run dev` o el preview) y confirman logo, isotipo, favicon, avatares y colores; ver decisiones abajo.
</verification>

<success_criteria>
- DSGN-02: el logo horizontal está en el header como SVG en línea tomado del vectorial de Ari, accesible, sin enlace en `/`, con `--logo-clear` (1 X, BrandBook página 7) respetado a 320, 640, 768 y 1280 px; el isotipo Loopy y el favicon salen del mismo vectorial.
- DSGN-01: existen el sprite de 15 símbolos, `CollagePiece` con validación de tono en el build, `AgendaCollage` y cuatro `Avatar`, todos SVG estático decorativo dentro de presupuesto y visibles sin JavaScript (la composición del hero la entrega 02-01).
- DSGN-04: cada tarea de UI invocó `impeccable` y `design-taste-frontend` con la herramienta Skill, hizo el ciclo acotado y dejó el registro en el SUMMARY; el punto 5 de la lista de vibra (avatares como set uniforme) se cumple en la hoja.
- Cero animaciones por defecto, cero JS propio nuevo, cero dependencias nuevas, cero archivos compartidos con 02-01 y la suite de la fase 1 sigue en verde.
</success_criteria>

<output>
Al terminar crea `.planning/phases/02-secciones-marca-y-copy/02-02-SUMMARY.md` con: archivos creados y cambiados; los supuestos y desviaciones de abajo con lo que se eligió; el hallazgo de colores del logo; el registro visual del lote 0 (marca y collage) por ronda; pesos medidos; capturas; y las tareas pendientes para otros planes. Si el contexto pasa de 50 % entre tareas, escribe un SUMMARY parcial con `status: partial`.
</output>

## Presupuesto de contexto

Estimación: unos 80.000 tokens de trabajo del ejecutor (tarea 1 cerca de 20k, tarea 2 cerca de 18k, tarea 3 cerca de 26k, tarea 4 cerca de 16k tras mover la composición del hero a 02-01), con confianza baja porque no hay muestras de calibración. Sube el costo del ciclo visual: cada captura leída pesa, así que lee las capturas de a una y solo las necesarias (una por ancho crítico, no las 5 en cada ronda).

Puntos de corte (cada uno deja el repositorio en verde y con commit):
1. Tras la tarea 1: header con el logo del Drive. Si el contexto pasa de 50 %, commit y SUMMARY parcial.
2. Tras la tarea 2: identidad completa (logo, isotipo, favicon, hoja de revisión). Es el corte natural más importante.
3. Tras la tarea 3: sprite y primitivas con sus guardas. Un ejecutor nuevo retoma la tarea 4 leyendo el SUMMARY parcial y la hoja.
4. La tarea 4 cierra el plan.

Si el checker exige partir el plan por tamaño (la tarea 3 toca 7 archivos y la 4 toca 5, contando ediciones pequeñas), el corte limpio es 02-02a (tareas 1 y 2, identidad) y 02-02b (tareas 3 y 4, collage), con 02-02b dependiendo de la hoja y del `SiteHeader` de 02-02a. Ambos siguen sin compartir archivos con 02-01.

## Artifacts this phase produces

Nombres que los planes 02-03 a 02-07 consumen tal cual.

| Artefacto | Detalle | Lo usa |
|-----------|---------|--------|
| `src/components/brand/Logo.astro` | Props `variant` ('horizontal' o 'isotipo'), `name` (de `brand.name.text`), `href?`, `class?`. Clase `.brand-logo`, atributo `data-variant`, variables `--logo-h`, `--logo-min` y `--logo-clear` (1 X). Sin enlace si no hay `href`; `SiteHeader` calcula `href` con `Astro.url.pathname`, así que en `/privacidad` enlaza a `/` sin trabajo extra | 02-06 (footer) |
| `src/assets/brand/logo-horizontal.svg`, `isotipo.svg` | SVG limpios con `viewBox` recortado y colores entregados; se importan como componentes de Astro | Logo.astro |
| `public/favicon.svg`, `public/favicon.ico` | Isotipo, 16/32/48 px | Navegador (`/favicon.ico` por convención) |
| `src/components/collage/CollageSprite.astro` | Montado una vez por `SiteHeader`. Símbolos: `cs-lupa`, `cs-ojos-izq`, `cs-ojos-der`, `cs-ojos-abajo`, `cs-clic`, `cs-loop`, `cs-destello`, `cs-puntos`, `cs-sticker-ojos`, `cs-sticker-clic`, `cs-sticker-lupa`, `cs-chip-lupa`, `cs-chip-ojos`, `cs-chip-loop`, `cs-chip-clic`. Rellenos por `--cf-a`, `--cf-b`, `--cf-c`; contorno por `--collage-stroke` | Todas las piezas |
| `src/components/collage/CollagePiece.astro` | `<CollagePiece name tone a? b? c? size? rotate? class? />`. `tone` es obligatoria y valida el build. Emite `svg.collage-piece[data-collage][data-collage-piece]` (no usa `data-piece`, que es del hero de 02-01) | 02-03 (pegatinas de PainCard, chips de PillarCard, mini lupa y ojos), 02-05 (`loop` como conector de fases) |
| `src/components/collage/collage-rules.mjs` | `PIECES`, `TONES`, `BRAND_COLORS`, `ALLOWED_FILLS`, `fillVar`, `assertToneSafe` | CollagePiece, AgendaCollage, Avatar y las guardas |
| `src/components/collage/AgendaCollage.astro` | viewBox 480x480, tono purple, oculto bajo 64em | 02-06 (`AgendaSection`) |
| `src/components/collage/Avatar.astro` | `variant` 'lupa', 'auriculares', 'gafas' o 'gorro'; viewBox 120x120; tono light | 02-05 (TeamCard) |
| `src/pages/marca/[sheet].astro` | Ruta `/marca/hoja/`, solo fuera de producción | Revisión de Juan y Ari; capturas de lote 0 |
| `tests/e2e/brand-assets.spec.ts`, `tests/guards/brand-assets.test.mjs` | Pruebas de logo, isotipo, favicon, sprite, piezas, AgendaCollage, avatares y pesos | Regresión de toda la fase |

## UI Considerations (lift del UI-SPEC para este plan)

- covered (verdades ya en `must_haves.truths`): loading (SVG en línea sin carga, sin recursos externos), overflow (piezas con `max-width: 100%`, sin scroll horizontal a 5 anchos), a11y-motion (cero animaciones por defecto, `getAnimations()` en 0), a11y-color (validación de tono en el build, contornos por tono).
- backstop (en `must_haves.truths` como `{statement, verification: backstop}`): los cuatro avatares como set uniforme (lista de vibra, punto 5), por crítica visual sobre la hoja. La fila visual del `HeroCollage` a 320 px pasó a 02-01, que crea y monta esa composición; este plan comprueba lo que sí controla: cada elemento de AgendaCollage queda dentro de su viewBox.
- unresolved que este plan resuelve: fila assets. El vectorial y el BrandBook estaban en el Drive público, el área de salvado se midió (1 X, no media altura) y no se redibuja nada. Queda una decisión abierta para Ari y Juan: los colores del logo.

## Cobertura de fuentes (alcance de 02-02)

| Fuente | Elemento | Estado |
|--------|----------|--------|
| GOAL (ROADMAP fase 2) | Identidad de marca aplicada: collage pop en SVG estático, logo e isotipo en SVG | Cubierto: tareas 1 a 4 |
| REQ | DSGN-01 (collage estático), DSGN-02 (logo e isotipo con área de salvado), DSGN-04 (skills) | Cubierto: DSGN-01 tareas 3 y 4, DSGN-02 tareas 1 y 2, DSGN-04 en todas |
| CONTEXT | Estilo visual: SVG estático con aria-hidden y focusable false; logo e isotipo con área de salvado del Drive; método impeccable más design-taste-frontend; equipo con avatar SVG aria-hidden | Cubierto: tareas 1 a 4 |
| UI-SPEC | Collage and Brand Assets Contract (piezas, reglas de tono, peso, sprite, logo, área de salvado, favicon) | Cubierto salvo la composición del hero (la entrega 02-01); desviaciones en "Supuestos y desviaciones" |
| RESEARCH | No existe `02-RESEARCH.md`; la investigación de stack de CLAUDE.md (fuentes, sin islas, sin JS) se respeta | Sin ítems pendientes |
| Fuera de este plan (no son huecos) | Composición del hero y su montaje en el Hero (02-01, por decisión del orquestador); movimiento de entrada del hero y pupilas (02-07, DSGN-05); integración de `AgendaCollage` en `#agenda` y de pegatinas, chips y avatares en las secciones (02-03, 02-05, 02-06); tokens y tonos (02-01) | Diferido a esos planes |

## Supuestos y desviaciones (revertir o confirmar)

1. Área de salvado: `--logo-clear` es 1 X (X igual a la altura del logo o isotipo), leída del BrandBook página 7. Corrige la suposición de media altura del UI-SPEC (decisión 14), que solo vale para el logo apilado. Se comprueba como zona de exclusión (ningún elemento de contenido dentro), no como relleno visible, para no engordar el header de 64 px que 02-01 usa en su cuenta del primer pantallazo.
2. Colores del logo: se usan los del archivo de Ari (#4228D1, #6C61DB, #F4F3E0, #1E1E1E), no el #73187F de `tokens.css`. El texto del BrandBook dice #73187F pero su muestra se dibuja en #4228D1; solo Ari sabe cuál es el correcto y, si fuera el azul violeta, cambia el morado de toda la fase (tokens, pares de contraste). Alinear el logo a la paleta es editar los fills de dos assets. Decisión alta para Ari y Juan.
3. Sprite montado en `SiteHeader`, no en `BaseLayout` como pide el UI-SPEC, porque `BaseLayout` es de 02-01 en la misma wave y este plan no puede editarlo. Mover una línea a `BaseLayout` después es trivial; la guarda exige exactamente un sprite por página.
4. Nombre accesible del logo con `aria-label` en un `svg` con `role="img"` (el componente SVG de Astro no inyecta un elemento de título). Da el mismo nombre accesible que pide el UI-SPEC; si el import como componente no se puede usar, el respaldo es pegar los paths con título y `aria-labelledby`.
5. Se agrega una superficie que el UI-SPEC no lista: la hoja `/marca/hoja/`, solo fuera de producción, para verificar y aprobar piezas antes de que existan las secciones que las usan. Sin ella, AgendaCollage, los avatares y las primitivas no serían verificables en la wave 1.
6. Altura del logo horizontal fija en el mínimo de 32 px (`max(2rem, 32px)`) en todos los anchos, para no mover la altura del header que 02-01 supone. Un ajuste de tamaño en escritorio es trabajo de `polish` en el cierre.
7. Favicon con el isotipo de dos ojos y lupa (mesa 13); si no se lee a 16 px, mesa 18 (un ojo con lupa) marcada para aprobación de Ari.
8. La clase `.wordmark` se conserva en el contenedor del logo para que la aserción (h) de `a11y-base` siga en verde sin editar ese archivo (que 02-01 puede estar editando); la aserción queda obsoleta y 02-07 la retira.
9. El registro visual del lote 0 va en el SUMMARY de este plan, no en `02-VISUAL-LOG.md`, para no crear el mismo archivo que 02-01 en la misma wave; 02-07 lo consolida.
10. Sin `impeccable init` ni `document` en este plan (los ejecuta 02-01 para no duplicar `PRODUCT.md` y `DESIGN.md`).
11. HeroCollage pasó a 02-01 por decisión del orquestador: 02-01 lo crea como composición autocontenida y lo monta en el Hero, así que este plan ya no lo crea, no lo renderiza en la hoja y no comparte ese archivo.
12. `CollagePiece` emite `data-collage-piece` en lugar de `data-piece`, y las guardas de fuentes de este plan usan una lista explícita de archivos propios (no el directorio entero), para no chocar con los ganchos `data-piece` del hero ni con las reglas propias del archivo de 02-01.

## Pendientes para el conjunto de planes (no editables desde este plan)

- `AgendaCollage` en `AgendaSection` (02-06), avatares en `TeamCard` (02-05), pegatinas y chips en `PainCard` y `PillarCard` (02-03).
- En `/privacidad` el logo pasa a ser un enlace y suma una parada de teclado: 02-06 debe actualizar su prueba de orden de tabulación.
- Enlazar `favicon.svg` con un link de tipo icon en el head y, si se quiere, mover el sprite a `BaseLayout`: quien edite `BaseLayout` (02-01 o 02-07).
