---
phase: 02-secciones-marca-y-copy
plan: 09
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/styles/tokens.css
  - scripts/lib/contrast.mjs
  - src/components/collage/collage-rules.mjs
  - src/components/collage/AgendaCollage.astro
  - src/components/SkipLinks.astro
  - tests/guards/contrast.test.mjs
  - tests/guards/brand-palette.test.mjs
  - tests/guards/brand-assets.test.mjs
  - tests/e2e/lib/brand.ts
  - tests/e2e/page-structure.spec.ts
  - tests/e2e/sections-problem-solution.spec.ts
  - tests/e2e/a11y-base.spec.ts
  - tests/e2e/brand-assets.spec.ts
  - scripts/lib/brand-svg.mjs
  - scripts/brand/extract-artboards.mjs
  - src/components/brand/logo-variants.mjs
  - src/components/brand/Logo.astro
  - src/components/SiteHeader.astro
  - src/pages/marca/[sheet].astro
  - src/assets/brand/apilado-01-blanco.svg
  - src/assets/brand/apilado-03-morado.svg
  - src/assets/brand/apilado-05-amarillo.svg
  - src/assets/brand/apilado-08-oscuro.svg
  - src/assets/brand/horizontal-06-blanco.svg
  - src/assets/brand/imagotipo-07-blanco.svg
  - src/assets/brand/emblema-10-blanco.svg
  - src/assets/brand/emblema-12-morado.svg
  - src/assets/brand/emblema-24-amarillo.svg
  - src/assets/brand/isotipo-13-blanco.svg
  - src/assets/brand/isotipo-14-morado.svg
  - src/assets/brand/isotipo-16-amarillo.svg
  - src/assets/brand/isotipo-17-oscuro.svg
  - src/assets/brand/ojo-18-blanco.svg
  - src/assets/brand/ojo-19-morado.svg
  - src/assets/brand/ojo-21-amarillo.svg
  - src/assets/brand/ojo-22-oscuro.svg
  - src/assets/brand/logo-horizontal.svg   # se elimina: es la mesa 06, ahora con nombre de mesa
  - src/assets/brand/isotipo.svg           # se elimina: es la mesa 13, ahora con nombre de mesa
  - public/favicon.svg
  - public/favicon.ico
  - .gitignore
  - DESIGN.md
  - PRODUCT.md
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, FND-03]

estimate:
  tokens: 116000
  raw_tokens: 116000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "El morado de la página es #4228D1 (decisión de Juan, 2026-09-19, CONTEXT sección 'Morado de marca y logo', vigente). `--color-brand-purple` vale `#4228d1` en `src/styles/tokens.css`, único lugar del hex fuera de los SVG oficiales, y `node scripts/check-contrast.mjs` sale 0 con 14 de 14 pares aprobados y 11 prohibidos verificados. El morado anterior (la etiqueta errónea R115 G24 B127 de la página 8 del BrandBook) tiene cero apariciones en `src`, `public`, `tests` y `scripts`, comprobado por la guarda `tests/guards/brand-palette.test.mjs`."
    - "Pares medidos con la calculadora del repo (nunca estimados). Aprobados: morado sobre blanco 8.55, blanco sobre morado 8.55, amarillo sobre morado 5.43, morado sobre amarillo 5.43, crema sobre morado 7.63, morado sobre crema 7.63, oscuro sobre crema 14.37, crema sobre oscuro 14.37, más los seis de la fase 1 que no cambian (oscuro sobre blanco 16.10, oscuro sobre amarillo 10.22, oscuro sobre naranja 5.56, amarillo sobre oscuro 10.22, naranja sobre oscuro 5.56, blanco sobre oscuro 16.10)."
    - "Pares prohibidos vigentes (medidos, cada uno bajo 3): blanco sobre naranja 2.89, naranja sobre blanco 2.89, amarillo sobre blanco 1.58, blanco sobre amarillo 1.58, morado sobre oscuro 1.88, naranja sobre amarillo 1.84, y nuevos: oscuro sobre morado 1.88, naranja sobre morado 2.95 (antes 3.35 y aprobado como UI; con el morado nuevo baja de 3), amarillo sobre crema 1.41, naranja sobre crema 2.58 y blanco sobre crema 1.12."
    - "El crema #F4F3E0 entra como token primitivo `--color-brand-cream` (lo miden los pares de arriba y lo usan los logos oficiales sobre morado y oscuro) y NO es un quinto tono: `REQUIRED_TONES` sigue en cuatro (light, yellow, dark, purple) y ningún `data-tone` de `src` usa otro valor. Motivo: en el moodboard el crema es superficie de píldoras, chips y recortes (la píldora 'team work' es crema con texto morado, 7.63) y no fondo de sección, y contra el blanco mide 1.12, así que una sección crema junto a una blanca no marcaría el ritmo de tonos alternados. El iris #6C61DB NO es token: solo vive dentro de los SVG oficiales y ningún componente ni par lo usa todavía."
    - "El naranja sobre morado (2.95) queda prohibido para texto, bordes de UI y toda pieza informativa. Sigue permitido solo como relleno decorativo del collage (aria-hidden, contorno blanco de 3 px que mide 8.55 sobre morado y es el que define la forma). La excepción está a la vista en dos lugares y una guarda exige que ambos coincidan: `ALLOWED_FILLS.purple` conserva `orange` y `FORBIDDEN_PAIRS` lista naranja sobre morado."
    - "Los logos, imagotipo, emblemas e isotipos salen de las mesas del `.ai` de Ari (32 mesas de 800x800 pt) como SVG limpios de un solo elemento raíz y rutas: `src/assets/brand/` trae exactamente 17 archivos con nombre `<variante>-<mesa>-<fondo>.svg`, sin el rectángulo de fondo de la mesa (la superficie la pinta el tono), con `viewBox` recortado a la caja del arte dentro de 0 a 800, sin `width` ni `height`, menos de 8192 bytes cada uno y solo cuatro rellenos (#4228d1, #6c61db, #f4f3e0, #1e1e1e). Las 32 mesas están contabilizadas en `src/components/brand/logo-variants.mjs`: 17 usadas y 15 con motivo medido para no extraerlas."
    - "`Logo.astro` elige la mesa oficial por variante y tono (`resolveLogo(variant, tone)`), sin recolorear nada por CSS (sin regla de relleno) y sin `data-tone` propio. El logo horizontal y el imagotipo solo existen sobre `light`; el emblema no tiene versión sobre `dark`; pedir una combinación sin mesa oficial rompe el build con un error en español que nombra variante, tono y los tonos disponibles. Se conserva el contrato de 02-02: `role='img'`, nombre accesible igual a `brand.name.text` del YAML, sin aria-hidden, sin enlace en `/`, y área de salvado `--logo-clear` de 1 X (X igual al alto del arte; para el logo apilado X es la mitad del alto, BrandBook página 7)."
    - "El logo del header es la mesa 06 sobre `light` y su relleno calculado es el morado del token, el mismo del h1. Ese es el rastreador de la tarea 1: antes del cambio el logo era #4228d1 y el h1 el morado anterior."
    - "Contraste por mesa medido y fijado en el catálogo: cada mesa usada mide su color principal (o aro exterior en isotipos) contra la superficie de su tono y solo dos quedan bajo su umbral, ambas registradas como excepción de marca con razón: el isotipo (mesa 17) y el ojo (mesa 22) sobre oscuro, cuyo aro morado mide 1.88 sobre #212121 y cuya figura se lee por el anillo crema (14.37). El emblema sobre oscuro (mesa 25, texto morado sobre oscuro 1.88) no se extrae y se pide a Ari una versión con texto crema. Ninguna excepción se agrega sin listarla (guarda con lista independiente)."
    - "Favicon: `public/favicon.svg` y `public/favicon.ico` (16, 32 y 48 px) salen de la mesa 18 (un ojo con lupa), no de la 13. Evidencia a 16 px con margen de 4 %: la mesa 13 ocupa 14.7 x 9.0 px con pupilas de 2.2 px; la mesa 18 ocupa 14.3 x 14.7 px con pupila de 3.7 px. Regla de decisión: gana la que cumple alto de la marca de 12 px o más y pupila de 3 px o más; si ninguna o ambas cumplen se prefiere la de dos ojos por identidad. Una guarda exige que los trazos de `favicon.svg` sean los de `ojo-18-blanco.svg`."
    - "`/marca/hoja/` (solo fuera de producción, noindex) muestra las 17 mesas, cada una sobre el tono al que pertenece (light 6, yellow 4, dark 3, purple 4) con su área de salvado dibujada y el rótulo `<variante> / mesa NN` en un elemento code, más una franja de paleta con el hex leído de `tokens.css` y el favicon a 16, 32 y 48 px. Sin scroll horizontal de 320 a 1280 px."
    - "Los originales de Ari no se versionan: `git ls-files` no lista archivos `.ai` ni `.pdf` ni rutas dentro del directorio `brand-inventory/` (el archivo `02-BRAND-INVENTORY.md` sí se versiona: es texto propio), y `.gitignore` los cubre (el BrandBook es CONFIDENCIAL)."
    - "Sin cambios de copy ni de estructura de secciones: el diff del plan contra `plan_head_before` (el `HEAD` al empezar la tarea 1) no lista archivos de `src/content`, `PENDING-COPY.md`, `src/components/sections`, `src/components/ui` ni `src/layouts`, y `node scripts/list-pending.mjs --check` sale 0. De 02-03 solo cambia `tests/e2e/sections-problem-solution.spec.ts` (la constante del morado pasa a leerse de `tests/e2e/lib/brand.ts`)."
    - "Prohibido: inserción de HTML crudo, hex en `src/components`, `src/pages` o `src/layouts` (los hex viven solo en `tokens.css`, en los SVG de `src/assets/brand` y en `public/favicon.svg`), anular el contorno de foco, animaciones nuevas, dependencias nuevas en `package.json`, versionar el `.ai` o el PDF del BrandBook, leer `.env`, enviar el formulario de ClickUp y tocar el copy."
    - statement: "El azul violeta se lee como una sola familia de color entre el logo, el h1, los numerales y la banda de `#agenda` (ya no hay dos morados distintos en el mismo header), y cada logo de la hoja se lee sobre su tono"
      verification: backstop
  artifacts:
    - path: src/styles/tokens.css
      provides: "Morado #4228d1 y crema #f4f3e0 como primitivos, con las reglas de pares prohibidos actualizadas"
      contains: "--color-brand-cream"
    - path: scripts/lib/contrast.mjs
      provides: "14 pares aprobados y 11 prohibidos con el morado nuevo y el crema"
      contains: "--color-brand-cream"
    - path: tests/guards/brand-palette.test.mjs
      provides: "Guarda del morado oficial: cero apariciones del morado anterior, hex solo en tokens y SVG, excepción del naranja sobre morado"
      contains: "tokens.css"
    - path: tests/e2e/lib/brand.ts
      provides: "Colores de marca en formato rgb leídos de tokens.css, para que ningún spec repita un valor"
      contains: "parseTokens"
    - path: src/components/brand/logo-variants.mjs
      provides: "Catálogo de las 32 mesas y resolveLogo(variante, tono)"
      contains: "resolveLogo"
    - path: scripts/lib/brand-svg.mjs
      provides: "cleanArtboard: quita fondos, convierte a hex y ajusta a la paleta oficial"
      contains: "cleanArtboard"
    - path: scripts/brand/extract-artboards.mjs
      provides: "Extracción reproducible de las mesas del .ai a SVG limpios y del favicon"
      contains: "pdftocairo"
    - path: src/assets/brand/horizontal-06-blanco.svg
      provides: "Logo horizontal oficial, mesa 06"
      contains: "viewBox"
    - path: src/assets/brand/apilado-08-oscuro.svg
      provides: "Logo apilado oficial sobre oscuro, mesa 08 (texto crema)"
      contains: "viewBox"
    - path: src/assets/brand/ojo-18-blanco.svg
      provides: "Isotipo de un ojo con lupa, mesa 18, fuente del favicon"
      contains: "viewBox"
    - path: src/components/brand/Logo.astro
      provides: "Logo por variante y tono con la mesa oficial, sin recolorear"
      contains: "resolveLogo"
    - path: src/pages/marca/[sheet].astro
      provides: "Hoja de revisión con las 17 mesas por tono, paleta y favicon"
      contains: "identidad"
    - path: public/favicon.svg
      provides: "Favicon con los trazos de la mesa 18"
      contains: "viewBox"
  key_links:
    - from: src/components/brand/Logo.astro
      to: src/components/brand/logo-variants.mjs
      via: "resolveLogo(variant, tone) elige el archivo y falla con error en español si no hay mesa oficial"
      pattern: "resolveLogo"
    - from: src/components/SiteHeader.astro
      to: src/components/brand/Logo.astro
      via: "el mismo tono se pasa a data-tone del header y a la prop tone del logo"
      pattern: "tone"
    - from: tests/e2e/lib/brand.ts
      to: src/styles/tokens.css
      via: "parseTokens y hexToRgb de scripts/lib/contrast.mjs"
      pattern: "tokens.css"
    - from: scripts/brand/extract-artboards.mjs
      to: src/components/brand/logo-variants.mjs
      via: "ARTBOARDS decide qué mesas se extraen y con qué nombre de archivo"
      pattern: "ARTBOARDS"
    - from: tests/guards/brand-assets.test.mjs
      to: src/assets/brand
      via: "la lista de archivos del directorio debe ser exactamente la del catálogo"
      pattern: "assets/brand"
    - from: public/favicon.svg
      to: src/assets/brand/ojo-18-blanco.svg
      via: "mismos datos de trazo (atributo d) recortados a un viewBox cuadrado"
      pattern: "ojo-18"
---

<objective>
Corregir la identidad de color y de logos de la fase 2 con la decisión de Juan del 2026-09-19 (CONTEXT, "Morado de marca y logo", vigente): el morado de la página es #4228D1 (azul violeta, el color del logo, de la muestra del BrandBook, de la papelería y de todo el moodboard) y el morado anterior, que salió de una etiqueta errónea de la página 8 del BrandBook, deja la página. Los logos, el imagotipo, los emblemas y los isotipos dejan de ser un solo SVG recoloreado por CSS y pasan a ser las mesas oficiales del `.ai` de Ari, una por fondo (DSGN-01, DSGN-02, DSGN-03, DSGN-04, FND-03).

Purpose: los planes 02-01 a 02-03 se construyeron con el morado equivocado (el h1 y el logo conviven hoy en el header como dos morados distintos). Los planes 02-04 a 02-08 y el rehacer del collage dependen de tokens, pares de contraste y catálogo de logos correctos. Este plan corre antes de retomar la tarea 4 de 02-03 y antes de 02-04.

Output: token del morado y del crema con 14 pares aprobados y 11 prohibidos medidos y probados por mutación; 17 SVG oficiales limpios con su catálogo de 32 mesas y un script de extracción reproducible; `Logo.astro` por variante y tono; favicon de la mesa 18 con evidencia; hoja `/marca/hoja/` con cada mesa sobre su tono; ciclo visual con `impeccable` y `design-taste-frontend` a 320, 390, 768, 1024 y 1280 px registrado en `02-VISUAL-LOG.md`.

Fuera de este plan (por instrucción del orquestador, ver "Cobertura de fuentes"): rehacer el collage con las piezas reales de Loopy, las píldoras con palabra, los recortes fotográficos con licencia y el reemplazo de avatares y sprite. Este plan solo cambia de ellos lo que el color fuerza.

Discovery (nivel 0): todo el trabajo sigue patrones ya usados en 02-02 (SVG importado como componente de Astro, `pdftocairo`, SVGO de `node_modules/.bin`, Chromium de Playwright, guardas con mutación). No se instala nada.
</objective>

<!-- planner-discipline-allow: data-tone -->
<!-- planner-discipline-allow: 73187f -->
<!-- planner-discipline-allow: rgb(115, 24, 127) -->
<!-- planner-discipline-allow: 115, 24, 127 -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-BRAND-INVENTORY.md
@src/styles/tokens.css

<!-- Lecturas obligatorias para el verificador visual (tarea 4): 02-BRAND-INVENTORY.md y las imágenes ai_a.png y ai_b.png de brand-inventory/ (las 32 mesas). Las demás imágenes (bb_a, bb_b, moodboard, site-hero) solo si un hallazgo lo pide. Sin versionar: el BrandBook es CONFIDENCIAL. -->
<!-- 02-UI-SPEC.md pesa 73 KB: NO se carga entero. Solo la tarea 3 toca su sección Color, con grep -n '^## ' y Read con offset y limit. -->
<!-- Lo que ya se investigó al planificar (no repetir):
     - Fuente: `.ai` (PDF de 32 mesas de 800x800 pt) en el Drive público de Ari, id 1fBrYK6dJJwBRERVetXYePqSSUIMrcZ0C (LOGO_LOOPSGRWOTH.ai); BrandBook id 1qxC5Tow2cx4fAAW-Kt0tHVw-E6szyVtK. Descarga: curl -sL 'https://drive.google.com/uc?export=download&id=<id>' a un directorio temporal fuera del repo; `file` debe decir PDF document. Copia ya bajada en esta sesión: /private/tmp/claude-501/-Users-juan-Documents-Codigo-Arianna-loopsgrowth/95b7453c-d146-479b-b61b-39c583fd14fc/scratchpad/brand/logo.ai
     - `pdftocairo -svg -f N -l N` entrega cada mesa como SVG de 800x800 con fills en `rgb(p%, p%, p%)`. Las mesas con fondo traen uno o varios `<rect>` de -80 a 880 (la mesa 24 trae tres). El crema del texto apilado de las mesas 3 y 8 llega como #f4f3e1 (un punto de diferencia con #f4f3e0). Sin `<defs>`, `<use>`, `<image>` ni clip: solo `<rect>` y `<path>`.
     - Prototipo hecho al planificar (directorio temporal): quitar todos los `<rect>`, pasar a hex, ajustar el crema, SVGO con `-p 2 --multipass` y recortar el `viewBox` con getBBox en Chromium da SVG de 1.5 a 5.7 KB con un solo elemento raíz y rutas. Los recortes son idénticos entre mesas de una misma variante (apilado 482.38 x 267.48, horizontal e imagotipo 712.6 x 125.21, emblema 631.16 x 309.15, isotipo 543.02 x 331.99, ojo 437.34 x 449.09) y las mesas 13, 16 y 17 traen el mismo arte (también 18, 21 y 22; 1 y 5; 3 y 8; 10 y 24): solo cambia el fondo que se quita.
     - Colores de las mesas: #4228D1, #6C61DB (iris), #F4F3E0 (crema), #1E1E1E (pupila), más fondos #FFC602, #FD6938 y #212121.
     - Contrastes medidos con `contrastRaw` de scripts/lib/contrast.mjs: ver must_haves.truths y las tablas del final. -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Morado oficial #4228D1 de punta a punta (token, pares medidos, guardas y logo del header al mismo color)</name>
  <files>src/styles/tokens.css, scripts/lib/contrast.mjs, src/components/collage/collage-rules.mjs, src/components/SkipLinks.astro, tests/guards/contrast.test.mjs, tests/guards/brand-palette.test.mjs, tests/e2e/lib/brand.ts, tests/e2e/page-structure.spec.ts, tests/e2e/sections-problem-solution.spec.ts, tests/e2e/a11y-base.spec.ts, tests/e2e/brand-assets.spec.ts</files>
  <precondition>`node --test tests/guards/*.test.mjs` pasa antes de empezar (línea base verde de 02-03), el árbol de `src`, `tests`, `scripts` y `public` no tiene cambios sin commit, y anotas `git rev-parse HEAD` como `plan_head_before` en el SUMMARY (los criterios de diff de todo el plan lo usan como `PLAN_HEAD_BEFORE`).</precondition>
  <read_first>
    - src/styles/tokens.css (completo, 151 líneas).
    - scripts/lib/contrast.mjs solo de la línea 123 a la 170 (constantes y las tres listas de pares); el resto ya se describe aquí.
    - tests/guards/contrast.test.mjs (completo, 278 líneas).
    - Con grep -n y sin leer el archivo entero: `tests/e2e/page-structure.spec.ts` (constante del morado, línea 25), `tests/e2e/sections-problem-solution.spec.ts` (línea 27) y `tests/e2e/a11y-base.spec.ts` (líneas 145 a 152).
    - tests/e2e/brand-assets.spec.ts líneas 1 a 30 y 86 a 95 (importaciones y la prueba (e) del relleno del logo).
    - src/components/SkipLinks.astro líneas 44 a 52 y src/components/collage/collage-rules.mjs líneas 30 a 40 (solo para dos comentarios).
    - 02-BRAND-INVENTORY.md secciones 3 y 5 (líneas 33 a 40 y 54 a 67).
    - NO leas scripts/check-contrast.mjs (su mecánica va descrita abajo) ni 02-UI-SPEC.md (lo lee la tarea 3).
    - Skills a invocar con la herramienta Skill: `design-taste-frontend` e `impeccable`.
  </read_first>
  <behavior>
    - Con el token nuevo, `check-contrast` mide 14 pares aprobados (ratio medido dentro de 0.01) y 11 prohibidos (todos bajo 3), y los cuatro tonos siguen pasando sus pares por tono.
    - Cambiar `--color-brand-purple` a cualquier otro morado o `--color-brand-cream` a otro crema rompe la guarda con código 1 y nombra el par.
    - Un tono `purple` con `--bar` naranja falla y dice par prohibido con 2.95; con `--on-surface` oscuro falla con 1.88.
    - El morado anterior (hex y forma rgb) no aparece en `src`, `public`, `tests` ni `scripts`; el hex fuera de `tokens.css` y de los SVG oficiales es cero en `src/**/*.{astro,ts,mjs}`.
    - En el navegador, el relleno del logo del header y el color del h1 son iguales entre sí y a `--color-brand-purple`.
  </behavior>
  <action>
    Per CONTEXT (Morado de marca y logo: vigente, tokens, pares de contraste y guardas se recalculan) y BRAND-INVENTORY (secciones 3 y 6, punto 1).

    1. Skills (DSGN-04). Con la herramienta Skill invoca `design-taste-frontend` (lectura de diseño: landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, accesibilidad por encima de la estética; diales 7/3/4) e `impeccable` (context y el verbo `colorize`). Conflicto ya resuelto a favor de la marca: la taste skill desaconseja el morado "de IA"; aquí el azul violeta es el color del BrandBook. Si tu contexto es nuevo porque retomas el plan en otra tarea, invócalas al empezar tu primera tarea de UI y anótalo en el SUMMARY.
    2. Valores medidos por la planificación con `contrastRaw`. Confírmalos con un `node -e` sobre esa calculadora antes de escribir cifras en pruebas; si alguno difiere en el segundo decimal manda la calculadora y lo anotas. Aprobados: morado #4228d1 sobre blanco 8.55, blanco sobre morado 8.55, amarillo sobre morado 5.43, morado sobre amarillo 5.43, crema #f4f3e0 sobre morado 7.63, morado sobre crema 7.63, oscuro sobre crema 14.37, crema sobre oscuro 14.37. Prohibidos nuevos: oscuro sobre morado 1.88, naranja sobre morado 2.95, amarillo sobre crema 1.41, naranja sobre crema 2.58, blanco sobre crema 1.12; el de morado sobre oscuro pasa de 1.66 a 1.88.
    3. Rojo primero (pruebas antes del código; comprueba que fallan por aserción y no por sintaxis).
       a. Crea `tests/e2e/lib/brand.ts`: exporta `PURPLE_RGB` y `rgbOfToken(nombre)`, calculados al importar leyendo `src/styles/tokens.css` con `parseTokens` y `hexToRgb` de `../../../scripts/lib/contrast.mjs` y devolviendo el formato que da Chromium (por ejemplo `rgb(66, 40, 209)`). Así ningún spec repite el valor y, si Ari cambia el morado, los specs siguen.
       b. En `page-structure.spec.ts` (línea 25), `sections-problem-solution.spec.ts` (línea 27) y `a11y-base.spec.ts` (línea 150) quita el literal rgb del morado anterior y usa `PURPLE_RGB` importado de `./lib/brand`. En `a11y-base.spec.ts` corrige además el comentario de la línea 148 (el anillo amarillo sobre morado mide 5.43).
       c. En `brand-assets.spec.ts` agrega la prueba con título `(e2) el logo del header y el h1 comparten el morado de marca` a 1280 px: el relleno calculado del primer path de `header .brand-logo svg` y el color calculado del `h1` son iguales entre sí y a `PURPLE_RGB`. Es el rastreador: hoy falla porque el logo ya es #4228d1 y el h1 no.
       d. `contrast.test.mjs`: `HEX.purple` pasa a `#4228d1` y se agrega `HEX.cream` `#f4f3e0`; la prueba de ratios conocidos (hoy "reproduce los 15 ratios medidos") pasa a listar los 25 pares de las dos listas, 14 aprobados y 11 prohibidos, con sus valores nuevos y con el título actualizado (sigue siendo un oráculo escrito a mano e independiente de las listas del código); las cuentas pasan a 14 aprobados y 11 prohibidos (en la prueba de listas y en la de ejecución por defecto); la prueba de "pares nuevos" mide morado sobre amarillo 5.43; la de `--focus-ring` del morado a oscuro espera 1.88 en el mensaje; el css de la línea 100 usa el morado nuevo. Agrega mutaciones: (i) un tono `purple` con `--bar` naranja sale con 1, dice par prohibido y menciona 2.95; (ii) un tono `purple` con `--on-surface` oscuro sale con 1 y menciona 1.88; (iii) cambiar `--color-brand-purple` a otro morado cualquiera (por ejemplo un valor cercano al magenta antiguo pero distinto de él) rompe con 1 y nombra `purple sobre white` porque los ratios aprobados dejan de coincidir; (iv) cambiar `--color-brand-cream` a blanco rompe con 1 y nombra `cream sobre purple`; (v) un tono `light` con `--link` crema sale con 1 y menciona 1.12.
       e. Crea `tests/guards/brand-palette.test.mjs` (node:test, ESM): (1) recorre `src`, `public`, `tests` y `scripts` (sin `node_modules`, `dist`, `.astro` ni `test-results`) y falla si algún archivo de texto contiene el morado anterior en hex o en forma rgb; los dos literales se arman por partes dentro de la prueba para que el propio archivo no los contenga. (2) `tokens.css` declara exactamente estos seis hex en su bloque `@theme`: #4228d1 (purple), #fd6938, #ffc602, #212121, #ffffff y #f4f3e0 (cream), y no declara iris. (3) Ningún archivo `.astro`, `.ts` ni `.mjs` de `src` contiene hex, salvo `src/styles/tokens.css` (los SVG de `src/assets/brand` y `public/favicon.svg` quedan fuera del barrido); reutiliza la expresión de hex de la regla `hex` de `brand-assets.test.mjs`. (4) La excepción decorativa: `ALLOWED_FILLS.purple` incluye `orange` y `FORBIDDEN_PAIRS` incluye naranja sobre morado; si falta uno de los dos la prueba falla con un mensaje que explica que ambos se cambian juntos. (5) Mutación del barrido: una función de barrido sobre cadenas detecta el morado anterior en hex y en forma rgb y deja pasar el nuevo. (6) `tests/e2e/lib/brand.ts` existe, menciona `tokens.css`, y los tres specs de arriba lo importan y ya no definen un literal del morado.
       f. Corre `node --test tests/guards/contrast.test.mjs tests/guards/brand-palette.test.mjs`: debe salir distinto de 0. Construye con `npx astro build`, levanta `npx astro preview --port 4322` y corre solo la prueba nueva sobre `tests/e2e/brand-assets.spec.ts` con `-g "comparten el morado"`: debe fallar. Anota los nombres que fallan en el SUMMARY.
    4. Verde (código mínimo).
       a. `src/styles/tokens.css`: `--color-brand-purple: #4228d1;` (conserva el nombre del token y del tono `purple` para no tocar a ningún consumidor: `fillVar`, `HeroCollage`, `CollageSprite`, `SkipLinks`, `SectionShell`) y agrega `--color-brand-cream: #f4f3e0;` junto a los otros cuatro primitivos. Reescribe el comentario de cabecera: los colores de marca son el morado azulado, el naranja, el amarillo, el oscuro y el blanco, más el crema del logo; existen una sola vez aquí, salvo los SVG oficiales de `src/assets/brand`, que traen sus colores por diseño; el iris y la pupila del isotipo viven solo en esos SVG; sustituye la frase "No agregar neutrales: el brandbook no define crema ni grises" por la regla nueva (el crema no es un tono ni un neutral libre: es superficie de chips y píldoras con las reglas de abajo). Cuenta en un comentario que la página 8 del BrandBook rotula el morado con otro valor y que se trata como etiqueta errónea, sin repetir ese hex ni su valor RGB (la guarda los rechaza en `src`). En el comentario "Reglas que la guarda protege" agrega: nunca naranja sobre morado (2.95), nunca oscuro sobre morado, y sobre crema nunca amarillo, naranja ni blanco; y una línea que diga que cambiar el morado exige actualizar este token y las razones medidas de `scripts/lib/contrast.mjs` (no basta un solo lugar). Los cuatro bloques `[data-tone]` no cambian: ya leen tokens y los pares por tono de la lista de arriba pasan con el valor nuevo.
       b. `scripts/lib/contrast.mjs`: agrega la constante del crema junto a P, O, Y, D y W. `APPROVED_PAIRS`: ratios nuevos 8.55 (morado sobre blanco), 8.55 (blanco sobre morado), 5.43 (amarillo sobre morado) y 5.43 (morado sobre amarillo); quita naranja sobre morado; agrega crema sobre morado 7.63 (uso: logo apilado y texto crema sobre morado), morado sobre crema 7.63 (chips y píldoras), oscuro sobre crema 14.37 (texto sobre crema) y crema sobre oscuro 14.37 (logo apilado sobre oscuro), todos con umbral 4.5; el comentario de la lista dice ahora 14 pares. `FORBIDDEN_PAIRS`: morado sobre oscuro con 1.88; agrega oscuro sobre morado 1.88, naranja sobre morado 2.95 (explica que ya no sirve ni como borde de UI), amarillo sobre crema 1.41, naranja sobre crema 2.58 y blanco sobre crema 1.12, todos con umbral 3; el comentario dice ahora 11. La mecánica de `scripts/check-contrast.mjs` no cambia: cada aprobado debe cumplir su umbral con el valor exacto y coincidir con su `ratio` dentro de 0.01, cada prohibido debe medirse bajo su umbral, y un tono que declare un par prohibido falla con la etiqueta "par prohibido".
       c. Comentarios que quedaron viejos: `SkipLinks.astro` (cita 6.15 sobre morado; ahora 5.43) y `collage-rules.mjs` (agrega junto a `ALLOWED_FILLS` una línea que explique que el naranja sobre morado mide 2.95, está prohibido para texto y UI en `scripts/lib/contrast.mjs` y solo se permite aquí como relleno decorativo con contorno blanco).
    5. Corre todo lo de la sección de verificación de esta tarea. Si el ejecutor puede correr los e2e de las cinco specs, confirma que pasan sin editar más pruebas que las de la lista.
    6. Commit atómico: `feat(02-09): morado oficial #4228D1, crema y pares de contraste medidos`. Punto de corte 1: si tu contexto pasa de 50 %, deja SUMMARY parcial (`status: partial`) y detente aquí.
    Protocolo de navegador (lección de la fase 1): `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio de código y `npx astro preview stop` al final. No leas `.env` ni envíes el formulario de ClickUp.
  </action>
  <acceptance_criteria>
    - `grep -q -e '--color-brand-purple: #4228d1' src/styles/tokens.css && grep -q -e '--color-brand-cream: #f4f3e0' src/styles/tokens.css` (dos comprobaciones separadas) y `! grep -rniE '73187f' src public tests scripts` sale limpio; `! grep -rnE 'rgb\(115, ?24, ?127\)' src public tests scripts` sale limpio.
    - `node scripts/check-contrast.mjs` sale 0 e imprime `14/14 pares aprobados, 11 prohibidos verificados`.
    - `node --test tests/guards/*.test.mjs` pasa, incluida la mutación que cambia el morado y la del crema, y la guarda `brand-palette` (que falló antes de escribir el código).
    - Ningún hex en `src/components`, `src/pages` ni `src/layouts`: `! grep -rnE '#[0-9a-fA-F]{3}([^0-9a-zA-Z_-]|$)|#[0-9a-fA-F]{6}([^0-9a-zA-Z_-]|$)' src/components src/pages src/layouts`.
    - Con el build servido, la prueba `(e2)` pasa: el relleno del logo del header y el color del h1 son `rgb(66, 40, 209)` y coinciden con el token; las specs `page-structure`, `sections-problem-solution`, `a11y-base`, `cta-focus` y `brand-assets` pasan.
    - `[ -z "$(git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- src/content PENDING-COPY.md src/components/sections src/components/ui src/layouts)" ]` (con `PLAN_HEAD_BEFORE` igual al `plan_head_before` anotado, tras el commit de la tarea).
    - El SUMMARY lista los nombres de las pruebas que fallaron en rojo y las mediciones confirmadas.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/sections-problem-solution.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/cta-focus.spec.ts; s=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ]</automated>
    <fails_when>El morado anterior sigue en algún archivo de código; la guarda de contraste pasa con el token viejo o con el crema cambiado; el h1 y el logo del header no comparten color; algún par aprobado o prohibido queda con un ratio distinto del medido; el crema se vuelve un quinto tono; se agrega hex a un componente; una prueba de la fase 1 o de 02-03 se rompe.</fails_when>
  </verify>
  <done>La página usa #4228D1 desde un solo token, el h1 y el logo del header son el mismo color, los pares aprobados y prohibidos están medidos y probados por mutación (14 y 11), el crema existe como token sin ser un tono, ninguna prueba previa se rompe y todo queda en un commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Las 17 mesas oficiales como SVG limpios, catálogo por tono, Logo por tono y hoja de revisión</name>
  <files>scripts/lib/brand-svg.mjs, scripts/brand/extract-artboards.mjs, src/components/brand/logo-variants.mjs, src/assets/brand/apilado-01-blanco.svg, src/assets/brand/apilado-03-morado.svg, src/assets/brand/apilado-05-amarillo.svg, src/assets/brand/apilado-08-oscuro.svg, src/assets/brand/horizontal-06-blanco.svg, src/assets/brand/imagotipo-07-blanco.svg, src/assets/brand/emblema-10-blanco.svg, src/assets/brand/emblema-12-morado.svg, src/assets/brand/emblema-24-amarillo.svg, src/assets/brand/isotipo-13-blanco.svg, src/assets/brand/isotipo-14-morado.svg, src/assets/brand/isotipo-16-amarillo.svg, src/assets/brand/isotipo-17-oscuro.svg, src/assets/brand/ojo-18-blanco.svg, src/assets/brand/ojo-19-morado.svg, src/assets/brand/ojo-21-amarillo.svg, src/assets/brand/ojo-22-oscuro.svg, src/assets/brand/logo-horizontal.svg, src/assets/brand/isotipo.svg, src/components/brand/Logo.astro, src/components/SiteHeader.astro, src/pages/marca/[sheet].astro, tests/guards/brand-assets.test.mjs, tests/e2e/brand-assets.spec.ts, .gitignore</files>
  <precondition>El `.ai` de Ari (PDF de 32 mesas) existe en una ruta fuera del repo y `file <ruta>` dice PDF document; si no, bájalo con curl del id 1fBrYK6dJJwBRERVetXYePqSSUIMrcZ0C a un directorio temporal. Además `command -v pdftocairo` y `test -x node_modules/.bin/svgo` (herramientas locales; no se instala nada) y la tarea 1 está commiteada.</precondition>
  <read_first>
    - 02-BRAND-INVENTORY.md sección 2 (líneas 17 a 31): las 32 mesas por variante y fondo.
    - La sección "Catálogo de mesas" al final de ESTE plan: tablas de mesas usadas y no extraídas, con sus razones medidas. Es la fuente del catálogo; no vuelvas a medir.
    - src/components/brand/Logo.astro (completo, 95 líneas), src/components/SiteHeader.astro (completo, 59 líneas) y src/pages/marca/[sheet].astro (completo, 210 líneas).
    - tests/guards/brand-assets.test.mjs (completo, 220 líneas: se le agregan pruebas y no se rompe la lista `OWNED`, que incluye `Logo.astro`).
    - tests/e2e/brand-assets.spec.ts líneas 1 a 140 (encabezado, pruebas del header y hoja de identidad, que se reemplaza) y lo que agregó la tarea 1.
    - De los SVG generados solo `head -c 300` de uno para confirmar su forma; nunca leas uno entero.
    - NO leas 02-UI-SPEC.md, otros specs ni el resto de `brand-assets.spec.ts`.
    - Skills: las de la tarea 1 (reutiliza si sigues en el mismo contexto; si no, invoca `design-taste-frontend` e `impeccable`).
  </read_first>
  <behavior>
    - `resolveLogo('horizontal', 'light')` devuelve la mesa 06 y su archivo; `resolveLogo('horizontal', 'dark')`, `resolveLogo('imagotipo', 'yellow')` y `resolveLogo('emblema', 'dark')` lanzan un error en español que nombra variante, tono y los tonos disponibles.
    - `cleanArtboard` quita todos los `<rect>` (la mesa 24 trae tres), convierte `fill="rgb(p%, p%, p%)"` a hex en minúsculas con `Math.round(p * 255 / 100)` por canal, ajusta al color oficial más cercano si cada canal difiere en 2 o menos (`rgb(95.698547%, 95.298767%, 88.198853%)` sale como el crema oficial y no como #f4f3e1) y lanza si un color queda fuera de la paleta.
    - El catálogo cuenta las 32 mesas sin repetir: 17 usadas con archivo, tono y razón medida, y 15 con motivo.
    - Los 17 archivos de `src/assets/brand` son exactamente los del catálogo, con solo elementos `svg` y `path`, `viewBox` dentro de 0 a 800, menos de 8192 bytes y solo los cuatro rellenos oficiales.
    - Cada mesa usada mide su color principal contra la superficie de su tono con la razón registrada, y solo las mesas 17 y 22 quedan bajo su umbral, con excepción escrita.
    - En el navegador cada `Logo` trae la mesa de su tono, sus rellenos calculados son los del archivo (sin recolorear), su alto es al menos el mínimo de su variante y el del header es la mesa 06.
  </behavior>
  <action>
    Per CONTEXT (Morado de marca y logo: vigentes "el logo, imagotipo, emblema e isotipos salen de las 32 mesas del .ai con la versión oficial de cada fondo, no se recolorea un solo SVG por CSS" y el área de salvado X) y BRAND-INVENTORY (sección 2 y sección 6, punto 2).

    1. Skills (DSGN-04): ver la tarea 1. Este es trabajo de identidad visual: los verbos que aplican son `shape` (jerarquía de la hoja) y `adapt` (mínimos de tamaño por variante).
    2. Fuente. Registra en el SUMMARY el id de Drive y el `shasum -a 256` del `.ai` usado. Ninguno de los originales se versiona: agrega a `.gitignore` los patrones `*.ai`, `BrandBook*.pdf` y `.planning/phases/*/brand-inventory/` (las imágenes del BrandBook son CONFIDENCIAL y hoy aparecen sin ignorar en `git status`).
    3. Rojo primero.
       a. `tests/guards/brand-assets.test.mjs`: agrega estas pruebas (todas importan `src/components/brand/logo-variants.mjs` o leen `src/assets/brand`, que aún no existen, así que fallan). (i) Catálogo: `ARTBOARDS` trae las mesas 1 a 32 una sola vez, cada una con `use` verdadero o un `reason` no vacío. (ii) Archivos: los nombres de `src/assets/brand/*.svg` son exactamente los `file` de las mesas usadas, 17 en total (esto obliga a borrar los dos archivos viejos). (iii) Higiene de cada SVG: `viewBox` de cuatro números con x, y no negativos y x+ancho e y+alto menores o iguales a 800 (un fondo sin quitar daría -80 a 960), sin `width` ni `height` en la raíz, menos de 8192 bytes, únicamente los elementos `svg` y `path`, ningún atributo `href`, `style`, `on*` ni `xlink`, ningún comentario, metadato, texto, imagen, script, `defs` ni `use`, y rellenos contenidos en {#4228d1, #6c61db, #f4f3e0, #1e1e1e} (los dos últimos, iris y pupila, son colores propios de los SVG y solo se nombran en esta prueba). (iv) Contraste por mesa: para cada mesa usada, el color `fg` (nombre de token: `purple` o `cream`) contra `--surface` del tono, leído con `parseTokens` de `scripts/lib/contrast.mjs`, coincide con el `ratio` del catálogo dentro de 0.01; el hex de `fg` aparece entre los rellenos del SVG; una mesa queda bajo su `min` si y solo si trae `exception`, y el conjunto de mesas con excepción es exactamente {17, 22} según una lista escrita aparte en la prueba. (v) `resolveLogo`: la mesa correcta por variante y tono, y los tres errores de arriba con el mensaje esperado. (vi) `Logo.astro` importa exactamente los archivos del catálogo (mismo conjunto), no declara ninguna regla de relleno por CSS y no usa `data-tone` (usaría los bloques de tono y el fondo global). (vii) `cleanArtboard` con cadenas de prueba: tres `<rect>` repetidos salen todos, el crema #f4f3e1 sale ajustado, un gris fuera de paleta lanza, y quitar `width`, `height` y `xmlns:xlink`. (viii) Mutación: una copia del catálogo sin `exception` en la mesa 17, o con un `ratio` cambiado, es detectada por la comprobación (iv). (ix) Originales: `git ls-files` (omite la prueba si no es un repositorio) no lista archivos `.ai`, `.pdf` ni rutas dentro del directorio `brand-inventory/` (comparación que distingue mayúsculas, porque `02-BRAND-INVENTORY.md` sí se versiona).
       b. `tests/e2e/brand-assets.spec.ts`: importa el catálogo y `./lib/brand`. `LOGO_RATIO` pasa a leerse de `src/assets/brand/horizontal-06-blanco.svg`. En el header agrega: `.brand-logo` con `data-artboard` `06`, `data-variant` `horizontal` y `data-logo-tone` `light`, igual al `data-tone` del ancestro más cercano. La prueba `(e)` exige que el relleno sea `PURPLE_RGB`. Reemplaza la prueba de la hoja de identidad (la de "blanco sobre dark"; la de `noindex` se conserva) por un bloque "hoja de revisión: identidad por tono" a 320, 390, 768, 1024 y 1280 px: hay una sección `[data-sheet="identidad"]` por cada tono con los `data-artboard` esperados del catálogo (light 6, yellow 4, dark 3 y purple 4, 17 en total); cada svg de logo tiene `role="img"`, `aria-label` igual al nombre del YAML, `focusable="false"` y sin aria-hidden; su alto es al menos el mínimo de su variante (horizontal 32, imagotipo 32, apilado 48, emblema 96, isotipo 24, ojo 24 px); el relleno calculado de cada path es igual al atributo `fill` del propio path (no hay recolor por CSS) y el conjunto de rellenos contiene el `fg` del catálogo en formato rgb; hay una franja `[data-sheet="paleta"]` con seis `[data-swatch]` (purple, cream, orange, yellow, dark y white) cuyo hex impreso coincide con `tokens.css`; sin scroll horizontal. Agrega un bloque `captura de la hoja` que solo corre con `PHASE2_BATCH` (`test.skip` si no) y guarda `test-results/phase2/<lote>-hoja-<ancho>.png` a página completa en los cinco anchos con todo lo que no es localhost abortado.
       c. Corre `node --test tests/guards/brand-assets.test.mjs`: debe fallar por aserciones e importaciones que aún no existen. Anota los nombres.
    4. Catálogo. Crea `src/components/brand/logo-variants.mjs` (ESM plano con JSDoc, lo importan Astro y `node --test`; sin hex: `fg` es un nombre de token). Exporta `LOGO_TONES` (light, yellow, dark, purple), `LOGO_VARIANTS` (apilado, horizontal, imagotipo, emblema, isotipo, ojo), `MIN_HEIGHT_PX` (las mínimas de arriba), `ARTBOARDS` (las 32 mesas de las dos tablas del final: `n`, `variant`, `bg`, `tone` o nulo, `use`, `file` sin extensión, `fg`, `ratio`, `min`, y `exception` o `reason`), `availableTones(variant)` y `resolveLogo(variant, tone)`, que devuelve la mesa usada o lanza `Error` en español: `El logo "<variante>" no tiene mesa oficial para el tono "<tono>" (disponibles: ...). No se recolorea: usa una variante con mesa oficial.`
    5. Ayuda pura y script. `scripts/lib/brand-svg.mjs` exporta `cleanArtboard(svgText, mesa)` con las reglas de `<behavior>` y la lista de colores oficiales (los cinco del brandbook más iris y pupila). `scripts/brand/extract-artboards.mjs` recibe `--source <ruta al .ai>` (obligatorio), `--only <mesas>` y `--out` (por defecto `src/assets/brand`); cabecera en español con requisitos (poppler, SVGO de `node_modules/.bin`, Chromium de `@playwright/test`), que el `.ai` no se versiona y cómo bajarlo. Por cada mesa usada: comprueba con `file` que la fuente es PDF; `pdftocairo -svg -f N -l N`; `cleanArtboard`; SVGO por línea de comandos (`-p 2 --multipass -q`); abre el resultado en Chromium y mide `getBBox()` de la raíz; sustituye el `viewBox` por esa caja con dos decimales, sin holgura (el área de salvado es CSS, no arte); escribe `<out>/<file>.svg`. Todas las llamadas a procesos son `spawnSync` con lista de argumentos, nunca con shell. Sale con 1 si un color queda fuera de paleta, si queda algún `<rect>`, o si la caja se sale de 0 a 800. Imprime una tabla mesa, archivo, bytes, rutas y viewBox.
    6. Ejecuta la extracción sobre las 17 mesas. Borra `src/assets/brand/logo-horizontal.svg` e `isotipo.svg` (son la mesa 06 y la 13 con nombre viejo). Las mesas 13, 16 y 17 traen el mismo arte (y 18, 21 y 22; 1 y 5; 3 y 8; 10 y 24): se guardan aparte para poder auditar cada mesa contra el `.ai`; anótalo en el SUMMARY.
    7. `Logo.astro`. Props: `name` (obligatoria, de `brand.name.text`), `href?`, `variant?` (por defecto `horizontal`), `tone?` (por defecto `light`) y `class?`. Importa los 17 SVG como componentes de Astro con importaciones explícitas (no `import.meta.glob`: no está confirmado que entregue componentes) y un mapa por nombre de archivo; llama `resolveLogo(variant, tone)` en el frontmatter. Contenedor `<span class="brand-logo" data-variant data-logo-tone data-artboard>` con el número de mesa de dos dígitos. Nunca `data-tone` en el contenedor, y el comentario de cabecera tampoco nombra ese atributo (la guarda y un criterio de aceptación lo rechazan en todo el archivo, comentarios incluidos): usa las palabras "atributo de tono de la página". Conserva el contrato de 02-02: `role="img"`, `aria-label={name}`, `focusable="false"`, sin aria-hidden, enlace solo si hay `href`, foco visible con las variables existentes. Elimina la regla que recoloreaba el logo a blanco sobre dark y purple: ahora la mesa oficial ya viene en su color. Tamaños por variante con `--logo-min` y `--logo-h` (horizontal e imagotipo 32 px y 2 rem, 2.5 rem desde 40em; apilado 48 px y 4 rem; emblema 96 px y 7.5 rem; isotipo y ojo 24 px y 2.5 rem) y `--logo-clear` igual al alto efectivo, salvo el apilado, donde es la mitad (BrandBook página 7: el arte apilado mide 2 X). Actualiza el comentario de cabecera (mesas oficiales por tono, sin recolor, qué combinaciones no existen y por qué). Debe seguir cumpliendo las reglas de `OWNED`: sin hex, sin animaciones, sin título, sin inserción de HTML crudo.
    8. `SiteHeader.astro`: una constante `tone = 'light'` que alimenta el `data-tone` del `<header>` y la prop `tone` del logo, para que no puedan separarse. Nada más cambia en el header.
    9. Hoja `src/pages/marca/[sheet].astro`: reemplaza el bloque de identidad viejo por una sección `.sheet-band[data-tone][data-sheet="identidad"]` por tono (light, yellow, dark, purple), con una lista de las mesas usadas de ese tono (orden: apilado, horizontal, imagotipo, emblema, isotipo, ojo), cada una en una zona con borde punteado y padding igual a `--logo-clear` (por debajo de 64em el padding se limita a 1.5 rem para que no desborde; la zona reduce su padding, nunca el logo) y un rótulo `<code>` con `<variante> / mesa NN`. Agrega la franja `[data-sheet="paleta"]` con las seis muestras, cada una un elemento con `data-swatch` (purple, cream, orange, yellow, dark o white), el color de fondo del token y dos `code` (nombre del token y hex impreso): el hex se lee en el frontmatter con `readFileSync('src/styles/tokens.css', 'utf8')` y `parseTokens`, así la página no lleva ningún hex escrito. Conserva sin cambios los bloques de primitivas, composiciones y avatares. Rótulos solo con ids, sin frases.
    10. Corre el ciclo de verificación de la tarea, corrige y haz un commit atómico: `feat(02-09): 17 mesas oficiales, catalogo por tono y Logo sin recolor`. Punto de corte 2 (el más importante): identidad completa; si tu contexto pasa de 50 %, deja SUMMARY parcial y detente.
    Protocolo de navegador: `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio y `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - `ls src/assets/brand/*.svg | wc -l` da 17, los nombres son los del catálogo y `test ! -e src/assets/brand/logo-horizontal.svg && test ! -e src/assets/brand/isotipo.svg`.
    - `! grep -l '<rect' src/assets/brand/*.svg` sale limpio; `grep -ohE 'fill="#[0-9a-f]{6}"' src/assets/brand/*.svg | sort -u | wc -l` da 4; `find src/assets/brand -name '*.svg' -size +8192c` no lista nada.
    - `node --test tests/guards/*.test.mjs` pasa (incluidas las nueve pruebas nuevas y sus mutaciones, que fallaron antes del código).
    - `tracked=$(git ls-files) && ! printf '%s\n' "$tracked" | grep -Eiq '\.(ai|pdf)$' && ! printf '%s\n' "$tracked" | grep -Eq 'brand-inventory/'` (captura primero los archivos versionados y luego busca; el patrón del directorio va en minúsculas y con barra para no confundirlo con `02-BRAND-INVENTORY.md`) y `.gitignore` trae los tres patrones.
    - Sin hex en `src/components`, `src/pages` ni `src/layouts` (misma comprobación de la tarea 1) y `Logo.astro` no contiene el atributo de tono de la página: `! grep -n 'data-tone' src/components/brand/Logo.astro`.
    - Con el build servido: el header trae la mesa 06 con `data-logo-tone` `light`; la hoja muestra 17 logos con el reparto 6, 4, 3 y 4 por tono, cada uno con su alto mínimo, sin recolor y sin scroll horizontal a los cinco anchos; la paleta imprime seis hex iguales a los de `tokens.css`.
    - `PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build` no genera `dist/marca` y el build normal posterior sí genera `dist/marca/hoja/index.html`.
    - `dist/index.html` sigue por debajo de 61440 bytes (guarda existente).
    - El SUMMARY registra el id de Drive y el sha256 del `.ai`, la tabla del script (bytes y viewBox por mesa) y la lista de pruebas que estaban en rojo.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts tests/e2e/a11y-base.spec.ts tests/e2e/page-structure.spec.ts; s=$?; npx astro preview stop; PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build >/dev/null 2>&1; test ! -e dist/marca; p=$?; npx astro build >/dev/null 2>&1; [ $g -eq 0 ] && [ $s -eq 0 ] && [ $p -eq 0 ]</automated>
    <fails_when>Queda un archivo de logo viejo o falta una mesa; un SVG conserva el fondo, un color fuera de paleta, un elemento activo o más de 8192 bytes; el logo se recolorea por CSS; `resolveLogo` acepta horizontal sobre oscuro o emblema sobre oscuro; el catálogo pierde una de las 32 mesas o la excepción de las mesas 17 y 22 se amplía sin listarla; el `.ai`, el PDF o `brand-inventory` quedan versionables; el logo del header deja de ser la mesa 06, pierde su nombre accesible o su área de salvado; la hoja desborda a 320 px o se genera con PUBLIC_ENV=production.</fails_when>
  </verify>
  <done>Las 17 mesas oficiales viven como SVG limpios con nombre de mesa, el catálogo de 32 mesas está probado con mutación, `Logo` elige la mesa por tono sin recolorear, el header usa la mesa 06, la hoja muestra cada mesa sobre su tono con la paleta, los originales no son versionables y todo queda en un commit con la suite previa en verde.</done>
</task>

<task type="auto">
  <name>Task 3: Favicon de la mesa 18 con evidencia y documentación al día con el morado oficial</name>
  <files>scripts/brand/extract-artboards.mjs, public/favicon.svg, public/favicon.ico, tests/guards/brand-assets.test.mjs, tests/e2e/brand-assets.spec.ts, src/pages/marca/[sheet].astro, DESIGN.md, PRODUCT.md, .claude/CLAUDE.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md</files>
  <read_first>
    - public/favicon.svg solo con `head -c 300` (la forma vieja, mesa 13) y tests/e2e/brand-assets.spec.ts líneas 140 a 166 (pruebas del favicon).
    - scripts/brand/extract-artboards.mjs (de la tarea 2; solo para agregar la opción del favicon) y la sección de la hoja de `src/pages/marca/[sheet].astro` que agregó la tarea 2 (solo para sumar el bloque del favicon).
    - DESIGN.md líneas 1 a 12 y 66 a 116 (front matter, colores, pares y reglas) y PRODUCT.md línea 43.
    - Con grep -n y Read de 6 líneas cada uno: `.claude/CLAUDE.md` (tabla "Contraste medido de la paleta de marca", líneas 128 a 145), `.planning/REQUIREMENTS.md` (línea de FND-03) y `.planning/PROJECT.md` (línea 28).
    - 02-UI-SPEC.md solo la sección `## Color`: localízala con `grep -n '^## ' ` y lee con offset y limit de 20 líneas desde el encabezado.
    - NO leas el resto de la UI-SPEC ni los planes 02-04 a 02-08.
    - Skills: las de la tarea 1 (reutiliza si es el mismo contexto).
  </read_first>
  <action>
    Per CONTEXT (Morado de marca y logo: vigente) y UI-SPEC (Collage and Brand Assets Contract: el favicon reemplaza los de Astro por el isotipo; 02-02 dejó abierta la elección entre la mesa 13 y la 18).

    1. Evidencia del favicon (decide con números, no a ojo). Agrega a `scripts/brand/extract-artboards.mjs` la opción `--favicon-evidence`: renderiza en Chromium `isotipo-13-blanco.svg` y `ojo-18-blanco.svg` con fondo transparente a 16 y 32 px de lado, con un margen de 4 % por lado, mide el alto y el ancho de la caja de píxeles no transparentes y el diámetro equivalente de la pupila (píxeles oscuros de #1e1e1e), e imprime la tabla. La planificación midió sobre la geometría del vector, a 16 px: mesa 13, 14.7 x 9.0 px y pupila de 2.2 px; mesa 18, 14.3 x 14.7 px y pupila de 3.7 px. Regla de decisión: gana la que cumple alto de la marca de 12 px o más y pupila de 3 px o más; si ambas o ninguna cumplen, se prefiere la de dos ojos por identidad. Corre la opción, compara con los números de arriba (si tu medición contradice el resultado esperado, manda la regla aplicada a tu medición) y registra la tabla en el SUMMARY. El resultado esperado es la mesa 18, que además es el paso 3 de la construcción del isotipo del BrandBook (página 6).
    2. Favicon. Agrega la opción `--favicon <archivo del catálogo>` (por defecto `ojo-18-blanco`): toma el SVG ya extraído, arma `public/favicon.svg` con `xmlns`, un `viewBox` cuadrado centrado en la caja del arte con 4 % de margen por lado (el lado es el mayor de ancho y alto dividido entre 0.92) y los mismos trazos con sus rellenos, sin script, sin `style` y sin referencias externas, menos de 3072 bytes. Después renderiza ese SVG con Chromium a 16, 32 y 48 px con fondo transparente y empaqueta un `public/favicon.ico` con los tres PNG dentro (formato ICO con imágenes PNG, en ese orden, con un empaquetado propio de Node y sin dependencias nuevas). Corre la opción.
    3. Pruebas del favicon. En `brand-assets.test.mjs`: los datos de trazo (`d`) de `public/favicon.svg` son los de `src/assets/brand/ojo-18-blanco.svg` y solo esos (prueba de que sale de la mesa oficial y no se redibujó), con `xmlns`, viewBox cuadrado, sin `script`, `style`, `href` ni `xlink`, menos de 3072 bytes; mutación: un favicon con un trazo distinto es detectado. En `brand-assets.spec.ts` se conservan las pruebas de `/favicon.ico` (200, tres imágenes de 16, 32 y 48 px, más de 655 bytes) y de `/favicon.svg` (viewBox cuadrado, sin script ni referencias http). En la hoja suma un bloque `[data-sheet="favicon"]` con `favicon.svg` a 16, 32 y 48 px sobre una muestra `light` y otra `dark` (imágenes con `alt` que diga el tamaño) y una prueba que comprueba que las seis imágenes cargan (`naturalWidth` mayor que 0).
    4. Documentación al día (el contexto de los agentes futuros lee estos archivos, así que no pueden seguir citando el morado anterior como vigente). Todo con `Edit` de una sola línea o bloque, nunca `Write` sobre archivos existentes.
       a. `DESIGN.md`: el `purple` del front matter pasa a `#4228d1` y se agrega `cream: "#f4f3e0"`; el párrafo de "Pares medidos" pasa a los 14 aprobados con sus ratios nuevos; la regla de "Don't" cita morado sobre oscuro con 1.88; agrega tres líneas: el crema es superficie de chips y píldoras y no un tono, con sus pares; el naranja sobre morado (2.95) solo es relleno decorativo del collage; los logos salen de las mesas oficiales por tono y no se recolorean. Puedes explicar la etiqueta errónea de la página 8 del BrandBook citando su valor (este archivo no lo barre la guarda).
       b. `PRODUCT.md` línea 43: la paleta del brandbook pasa a Purblue #4228D1 con la nota de la etiqueta errónea de la página 8 y el crema del logo.
       c. `.claude/CLAUDE.md`, tabla "Contraste medido de la paleta de marca": cambia solo las cinco filas del morado anterior a las medidas nuevas (morado sobre blanco 8.55; blanco sobre morado 8.55; morado sobre amarillo 5.43; naranja sobre morado 2.95, que ahora Falla en texto y en UI y solo sirve como relleno decorativo con contorno; morado sobre oscuro 1.88, Falla) y agrega filas para el crema (crema sobre morado 7.63 y morado sobre crema 7.63 OK; oscuro sobre crema 14.37 OK; amarillo sobre crema 1.41, naranja sobre crema 2.58 y blanco sobre crema 1.12, Falla). No toques otras secciones de ese archivo.
       d. `.planning/REQUIREMENTS.md` (FND-03) y `.planning/PROJECT.md` (línea 28): "Purple #73187F" pasa a "Purple #4228D1" (el requisito sigue cumplido: el token y el script de contraste existen).
       e. `02-UI-SPEC.md`, sección Color: inserta justo bajo el párrafo "Sin neutrales inventados..." un bloque "Fe de erratas de color (2026-09-19)" de cinco líneas máximo: el morado vigente es #4228D1, el crema #F4F3E0 es token de superficie y no tono, los ratios de la tabla que involucran al morado quedan reemplazados por los de `scripts/lib/contrast.mjs` (8.55, 5.43 y naranja sobre morado 2.95 prohibido) y el resto de la tabla se conserva. No reescribas la tabla.
    5. Corre la verificación, corrige y haz un commit atómico: `feat(02-09): favicon de la mesa 18 con evidencia y docs con el morado oficial`. Punto de corte 3: si tu contexto pasa de 50 %, deja SUMMARY parcial y detente.
    Protocolo de navegador: igual que en las tareas anteriores.
  </action>
  <acceptance_criteria>
    - `public/favicon.svg` pesa menos de 3072 bytes, su `viewBox` es cuadrado, trae `xmlns` y sus datos de trazo son los de `ojo-18-blanco.svg` (guarda); `public/favicon.ico` supera 655 bytes, empieza con los bytes 00 00 01 00 y declara tres imágenes de 16, 32 y 48 px.
    - El SUMMARY trae la tabla de evidencia a 16 y 32 px de las mesas 13 y 18 y la decisión con la regla aplicada.
    - `grep -q 'purple: "#4228d1"' DESIGN.md`; `grep -q 'Fe de erratas de color' .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md`; `grep -q '#4228D1' .planning/REQUIREMENTS.md && grep -q '#4228D1' .planning/PROJECT.md && grep -q '#4228D1' .claude/CLAUDE.md` (una comprobación por archivo).
    - `node --test tests/guards/*.test.mjs` y `node scripts/check-contrast.mjs` pasan; `[ -z "$(git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- package.json package-lock.json)" ]` (tras el commit de la tarea).
    - En la hoja las seis imágenes del favicon cargan y no hay scroll horizontal a los cinco anchos.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/brand-assets.spec.ts; s=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ]</automated>
    <fails_when>El favicon no sale de la mesa 18 o se redibujó; falta una talla del `.ico` o pesa 655 bytes o menos; el `.svg` trae script, estilo o referencias externas; la decisión no cita la medición; algún documento vivo sigue citando el morado anterior como el vigente; se agrega una dependencia; se reescribe la tabla de la UI-SPEC en lugar de anotar la fe de erratas.</fails_when>
  </verify>
  <done>El favicon sale de la mesa oficial de un ojo con lupa con la medición a 16 px registrada, y DESIGN.md, PRODUCT.md, CLAUDE.md, REQUIREMENTS.md, PROJECT.md y la UI-SPEC dejan de contradecir el morado oficial. Todo queda en un commit.</done>
</task>

<task type="auto">
  <name>Task 4: Ciclo visual con impeccable y design-taste-frontend a cinco anchos y cierre de verificación</name>
  <files>.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md, src/styles/tokens.css, src/components/brand/Logo.astro, src/components/collage/AgendaCollage.astro, src/components/collage/collage-rules.mjs, tests/guards/brand-palette.test.mjs</files>
  <read_first>
    - 02-VISUAL-LOG.md solo las últimas 12 líneas (para seguir su formato) y `.planning/phases/02-secciones-marca-y-copy/02-BRAND-INVENTORY.md` sección 4 (líneas 41 a 52: rasgos del moodboard).
    - Las capturas de esta tarea, una por una y solo las necesarias: `M-390.png` y `M-1280.png` de la página, `M-hoja-1280.png` de la hoja y, si un hallazgo lo pide, `M-320.png`. Además `brand-inventory/ai_a.png` y `ai_b.png` (las 32 mesas) para comparar cada logo de la hoja con su mesa. Las demás imágenes de `brand-inventory/` solo si un hallazgo lo pide.
    - Lista de "vibra" de 02-UI-SPEC.md (`### Lista de "vibra"`, localízala con grep -n y lee 12 líneas) para los rasgos 1 y 7.
    - Los archivos de corrección (tokens, Logo, AgendaCollage, collage-rules) solo si el crítico pide un cambio en ellos.
    - Skills a invocar con la herramienta Skill: `design-taste-frontend` e `impeccable` (si tu contexto ya las cargó en las tareas anteriores, reutilízalas y anótalo; si no, invócalas ahora).
  </read_first>
  <action>
    Per CONTEXT (Estilo visual: método `impeccable` más `design-taste-frontend`, iteraciones hasta la vibra, límites que no se negocian; Morado de marca y logo: vigente) y UI-SPEC (Design Method and Iteration Protocol).

    1. Ronda de captura (una sola, en lote). Con el build servido y ClickUp bloqueado corre `PHASE2_BATCH=M E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts tests/e2e/brand-assets.spec.ts -g captura`: guarda en `test-results/phase2/` (no versionadas) `M-{320,390,768,1024,1280}` con sus variantes `-reduce` y `-nojs` de la página y `M-hoja-{320,390,768,1024,1280}` de la hoja. Mira solo las capturas listadas en `read_first`; a 320, 768 y 1024 px confía en las mediciones de los specs (sin desborde, alto mínimo de cada logo).
    2. Crítica con `impeccable` (verbos `colorize`, `critique` y `polish`; `adapt` solo si un logo se ve mal a 320 px) y `design-taste-frontend` (diales 7, 3 y 4). Puntos a juzgar, cada uno con veredicto en el registro: (a) el azul violeta se lee como una familia con el logo, el h1, los numerales y la banda de `#agenda` (rasgo 1 del moodboard: azul violeta eléctrico dominante) y ya no hay dos morados en el header; (b) el hero: la lupa y los aros de `HeroCollage` con el morado nuevo siguen leyéndose como lupa (rasgo 7, un foco claro); (c) `#agenda`: amarillo y blanco sobre morado se leen, y el naranja decorativo del collage (2.95 contra el morado) se distingue del fondo gracias al contorno blanco; si al primer vistazo el aro naranja o el mango no se distinguen del morado, aplica el respaldo: cambia esos dos rellenos de `AgendaCollage.astro` a amarillo o blanco y quita `orange` de `ALLOWED_FILLS.purple` en `collage-rules.mjs`, actualizando a la vez la guarda de excepción de `brand-palette.test.mjs`; (d) el logo del header a 32 px en 320 y 390 px se lee con nitidez; (e) en la hoja, cada logo se compara con su mesa de `ai_a.png` o `ai_b.png` (mismo dibujo, mismos colores, sin fondo) y se lee sobre su tono, incluidos los dos isotipos sobre oscuro con su aro morado (excepción registrada); (f) el favicon ampliado a 16 px sobre la muestra clara y la oscura se lee como un ojo con lupa. Conflicto que se resuelve a favor de la marca: `design-taste-frontend` desaconseja el morado "de IA"; el azul violeta es el color del BrandBook, del logo y del moodboard y no se cambia.
    3. Correcciones en un solo lote y solo de color y logos: tokens, tamaños del logo, asignación de tonos a piezas del collage. No se rediseñan secciones, no se toca el copy y no se rehace el collage con piezas reales (eso es el plan siguiente): si un hallazgo lo pide, se anota como pendiente para ese plan. Como máximo una ronda de confirmación (tope de tres ciclos por regla de `impeccable`); si tras eso un punto sigue sin cumplirse, detente y muestra las capturas a Juan.
    4. Registro. Agrega a `02-VISUAL-LOG.md` la sección "Lote M, ronda 0" (skills invocadas, lectura de diseño, conflictos resueltos y los verbos usados en las tareas 1 a 3) y "Lote M, ronda 1" (capturas, veredicto de los seis puntos, correcciones, ronda de confirmación y los rasgos 1 y 7 de la lista de vibra), con el mismo formato de los lotes anteriores.
    5. Cierre de verificación completa (todo verde antes del commit): guardas, contraste, pendientes sin cambios, suite completa de Chromium con ClickUp bloqueado, build con sus hooks, exclusión de la hoja en producción y barridos de la sección de verificación. Para DSGN-03 (vuelve a verificarse) cuenta `page-structure` y la hoja a los cinco anchos sin scroll horizontal.
    6. SUMMARY completo (sección "Output") y commit atómico: `docs(02-09): registro visual del lote M y cierre de la marca oficial`. Si hubo correcciones, van antes en un commit propio `fix(02-09): ...`. No marques requisitos ni avances contadores: eso lo hace el orquestador al cerrar.
    Protocolo de navegador: `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio y `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - Existen `test-results/phase2/M-320.png`, `M-390.png`, `M-768.png`, `M-1024.png`, `M-1280.png` y `M-hoja-320.png`, `M-hoja-390.png`, `M-hoja-768.png`, `M-hoja-1024.png`, `M-hoja-1280.png`.
    - `02-VISUAL-LOG.md` trae "Lote M, ronda 0" y "Lote M, ronda 1" con las skills invocadas, el veredicto de los seis puntos, correcciones y capturas.
    - Suite completa: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `node scripts/list-pending.mjs --check` y `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium` pasan; `npm run build` con sus hooks sale 0; con `PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build` no existe `dist/marca`.
    - Barridos: `! grep -rniE '73187f' src public tests scripts`; sin hex en `src/components`, `src/pages` ni `src/layouts`; sin `outline: none` (comprobación con `! grep -rn 'outline: none' src`); sin inserción de HTML crudo en `src`; `[ -z "$(git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- src/content PENDING-COPY.md src/components/sections src/components/ui src/layouts package.json package-lock.json)" ]` (rango del plan completo, con `PLAN_HEAD_BEFORE` igual al `plan_head_before` de la tarea 1).
    - `tracked=$(git ls-files) && ! printf '%s\n' "$tracked" | grep -Eiq '\.(ai|pdf)$' && ! printf '%s\n' "$tracked" | grep -Eq 'brand-inventory/'`.
    - El SUMMARY lista los efectos sobre los planes pendientes y las decisiones para Ari.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium; s=$?; npx astro preview stop; npm run build >/dev/null 2>&1; b=$?; PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build >/dev/null 2>&1; test ! -e dist/marca; p=$?; npx astro build >/dev/null 2>&1; [ $g -eq 0 ] && [ $s -eq 0 ] && [ $b -eq 0 ] && [ $p -eq 0 ]</automated>
    <human-check>Juan y Ari abren `/marca/hoja/` (con `npm run dev` o el preview) y aprueban de un vistazo: la franja de paleta (morado #4228D1 y crema), cada logo sobre su tono, la excepción de los dos isotipos sobre oscuro y el favicon a 16 px. No bloquea el cierre del plan; si Ari confirma otro morado, se cambia el token, las razones medidas de `scripts/lib/contrast.mjs` y se vuelve a correr `scripts/brand/extract-artboards.mjs` con la paleta objetivo.</human-check>
    <fails_when>Falta alguna captura de los cinco anchos o el registro visual; una corrección toca copy, secciones o el collage con piezas reales; el ciclo pasa de tres rondas sin avisar a Juan; algún barrido encuentra el morado anterior, hex en componentes, `outline: none`, inserción de HTML crudo o un cambio en `package.json`; se marca un requisito como cumplido dentro de este plan; una prueba de la suite completa falla.</fails_when>
  </verify>
  <done>El ciclo visual con `impeccable` y `design-taste-frontend` queda registrado con capturas a 320, 390, 768, 1024 y 1280 px de la página y de la hoja, el morado oficial y las mesas oficiales se ven como una sola identidad, toda la suite y los barridos pasan, y el plan cierra con SUMMARY completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drive público de Ari a repo (solo al autorar) | El `.ai` es un archivo de terceros. Se baja una vez con curl, se convierte con `pdftocairo` y se limpia con SVGO; su contenido no entra al repo hasta pasar por `cleanArtboard` y las guardas |
| script de extracción a procesos locales | `scripts/brand/extract-artboards.mjs` ejecuta `pdftocairo`, `file` y SVGO con rutas que vienen de la línea de comandos |
| SVG a página | Los SVG oficiales se insertan en línea en el HTML; cualquier elemento activo o referencia externa sería ejecutable en el navegador |
| repo a remoto | El BrandBook y sus renders son CONFIDENCIAL: no deben subirse |
| build a producción | La hoja `/marca/hoja/` es herramienta interna y no debe publicarse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Tampering | SVG convertidos del `.ai` (17 mesas y favicon) | medium | mitigate | `cleanArtboard` más guarda estática: solo elementos `svg` y `path`, sin script, image, foreignObject, xlink, `on*`, `href`, `style`, `defs`, `use`, comentarios ni metadatos, rellenos dentro de la paleta oficial, `viewBox` dentro de 0 a 800 y menos de 8192 bytes; el diff de los SVG se revisa antes del commit |
| T-02-09-02 | Tampering | Script de extracción (procesos externos) | low | mitigate | Todas las llamadas son `spawnSync` con lista de argumentos, sin shell; la fuente debe ser un PDF según `file`; el script solo escribe en `--out`; solo corre a mano al autorar |
| T-02-09-03 | Information Disclosure | Originales de Ari y renders del BrandBook (CONFIDENCIAL) | high | mitigate | `.gitignore` cubre `*.ai`, `BrandBook*.pdf` y `brand-inventory/`; la guarda `git ls-files` falla si aparece alguno; el registro guarda solo el id de Drive y el sha256 |
| T-02-09-04 | Information Disclosure | Metadatos del `.ai` (autor, rutas) en SVG e ICO | low | mitigate | SVGO los elimina y la guarda rechaza comentarios y metadatos |
| T-02-09-05 | Tampering | Excepciones de marca que crecen en silencio (isotipos sobre oscuro, naranja decorativo sobre morado) | medium | mitigate | Lista independiente de excepciones en la guarda (mesas 17 y 22) y guarda que exige que `ALLOWED_FILLS.purple` y `FORBIDDEN_PAIRS` coincidan; cualquier ampliación falla la prueba |
| T-02-09-06 | Tampering | Regresión del contraste por un token cambiado | medium | mitigate | `check-contrast` fija los 14 ratios aprobados y falla si un token cambia; se prueba por mutación del morado y del crema; corre en cada build (prebuild) |
| T-02-09-07 | Information Disclosure | Hoja `/marca/hoja/` publicada por error | low | mitigate | `getStaticPaths` vacío con `PUBLIC_ENV=production`, `noindex` fuera de producción; comprobación por build directo en las tareas 2 y 4 |
| T-02-09-08 | Denial of Service | Peso del HTML por el logo en línea | low | mitigate | El header incrusta un solo SVG de unos 3.6 KB; la guarda existente limita `dist/index.html` a 60 KB; cada SVG pesa menos de 8192 bytes |
| T-02-09-09 | Spoofing | Peticiones nuevas de terceros | low | mitigate | Todo va en línea y local; Drive solo se toca al autorar, no en el sitio; las pruebas corren con ClickUp bloqueado |
| T-02-09-10 | Repudiation | Licencia y autoría de los logos | low | accept | Los vectoriales son de Ari y son contornos (no hay fuente que licenciar); la licencia web de Hurme sigue pendiente desde la fase 1 |
| T-02-09-SC | Tampering | Instalación de paquetes npm, pip o cargo | low | accept | Este plan no instala nada: SVGO, Playwright y poppler ya están; no cambia `package.json` (lo verifica un criterio de aceptación); no aplica auditoría de legitimidad |
</threat_model>

<verification>
Al cerrar el plan, en este orden y con las lecciones de la fase 1 (el preview se va a segundo plano dentro de un agente; reconstruir tras cada cambio; detenerlo al final):
1. `npx astro build && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check`: guardas previas más las nuevas en verde, 14 pares aprobados y 11 prohibidos, `PENDING-COPY.md` sin cambios.
2. `npx astro preview --port 4322`, luego `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium` (todo el proyecto chromium) y `npx astro preview stop`. El proyecto `live` no se toca.
3. Exclusión de producción: `PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build` directo y `test ! -e dist/marca`; después `npx astro build` en modo normal y `npm run build` con sus hooks.
4. Barridos: `! grep -rniE '73187f' src public tests scripts`; sin hex en `src/components`, `src/pages` ni `src/layouts`; sin `outline: none` en `src`; los archivos versionados (`tracked=$(git ls-files)`) no incluyen `.ai`, `.pdf` ni rutas dentro de `brand-inventory/`; `[ -z "$(git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- src/content PENDING-COPY.md src/components/sections src/components/ui src/layouts package.json package-lock.json)" ]`.
5. `git status --short` limpio y `git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD` solo muestra archivos de `files_modified` (y el SUMMARY).
6. Revisión humana no bloqueante (tarea 4): Juan y Ari abren `/marca/hoja/` y aprueban paleta, logos por tono, excepción de los isotipos sobre oscuro y favicon.
</verification>

<success_criteria>
- FND-03: el morado #4228D1 y el crema #F4F3E0 existen una sola vez como tokens (más los colores propios de los SVG oficiales); `check-contrast` mide 14 pares aprobados y 11 prohibidos y falla si cualquiera cambia; el morado anterior tiene cero apariciones en código.
- DSGN-02: el logo, el imagotipo, los emblemas y los isotipos son las mesas oficiales del `.ai` de Ari, una por fondo, sin recolor por CSS, con área de salvado de 1 X (mitad del alto en el apilado) y con el catálogo de las 32 mesas probado; el favicon sale de la mesa 18 con evidencia medida.
- DSGN-01: los tokens y las reglas de tono del collage siguen validando en el build con el morado nuevo, y la excepción del naranja decorativo sobre morado es explícita y probada.
- DSGN-03: la página y la hoja se ven de 320 a 1280 px sin scroll horizontal (verificado de nuevo).
- DSGN-04: `impeccable` y `design-taste-frontend` se invocaron con la herramienta Skill, hubo un ciclo acotado con capturas a cinco anchos y quedó registrado en `02-VISUAL-LOG.md`.
- Cero copy cambiado, cero dependencias nuevas, cero originales versionados, y la suite de las fases 1 y 2 sigue en verde.
</success_criteria>

<output>
Al terminar crea `.planning/phases/02-secciones-marca-y-copy/02-09-SUMMARY.md` con: archivos creados, cambiados y eliminados; las mediciones confirmadas y los nombres de las pruebas que estaban en rojo por tarea; el id de Drive y el sha256 del `.ai`; la tabla de la extracción (bytes y viewBox por mesa); la tabla de evidencia del favicon; el registro visual del lote M por ronda; los supuestos y desviaciones de abajo con lo que se eligió; los efectos sobre los planes pendientes; las decisiones para Ari; y lo que queda para el plan siguiente (collage con piezas reales, píldoras, recortes con licencia). Si el contexto pasa de 50 % entre tareas, escribe un SUMMARY parcial con `status: partial`.
</output>

## Presupuesto de contexto

Estimación: unos 116.000 tokens de trabajo del ejecutor (tarea 1 cerca de 22k, tarea 2 cerca de 48k, tarea 3 cerca de 16k y tarea 4 cerca de 30k), con confianza baja porque `estimate-calibration` no tiene muestras (factor 1, sample_count 0). Sube el costo del ciclo visual: cada captura leída pesa, así que lee las capturas una por una y solo las listadas. Los SVG generados no se leen enteros (un `head -c 300`), el script de extracción se escribe una vez y no se relee, y el resto de los specs no se abren.

Puntos de corte (cada uno deja el repositorio en verde y con commit):
1. Tras la tarea 1: morado y pares de contraste oficiales en toda la página, con el rastreador en verde. Si el contexto pasa de 50 %, commit y SUMMARY parcial.
2. Tras la tarea 2: identidad completa (17 mesas, catálogo, Logo por tono, hoja). Es el corte natural más importante: un ejecutor nuevo retoma en la tarea 3 leyendo el SUMMARY parcial y `logo-variants.mjs`.
3. Tras la tarea 3: favicon y documentación al día.
4. La tarea 4 cierra el plan.

Si el checker exige partir por tamaño (la tarea 2 toca muchos archivos, aunque 17 son generados por el script), el corte limpio es 02-09a (tarea 1, color) y 02-09b (tareas 2 a 4, mesas oficiales), con 02-09b dependiendo de 02-09a. Se pueden ejecutar por separado sin compartir archivos salvo `brand-assets.spec.ts`.

## Catálogo de mesas (fuente de la tarea 2)

Razones medidas con `contrastRaw` sobre la superficie del tono en `tokens.css` (`light` blanco, `yellow` #ffc602, `dark` #212121, `purple` #4228d1). El color `fg` es el nombre de token del color principal del arte (o del aro exterior en isotipo y ojo). Umbral 4.5 para logotipos con texto y 3 para isotipos y ojos.

### Mesas usadas (17)

| Mesa | Variante | Tono | Archivo (`src/assets/brand/`) | fg | Razón | Umbral |
|------|----------|------|-------------------------------|----|-------|-------:|
| 01 | apilado | light | apilado-01-blanco.svg | purple | 8.55 | 4.5 |
| 05 | apilado | yellow | apilado-05-amarillo.svg | purple | 5.43 | 4.5 |
| 08 | apilado | dark | apilado-08-oscuro.svg | cream | 14.37 | 4.5 |
| 03 | apilado | purple | apilado-03-morado.svg | cream | 7.63 | 4.5 |
| 06 | horizontal | light | horizontal-06-blanco.svg | purple | 8.55 | 4.5 |
| 07 | imagotipo | light | imagotipo-07-blanco.svg | purple | 8.55 | 4.5 |
| 10 | emblema | light | emblema-10-blanco.svg | purple | 8.55 | 4.5 |
| 24 | emblema | yellow | emblema-24-amarillo.svg | purple | 5.43 | 4.5 |
| 12 | emblema | purple | emblema-12-morado.svg | cream | 7.63 | 4.5 |
| 13 | isotipo | light | isotipo-13-blanco.svg | purple (aro) | 8.55 | 3 |
| 16 | isotipo | yellow | isotipo-16-amarillo.svg | purple (aro) | 5.43 | 3 |
| 14 | isotipo | purple | isotipo-14-morado.svg | cream (aro) | 7.63 | 3 |
| 17 | isotipo | dark | isotipo-17-oscuro.svg | purple (aro) | 1.88 | 3, excepción |
| 18 | ojo | light | ojo-18-blanco.svg | purple (aro) | 8.55 | 3 |
| 21 | ojo | yellow | ojo-21-amarillo.svg | purple (aro) | 5.43 | 3 |
| 19 | ojo | purple | ojo-19-morado.svg | cream (aro) | 7.63 | 3 |
| 22 | ojo | dark | ojo-22-oscuro.svg | purple (aro) | 1.88 | 3, excepción |

Excepción de marca (mesas 17 y 22): el aro exterior morado mide 1.88 contra #212121; la figura se lee por el anillo crema (14.37), el iris y la pupila, y el logo lleva nombre accesible aparte. Es arte oficial de Ari, no un relleno que dibujemos nosotros. Se registra para el `EXCEPTIONS.md` de la fase 3 (dueño, aprobador, seguimiento y vencimiento).

### Mesas no extraídas (15) y motivo

| Mesas | Contenido | Motivo (medido) |
|-------|-----------|-----------------|
| 02, 11, 26, 27, 32 | Apilado, emblema, isotipo y ojo sobre crema; apilado monocromo crema | El crema no es un tono de la página (ver truths); la 32 además mide 1.12 sobre blanco y sobre oscuro y morado ya existen las mesas 08 y 03 |
| 04, 15, 20, 23, 29 | Apilado, isotipo, ojo y emblema sobre naranja; apilado monocromo naranja | El naranja nunca es fondo de sección (UI-SPEC); 04 y 23 traen amarillo sobre naranja (1.84) y 29 naranja sobre blanco (2.89) |
| 09, 31 | Apilado oscuro y negro, un solo color | Mismo trazo que la 01 en un solo color oscuro; el logo apilado de la página usa la 01 |
| 28 | Apilado monocromo morado | Mismos 11 trazos y un solo relleno que la 01 (9977 bytes las dos) |
| 30 | Apilado monocromo amarillo | Amarillo sobre claro mide 1.58 |
| 25 | Emblema sobre oscuro | Texto morado sobre oscuro mide 1.88; se pide a Ari una versión con texto crema (la 12 sirve de referencia) |

### Mínimos de tamaño por variante (px de alto)

| Variante | Mínimo | Alto por defecto | X del área de salvado |
|----------|-------:|------------------|-----------------------|
| horizontal | 32 | 2 rem (2.5 rem desde 40em) | alto del arte |
| imagotipo | 32 | 2 rem (2.5 rem desde 40em) | alto del arte |
| apilado | 48 | 4 rem | mitad del alto (el arte mide 2 X) |
| emblema | 96 | 7.5 rem | alto del arte |
| isotipo | 24 | 2.5 rem | alto del arte |
| ojo | 24 | 2.5 rem | alto del arte |

Los mínimos de 32 y 24 px son los del UI-SPEC; los de apilado (48) y emblema (96) son de este plan (con menos alto el texto queda por debajo de unos 11 px).

## Artifacts this phase produces

Nombres que los planes 02-04 a 02-08 y el plan del collage consumen tal cual.

| Artefacto | Detalle | Lo usa |
|-----------|---------|--------|
| `--color-brand-purple` (#4228d1) y `--color-brand-cream` (#f4f3e0) | Primitivos de `tokens.css`; el tono `purple` y todos los consumidores actuales siguen con el mismo nombre | Todos los planes |
| `tests/e2e/lib/brand.ts` | `PURPLE_RGB` y `rgbOfToken(nombre)`, leídos de `tokens.css`; los specs nuevos lo importan en vez de repetir `rgb(...)` | 02-04, 02-05, 02-06, 02-07, 02-08 |
| `scripts/lib/contrast.mjs` | 14 pares aprobados, 11 prohibidos; crema medido | Guarda de cada build |
| `src/components/brand/logo-variants.mjs` | `ARTBOARDS` (32), `resolveLogo`, `availableTones`, `MIN_HEIGHT_PX` | Logo.astro, la hoja, el plan del collage |
| `src/components/brand/Logo.astro` | Props `name`, `href?`, `variant?` (apilado, horizontal, imagotipo, emblema, isotipo, ojo), `tone?` (por defecto `light`), `class?`; emite `data-variant`, `data-logo-tone`, `data-artboard`; sin variante decorativa (la agrega el plan que use un isotipo como adorno) | 02-06 (footer: `<Logo name href />` sigue sirviendo porque el footer es `light`), plan del collage |
| `src/assets/brand/*.svg` (17) | Nombre `<variante>-<mesa>-<fondo>.svg`, sin fondo, viewBox recortado | Logo.astro, favicon |
| `scripts/brand/extract-artboards.mjs` | `--source <.ai>`, `--only`, `--out`, `--favicon`, `--favicon-evidence` | Quien reciba un `.ai` nuevo de Ari |
| `public/favicon.svg`, `public/favicon.ico` | Mesa 18, 16/32/48 px | 02-07 los enlaza en `BaseLayout` |
| `/marca/hoja/` | Identidad por tono, paleta, favicon, primitivas, composiciones y avatares; capturas `PHASE2_BATCH` de la hoja | Revisión de Juan y Ari |

## Cobertura de fuentes (alcance de 02-09)

| Fuente | Elemento | Estado |
|--------|----------|--------|
| GOAL (ROADMAP fase 2) | Identidad de marca aplicada (colores, logo e isotipo en SVG, collage) | Color, logo, isotipo y favicon: tareas 1 a 3. Collage: fuera de este plan |
| REQ | DSGN-01 (tonos y reglas del collage siguen válidos con el morado nuevo), DSGN-02 (logos por fondo, área de salvado), DSGN-03 (re-verificado a cinco anchos), DSGN-04 (skills, ciclo visual), FND-03 (tokens y pares) | Cubierto: FND-03 tarea 1, DSGN-02 tareas 2 y 3, DSGN-01 tarea 1 (excepción explícita) y tarea 4, DSGN-03 y DSGN-04 tarea 4 |
| CONTEXT | "Morado de marca y logo": morado #4228D1 con crema, tokens, pares y guardas recalculados, #73187F fuera de la página | Cubierto: tarea 1 |
| CONTEXT | "Morado de marca y logo": logos, imagotipo, emblema e isotipos de las 32 mesas por fondo, sin recolor | Cubierto: tarea 2 |
| CONTEXT | Área de salvado X en los cuatro lados (X igual al alto del logo o isotipo) | Cubierto: tarea 2 (con el apilado en mitad del alto, según el BrandBook) |
| CONTEXT | Método `impeccable` más `design-taste-frontend`, límites de contraste y A11Y.md | Cubierto: tareas 1 a 4 |
| CONTEXT | "Morado de marca y logo": reconstruir el collage con las piezas reales de Loopy y el lenguaje del moodboard; recortes fotográficos con licencia comercial | No cubierto aquí, por instrucción del orquestador: necesita su propio plan (rehacer collage, píldoras, recortes, sprite y avatares) |
| BRAND-INVENTORY | Sección 3 (hallazgo de color) y sección 6 puntos 1 y 2 (color y mesas por fondo) | Cubierto: tareas 1 y 2 |
| BRAND-INVENTORY | Sección 5 filas "Ojos Loopy", "Lupa", "Recortes fotográficos", "Píldoras", "Avatares y sprite"; sección 6 puntos 3 a 5 | Fuera de este plan (plan del collage); la fila "Crema" queda resuelta con la decisión de token sin tono; la fila "Logo por fondo" queda cubierta |
| UI-SPEC | Color (tonos, pares, reglas derivadas), Collage and Brand Assets Contract (logo por tono, área de salvado, favicon) | Cubierto con la fe de erratas de la tarea 3; la tabla de pares vieja no se reescribe |
| RESEARCH | No existe `02-RESEARCH.md`; se respeta la investigación de stack de CLAUDE.md (sin islas, sin JS, sin dependencias nuevas) | Sin ítems pendientes |

## Supuestos y desviaciones (revertir o confirmar)

1. Morado #4228D1 por decisión de Juan (2026-09-19); Ari confirma después. Si dijera otro valor, la superficie de cambio real no es un solo lugar: el token, las razones medidas de `scripts/lib/contrast.mjs`, los fills de los SVG (se vuelve a correr el script de extracción con la paleta objetivo) y las mediciones del catálogo. `tests/e2e/lib/brand.ts` evita tocar los specs. Se deja escrito en el comentario de `tokens.css`.
2. El token conserva el nombre `--color-brand-purple` y el tono `purple` (no se renombra a "blue"): evita tocar a todos los consumidores. Se documenta que el valor es azul violeta ("Purblue" del BrandBook).
3. El crema se promueve a token porque lo miden los pares de los logos oficiales (crema sobre morado y sobre oscuro) y lo necesitarán chips y píldoras. No es un quinto tono, por la evidencia del moodboard y porque mide 1.12 contra el blanco. El iris #6C61DB no se promueve: ningún componente ni par lo usa todavía; lo promueve el plan que dibuje los ojos de Loopy a mano.
4. Naranja sobre morado (2.95, antes 3.35): pasa de aprobado a prohibido para texto y UI y se conserva como relleno decorativo del collage con contorno blanco. Si la crítica visual lo ve confuso, el respaldo de la tarea 4 lo saca de `ALLOWED_FILLS.purple` y cambia dos rellenos de `AgendaCollage`.
5. `Logo` recibe `tone` con valor por defecto `light` para no romper a 02-06 (el footer es `light`). El logo horizontal y el imagotipo solo existen sobre `light`; el emblema no tiene versión sobre `dark`; sobre `dark` y `purple` el logo oficial es el apilado (mesas 08 y 03). No se inventa un horizontal blanco.
6. Isotipo y ojo sobre oscuro (mesas 17 y 22): se extraen con excepción de marca registrada (aro morado 1.88, figura legible por el anillo crema); el emblema sobre oscuro (mesa 25) no, porque su texto es morado sobre oscuro. Ari puede pedir una versión con texto crema.
7. Un archivo por mesa aunque varias traigan el mismo arte (13, 16 y 17; 18, 21 y 22; 1 y 5; 3 y 8; 10 y 24): cada archivo se audita contra su mesa del `.ai` y el peso en el repo es despreciable.
8. Los originales del `.ai`, del BrandBook y las imágenes de `brand-inventory/` no se versionan: se agrega `.gitignore` (hoy `brand-inventory/` aparece sin ignorar en `git status`).
9. El script de extracción sí se versiona (`scripts/brand/`) porque las 17 mesas y el favicon deben poder regenerarse si Ari entrega un `.ai` nuevo; requiere poppler y el `.ai` fuera del repo. Lo de la 02-02 era un script de una sola vez.
10. Favicon con la mesa 18 en vez de la 13 (evidencia a 16 px en los truths y en la tarea 3). Sustituye la decisión abierta de 02-02.
11. `--logo-clear` del logo apilado es la mitad de su alto (el arte mide 2 X en el BrandBook, página 7); las demás variantes conservan 1 X igual al alto. Los mínimos de 48 px (apilado) y 96 px (emblema) son de este plan.
12. `.claude/CLAUDE.md`, `DESIGN.md`, `PRODUCT.md`, `REQUIREMENTS.md`, `PROJECT.md` y la UI-SPEC se actualizan con ediciones de una línea o de un bloque (la UI-SPEC con fe de erratas, no reescrita) para que los agentes futuros no lean el morado anterior como vigente.
13. `tests/e2e/lib/brand.ts` deriva los colores de `tokens.css` en vez de repetir literales, por la lección de 02-01 a 02-03 (tres specs con el mismo `rgb(...)` a mano).

## Efectos sobre planes pendientes (para el orquestador)

- 02-03 (retomar tarea 4): solo cambia `tests/e2e/sections-problem-solution.spec.ts` (la constante del morado pasa a `PURPLE_RGB`). Ningún componente de 02-03 se toca.
- 02-04: las líneas 44, 379 y 446 citan `rgb(115, 24, 127)` para la cifra de los casos; deben pasar a `PURPLE_RGB` de `tests/e2e/lib/brand.ts` (contraste 8.55 sobre blanco, no 9.69).
- 02-06: la línea 61 (suposición del logo del footer) y la 483 (color del enlace de privacidad `rgb(115, 24, 127)`) deben pasar a `PURPLE_RGB`; `<Logo name href />` del footer sigue válido (tono `light` por defecto).
- 02-07: la mención de "Logo.astro (retirar una clase)" cambia (el componente se reescribe aquí; la clase `.wordmark` de `SiteHeader` se conserva) y el favicon que enlaza es el de la mesa 18.
- 02-08: `02-ARI-FINDINGS.md` sigue debiendo citar ambos morados (#73187F de la etiqueta y #4228D1 vigente); la línea "favicon (mesa 13 o 18)" pasa a "mesa 18, decidida con evidencia"; agrega los puntos nuevos: emblema sobre oscuro ausente, excepción de los isotipos sobre oscuro e imagotipo como posible logo del header.
- Plan siguiente (collage con piezas reales, píldoras, recortes con licencia): consume `resolveLogo`, los 17 SVG, `--color-brand-cream` y `tests/e2e/lib/brand.ts`; puede promover el iris a token.
