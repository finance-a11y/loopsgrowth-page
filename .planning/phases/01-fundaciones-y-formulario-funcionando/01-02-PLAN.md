---
phase: 01-fundaciones-y-formulario-funcionando
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/global.css
  - scripts/lib/contrast.mjs
  - scripts/check-contrast.mjs
  - scripts/lib/copy-rules.mjs
  - scripts/check-copy.mjs
  - scripts/list-pending.mjs
  - tests/guards/contrast.test.mjs
  - tests/guards/copy.test.mjs
  - tests/guards/list-pending.test.mjs
  - tests/guards/fixtures/clean.yaml
  - tests/guards/fixtures/pending.yaml
  - tests/guards/fixtures/verificar.yaml
  - tests/guards/fixtures/voseo.yaml
  - tests/guards/fixtures/voseo-accent-final.yaml
  - tests/guards/fixtures/em-dash.yaml
  - tests/guards/fixtures/en-dash.yaml
  - tests/guards/fixtures/aeo.yaml
  - tests/guards/fixtures/bare-string.yaml
  - tests/guards/fixtures/bad-status.yaml
  - package.json
  - PENDING-COPY.md
autonomous: true
requirements: [FND-02, FND-03, COPY-02]

estimate:
  tokens: 57000
  raw_tokens: 57000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Los cuatro colores de marca más el blanco existen una sola vez como primitivos `--color-brand-*` en `@theme` de `src/styles/tokens.css`, y los componentes consumen tokens semánticos por tono (`--surface`, `--on-surface`, `--link`, `--cta-bg`, `--cta-bg-hover`, `--on-cta`, `--focus-ring`) definidos en `:root, [data-tone=\"light\"]` y `[data-tone=\"purple\"]` (FND-03)"
    - "`node scripts/check-contrast.mjs` termina en 0 y verifica los 9 pares aprobados con sus ratios medidos (16.10, 9.69, 9.69, 6.15, 10.22, 5.56, 10.22, 5.56, 3.35, tolerancia 0.01) contra el umbral de cada uno (4.5 en texto, 3 en UI o texto grande)"
    - "Los 6 pares prohibidos (blanco sobre naranja 2.89, naranja sobre blanco 2.89, amarillo sobre blanco 1.58, blanco sobre amarillo 1.58, morado sobre oscuro 1.66, naranja sobre amarillo 1.84) son fixtures negativos: la calculadora los evalúa por debajo de su umbral, y declararlos en un tono del CSS hace fallar el script con código 1"
    - "Cambiar un valor de token (por ejemplo `--color-brand-dark` a `#777777`) o apuntar `--on-cta` del tono claro a blanco rompe `check-contrast` con código 1 y nombra el par que falla"
    - "`npm run build` ejecuta `prebuild` (contraste y luego copy) antes de `astro build` y `postbuild` (`check-copy --dist dist`) después; un fallo de `prebuild` detiene el build antes de `astro build` y un fallo de `postbuild` hace que `npm run build` termine con código distinto de 0 (FND-03, COPY-02)"
    - "Con `PUBLIC_ENV=production`, `check-copy` sale con código 1 y lista cada afirmación `pending` con su ruta YAML (hoy, cinco: agenda.intro, brand.term, call.duration, hero.subtitle, meta.title_template), cada `text` que contenga `FALTA CONFIRMAR` y cada `[VERIFICAR]`, voseo, guion largo (U+2014 y U+2013) y el término AEO encontrado (COPY-02)"
    - "Fuera de producción, `check-copy` imprime las mismas violaciones de contenido como advertencias y sale con 0, de modo que el desarrollo local sigue funcionando con copy pendiente"
    - "Con `PUBLIC_ENV=production`, `node scripts/check-copy.mjs --dist dist` sale con 1 si algún archivo de texto de `dist` contiene `FALTA CONFIRMAR` y nombra el archivo; fuera de producción imprime `WARN` y sale con 0; un directorio inexistente sale con 1 en cualquier entorno. El modo `--dist` solo escanea `dist` y corre en `postbuild`, no en `prebuild`, porque en `prebuild` `dist` todavía es el de la corrida anterior (COPY-02)"
    - "En cualquier entorno, `check-copy` sale con 1 si hay texto fuera de una reclamación `{text, status}` (fuera de `config`), un `status` distinto de `verified` o `pending`, un `text` vacío o una reclamación con claves distintas de `text`, `status`, `confirm_by` y `reason`: el contrato de FND-02 no depende del entorno"
    - "`npm run pending` genera `PENDING-COPY.md` en la raíz con una tabla de columnas Clave, Texto actual, Motivo y Quién confirma y una fila por cada afirmación `pending`, en el orden del YAML; `confirm_by` da Quién confirma (por defecto `Ari`) y `reason` da Motivo; la salida es determinista (sin fecha ni hora, sin rutas absolutas, idéntica con `PUBLIC_ENV` en `production`, `local` o sin definir), y `--check` sale con 1 si el archivo versionado difiere del que se generaría (FND-02)"
    - "`PENDING-COPY.md` está versionado en la raíz del repositorio y hoy lista exactamente las cinco afirmaciones `pending` de la Fase 1 (FND-02)"
    - "`PUBLIC_ENV` se resuelve con `loadEnv` de `vite` igual que en `astro.config.mjs`: un `PUBLIC_ENV=production` puesto solo en `.env` también activa el bloqueo"
    - "La detección de voseo usa límites Unicode (`\\p{L}`), no `\\b`: formas con vocal acentuada final como agendá, llená o tenés se detectan; y `check-copy` es de solo lectura: nunca modifica `landing.es.yaml`"
    - "Los textos estructurales (skip links, iframe title, fallback, noscript, intro y título de agenda) pasan por las mismas reglas que los del hero, no solo el hero"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Tokens de marca, semánticos por tono, escala tipográfica de 4 roles, contenedor, radios, sombra pop, foco, movimiento y `--form-min-h-sm` y `--form-min-h-lg`"
      contains: "--color-brand-purple"
    - path: "scripts/lib/contrast.mjs"
      provides: "Luminancia WCAG, contrastRatio, parseo de tokens, APPROVED_PAIRS (9) y FORBIDDEN_PAIRS (6)"
      exports: ["contrastRatio", "parseTokens", "APPROVED_PAIRS", "FORBIDDEN_PAIRS"]
    - path: "scripts/check-contrast.mjs"
      provides: "CLI de la guarda de contraste con `--tokens` y `--json`"
    - path: "scripts/lib/copy-rules.mjs"
      provides: "Reglas de copy reutilizables: walkClaims, pending, FALTA CONFIRMAR, marcador de verificación, voseo, guiones, AEO, texto vacío y claves permitidas (`text`, `status`, `confirm_by`, `reason`)"
      exports: ["walkClaims", "checkCopy", "MISSING_MARK"]
    - path: "scripts/check-copy.mjs"
      provides: "CLI de la guarda de copy con `--file`, `--root`, `--json` y `--dist`; bloquea solo en producción salvo las reglas estructurales"
    - path: "scripts/list-pending.mjs"
      provides: "CLI que genera `PENDING-COPY.md` desde el YAML (`--file`, `--out`, `--check`); determinista y sin leer el entorno"
    - path: "PENDING-COPY.md"
      provides: "Lista versionada de los textos `pending` para que Ari confirme: clave, texto actual, motivo y quién confirma"
      contains: "Quién confirma"
    - path: "tests/guards/contrast.test.mjs"
      provides: "Pruebas node:test de la calculadora, de los 9 pares y de cada fixture negativo"
    - path: "tests/guards/copy.test.mjs"
      provides: "Pruebas node:test de cada regla de copy con fixtures, del modo `--dist` y del arranque de `PUBLIC_ENV` desde `.env`"
    - path: "tests/guards/list-pending.test.mjs"
      provides: "Pruebas node:test del generador: columnas, valores por defecto, determinismo entre entornos, escape de celdas y `--check`"
  key_links:
    - from: "src/styles/tokens.css"
      to: "scripts/check-contrast.mjs"
      via: "parseTokens lee los valores hex y los tonos del CSS; un cambio de token cambia el resultado del script"
    - from: "package.json"
      to: "scripts/check-contrast.mjs y scripts/check-copy.mjs"
      via: "script `prebuild` que npm ejecuta antes de `build` y script `postbuild` (`check-copy --dist dist`) que ejecuta después"
    - from: "src/content/landing.es.yaml"
      to: "scripts/check-copy.mjs"
      via: "walkClaims recorre las secciones de `es` y valida cada reclamación"
    - from: "src/content/landing.es.yaml"
      to: "PENDING-COPY.md"
      via: "scripts/list-pending.mjs (`npm run pending`) recorre las reclamaciones con walkClaims y escribe una fila por cada `pending`"
  prohibitions:
    - requirement_id: COPY-02
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT permitir que un build de producción termine con éxito mientras exista una afirmación pending, la cadena FALTA CONFIRMAR en el YAML o en dist, un marcador de verificación pendiente, voseo, guion largo o el término AEO en el copy"
    - requirement_id: COPY-02
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT dejar pasar un texto vacío ni ocultar contenido pending para hacer pasar la guarda: un text vacío es violación estructural en cualquier entorno y la única marca de dato faltante permitida es el texto visible FALTA CONFIRMAR, que bloquea producción"
    - requirement_id: COPY-02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT agregar etiquetas de borrador visibles (por ejemplo borrador, TBD o lorem ipsum) como relleno de un texto que falta: solo se usa FALTA CONFIRMAR y la afirmación se lista en PENDING-COPY.md para pedírsela a Ari"
    - requirement_id: FND-02
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT dejar que PENDING-COPY.md omita una afirmación pending ni que dependa del entorno, de la fecha o del orden del sistema de archivos: el generador es determinista y solo escribe la ruta de salida, nunca el YAML"
    - requirement_id: COPY-02
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT reescribir, corregir ni marcar como verified el copy de Ari para hacer pasar la guarda: check-copy solo lee y reporta, y el texto lo aprueba una persona"
    - requirement_id: FND-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT bajar un umbral de contraste, quitar un par prohibido de los fixtures ni aprobar un par con menos de 4.5 en texto o 3 en UI para que pase una paleta que falla"
---

<objective>
Proteger el resto del proyecto con guardas de build: los tokens de marca con su guarda de contraste (FND-03) y la guarda de copy (COPY-02) que valida `landing.es.yaml` en `prebuild` y escanea `dist` en `postbuild`, más la lista `PENDING-COPY.md` que genera `npm run pending`, antes de que existan las secciones de las Fases 2 a 4.

Purpose: que un par de colores sin contraste, o una afirmación sin respaldo, rompa el build en vez de llegar a producción, y que Ari tenga a mano la lista de textos que debe confirmar. Historia de usuario: **As a** dueño de un negocio, **I want to** llenar el formulario de ClickUp sin salir de la página, **so that** Ari y Camila reciban mi solicitud de llamada; estas guardas evitan que el camino hasta el formulario publique colores ilegibles o promesas no verificadas.

Output: `tokens.css` como fuente única de marca, `check-contrast.mjs` con 9 pares aprobados y 6 fixtures prohibidos, `check-copy.mjs` con las reglas de COPY-02 (incluida `FALTA CONFIRMAR` en el YAML y en `dist`) más el contrato estructural del YAML, `list-pending.mjs` con `PENDING-COPY.md` versionado, pruebas `node:test` y los scripts `prebuild`, `postbuild` y `pending`.

Decisiones de CONTEXT.md que implementa (por nombre de sección): "Guardas de build y accesibilidad base" (`scripts/check-copy.mjs` en `prebuild` con `PUBLIC_ENV=production` falla ante `[VERIFICAR]`, `pending`, voseo, guion largo o AEO; tokens en `src/styles/tokens.css` con `@theme` más `scripts/check-contrast.mjs` que rompe bajo 4.5:1 en texto y 3:1 en UI) y "Scaffold y contrato del copy" (cada afirmación `{text, status}` validada; textos `pending` listados en `PENDING-COPY.md` con clave, texto actual, motivo y quién confirma; el build de producción falla con cualquier `pending` o con `FALTA CONFIRMAR`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaciones-y-formulario-funcionando/01-CONTEXT.md
@.planning/phases/01-fundaciones-y-formulario-funcionando/01-UI-SPEC.md
@.planning/phases/01-fundaciones-y-formulario-funcionando/SKELETON.md
@.planning/phases/01-fundaciones-y-formulario-funcionando/01-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Este plan produce (rutas relativas a la raíz del repositorio):

- `src/styles/tokens.css` (fuente única de marca) y el import en `src/styles/global.css`.
- `scripts/lib/contrast.mjs`, `scripts/check-contrast.mjs`, `scripts/lib/copy-rules.mjs`, `scripts/check-copy.mjs`, `scripts/list-pending.mjs`.
- `tests/guards/contrast.test.mjs`, `tests/guards/copy.test.mjs`, `tests/guards/list-pending.test.mjs` y diez fixtures YAML en `tests/guards/fixtures/` (los casos nuevos de `FALTA CONFIRMAR`, texto vacío, claves permitidas y directorios `dist` se arman dentro de las pruebas en `os.tmpdir()`, sin archivos de fixture adicionales).
- `PENDING-COPY.md` en la raíz del repositorio, generado por `npm run pending` y versionado.
- Scripts npm: `prebuild`, `postbuild`, `check:contrast`, `check:copy`, `pending`, `test:guards`.
- Comportamiento verificable: `PUBLIC_ENV=production npm run build` falla mientras existan afirmaciones `pending` o la cadena `FALTA CONFIRMAR`; `npm run build` sin producción pasa.
- Evidencia: `.planning/phases/01-fundaciones-y-formulario-funcionando/01-02-SUMMARY.md` con el resultado de las pruebas y la lista de afirmaciones `pending` que hoy bloquean producción (la misma que `PENDING-COPY.md`).

## Supuestos marcados por el probe de bordes

Los 12 requisitos llegaron `unclassified`; los de este plan (FND-02, FND-03, COPY-02) se convierten en las verdades de arriba. Supuestos: (1) fuera de producción las violaciones de contenido son advertencias, porque COPY-02 exige que falle el build de producción y CONTEXT dice que la guarda corre con `PUBLIC_ENV=production`; las violaciones estructurales sí fallan siempre; (2) la lista de voseo parte de la de PITFALLS.md (vos, tenés, querés, podés, sabés, sos, hacé, agendá, escribí, contanos, llená, descubrí, empezá, mirá, necesitás) y se amplía con contactanos, escribinos, reservá, descargá, registrate y sumate; (3) `AEO` se detecta como palabra completa sin distinguir mayúsculas; (4) los pares semánticos por tono se verifican además de los 9 pares hex, porque el riesgo real es que un tono nuevo (Fase 2 agrega amarillo y oscuro) declare una combinación prohibida; (5) la regla de `status` al nacer vive en el Plan 01 (solo respaldo en el doc de Ari o en los documentos del proyecto, o microcopy estructural, nace `verified`), por eso hoy son cinco las afirmaciones que bloquean producción (`agenda.intro`, `brand.term`, `call.duration`, `hero.subtitle` y `meta.title_template`); esta guarda no clasifica, solo lee el `status` que el YAML declara; (6) `FALTA CONFIRMAR` es la única marca de dato faltante y se detecta por la cadena, con cualquier `status`, en el YAML y en `dist`; el modo `--dist` corre en `postbuild` porque en `prebuild` `dist` es el de la corrida anterior; (7) `confirm_by` y `reason` son metadatos permitidos y no se escanean como copy; en `PENDING-COPY.md` el valor por defecto de Quién confirma es `Ari` y el de Motivo es `Sin motivo indicado`; (8) el orden de `PENDING-COPY.md` es el del YAML (no alfabético), que es estable mientras el YAML no cambie; (9) `--check` de `list-pending.mjs` queda disponible pero no se encadena en `prebuild` ni en `postbuild`, para que editar copy en desarrollo no bloquee el build; la Fase 4 (LNCH-03) puede exigirlo.

## Presupuesto de contexto (advertencia scope_sanity del checker: 22 archivos en `files_modified`)

El conteo de 22 sobrestima el costo real. Trece archivos casi no tienen autoría: los diez fixtures YAML son de 10 a 15 líneas cada uno (se escriben primero, según el flujo TDD del Task 2), `src/styles/global.css` suma una línea de import, `package.json` suma seis scripts y `PENDING-COPY.md` lo genera `npm run pending`. Los nueve archivos con autoría real son `src/styles/tokens.css`, `scripts/lib/contrast.mjs`, `scripts/check-contrast.mjs`, `scripts/lib/copy-rules.mjs`, `scripts/check-copy.mjs`, `scripts/list-pending.mjs` y las tres pruebas `node:test`. La estimación subió de 45k a 57k tokens por la regla `FALTA CONFIRMAR`, el modo `--dist` y el generador de la lista; no hay historial de calibración (confianza baja).

Punto de corte: los tres tasks tocan archivos distintos (contraste en el Task 1, guarda de copy en el Task 2, lista de pendientes en el Task 3; solo `package.json` es común y cada uno agrega sus propios scripts), y cada uno termina con el build verde y commiteado. Si al cerrar el Task 1 o el Task 2 la sesión ya está en la zona DEGRADING de `context-budget.md` (más del 50 % del contexto), el ejecutor commitea, escribe el `01-02-SUMMARY.md` parcial (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. No se recorta ninguna tarea para que quepa.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archivo YAML del repo a check-copy | Contenido editado por Ari o el equipo; confiable pero puede traer errores o texto sin respaldo |
| variables de entorno y `.env` a check-copy | Deciden si la guarda bloquea o solo advierte |
| tokens.css a check-contrast | Un cambio de color debe reflejarse en el resultado |
| `dist/` a check-copy `--dist` | El HTML construido puede traer una marca de dato faltante que el YAML no muestra por sí solo |
| YAML del repo a PENDING-COPY.md | Ari decide qué aprobar a partir de esta lista; si omite un `pending` aprueba a ciegas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `status: verified` puesto en el YAML sin la aprobación de Ari | medium | accept | Es control de proceso, no técnico: el cambio queda en el historial de git y la aprobación de Ari es la puerta de la Fase 4 (LNCH-03). `check-copy` imprime el conteo de `verified` y `pending` para que la revisión sea visible |
| T-02-02 | Tampering | `PUBLIC_ENV=production` solo en `.env` no visto por la guarda (bypass) | medium | mitigate | `check-copy` resuelve el entorno con `loadEnv` de `vite`, igual que `astro.config.mjs`; prueba `node:test` con un directorio temporal que solo tiene `.env` |
| T-02-03 | Tampering | cálculo de contraste incorrecto que deja pasar un par malo | medium | mitigate | Fixtures con ratios medidos (16.10, 2.89, 1.58, 1.66, 1.84...) y tolerancia 0.01; los 6 pares prohibidos se comprueban en cada corrida |
| T-02-04 | Elevation of Privilege | parseo de YAML con etiquetas que ejecuten código | low | mitigate | Solo `parse` del paquete `yaml` con el esquema por defecto (sin etiquetas personalizadas) sobre un archivo del propio repo |
| T-02-05 | Denial of Service | expresión regular con retroceso catastrófico en las reglas de voseo | low | accept | Entrada acotada al propio YAML; las expresiones son alternaciones simples de palabras con lookarounds de largo fijo |
| T-02-06 | Tampering | `PENDING-COPY.md` desactualizado u omite un `pending`, y Ari aprueba a partir de una lista incompleta | medium | mitigate | El generador recorre el YAML completo con `walkClaims` y una prueba compara el número de filas con el número de `pending` del YAML real; `--check` compara el archivo versionado con el que se generaría byte a byte; la salida es determinista, así que cualquier diferencia es un cambio real del YAML |
| T-02-07 | Tampering | la cadena `FALTA CONFIRMAR` (o un `pending`) llega a una página de producción porque el escaneo del YAML no ve el HTML construido | high | mitigate | `check-copy` en producción rechaza `FALTA CONFIRMAR` en cualquier `text` del YAML (`prebuild`) y en cualquier archivo de texto de `dist` (`postbuild`), y rechaza cualquier `pending`; pruebas con directorios `dist` temporales cubren ambos sentidos |
| T-02-SC | Tampering | instalaciones npm de este plan | low | accept | Este plan no instala paquetes: `yaml` y `@playwright/test` se aprobaron y fijaron en el Plan 01 (Task 1) |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer de contraste: tokens.css como fuente única y un par roto rompe el build</name>
  <files>src/styles/tokens.css, src/styles/global.css, scripts/lib/contrast.mjs, scripts/check-contrast.mjs, tests/guards/contrast.test.mjs, package.json</files>
  <read_first>
    - .planning/phases/01-fundaciones-y-formulario-funcionando/01-UI-SPEC.md (Color: Tokens, Pares aprobados, Pares prohibidos; Typography; Spacing Scale; Interaction Contract > Reserva de altura del iframe)
    - .planning/research/ARCHITECTURE.md (Design Tokens: colores, escala tipográfica, espaciado, forma y movimiento; Pattern 2: tokens de tres niveles)
    - .claude/CLAUDE.md (tabla "Contraste medido de la paleta de marca")
    - src/styles/global.css y package.json (creados en el Plan 01)
  </read_first>
  <action>
    Implementa FND-03 (per CONTEXT "Guardas de build y accesibilidad base").

    `src/styles/tokens.css`. Bloque `@theme static` (la variante `static` hace que Tailwind 4 emita siempre todas las variables aunque un componente solo las use desde su propio bloque de estilo) con los primitivos: `--color-brand-purple` `#73187f`, `--color-brand-orange` `#fd6938`, `--color-brand-yellow` `#ffc602`, `--color-brand-dark` `#212121`, `--color-brand-white` `#ffffff` (sin neutrales inventados: el brandbook no define crema ni grises). En el mismo bloque, los cuatro roles tipográficos de UI-SPEC como `--text-body` (`clamp(1rem, 0.96rem + 0.20vw, 1.125rem)`, interlineado 1.6), `--text-label` (`0.875rem`, 1.5), `--text-heading` (`clamp(1.75rem, 1.40rem + 1.70vw, 2.75rem)`, 1.2) y `--text-display` (`clamp(2.25rem, 1.60rem + 3.00vw, 4rem)`, 1.2). Los pesos son los tres del brandbook (Regular 400, SemiBold 600 y Bold 700) con la asignación de UI-SPEC Typography, decisión de Juan del 2026-09-18: Body y párrafos 400, Label (skip links) y todo texto secundario 600, Heading, Display, botones y wordmark del header 700; no se usa ningún otro peso. En el mismo bloque, `--container` `72rem`, `--gutter` `clamp(1rem, 4vw, 2rem)`, `--radius-s` `0.5rem`, `--radius-m` `1rem`, `--radius-pill` `999px`, `--border-pop` (3px sólido `--color-brand-dark`), `--shadow-pop` (`4px 4px 0` `--color-brand-dark`), `--focus-width` `3px`, `--focus-offset` `2px`, `--target-min` `2.75rem`, `--section-y` `4rem`, `--ease-out` `cubic-bezier(.2,.7,.2,1)`, `--dur-1` `150ms` (con `0ms` dentro de `@media (prefers-reduced-motion: reduce)`), y los tokens de reserva del iframe `--form-min-h-sm` `1100px` y `--form-min-h-lg` `900px` (valores iniciales que el Plan 04 reemplaza por los medidos). El espaciado usa la escala por defecto de Tailwind (4 px por unidad); no se agregan valores fuera de 4, 8, 16, 24, 32, 48 y 64 px.

    Tonos semánticos fuera de `@theme`: `:root, [data-tone="light"]` con `--surface` blanco, `--on-surface` oscuro, `--link` morado, `--cta-bg` naranja, `--cta-bg-hover` amarillo, `--on-cta` oscuro, `--focus-ring` morado; `[data-tone="purple"]` con `--surface` morado, `--on-surface` blanco, `--link` blanco, `--cta-bg` amarillo, `--cta-bg-hover` naranja, `--on-cta` oscuro, `--focus-ring` amarillo. Todos referencian los primitivos con `var(--color-brand-*)`.

    `src/styles/global.css`: agrega el import de `./tokens.css` justo después del import de Tailwind.

    `scripts/lib/contrast.mjs` (módulo puro, sin dependencias): `hexToRgb`, luminancia relativa WCAG 2.x, `contrastRatio(a, b)` con dos decimales, `parseTokens(cssText)` que devuelve las variables de `@theme` (también en su forma `@theme static`) y de cada bloque de tono resolviendo `var(--x)` hasta dos niveles, y dos listas exportadas. `APPROVED_PAIRS` con los 9 pares de UI-SPEC expresados por nombre de token: oscuro sobre blanco 16.10 (umbral 4.5), morado sobre blanco 9.69 (3), blanco sobre morado 9.69 (4.5), amarillo sobre morado 6.15 (4.5), oscuro sobre amarillo 10.22 (4.5), oscuro sobre naranja 5.56 (4.5), amarillo sobre oscuro 10.22 (4.5), naranja sobre oscuro 5.56 (4.5), naranja sobre morado 3.35 (3, solo texto grande, íconos o bordes). `FORBIDDEN_PAIRS` con los 6 pares prohibidos y sus ratios: blanco sobre naranja 2.89, naranja sobre blanco 2.89, amarillo sobre blanco 1.58, blanco sobre amarillo 1.58, morado sobre oscuro 1.66, naranja sobre amarillo 1.84.

    `scripts/check-contrast.mjs` (CLI): opciones `--tokens <ruta>` (por defecto `src/styles/tokens.css`) y `--json`. Comprueba en este orden y sale con 1 ante cualquier `FAIL`: (1) cada par aprobado, con los valores hex leídos de los tokens: ratio mayor o igual al umbral y a menos de 0.01 del ratio esperado (esto detecta también un token cambiado a un color que casualmente pasa); (2) para cada tono, los pares semánticos `--on-surface` sobre `--surface` y `--link` sobre `--surface` (4.5), `--on-cta` sobre `--cta-bg` y sobre `--cta-bg-hover` (4.5) y `--focus-ring` sobre `--surface` (3); (3) autoverificación de los 6 pares prohibidos: cada uno debe calcularse por debajo de su umbral, y si alguno lo supera la calculadora está mal y el script falla. Imprime una línea `PASS` o `FAIL` por par con ratio y umbral.

    `tests/guards/contrast.test.mjs` con `node:test` y `node:assert/strict`: ratios conocidos de `contrastRatio` dentro de 0.01 (los 15 valores de arriba); ejecución por defecto con código 0 y 9 pares aprobados en `--json`; copias temporales de `tokens.css` en `os.tmpdir()` mutadas con `--tokens` que deben salir con 1: `--color-brand-dark` cambiado a `#777777`, `--on-cta` del tono claro apuntando a `var(--color-brand-white)` (debe mencionar 2.89) y `--focus-ring` del tono morado apuntando a `var(--color-brand-dark)` (1.66).

    `package.json`: agrega los scripts `check:contrast` (`node scripts/check-contrast.mjs`), `test:guards` (`node --test tests/guards/*.test.mjs`) y `prebuild` (`node scripts/check-contrast.mjs`); el Task 2 lo extiende con la guarda de copy. `npm run build` debe seguir pasando con los tokens reales.
  </action>
  <verify>
    <automated>node --test tests/guards/contrast.test.mjs</automated>
    <fails_when>non-zero exit, o el resumen muestra "# fail" mayor que 0, o "# pass 0"</fails_when>
    <automated>node scripts/check-contrast.mjs && npm run build</automated>
    <fails_when>non-zero exit del script (alguna línea FAIL) o del build de Astro con el `prebuild` activo</fails_when>
  </verify>
  <done>`tokens.css` es la única fuente de los colores de marca; los 9 pares aprobados pasan con sus ratios medidos, un color cambiado o un par prohibido declarado en un tono rompe `check-contrast` y por tanto `npm run build`.</done>
  <acceptance_criteria>
    - `node scripts/check-contrast.mjs --json` imprime un arreglo con 9 pares aprobados todos `ok` y 6 pares prohibidos todos por debajo de su umbral
    - Las tres mutaciones de la prueba (`#777777`, `--on-cta` blanco sobre naranja, `--focus-ring` oscuro sobre morado) salen con código 1
    - `grep -c "#73187f\|#fd6938\|#ffc602\|#212121" src/styles/tokens.css` cuenta las definiciones primitivas y `grep -rn "#73187f\|#fd6938\|#ffc602\|#212121" src --include=*.astro --include=*.ts` no devuelve nada (los colores no se repiten fuera de `tokens.css`)
    - `tokens.css` define exactamente 4 tamaños tipográficos (`--text-body`, `--text-label`, `--text-heading`, `--text-display`), `--form-min-h-sm` y `--form-min-h-lg`
    - `package.json` contiene los scripts `check:contrast`, `test:guards` y `prebuild`
    - `npm run build` termina en 0 con `PUBLIC_ENV` sin definir
    - El CSS emitido en `dist/` contiene `--color-brand-purple`, `--surface` y `--form-min-h-sm` (`grep -rl -- "--form-min-h-sm" dist` lista al menos un archivo), es decir que Tailwind no descartó ninguna variable
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guarda de copy: producción falla ante pending, FALTA CONFIRMAR (en el YAML y en dist), [VERIFICAR], voseo, guion largo y AEO</name>
  <files>scripts/lib/copy-rules.mjs, scripts/check-copy.mjs, tests/guards/copy.test.mjs, tests/guards/fixtures/*.yaml, package.json</files>
  <read_first>
    - .planning/phases/01-fundaciones-y-formulario-funcionando/01-CONTEXT.md (Guardas de build y accesibilidad base; Scaffold y contrato del copy: textos `pending` y FALTA CONFIRMAR)
    - .planning/phases/01-fundaciones-y-formulario-funcionando/01-UI-SPEC.md (Copywriting Contract: regla de Juan sobre pending y FALTA CONFIRMAR, y reglas de redacción para el planner)
    - .planning/research/PITFALLS.md (Pitfall 4: `[VERIFICAR]` en producción; Pitfall 11: consistencia del español y expresión de voseo)
    - .planning/research/ARCHITECTURE.md (Content Layer: reglas 1 a 4)
    - src/content/landing.es.yaml y astro.config.mjs (Plan 01: forma del YAML y uso de `loadEnv`)
  </read_first>
  <behavior>
    - Test 1: `clean.yaml` (todas las reclamaciones `verified`, sin violaciones) sale con 0 en producción y fuera de ella.
    - Test 2: `pending.yaml` sale con 1 en producción y la salida lista la ruta YAML de la afirmación; sale con 0 y advierte fuera de producción.
    - Test 3: `verificar.yaml` con un marcador de verificación pendiente en `text` sale con 1 en producción y con 0 fuera de ella.
    - Test 4: `voseo.yaml` (por ejemplo tenés) y `voseo-accent-final.yaml` (agendá y llená, vocal acentuada al final) salen con 1 en producción: el límite Unicode los detecta.
    - Test 5: `em-dash.yaml` (U+2014) y `en-dash.yaml` (U+2013) salen con 1 en producción.
    - Test 6: `aeo.yaml` con el término AEO como palabra completa sale con 1 en producción; una palabra que solo lo contenga dentro de otra no se marca.
    - Test 7: `bare-string.yaml` (texto sin `{text, status}` fuera de `config`) y `bad-status.yaml` (`status: draft`) salen con 1 en cualquier entorno.
    - Test 8: un directorio temporal con `.env` que contiene `PUBLIC_ENV=production` y `--root` apuntando a él hace que `pending.yaml` salga con 1 sin variable de entorno del proceso.
    - Test 9: el YAML real `src/content/landing.es.yaml` sale con 0 fuera de producción y su contenido en disco es idéntico antes y después de la corrida (solo lectura).
    - Test 10: un YAML con una reclamación cuyo `text` es `FALTA CONFIRMAR`, con `status` `pending` y también con `verified`, sale con 1 en producción con la regla `MISSING` y la ruta de la reclamación, y con 0 y `WARN` fuera de producción.
    - Test 11: `--dist <dir>` sobre un directorio temporal con un `index.html` que contiene `FALTA CONFIRMAR` sale con 1 en producción nombrando el archivo, y con 0 y `WARN` fuera de producción; un directorio sin esa cadena sale con 0 en ambos; un directorio inexistente sale con 1 en cualquier entorno.
    - Test 12: `confirm_by` y `reason` son claves permitidas (un YAML limpio que las usa sale con 0 en producción); una clave extra desconocida y un `text` vacío o de solo espacios salen con 1 en cualquier entorno.
  </behavior>
  <action>
    Implementa COPY-02 (per CONTEXT "Guardas de build y accesibilidad base") y el lado de guarda de FND-02. Escribe primero los fixtures y `tests/guards/copy.test.mjs` con `node:test` y confirma que fallan; luego implementa hasta que pasen.

    `scripts/lib/copy-rules.mjs` (módulo puro que importa `parse` del paquete `yaml`, esquema por defecto): `walkClaims(doc)` recorre cada sección bajo las claves de idioma de nivel superior (hoy `es`) y devuelve por cada nodo su ruta con puntos (por ejemplo `hero.h1`), su tipo y su contenido. Un objeto con `text` y `status` es una reclamación y solo admite además `confirm_by` y `reason` (cadenas no vacías; son metadatos, no copy, y no se escanean con las reglas de contenido); cualquier otra clave extra es violación estructural. Toda cadena fuera de una reclamación y fuera de la sección `config` es violación estructural `BARE_STRING` (los valores de `config`, como las URL, no son copy y se ignoran). `status` distinto de `verified` o `pending` es violación estructural `BAD_STATUS`, y un `text` vacío o de solo espacios lo es `EMPTY_TEXT` (nunca hay un placeholder vacío). `checkCopy(doc)` devuelve `{structural, content}`: las de contenido son `PENDING` (`status` igual a `pending`), `MISSING` (la cadena `FALTA CONFIRMAR`, exportada como `MISSING_MARK`, en cualquier `text` y con cualquier `status`), `VERIFICAR` (el marcador `[VERIFICAR]` en cualquier `text`, sin distinguir mayúsculas), `VOSEO`, `DASH` (caracteres U+2014 y U+2013) y `AEO` (palabra completa, sin distinguir mayúsculas). El módulo exporta también `findMissingMark(text)`, que devuelve los extractos donde aparece `MISSING_MARK` y que reutiliza el modo `--dist`. `VOSEO` usa lookarounds Unicode con la bandera `u` (`(?<![\p{L}\p{N}_])` y `(?![\p{L}\p{N}_])`) y no `\b`, porque `\b` de JavaScript es ASCII y no detecta formas que terminan en vocal acentuada; parte de la lista de PITFALLS.md y las ampliaciones del apartado de supuestos. Cada violación lleva regla, ruta YAML y un extracto corto del texto. Se escanean todos los `text`, incluidos los estructurales (skip links, título del iframe, fallback, noscript).

    `scripts/check-copy.mjs` (CLI): opciones `--file <ruta>` (por defecto `src/content/landing.es.yaml`), `--root <dir>` (directorio desde donde se lee `.env`, por defecto el actual), `--json` y `--dist <dir>`. Resuelve `PUBLIC_ENV` con `loadEnv` de `vite` (lee `.env`, `.env.local` y `process.env`, igual que `astro.config.mjs`), de modo que un `production` puesto solo en `.env` también bloquea. Reglas de salida: las violaciones estructurales salen con código 1 en cualquier entorno; con `PUBLIC_ENV` exactamente `production`, cualquier violación de contenido sale con 1; en otro entorno se imprimen como `WARN` y el código es 0. Cada línea `FAIL` o `WARN` muestra regla, ruta y extracto, y el cierre imprime los conteos de reclamaciones `verified` y `pending`. El script es de solo lectura: nunca escribe ni corrige el YAML, y nunca cambia un `status` (per prohibición: el texto de Ari lo aprueba una persona).

    Modo `--dist <dir>` (per CONTEXT: el build de producción falla con `FALTA CONFIRMAR` en el YAML o en `dist`). En vez de validar el YAML, recorre `<dir>` de forma recursiva y lee como UTF-8 los archivos de texto (`.html`, `.xml`, `.txt`, `.json`, `.svg`, `.css`, `.js` y `.webmanifest`) buscando `MISSING_MARK` con `findMissingMark`; cada hallazgo es una violación de contenido `MISSING` con archivo y extracto y se trata igual que las demás: con `PUBLIC_ENV` exactamente `production` sale con 1, en otro entorno imprime `WARN` y sale con 0. Un directorio inexistente sale con 1 en cualquier entorno con un mensaje claro. El modo solo escanea `dist` y no vuelve a validar el YAML (así no duplica la salida de `prebuild`). Corre en `postbuild` y no en `prebuild` porque en `prebuild` `dist` todavía es el de la corrida anterior y el resultado sería falso.

    Fixtures en `tests/guards/fixtures/` (`clean`, `pending`, `verificar`, `voseo`, `voseo-accent-final`, `em-dash`, `en-dash`, `aeo`, `bare-string`, `bad-status`): YAML mínimos con la forma real (clave `es`, secciones, reclamaciones y una sección `config`). Los fixtures están fuera de `src/` para que la guarda real nunca los escanee. Los casos de los tests 10 a 12 (cadena de dato faltante en YAML y en `dist`, claves permitidas, clave desconocida y texto vacío) se arman dentro de `copy.test.mjs` con `fs.mkdtempSync` en `os.tmpdir()` a partir de cadenas, sin archivos de fixture nuevos en el repositorio.

    `package.json`: extiende `prebuild` a `node scripts/check-contrast.mjs && node scripts/check-copy.mjs` y agrega `check:copy` (`node scripts/check-copy.mjs`) y `postbuild` (`node scripts/check-copy.mjs --dist dist`, que npm ejecuta después de `build`). Con `PUBLIC_ENV=production`, `npm run build` debe fallar hoy en `prebuild` por las cinco afirmaciones `pending` del YAML del Plan 01 (`agenda.intro`, `brand.term`, `call.duration`, `hero.subtitle` y `meta.title_template`); sin producción debe pasar, incluido el `postbuild`.
  </action>
  <verify>
    <automated>node --test tests/guards/copy.test.mjs</automated>
    <fails_when>non-zero exit, o el resumen muestra "# fail" mayor que 0, o "# pass 0"</fails_when>
    <automated>node --test tests/guards/contrast.test.mjs tests/guards/copy.test.mjs && npm run build && bash -c 'if PUBLIC_ENV=production npm run build >/dev/null 2>&1; then echo "UNEXPECTED: production build passed"; exit 1; else exit 0; fi'</automated>
    <fails_when>non-zero exit de las pruebas o del build no productivo, o stdout contiene "UNEXPECTED: production build passed"</fails_when>
    <automated>npm run build && PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist</automated>
    <fails_when>non-zero exit del build sin producción (incluido su `postbuild`) o del escaneo de `dist` en modo producción, es decir que el sitio real construido contiene la cadena `FALTA CONFIRMAR`</fails_when>
  </verify>
  <done>Con `PUBLIC_ENV=production`, `npm run build` falla en `prebuild` mientras existan afirmaciones `pending`, `FALTA CONFIRMAR`, `[VERIFICAR]`, voseo, guion largo o AEO, y `postbuild` rechaza `FALTA CONFIRMAR` en `dist`; en desarrollo el mismo copy solo genera advertencias; el YAML nunca se modifica.</done>
  <acceptance_criteria>
    - `node --test tests/guards/copy.test.mjs` pasa los 12 casos del bloque `<behavior>` (cada fixture negativo prueba su regla por separado y `clean.yaml` sale con 0)
    - `PUBLIC_ENV=production node scripts/check-copy.mjs` sale con 1 y su salida contiene las cinco rutas `agenda.intro`, `brand.term`, `call.duration`, `meta.title_template` y `hero.subtitle`
    - `PUBLIC_ENV=local node scripts/check-copy.mjs` sale con 0 y su salida contiene "WARN" y los conteos de `verified` y `pending`
    - Tras `npm run build` sin producción, `PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist` sale con 0 (el sitio real no contiene `FALTA CONFIRMAR`), y la suite prueba que sale con 1 cuando un `dist` temporal sí la contiene
    - La prueba de `.env` (directorio temporal con `PUBLIC_ENV=production` y `--root`) sale con 1 sin variable de proceso
    - `git diff HEAD --stat -- src/content/landing.es.yaml` queda vacío después de correr `check-copy` (solo lectura)
    - `grep -c "verified\|pending" src/content/landing.es.yaml` no aumentó respecto al Plan 01 (esta tarea no agrega ni cambia copy)
    - `package.json` contiene `check:copy` y `postbuild` (con `--dist dist`), y `prebuild` encadena las dos guardas en ese orden
    - La guarda cubre los textos estructurales: una copia del YAML con voseo en `agenda.fallback_lead` sale con 1 en producción (caso incluido en la suite)
    - Sobre el YAML real, la salida `--json` de `PUBLIC_ENV=production node scripts/check-copy.mjs` solo contiene violaciones `PENDING` (las cinco): las cadenas de Ari de `hero.h1`, `hero.subtitle` y `agenda.intro` no disparan `VERIFICAR`, `VOSEO`, `DASH`, `AEO` ni `MISSING`; si alguna lo hiciera, el ejecutor no la edita y la reporta en el SUMMARY
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lista de pendientes: `npm run pending` genera PENDING-COPY.md de forma determinista</name>
  <files>scripts/list-pending.mjs, tests/guards/list-pending.test.mjs, PENDING-COPY.md, package.json</files>
  <read_first>
    - .planning/phases/01-fundaciones-y-formulario-funcionando/01-CONTEXT.md (Scaffold y contrato del copy: textos `pending`, `PENDING-COPY.md` y FALTA CONFIRMAR)
    - .planning/phases/01-fundaciones-y-formulario-funcionando/01-UI-SPEC.md (Copywriting Contract: regla de Juan sobre `PENDING-COPY.md`)
    - scripts/lib/copy-rules.mjs y scripts/check-copy.mjs (Task 2: `walkClaims` y la forma del CLI)
    - src/content/landing.es.yaml (Plan 01: `confirm_by` y `reason` opcionales, cinco `pending` con `reason`)
  </read_first>
  <behavior>
    - Test 1: un YAML con tres reclamaciones (dos `pending` y una `verified`) genera un archivo con el encabezado exacto `| Clave | Texto actual | Motivo | Quién confirma |`, su fila separadora y exactamente dos filas de datos en el orden del YAML; la `verified` no aparece.
    - Test 2: `confirm_by` presente da el valor de Quién confirma y ausente da `Ari`; `reason` presente da el valor de Motivo y ausente da `Sin motivo indicado`.
    - Test 3: determinismo: dos corridas sobre el mismo YAML dan bytes idénticos, también con `PUBLIC_ENV` igual a `production`, igual a `local` y sin definir, y desde otro directorio de trabajo; el archivo no contiene fecha, hora ni ruta absoluta.
    - Test 4: una celda con `|` sale con `\|` y un salto de línea sale como un espacio, de modo que cada fila conserva cuatro columnas.
    - Test 5: `--check` sale con 0 cuando el archivo coincide con el que se generaría y con 1 cuando difiere o no existe, con un mensaje que indica ejecutar `npm run pending`; `--check` no escribe nada.
    - Test 6: sin ninguna afirmación `pending` el archivo trae el título, la línea de generación y la frase `No hay textos pendientes.`, sin tabla, y sigue siendo determinista.
    - Test 7: una reclamación con `text` igual a `FALTA CONFIRMAR` y `status` `pending` aparece en la lista con su texto visible, como cualquier otra `pending`.
    - Test 8: con el YAML real, el archivo generado tiene tantas filas como `pending` cuenta `walkClaims` y ninguna para las `verified`, y el YAML en disco es idéntico antes y después (solo lectura).
  </behavior>
  <action>
    Implementa la decisión de los textos `pending` (per CONTEXT "Scaffold y contrato del copy": lista `PENDING-COPY.md` generada con `npm run pending`, con clave, texto actual, motivo y quién confirma). Escribe primero `tests/guards/list-pending.test.mjs` con `node:test` (los YAML de prueba son cadenas escritas en `os.tmpdir()` con `fs.mkdtempSync`, sin archivos de fixture nuevos) y confirma que fallan; luego implementa hasta que pasen.

    `scripts/list-pending.mjs` (CLI de Node; importa `parse` del paquete `yaml` y `walkClaims` de `scripts/lib/copy-rules.mjs`): opciones `--file <ruta>` (por defecto `src/content/landing.es.yaml`), `--out <ruta>` (por defecto `PENDING-COPY.md` en el directorio actual, que es la raíz del repositorio) y `--check`. Recorre el YAML con `walkClaims`, en el orden del documento, y toma cada reclamación con `status` igual a `pending` (una reclamación cuyo `text` es `FALTA CONFIRMAR` es una más). Escribe un archivo Markdown con un título de nivel 1 `Textos pendientes de confirmar`, una línea que dice que el archivo se genera con `npm run pending` desde `src/content/landing.es.yaml` y que no se edita a mano, y una tabla de cuatro columnas con el encabezado exacto `| Clave | Texto actual | Motivo | Quién confirma |`: Clave es la ruta con puntos (por ejemplo `hero.subtitle`); Texto actual es el `text` tal como está en el YAML, sin resolver llaves (una plantilla con `{term}` se ve como está escrita); Motivo es `reason` o, si falta, `Sin motivo indicado`; Quién confirma es `confirm_by` o, si falta, `Ari` (constantes `DEFAULT_REASON` y `DEFAULT_CONFIRM_BY` definidas en este script). Sin pendientes, el archivo trae el título, la línea de generación y la frase `No hay textos pendientes.` y ninguna tabla. En cada celda escapa `|` como `\|` y reemplaza los saltos de línea por un espacio. La salida es determinista: sin fecha ni hora, sin rutas absolutas, saltos de línea `\n` y un solo salto de línea final; el script no lee `PUBLIC_ENV`, ni `.env`, ni usa `loadEnv`, así que corre igual en cualquier entorno. `--check` no escribe: compara byte a byte el contenido que se generaría con el archivo de `--out` y sale con 0 si coinciden y con 1 si difieren o el archivo no existe, con un mensaje que indica ejecutar `npm run pending`. El script solo escribe en la ruta de `--out` y nunca modifica el YAML ni cambia un `status` (el texto de Ari lo aprueba una persona).

    `package.json`: agrega el script `pending` igual a `node scripts/list-pending.mjs`. `--check` no se encadena en `prebuild` ni en `postbuild` (editar copy en desarrollo no debe bloquear el build); queda disponible para la verificación de la Fase 4 (LNCH-03).

    Cierre. Ejecuta `npm run pending` para generar `PENDING-COPY.md` en la raíz con las cinco afirmaciones `pending` de la Fase 1 y deja el archivo incluido en el commit de la tarea.
  </action>
  <verify>
    <automated>node --test tests/guards/list-pending.test.mjs</automated>
    <fails_when>non-zero exit, o el resumen muestra "# fail" mayor que 0, o "# pass 0"</fails_when>
    <automated>npm run pending && node scripts/list-pending.mjs --check && test "$(grep -c '| Ari |$' PENDING-COPY.md)" = "5"</automated>
    <fails_when>non-zero exit de `npm run pending` o de `--check` (el archivo generado difiere del que se generaría), o `PENDING-COPY.md` no tiene exactamente cinco filas con `Ari` como quien confirma</fails_when>
  </verify>
  <done>`npm run pending` genera en cualquier entorno un `PENDING-COPY.md` idéntico byte a byte con una fila por cada afirmación `pending` (clave, texto actual, motivo y quién confirma), el archivo está versionado en la raíz con las cinco de la Fase 1, y `--check` detecta cuando quedó desactualizado.</done>
  <acceptance_criteria>
    - `node --test tests/guards/list-pending.test.mjs` pasa los 8 casos del bloque `<behavior>`
    - `PENDING-COPY.md` existe en la raíz, está versionado (`git ls-files PENDING-COPY.md` lo lista) y `node scripts/list-pending.mjs --check` sale con 0
    - El encabezado de la tabla de `PENDING-COPY.md` es exactamente `| Clave | Texto actual | Motivo | Quién confirma |`
    - Hoy tiene exactamente cinco filas, para `brand.term`, `call.duration`, `meta.title_template`, `hero.subtitle` y `agenda.intro` (en el orden en que aparecen en el YAML), todas con `Ari` como quien confirma y ninguna con `Sin motivo indicado`; no hay fila para ninguna afirmación `verified`
    - `PUBLIC_ENV=production npm run pending` y `PUBLIC_ENV=local npm run pending` dejan `PENDING-COPY.md` sin cambios (`git status --short PENDING-COPY.md` vacío tras cada uno)
    - `git diff HEAD --stat -- src/content/landing.es.yaml` queda vacío después de correr el generador (solo lectura)
    - `package.json` contiene el script `pending` y `prebuild` sigue encadenando solo las dos guardas de contraste y de copy
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test tests/guards/*.test.mjs` termina en 0.
- `node scripts/check-contrast.mjs` termina en 0 con 9 pares aprobados.
- `npm run build` pasa sin `PUBLIC_ENV` (con `prebuild` y `postbuild`); `PUBLIC_ENV=production npm run build` falla en `prebuild` con la lista de `pending`.
- `npm run pending` regenera `PENDING-COPY.md` sin cambios y `node scripts/list-pending.mjs --check` sale con 0.
- `src/content/landing.es.yaml` no fue modificado por este plan.
</verification>

<success_criteria>
Un cambio de color que rompa el contraste, una afirmación `pending` o la cadena `FALTA CONFIRMAR` que lleguen al build de producción detienen el build con un mensaje que nombra el par, la ruta del YAML o el archivo de `dist`. El desarrollo local sigue funcionando con advertencias, y Ari tiene en `PENDING-COPY.md` la lista exacta de textos que debe confirmar.
</success_criteria>

<output>
Al terminar crea `.planning/phases/01-fundaciones-y-formulario-funcionando/01-02-SUMMARY.md` con el resultado de las pruebas y la lista de afirmaciones `pending` que hoy bloquean producción (la misma de `PENDING-COPY.md`, para que Ari las apruebe en la Fase 4), y con cualquier cadena de Ari que las reglas de contenido hayan rechazado (se reporta, no se edita).
</output>
